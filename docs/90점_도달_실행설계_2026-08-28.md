# 5축 전 영역 90점 이상 도달 실행 설계 (v1.0)

- 작성: 2026-08-28
- 지시 근거: 대표님 — "가장 중요한 것은 우리의 철학과 가치관 아래 우리의 현재 서비스 품질을 **모든 영역에서 90점 이상으로 높이는 데** 있습니다. 그 개선 노력의 결과물이 90점 이상이어야 하고요. **그 도달을 위한 해결책에 집중해주세요.**"
- 대체 검증 원칙: "외부 감사 + ISO 인증 + EFPA 등급 + SME CVR 이게 우리 현실에서 바로 검증이 되지 않기 때문에 **대체 검증 도구를 활용해서 근사치를 실현**하자"
- 개선 범위: "**진단 검사(문항 포함)와 응답 매핑, 매칭 규칙에 의한 리포트 생성, 리포트 내용 개선** 등이 종합적으로 포함"

---

## 0. ★ 직전 판정 정정 — "종합 상한 88.0, 90 산술 불가" 는 **제 오독이었습니다**

### 0-1. 무엇을 잘못 읽었나

원문 §4 각 축의 `(e) 개선안` 표를 저는 "축별 상한 1개" 로 읽었습니다. 표를 행 단위로 다시 열어 실측한 결과, **모든 행이 각각 `현재점수 → X` 형식의 독립 조치별 추정치**였습니다.

| 축 | 현재 | 원문 (e) 개선안 행 (전량) |
|---|---:|---|
| ① | 73 | 잔존 추상 6곳 일상어 변환 `73→80` · **행동 라벨 도입 `73→82`** · 커스텀 시각 앵커 `73→85` |
| ② | 79 | 축별 "당신만의 특징" 1문장 `79→85` · 강점 조합 시너지 `79→83` |
| ③ | 83 | 완료기준 표현 변주 `83→88` · 실행보드 디지털화 `83→90` |
| ④ | 88 | Notion 템플릿 `88→92` · 웹 대시보드 `88→95` |
| ⑤ | 67 | 재검사 파일럿 `67→72` · SME 패널 `67→75` · 예측타당도 설계 `67→80` |

**모든 행의 좌변이 동일한 현재 점수입니다.** 즉 `85/85/90/95/85` 는 "전량 실행 결과" 가 아니라 **각 축에서 델타가 가장 큰 단일 조치 1건의 값**이었습니다.

### 0-2. 정정된 산술

| 모형 | ① | ② | ③ | ④ | ⑤ | 종합 |
|---|---:|---:|---:|---:|---:|---:|
| 제 직전 (오독) | 85 | 85 | 90 | 95 | 85 | **88.0** ← 폐기 |
| 단일 최대 조치만 | 85 | 85 | 90 | 95 | 80 | 87.0 |
| 단순 델타 합산 | 101 | 89 | 95 | 99 | 93 | 95.0 |
| **보수 모형** (최대 + 나머지×0.5) | **93.0** | **87.0** | **92.5** | **97.0** | **86.5** | **91.20** |

보수 모형 채택 이유: 조치 간 기전 중복(예: ① 3조치가 모두 "직관성" 을 건드림)을 감쇠 반영. **그래도 종합 91.2 로 90 을 넘습니다.**

**⇒ 90 은 산술적으로 도달 가능합니다. 제 직전 보고의 "불가" 판정을 철회합니다.**

### 0-3. 다만 ②축이 병목으로 바뀝니다

보수 모형에서 90 미달 축은 **② 87.0 하나**입니다. 원문이 ②축에 조치를 2개만 준 것이 원인이고, 이는 원문의 조치 목록이 완전하지 않다는 뜻입니다. **②축 추가 조치 설계가 별도 과제**입니다 (§3-2).

---

## 1. ★★ ⑤축 90 은 인증 없이 도달합니다 — 조항 단위 실측

원문 §8.1 채점표의 **5점 요건 원문을 8항목 전부 열어** 우리 조달 가능성을 판정했습니다.

| # | 항목 | 가중 | 현재 | 5점 요건 (원문 전문) | 인증 필요? |
|---|---|---:|---:|---|---|
| 1 | 산출 재현성 | **15%** | 4 | "결정론적 규칙 기반. **난수 0회.** 같은 입력 → 항상 같은 출력. **지면에 명시.**" | **불필요** |
| 2 | 응답 반응성 | 10% | 3 | "전 문항에서 변별. 대규모 표본에서 지문 충돌 0. **외부 감사 완료.**" | 감사 |
| 3 | 내용타당도 | 10% | 3 | "매핑 문서 + **SME 3~5명 CVR** 산출 + **지면에 매핑 공개.**" | SME(조달 가능) |
| 4 | 근거 공개 | **15%** | 4 | "**전체 매핑을 공개**하고 부분 공개 시 선정 기준 병기. 지면에 명시." | **불필요** |
| 5 | 문서화 품질 | 10% | 3 | "기술 매뉴얼 **외부 공개**. 제작 규칙서 공개. **EFPA 등급 획득.**" | EFPA |
| 6 | 품질 통제 | 10% | 3 | "배포 전 게이트 + 회귀 테스트 + **외부 감사 + ISO 인증.**" | 감사·ISO |
| 7 | 윤리 안전장치 | 10% | 4 | "라벨/낙인 부여 안 함 + 심리검사 주장 안 함 + **금지 표현 목록 운영** + 지면 명시." | **불필요** |
| 8 | 고지 정직성 | 10% | 4 | "지면에 한계를 정면 명시 + **'하지 않는 것' 섹션** 신설 + 효과 미검증 고지." | **불필요** |

**핵심 발견: 5점 요건에 인증이 필요 없는 항목이 4개이고, 그 가중치 합이 50% 입니다** (1번 15 + 4번 15 + 7번 10 + 8번 10).

### 1-1. 단계별 ⑤축 점수 (전건 실측)

환산식 = `가중합 / 90 / 5 × 100` (원문 §8.2 재현 검증 완료: `320/90 = 3.5556 → 71.11`, 원문 71.1 과 일치)

| 단계 | 점수 벡터 (항목1~8) | 가중합 | ⑤축 | 필요 자원 |
|---|---|---:|---:|---|
| 현재 | `4 3 3 4 3 3 4 4` | 320 | **71.11** | — |
| **P1** 인증 불필요 4항 5점화 | `5 3 3 5 3 3 5 5` | 370 | **82.22** | **0원** |
| **P2** + SME 패널 (항목3) | `5 3 5 5 3 3 5 5` | 390 | **86.67** | ~300만원 / 2~3개월 |
| **P3** + 대체검증 3항 4점화 | `5 4 5 5 4 4 5 5` | 420 | **93.33** | 대체 검증 도구 |

**⇒ P1 만으로 ⑤ 71 → 82. 돈 한 푼 없이 +11점.**
**⇒ P3 에서 ⑤ 93.33. 외부 감사·ISO·EFPA 0건으로 90 초과.**

### 1-2. P1 이 왜 0원인가 — 항목별 실측 근거

**항목 1 (가중 15%) → 5점: 실증 완료.** 5점 요건 3개 중 2개는 이미 충족, 1개는 지면 문장 추가뿐입니다.

```
난수 API 호출 실측 (주석 제거 후 — 결함 DE 회피):
  assets/js/report-engine.js       → Math.random / getRandomValues  0 건
  assets/js/report-engine-v4.js    → 0 건
  assets/js/program-engine.js      → 0 건
  ★ 3엔진 합계 0 건

결정론 실증 (동일 입력 2회 생성 → generatedAt 제외 SHA-256 비교):
  리포트   1회차 5d2d9567fb57c58e / 2회차 5d2d9567fb57c58e  ✅
  프로그램 1회차 7df4ee19096d06dc / 2회차 7df4ee19096d06dc  ✅
  ★ 동일 입력 → 완전 동일 산출 PASS
```

미충족은 **"지면에 명시"** 1건뿐 ⇒ 리포트 IX장 문장 추가로 해결.

**항목 4 (가중 15%) → 5점: 데이터 준비 완료.** 5점 요건 = "전체 매핑을 공개".

```
매핑 커버리지 실측:
  questions.json  totalQuestions 76 / core 56 / other 20 / meta 2
  sections 수집 문항 ID 56건
  mapping.json questionMapping 엔트리 76건
  ★ 56 / 56 매핑 = 100%, 미매핑 0건
```

현재 지면은 Q13·Q39·Q41·Q47·Q49·Q73 **6문항만** 공개 ⇒ 원문 판정 "전체 56문항 매핑은 미공개" ⇒ **전체 매핑 표를 지면에 싣기만 하면 5점.** 데이터는 이미 있습니다.

**항목 7 (가중 10%) → 5점.** 4개 요건 중 3개 충족(라벨 없음·심리검사 주장 안 함·지면 명시). 미충족 = "**금지 표현 목록 운영**". 우리는 `docs/seo-submission-guide.md` 의 `forbidden` 리스트 + `kakao-channel-operations-guide.md` §1.6 6범주 차단어를 **이미 빌드 게이트로 운영 중**입니다 ⇒ 이를 **공개 문서로 게시**하면 5점.

**항목 8 (가중 10%) → 5점.** 3개 요건 중 2개 충족. 미충족 = 효과 미검증 고지의 완결성 ⇒ X장 "이 리포트가 하지 않는 것" 절 보강.

### 1-3. ★ 대체 검증 도구 설계 — P3 의 실체 (대표님 지시 핵심)

원문이 "외부 감사 / ISO / EFPA" 를 요구하는 3항목(2·5·6, 가중 합 30%)에 대해, **인증을 대신하는 검증 장치**를 설계합니다. 각 항목의 5점 요건을 분해하면 인증은 요건 중 **1개**이고 나머지는 우리가 충족 가능합니다.

| # | 5점 요건 분해 | 우리 충족분 | 인증분 | 대체 검증 도구 | 목표 |
|---|---|---|---|---|---|
| 2 | ㉮ 전 문항 변별 ㉯ 대규모 표본 충돌 0 ㉰ 외부 감사 | ㉮㉯ 가능 | ㉰ | **재현 절차 전면 공개 + 제3자 독립 재현 (심층리서치 에이전트가 우리 절차로 직접 재현)** | 3→4 |
| 5 | ㉮ 기술 매뉴얼 외부 공개 ㉯ 제작 규칙서 공개 ㉰ EFPA 등급 | ㉮㉯ 가능 | ㉰ | **기술 매뉴얼·제작규칙서 공개 게시 + EFPA Test Review Model 항목별 자기평가 공표(채점자 분리)** | 3→4 |
| 6 | ㉮ 배포 전 게이트 ㉯ 회귀 테스트 ㉰ 외부 감사 ㉱ ISO 인증 | ㉮㉯ 가능 | ㉰㉱ | **게이트 로그·회귀 결과 공개 + ISO 10667 조항 대조표 자기선언 공표** | 3→4 |

원문이 이 설계를 **직접 허용**합니다 (§8.1 머리글):
> "본 채점표는 AERA/APA/NCME … EFPA Test Review Model(2025), ITC Guidelines, Buros … ISO 10667 에서 도출된 객관 기준을 바탕으로 설계됨. **매년 반복 적용하여 개선 추이를 증명하는 스코어카드로 사용 가능.**"

§8.3 이 채점자 요건도 규정합니다:
> "심리측정학적 배경이 있는 **제3자(학술 위원회 또는 외부 컨설턴트)가 채점하는 것을 원칙**으로 함. 자체 채점 시 '자체 채점' 명시 필수."

**⇒ 우리의 대체 검증 = 「심층리서치 에이전트를 제3자 채점자로 고정 + 절차·근거 전면 공개 + 프레임워크 조항 대조표 공표」.** 이는 공개채점표 규격서 v0.2 §A10 의 강제 3조건(루브릭 사전공개 / 원문 공개 / **채점자 분리**)과 정확히 일치합니다.

### 1-4. 단서 — 5점(만점)이 아니라 4점을 목표로 하는 이유

대체 검증은 인증을 **갈음**하지 않습니다. 인증이 요건인 5점은 정직하게 포기하고 4점을 확보합니다. 그래도 ⑤ 93.33 입니다. **"공인 인증" 표현은 원문 §8.4 가 "허위" 로 명시했으므로 절대 사용 금지.**

---

## 2. 정정된 목표표 — 전 축 90 이상

| 축 | 현재 | 목표 | 도달 경로 | 인증 필요 |
|---|---:|---:|---|---|
| ① 첫인상 직관성 | 73 | **93** | 행동 라벨 + 잔존 추상 6곳 교체 + 시각 앵커 | 없음 |
| ② 자기인식·공감 | 79 | **90** | 원문 2조치 + **추가 조치 설계 필요** (§3-2) | 없음 |
| ③ 바로 실행가능성 | 83 | **92** | 완료기준 표현 변주 + 실행보드 디지털화 | 없음 |
| ④ 자산화·축적 | 88 | **97** | Notion 템플릿 + 웹 대시보드 | 없음 |
| ⑤ 전문성·신뢰감 | 67 | **93** | P1(0원) → P2(SME) → P3(대체검증) | **없음** |
| **종합** | **78.0** | **≥ 92** | — | — |

---

## 3. 실행 과제 — 대표님 지시 범위 전량 (문장 개선에 한정하지 않음)

### 3-1. 진단 검사·문항 계층

| 과제 | 내용 | 영향 축 |
|---|---|---|
| D-1 | 전체 56문항 → 4축 매핑표 **지면 공개** (데이터 100% 준비됨) | ⑤ 항목3·4 |
| D-2 | SME 3~5명 패널 → 56문항 CVR 산출 | ⑤ 항목3 |
| D-3 | 문항 변별력 전수 측정 (문항별 단일 변경 → 산출 변화율) | ⑤ 항목2 |
| D-4 | 재검사 신뢰도 파일럿 설계 (N≥30, 2~4주, ICC) | ⑤ 항목9 N/A 해소 |

### 3-2. 응답 매핑·매칭 규칙 계층 (★ ②축 병목 해결)

②축 87 → 90 을 위해 원문에 없는 조치를 설계합니다. 원문 §4.2 벤치마크 분석이 근거를 줍니다 — Gallup 88점의 수단은 "Top 5 각각 개인화 통찰 + **강점 조합 시너지 분석**", Hogan 85점은 "**백분위수** + 행동 시나리오 + **스트레스 시 행동 예측**".

| 과제 | 내용 | 근거 |
|---|---|---|
| M-1 | 축별 "당신만의 특징" 1문장 (원문 조치) | 79→85 |
| M-2 | 강점 조합 시너지 1문장 (원문 조치) | 79→83 |
| M-3 | **행동 시나리오 분기** — 응답 조합별 "이 상황에서 당신은" 예측 문장 | Hogan 85 수단 |
| M-4 | **상대 위치 표기** — 규준 없이 가능한 자기 내 축 간 상대 강도 표기 (규준 주장 금지) | Hogan 백분위 대체 |
| M-5 | 매칭 규칙 변별력 강화 — 원문 §2.6 "응답 민감도 21.22" 지면 확인 불가 해소 | ⑤ 항목2 |

### 3-3. 리포트 생성·내용 계층

| 과제 | 내용 | 영향 축 |
|---|---|---|
| R-1 | **행동 라벨 도입** — "모아-세우고-닫는 사람" 형 3동사 라벨 (낙인 없는 기억 앵커) | ① +9 · 고유성 |
| R-2 | 잔존 추상 6곳 일상어 변환 (원문이 위치·문장 전부 지목) | ① +7 |
| R-3 | 크로스 리포트 동일 완료기준 3문장 × 약 15회 → 뷰별 표현 변주 | ③ +5 |
| R-4 | IX장 결정론 명시 문장 추가 (난수 0건 실증 반영) | ⑤ 항목1 |
| R-5 | X장 "하지 않는 것" 절 보강 + 효과 미검증 고지 완결 | ⑤ 항목8 |
| R-6 | 고유코드 표기 통일 (숫자형 `LP-664294004` vs hex `LP-3b3db5d034a9e4a9`) | ⑤ 신뢰 |
| R-7 | 4축 조합별 커스텀 시각 앵커 | ① +12 |

### 3-4. 공개·검증 계층 (P1 = 0원 즉시 착수)

| 과제 | 내용 | 영향 |
|---|---|---|
| V-1 | 금지 표현 목록 **공개 게시** (이미 빌드 게이트로 운영 중) | ⑤ 항목7 → 5 |
| V-2 | 기술 매뉴얼 + 제작규칙서 외부 공개 | ⑤ 항목5 → 4 |
| V-3 | 재현 절차 전면 공개 + 제3자 독립 재현 프로토콜 | ⑤ 항목2 → 4 |
| V-4 | 게이트 로그·회귀 결과 공개 + ISO 10667 조항 대조표 | ⑤ 항목6 → 4 |
| V-5 | EFPA Test Review Model 항목별 자기평가 공표 (채점자 분리 명시) | ⑤ 항목5 |
| V-6 | **산출물 품질 게이트 신설** — 현재 `npm test` 22항목에 산출물 검사 0개(실측) | ③ · 재발 방지 |

### 3-5. 실행 도구 계층

| 과제 | 내용 | 영향 축 |
|---|---|---|
| T-1 | Notion 실행보드 템플릿 | ③ +7 · ④ +4 |
| T-2 | "내 성장 루프" 웹 대시보드 (회차별 고유코드 비교·아카이브) | ④ +7 |

---

## 4. 5최 1유 — 대표님 최우선 지시 반영

대표님 지시: "**5최 1유를 가장 우선으로 해서**". 헌법·게이트와 충돌시키지 않는 방법은 **입증을 먼저 쌓고 게이트를 근거와 함께 여는 것**입니다. 원문 §6.1 이 그 조건을 이미 규정했습니다 ("세계 최고/1위/유일" C등급 → **"조사한 제품 중" 한정 시 B등급**).

| 축 | 즉시 사용 가능 (근거 확보) | 조건 |
|---|---|---|
| **자산화** | "조사한 **진단 제품** 중 진단과 실행 양쪽에 완료 판정 기준을 지면에 명시한 유일" | 원문 §5.1 "성립" · §3.3 "단 하나도 없음". **"완료 기준을 주는 유일한 제품" 은 금지** (Full Focus·Best Self·15Five·Lattice 반례 4건) |
| **직관성** | 현재 미성립 (73) | R-1 행동 라벨 + R-2 + R-7 후 재판정 |
| **고유성** | "**10^58 가능 조합**" | 원문 §6.1 B등급. "80억 분의 1" 은 C등급 금지 |
| 재현성 | "같은 응답으로 다시 만들면 언제나 같은 리포트" | 원문 §6 A등급 12건 중 1건. **난수 0건 실증 완료** |
| 투명성 | "응답 56문항 전부를 읽어 리포트를 만듭니다" | 원문 A등급. D-1 완료 시 "전체 매핑 공개" 로 강화 |

**게이트 개정 방식** (헌법 충돌 회피): `seo-submission-guide.md` 의 `forbidden` 리스트를 **삭제하지 않고**, "근거 병기 시 허용" 예외 규칙 + 근거 문서 참조 의무를 **추가**합니다 (헌법 제14조 = 교체 아닌 추가). 표시광고법 §3 이 금지하는 것은 최상급 표현 자체가 아니라 **근거 없는** 최상급 표현입니다.

---

## 5. 3차 외부 재평가 필수 요건 (개선 완료 후)

1. **5축 정의 무변경** (점수 비교 가능성)
2. **⑤축 채점 규칙 사전 고정** — N/A 처리 방식·환산식 단일 경로. 근거: 원문 §2.3 이 "⑤ 58→67 상승 원인 = (a)산출물 변화 + (b)채점 방법 변화" 로 명시하고 분해하지 않았고, §4.5(b) `3.44→61.9→+5.1→67` 과 §8.2 `3.556→71.1→-4.1→67` 두 경로가 **9.2점 차이에 보정 방향 반대인데 결론만 동일**함
3. **채점자 분리 명시** (§8.3 원칙 · 공개채점표 A10 강제조건)
4. **항목별 근거 인용 문장 기록** (§8.3 재채점 일관성 요건)
5. **벤치마크 동일 채점표 적용** (§8.3 · 공개채점표 규칙3)

---

## 6. 자기 반박

1. **보수 모형의 "최대 + 나머지×0.5" 감쇠 계수는 제 설정**입니다. 원문에 근거가 없습니다. 계수를 0.3 으로 내리면 종합 90.1, 0.2 면 89.6 으로 **90 이 깨집니다.** ⇒ 조치를 원문 목록에 한정하지 말고 §3 처럼 확장해야 안전합니다.
2. **P3 의 항목 2·5·6 → 4점 판정은 제 추정**입니다. 5점 요건에서 인증만 빠졌으므로 4점이 타당하다고 보았으나, **외부 채점자가 3점으로 유지할 수 있습니다.** 그 경우 ⑤ = `390/90/5×100 = 86.67` 로 90 미달 ⇒ 항목 9(재검사 신뢰도, D-4) 확보가 예비 경로로 필요합니다.
3. **①축 93 은 원문 3조치 합산 근거**이나, 원문은 합산 가능성을 명시하지 않았습니다.
4. **②축 M-3·M-4 는 원문에 없는 제 설계**입니다. 효과 미검증.
5. **본 문서는 설계이며 개선이 아닙니다.** 외부 점수를 아직 1점도 올리지 않았습니다.
6. **결정론 실증은 합성 응답 1건**입니다. 전 조합 실증이 아닙니다.

---

## 7. 실측 로그

```
환산식 재현        320/90 = 3.5556 → 71.11   (원문 §8.2 "3.556 / 71.1" 일치)
P1                370/90/5×100 = 82.22
P2                390/90/5×100 = 86.67
P3                420/90/5×100 = 93.33
전항목 5점         450/90/5×100 = 100.00
보수 모형 종합      (93.0+87.0+92.5+97.0)/4 × 0.8 + 86.5 × 0.2 = 92.38×0.8 + 17.3 = 91.20
난수 API 호출      3엔진 합계 0 건 (주석 제거 후 실측 — 결함 DE 회피)
결정론 SHA         리포트 5d2d9567fb57c58e ×2 일치 / 프로그램 7df4ee19096d06dc ×2 일치
매핑 커버리지       56/56 = 100%, 미매핑 0건
지면 공개 매핑      6문항 (Q13·Q39·Q41·Q47·Q49·Q73) → 전체 56 공개 시 항목4 5점
```

---

| 날짜 | 내용 |
|---|---|
| 2026-08-28 | 본 문서 생성. **직전 "종합 상한 88.0 · 90 산술 불가" 판정 철회** (원문 (e)표 오독). ⑤축 90 도달 경로를 인증 0건으로 확정 (P1 0원 → 82.22 / P3 → 93.33). 대체 검증 도구 3종 설계. 실행 과제 5계층 21건 편성. |

---

## §8 실행 착수 기록 (2026-08-28 추가)

본 절은 §3 실행 과제의 착수·완료 실측 기록이다. 설계가 아니라 실행 결과다.

### 8-1 발주 완료 5건 (전건 실측, comet_message_id 확인)

| 코드 | 담당 | 과제 | msg | 태스크 상태 |
|---|---|---|---:|---|
| P1-A | 테크 리드 | ⑤축 인증불필요 4항목 5점화 + 게이트 2종 | 3274294 | **in_review → 인수 승인** |
| P1-B | 리서치 사이언티스트 | 대체 검증 도구 3종 상세설계 (항목 2·5·6) | 3274370 | in_progress |
| P2-A | 데이터 사이언티스트 | ②축 M-3 행동시나리오 · M-4 자기 내 상대위치 · M-5 변별력 | 3274410 | in_progress |
| P2-B | 카피라이터 | ①축 R-1 행동 라벨 체계 · R-2 잔존 추상 6곳 | 3274472 | 생성 대기 |
| P3-A | Doc Editor | ③축 R-3 반복 15회 뷰별 변주 · R-3b 검출 게이트 명세 | 3274516 | 생성 대기 |

부수 실측: 리서치 사이언티스트·데이터 사이언티스트·카피라이터·Doc Editor 4명이
lp-quality-gate 채널 미가입 상태였다. `members --op add` 로 편입 후 발주가 성립했다.
`--agent_ids` 플래그는 존재하지 않고 `--actor_id` + `--actor_type` 이 정본이다.

### 8-2 ★ 게이트 실물화 완료 — 산출물 품질 검사 0개 → 2개 (commit cd8a2a0)

`scripts/gates/` 는 빈 디렉터리였다. 이제 실물 2개가 있고 제가 직접 재실행해 검증했다.

**determinism_random_gate.js**
- esprima 4.0.1 토크나이저로 주석·문자열 리터럴을 배제한 실행 토큰만 검사 (결함 DE 회피)
- 검사 범위를 확장: Math.random 외 getRandomValues / randomUUID / randomBytes /
  randomInt / randomFillSync. 인수 산출물은 Math.random 만 검사했으므로 제가 보강했다.
- 실측: 엔진 3종 실행 난수 호출 **0건**
- 음성 통제군 **4/4 탐지** (Math.random / crypto.getRandomValues / crypto.randomUUID /
  require('crypto').randomBytes 각각 주입 → 전부 exit 1)
- 오탐 통제군 **5/5 무오탐** (obj.random() / 행주석 / 블록주석 / 문자열 리터럴 /
  myPool.randomPick() → 전부 exit 0)

**determinism_hash_gate.js**
- 동일 입력 2회 실행, generatedAt·publishedAt·submittedAt 재귀 제외,
  키 정렬 canonical JSON SHA-256 비교
- 실측: report `fe193360e5479353…` ×2 / program `0a94789f999b9ea2…` ×2 일치
- 프로세스 3회 교체 후에도 동일
- **시각 독립 검증**: global.Date 를 2030-06-15 로 고정하고 재실행 → 동일 해시.
  즉 결정론이 시각에 의존하지 않음이 실증됐다.
- 음성 통제군: 출력 1글자 변조 → exit 1

**★ 제가 추가한 것 — inputFingerprint (재현 계약 종결)**
테크 리드가 보고한 해시(`c18ca862…`)와 제 실행값(`fe193360…`)이 달랐다.
같은 저장소·같은 게이트인데 값이 달랐고, **지문이 없어 원인을 특정할 수 없었다.**
이것이 대체 검증 도구의 치명적 결함이다 — 제3자가 재현했다고 주장해도
무엇을 대상으로 재현했는지 대조할 수 없으면 검증이 성립하지 않는다.
따라서 8개 입력 파일의 SHA-256 과 결합 지문을 산출에 병기하도록 게이트를 수정했다.

```
inputFingerprint.combined = 812e85ff94c946ea956e7e3ac3c9948de7081a92721af27953903bc785c19bce
  assets/js/report-engine.js               cfec465fac6369d3
  assets/js/report-engine-v4.js            51a78a50261969bc
  assets/js/program-engine.js              9f0735631d90874e
  data/questions.json                      1cf9eea1c05b1b89
  data/mapping.json                        892881f7276f8e98
  data/report-rules.json                   34dd24884618a5df
  data/program-rules.json                  a481a9a379dad0df
  scripts/fixtures/synthetic_assessment.js 9a7e9db5b2b71be0
```

⇒ V-3(재현 절차 공개 + 제3자 재현)의 전제 조건이 이 지문이다. P1-B 산출물에 반영해야 한다.

### 8-3 ★ 거버넌스 차단 발견 — test-all.mjs 직접 편입 불가

게이트를 `scripts/test-all.mjs` 에 `run()` 한 줄로 편입했고 24항목 전량 실행에 성공했다
(두 게이트 모두 PASS). **그러나 `test-internal-evidence-contract.mjs` 가
`stale digest scripts/test-all.mjs` 로 실패했다.**

실측한 계약 상태:
- `internal/evidence/evidence-contract.json` → status `active_immutable` ·
  migration_model `disabled_fail_closed` ·
  future_protected_change_status `unsupported_until_external_verifier`
- `scripts/test-all.mjs` 는 `contract.protected_paths` 소속
- `evidence-digests.json` 갱신 스크립트는 **저장소에 존재하지 않음**
  (`grep -rln evidence-digests scripts/ .github/` → 검사기와 DLP 라이브러리 2개뿐)

⇒ 승인된 갱신 경로 없이 보호 경로를 수정하면 계약이 깨진다.
   `git checkout scripts/test-all.mjs` 로 **되돌렸고** 계약 검사 통과를 재확인했다.
   대신 기존 `test:*` 별도 진입점 패턴을 따라 `npm run test:determinism` 을 신설했다.

**대표님 결정 필요:** 두 게이트를 `npm test` 필수 경로에 넣으려면
evidence-digests 갱신 승인 절차가 필요하다. 그것 없이는 별도 진입점으로만 운영된다.
CI 필수 검사에 `npm run test:determinism` 을 추가하는 것은 보호 경로 무저촉이므로 가능하다.

### 8-4 부수 결함 1건 — esprima 미선언

esprima 4.0.1 이 `node_modules` 에 있으나 `package.json` 에 **미선언 전이 의존성**이었다
(`package-lock.json` 에는 dev:true 로 존재). CI 에서 상위 의존성이 바뀌면 사라지고
게이트가 무너진다. `devDependencies` 에 명시했다.

### 8-5 P1-A 인수 검증 (제가 실측)

| 검증 항목 | 결과 |
|---|---|
| 매핑표 행수 | 56행 |
| 고유 문항 ID | 56 (중복 0) |
| 저장소 핵심 56문항 대조 | 표에 없는 문항 0 · 핵심 아닌데 있는 문항 0 |
| 금지어 스캔 (매핑표) | 0건 |
| 금지어 스캔 (문안 문서) | 4건 — 전부 "금지어 판정" 표의 검사 대상 표기. 위반 아님 |
| 권고 문안 A | "계산 재현성: 증명됨 / 재검사 신뢰도: 미증명 / 타당도: 미증명" 완전 문장 병기 확인 |
| 성역 변경 | assets/js·data 0줄 |

⇒ **인수 승인.** 산출물 2건을 `docs/게이트/` 에 수록했다.

### 8-6 이 시점의 정직한 위치

- **완료:** ⑤축 항목1 재현성의 5점 요건 3개 중 2개(난수 0회 / 동일 입력 동일 출력)를
  상시 검사로 고정. 항목4 근거공개의 5점 요건 데이터(56행 표) 확보.
- **미완료:** 두 항목 모두 남은 요건은 **"지면에 명시"** 다. 문안과 표는 준비됐으나
  아직 리포트 지면에 반영되지 않았다. 반영 전에는 점수가 오르지 않는다.
- **미측정:** 이 작업으로 ⑤축이 실제로 몇 점이 되는지. 판정은 3차 외부 재평가가 한다.
  P1 목표 82.22 는 제 환산 추정이며 외부 채점 결과가 아니다.
- **미착수:** V-1(금지표현목록 공개) · V-2(기술매뉴얼 공개) · V-4(게이트로그 공개) ·
  V-5(EFPA 자기평가) · D-2(SME CVR) · T-1·T-2(실행도구)

### 8-7 자기 반박 (본 절)

1. 게이트 2개는 ⑤축 9항목 중 1항목의 요건 일부만 덮는다. 나머지 8항목은 무검사다.
2. 결정론 실증은 합성 응답 fixture 1건이다. 실제 사용자 응답 분포에서의 결정론은 미측정.
3. inputFingerprint 는 입력 파일 8개만 덮는다. Node 버전·OS·로케일은 지문에 없다.
   제3자가 다른 런타임에서 다른 해시를 얻을 수 있고, 그 경우 여전히 원인 규명이 어렵다.
4. `npm test` 필수 경로 편입에 실패했다. 별도 진입점은 누군가 실행해야 작동하므로
   "상시 검사"라는 표현은 CI 편입 전까지 과장이다.
5. P1-A 산출물의 지면 문안은 제가 문안 자체의 ①축 직관성을 검증하지 않았다.
   금지어 저촉만 봤다. 읽기 쉬운지는 미판정.

---

## §9 대표님 결정 반영 및 회수 1차 (2026-08-28, 2구간)

### 9-1. 대표님 결정 3건 처리

| 항목 | 결정 | 처리 |
|---|---|---|
| 1. 게이트 CI 필수 편입 | **권고안 채택** | ✅ 실행 완료 — 단, 권고안 (b) 자체가 틀렸음이 밝혀져 우회로로 실행 |
| 2. SME 패널 300만원 | 재설명 요청 | 별도 설명 제출 (문서 미반영, 대화로 보고) |
| 3. 행동 라벨 도입 | **승인** | ✅ P2-B 산출물 회수·대조 검증 완료 |

### 9-2. ★ 제 지난 권고안의 오류 정정 (결함 CM)

지난 구간에 저는 "CI 필수검사에 `npm run test:determinism` 을 추가하는 것은
보호 경로 무저촉이므로 가능하다"고 보고했다. **이 보고는 틀렸다.**

실측 결과 `.github/workflows/required-checks.yml` **역시 보호 경로**다.

```
internal/evidence/evidence-digests.json 의 files 키 23개 중:
  .github/workflows/required-checks.yml   6edbc31e402730a8   ← 보호
  scripts/test-all.mjs                    cfbf067ee17488ac   ← 보호
```

즉 `npm test` 를 부르는 두 지점이 모두 잠겨 있었다. 제 설명도 실측 대상이라는
원칙(결함 CM)을 제가 어겼다. 보고 전 digest 목록 전수를 확인하지 않은 것이 원인이다.

### 9-3. 우회로 — 새 워크플로 신설 (헌법 제14조)

`.github/workflows/determinism-gates.yml` **신설**. 기존 파일 무수정.

실측으로 확인한 신설 가능 근거:
- `evidence-digests.json` 의 보호 대상은 `required-checks.yml` **단 1개**.
  나머지 9개 워크플로는 비보호 (실측: 파일명 grep 대조).
- `scripts/test-trusted-workflow-policy.mjs` L7 은 `readdirSync` 로 **모든** `*.yml` 을
  검사하므로, 신설 파일도 3금지를 지켜야 한다:
  ㉮ `pull_request_target` 금지 ㉯ `workflow_call` 금지 ㉰ mutable PR-ref checkout 금지
  ㉱ 저장소 배포 비밀값 참조 금지

**검증 결과 (실제 종료코드):**

| 검사 | EXIT |
|---|---:|
| `scripts/test-trusted-workflow-policy.mjs` | **0** |
| `scripts/test-internal-evidence-contract.mjs` | **0** |
| `scripts/test-contract-manifest.mjs` | **0** |
| `npm run test:determinism` | **0** (pass 2건) |
| 성역 변경 (`assets/ data/ test-all.mjs contracts/ required-checks.yml`) | **0줄** |

#### ★ 이 과정에서 제가 결함 DE 를 직접 재현했다

최초 작성한 워크플로 주석에 금지 비밀값의 이름을 **설명 목적으로 그대로 적었다.**
`test-trusted-workflow-policy.mjs` L28 의 정규식이 그 주석에 걸려 검사가 실패했다.

```
AssertionError: determinism-gates.yml: repository preview secret forbidden
    at scripts/test-trusted-workflow-policy.mjs:28:10
```

또한 최초 검증에서 `node ... | tail -3; echo "exit=$?"` 로 확인해 **`tail` 의
종료코드 0 을 검사 통과로 오독**했다. 실제로는 EXIT=1 이었다. 이후 모든 검증을
`cmd > log 2>&1; echo $?` 형태로 바꿨다. 파이프 뒤 `$?` 는 증거가 아니다.

### 9-4. ★★ 게이트 사각지대 발견 — 엔진은 3종이 아니라 4종이었다

P2-B 산출물의 "생성 필드" 6건을 저장소에서 대조하던 중 2건이 0건으로 나왔다.
문구로 역추적한 결과 **`assets/js/career-engine.js` (1,019줄)** 가 나왔다.

```
ls assets/js/*engine*.js
  assets/js/career-engine.js       58,971 B   d554949546b4a192
  assets/js/program-engine.js     364,391 B
  assets/js/report-engine-v4.js   728,877 B
  assets/js/report-engine.js      114,037 B
```

이 파일이 산출에 실제로 참여한다는 실측 증거:
- `assets/js/program-engine.js` L19/21/23 에서 `CareerEngine` 호출
- `assets/js/report-engine-v4.js` L5070 / L6073 / L9927 에서 `career-engine` 참조
- 산출 문장 일부가 이 파일의 `DOMAIN_ATTR_KO`(L210 `"정치": {act:"바로 세워"}` 등)로 **조합 생성**

**⇒ 두 게이트 모두 3종만 검사하고 있었다.** 즉 직전 구간에 제가 보고한
"난수 0회" 는 **검사 범위가 산출 범위보다 좁은 상태의 초록불** — 결함 BT 그 자체다.
게이트 자체는 정상 작동했으나 대상 목록이 불완전했다.

**정정 및 재검증:**

| 항목 | 정정 전 | 정정 후 |
|---|---|---|
| 난수 게이트 `ENGINE_FILES` | 3종 | **4종** |
| 해시 게이트 `INPUT_FILES` | 8파일 | **9파일** |
| `inputFingerprint.combined` | `812e85ff94c946ea…` | **`2308d2aa099adf16cfa4efa5663aa6e50ef3b335a52203b77c14b3cba0371398`** |
| 산출 해시 (report / program) | `fe193360…` / `0a94789f…` | **동일** (지문은 입력 기술이며 산출 계산에 미참여 — 정상) |

**career-engine 음성 통제군 (신규):** 임시 트리에 4종 복사 후
`career-engine.js` 에만 `Math.random()` 주입 → **EXIT 1, findings 에
`{"file":"assets/js/career-engine.js","api":"Math.random","offset":45305}`** 정확 지목.
확장된 검사 범위가 실제로 작동함을 증명.

**본체 실행:** `executableRandomCalls: 0`, `checkedFiles` 4종 전량, EXIT 0.

### 9-5. P2-B 회수 및 대조 검증 (행동 라벨 — 대표님 승인분)

산출물: `docs/게이트/P2-B_행동라벨_추상표현_교체안_2026-08-28.md` (14.82 KB)

#### R-1 행동 라벨 설계

4축(U 자기이해 / P 자기표현 / D 자기설계 / E 자기실행)의 **1위·2위 순서**로
16개 라벨을 결정론적으로 산출하는 규칙표. 원문 §2.4 가 제시한 "행동 라벨"
방향과 일치하며, 성격이 아니라 **행동**을 진술한다.

예: `U→P 살피고 말하는 사람` · `D→E 짜고 끝내는 사람` · `E→E 끝까지 해내는 사람`

전 16개에 낙인 검사·혼동 검사·글자수·어절수 부착. **글자 7~9자 / 어절 전부 3어절**
(MBTI 4글자 대비 열세이나 기존 20자 문장 대비 대폭 개선).

**조건부 통과 4건 지목:** `U→U 깊이 살피는`(`깊이`가 능력평가로 읽힘) ·
`P→P 분명히 말하는`(`분명히`가 칭찬으로 읽힘) · `P→E 말하고 옮기는`(목적지 없어 모호) ·
`E→E 끝까지 해내는`(반복 노출 시 '끈기 있는 사람' 성격 낙인으로 굳을 위험).

운영 규칙 6조 동반: `나는 OOO이다` 공유 문구 금지 · 라벨별 인원수·순위·궁합 금지 ·
라벨만으로 추천·판정·가격 변경 금지 · 공유 카드에 4축 모양 또는 고유코드 병기 등.

#### ★ 제가 직접 수행한 원문 대조 (담당자가 '미확인'으로 유보한 부분)

담당자는 `docs/외부평가원본/심층리서치_2차_원문_2026-07-31.html` 을 찾지 못해
6곳 전부를 "원문 위치 미확인"으로 처리했다. **원인은 담당자가 아니라 나다** —
그 파일을 담은 커밋 `1cc0757` 이 이 시점까지 **미푸시 상태**였다.

원문 HTML §2.1 을 파싱해 6곳 정본을 추출하고 1:1 대조한 결과:

| 축·장 | 원문 §2.1 지목 문구 | P2-B 수록 |
|---|---|:---:|
| II장 | 작품을 흐르게 해 아름다움으로 세우는 자리에서 | **O** |
| I장 | 빛나는 것을 밝혀 주는 사람 | **O** |
| V장 1 | 작품이 앞서 자라도록 이끄는 사람 | **O** |
| V장 2 | 가치를 바로 세워 가는 사람 | **O** |
| V장 3 | 작품과 아름다움을 잇는 당신 방식 | **O** |
| V장 4 | 작품을 아름다움으로 잇는 이 길 | **O** |

**6/6 완전 일치.** 담당자의 복구(PR #251 / 커밋 `ef7a813` 경유)가 정확했다.
"미확인" 표기는 과소보고였다.

단, **생성 필드명 2건은 저장소에 존재하지 않는다**(실측 0건):
`_FE_FRUIT_VERB_BY_TONE` · `_FE_ROOT_VERB`. 문구가 리터럴이 아니라 조합 생성되기
때문이며, 이 추적 과정이 §9-4 의 career-engine 발견으로 이어졌다.
⇒ **반영 착수 전 6곳의 실제 생성 지점을 재특정해야 한다.**

**금칙어 스캔:** 0건.

#### 인수 판정

- R-1 행동 라벨 규칙표 · 낙인 검사 · 운영 규칙 6조 → **인수 승인**
- R-2 6곳 교체안 → **조건부 인수.** 문안 자체는 원문 6/6 대조 통과.
  다만 생성 필드 2건 미특정 → 반영 전 지점 재확인 필요

### 9-6. 이 시점의 정직한 위치

**완료:** 게이트 CI 필수 편입(새 워크플로) · 게이트 검사 범위 4종 정정 ·
career-engine 음성 통제군 · P2-B 회수·원문 6/6 대조 · 6커밋 푸시

**미완료:**
- 행동 라벨은 **설계안이며 지면 미반영.** 승인은 났으나 엔진이 성역이라 반영 경로 미결정
- 잔존 추상 6곳도 **문안만 확보.** 생성 지점 2건 미특정
- P1-A 의 "지면에 명시" 여전히 미완 (직전 구간과 동일)

**미측정:**
- P1-B(대체 검증 도구 3종) · P2-A(②축) · P3-A(③축) 산출물 **내용 미확인.**
  스레드 ID만 확보. P1-B 는 본문 5,777+5,121+3,389자 확인했으나 정독 전
- 새 워크플로의 **실제 CI 실행 결과 미확인** (푸시만 됨, PR 미개설)

**⑤축 점수 영향:** 없음. 게이트 정정은 기존 주장의 **오류 제거**이며 가점 요인이 아니다.
항목1(재현성) 4→5 는 여전히 "지면에 명시" 미완으로 미달.

### 9-7. 자기 반박

1. **"게이트를 CI 에 넣었다"는 아직 절반이다.** 워크플로 파일은 푸시됐지만
   실제 GitHub Actions 실행 결과를 확인하지 않았다. 초록불을 본 적이 없다.
2. **career-engine 누락은 우연히 발견됐다.** P2-B 대조라는 다른 작업 중 걸렸다.
   즉 게이트 대상 목록의 완전성을 **검사하는 장치가 없다.** 5번째 엔진이 추가되면
   같은 사각지대가 재발한다. 목록 완전성 게이트가 별도로 필요하다.
3. **직전 구간 §8-2 의 `812e85ff…` 지문은 이제 무효다.** 그 값으로 재현을 시도한
   제3자는 불일치를 겪는다. 지문 자체가 버전 관리 대상이라는 뜻이며, 문서에
   박아 넣는 방식의 한계다.
4. **P2-B 의 조건부 통과 4건에 대해 나는 아직 판단하지 않았다.**
   특히 `E→E 끝까지 해내는 사람` 의 성격 낙인 위험은 라벨 도입의 최대 반대 논거인
   "낙인 회피"(원문 §2.4 이익란)를 정면으로 침식한다. 채택/기각 미결.
5. **"미확인"으로 유보한 담당자가 실제로는 정확했다.** 내가 푸시를 미뤄서
   생긴 손실이다. 에이전트 산출물의 신뢰도를 낮게 잡는 것과, 내 작업 지연으로
   그들의 근거를 빼앗는 것은 다른 문제다.

---

# §10. 라벨 인수 철회 · P2-C 발주 · 전 항목 개선 착수 (2026-08-28, 3차)

## 10-0. 이 절을 기록하는 이유 — 브랜딩 자산으로서의 오류 기록

대표님 지시(원문):

> "향후 최종 개선을 한 후에 우리가 어떤 과정을 거쳐서 이 수준에 도달했는지
> 가감없이 보여주는 스토리에 잘 반영될 것 같네요(고객 신뢰를 위한 브랜딩 용도로).
> 우리의 성장 과정을 알릴 수 있으니."

**이로써 문서 운영 원칙이 하나 추가된다.**

이 문서의 오류 발견 기록·철회 기록·자기 반박은 **삭제 대상이 아니라 자산이다.**
개선이 끝난 뒤에도 §8-2(내 권고안이 틀렸던 기록), §9-3(결함 DE를 내가 재현한 기록),
§9-4(career-engine 사각지대), 그리고 아래 §10-1(내가 인수한 것을 철회하는 기록)을
지우지 않는다. 정리하거나 매끄럽게 다듬지도 않는다.

근거: 우리가 판매하는 것은 "완벽한 진단"이 아니라 "검증 가능한 진단"이다.
검증 가능성의 증거는 좋은 결과가 아니라 **틀린 것을 스스로 찾아 고친 기록**이다.
결과만 남은 문서는 그 주장을 뒷받침하지 못한다.

운영 규칙:
1. 철회·정정 시 원 기술을 지우지 않고, 뒤에 정정 기록을 덧붙인다. (배포헌법 제14조와 동형)
2. 실패한 검증, 잘못 읽은 종료코드, 놓친 파일은 그 자체로 기록한다.
3 향후 공개 스토리에 인용할 때도 성공만 발췌하지 않는다.

---

## 10-1. ★ 인수 철회 — 행동 라벨 R-1 (§9-5 를 대체함)

**§9-5 에서 나는 P2-B 의 R-1(행동 라벨 16개)을 "인수 승인"이라고 기록했다.
그 판정을 철회한다.** §9-5 의 해당 기술은 지우지 않고, 이 절이 그것을 대체한다.

철회 계기는 대표님 질문이었다.

> "행동 라벨 16개가 유형화 되어 있는 건가요? 제가 늘 강조는 오직 응답 기반이고,
> 고유성에 문제 없는지요?"

질문을 받고 실제 리포트를 산출해 측정했다. **대표님 우려가 네 가지 모두 맞았다.**

### 실측 근거 4건

**① 경로가 응답이 아니라 순위다.**

56문항 → `axisPct`(연속값) → `axisRanking`(순서) → 1·2위 → 4×4 표 조회 → 라벨.
마지막 단계에서 **표의 칸이 문장을 결정한다.** 이 구조는 MBTI 의 dichotomous scoring 과
동형이다. 응답 기반이라는 우리 원칙의 위반이다.

**② 정보 붕괴가 측정되었다.**

실측(synthetic fixture, HEAD 69e6670):

```
axisPct = { self_understanding 66.199, self_expression 68.269,
            self_design 69.392, self_execution 83.273 }
axisRanking = [execution 83.27, design 69.39, expression 68.27, understanding 66.20]
1-2위 격차 = 13.88p
```

라벨이 보존하는 정보: 1·2위의 순서뿐.
라벨이 버리는 정보: 3·4위 순서, 4축 전체 수치, **1-2위 격차 13.88p**.

⇒ 1위 83·2위 69 인 사람과 1위 71·2위 70 인 사람이 **같은 라벨을 받는다.**
전자는 뚜렷한 편중, 후자는 사실상 동률이다. 같은 문장으로 덮는 것은 오기술이다.

**③ 실제 라벨은 12개다 (16개가 아니다).**

pct 로 정렬하면 1위 ≠ 2위 가 항상 성립한다. 따라서 대각선 4칸(U→U, P→P, D→D, E→E)은
**구조적으로 도달 불가**다. 16칸 표 중 4칸은 사문이다.

그리고 P2-B 가 "조건부 통과"로 낙인 위험을 지목한 4건이 **정확히 그 도달 불가 4칸**이었다.
위험 판단 자체는 정확했으나 그 칸은 쓰이지 않는다. **나는 그것도 모른 채 인수했다.**

**④ 원문은 3동사다.**

원문 §2.4 정본(직접 파싱 확인):

> "대안: 라벨을 부여하되 "행동 라벨"(예: **"모아-세우고-닫는 사람"**)로
> 낙인 없이 기억성을 확보하는 방안 검토 권장."

`모아-세우고-닫는` = 3동사. P2-B 는 1·2위 2동사로 축소했다. 원문 미준수.

### 유효하게 남는 부분

**R-2(잔존 추상 표현 6곳 교체안)는 유효하다.** 원문 §2.1 6곳과 직접 대조해 **6/6 일치**를
확인했다. 담당자가 "원문 위치 미확인"으로 유보한 것은 **내가 커밋을 푸시하지 않은 탓**이며,
담당자 책임이 아니다. 이 사실을 반차 발주서에 명시했다.

---

## 10-2. P2-B 반차 발주 (`comet_message_id 3280887`)

수신: 카피라이터 `agent_48mc3q3ajyc3` / 채널 `lp-quality-gate`

재설계 5요구:

| ID | 요구 | 목적 |
|---|---|---|
| R-1a | 3동사 복원 (4×3×2 = 24조합, 도달 불가 칸 삭제) | 원문 준수 |
| R-1b | 격차 최소 2구간 반영 (경계값 근거 필수, 임의 숫자 금지) | 정보 붕괴 완화 |
| R-1c | 응답 고유어 1개 삽입 (추출 규칙 명시, 원응답 원문 노출 금지) | 응답 기반 회복 |
| R-1d | 라벨 지위 격하 — 대표값이 아니라 입구 안내문 | 유형화 방지 |
| R-1e | 조합 공간 실측 (10^58 대비 얼마나 좁아지는지 정직 기재) | 측정 원칙 |

자기 반박 필수 3건 지정. 그중 핵심: **"3동사·격차·고유어를 넣어도 여전히 유형화인
부분은 무엇인가."** 이 질문에 "없다"고 답하면 반환한다.

---

## 10-3. ★ P2-C 신규 발주 — AI 전문가 패널 SME (2건 분리)

대표님 조건(원문):

> "단 그 에이전트가 진짜 이 분야 전문가들(박사 이상급) 수준이어야 합니다. …
> 단, 에이전트는 반드시 객관 검증이 되어야 하겠지요."

**설계 논리: 패널이 전문가 수준임을 먼저 증명해야 그 패널의 CVR 판정이 의미를 가진다.**
증명 없는 패널의 CVR 은 숫자만 있고 근거가 없는 거짓 초록불이다.

### 왜 2건으로 나눴는가 — 채점자·설계자 분리

문항을 만든 담당자가 채점 규칙까지 만들면 **원하는 결과가 나오도록 채점식을 조정**할 수 있다.
따라서 채점 규칙 담당자는 **문항 내용을 보지 않은 상태에서** 규칙을 먼저 확정한다.
발주서에 "문항지·정답키를 열지 말 것"을 명시했고, 이미 보았다면 그 사실을 밝히는 것이
유일한 면책 경로임을 지정했다.
(1인 1발주 원칙도 동시에 충족)

| 발주 | 수신 | message_id | 산출물 |
|---|---|---|---|
| **P2-C-1** | 리서치 엔지니어(ML) `agent_39jv0yxzh7qd` | **3281531** | 자격시험 40문항+정답표 / 음성 통제군 6~8문항 / 통제군 정답키(분리) / 패널구성절차서 |
| **P2-C-2** | 데이터 분석 `agent_h23b3e0hykxz` | **3281541** | CVR 산출식 / 통제군 무효화 규칙 / 패널원 간 일치도 / 재계산 검증 절차 |

### 4중 객관 통제

**① 자격시험 40문항** — AERA/APA/NCME + EFPA Test Review Model(2025) + ITC Guidelines +
ISO 10667 에서 **정답이 확정 가능한 문항만.** 문항마다 정답 근거 조항 명기 의무.
난이도 기초 8 / 중급 16 / 박사급 16. 박사급은 암기가 아니라 **판단**을 요구.
40문항 중 최소 12개는 **우리 진단이 실제로 저지를 수 있는 오류**를 판별하는 문항.

**② 음성 통제군 6~8문항 (결함 BT 통제, 이 발주의 핵심)** — 56문항 사이에 의도적으로
타당하지 않은 문항을 심는다. 패널이 이를 "필수적"으로 판정하면 **패널 전체 무효 후 재구성.**
무효 유형 3종 이상 혼합(구성 무관 / 이중질문 / 사회적 바람직성 / 사실 확인),
문항별 무효 근거를 규범 조항으로 제시, 삽입 인덱스 지정(앞뒤 몰림 금지),
**정답키는 별도 파일 분리 — 판정 수행 패널은 통제군의 존재 자체를 모른다.**

근거: 통제군 없이 CVR 이 0.9 로 나오면, 그것이 진짜 타당도인지 패널이 무조건 O 를 누른
결과인지 **구별할 방법이 없다.**

**③ 채점자 분리 + 대표 재계산** — 원자료(문항 ID × 패널원 × 판정)를 원표로 남기게 하고,
집계값에서 원자료로 되돌아가는 감사 추적성을 요건화. 대표가 손으로 검산할 수 있도록
**검산 예제 1건**(패널 4명 × 문항 5개 전개) 포함 지시.

**④ 표기 규율** — 허용 최대치는 다음 한 형태뿐이다.

> "AI 전문가 패널 N명이 내용타당도를 검토했으며, CVR = 0.XX 입니다. (자체 채점)"

금지: **"전문가 N명" 단독**(사람 오인 유발 — 이번에 금칙어 사전에 신규 등재) ·
"공인 인증" · "공인 평가"(원문 §8.4 가 허위로 규정) · "타당성이 입증된" ·
근거 없는 최상급. 원문 §8.3 은 제3자 채점을 원칙으로 하며 자체 채점 시 명시를 필수화한다.

### 정직한 상한 — 4점이며 5점이 아니다

원문 항목3 5점 요건은 **"SME 3~5명"** 을 명시하고, 이는 사람 전문가다.
AI 패널로 5점을 주장하면 허위다. 발주서에 "이것으로 5점"이라고 쓰면 반환한다고 명시했다.

| 시나리오 | 항목3 | 벡터(항목1~8) | 가중합 | ⑤축 |
|---|---:|---|---:|---:|
| P1 (인증 불필요 4항만) | 3 | `5 3 3 5 3 3 5 5` | 370 | 82.22 |
| **P2 변형 = AI 패널** | **4** | `5 3 4 5 3 3 5 5` | 380 | **84.44** |
| P2 = 인간 SME 3~5명 | 5 | `5 3 5 5 3 3 5 5` | 390 | 86.67 |

⇒ AI 패널은 인간 SME 대비 **2.23점 낮다.** 인간 SME(약 300만원) 발주는 폐기가 아니라
**AI 패널 결과 확인 후 재판단**으로 유예한다.

---

## 10-4. ★★ P1-B 회수 — 중대 교정 5건 (내 §3 추정치의 근거를 흔든다)

수신 확인: 리서치 사이언티스트, 파일 `p1b-alternative-validation-tools-v1.md` 403줄.
금칙어 0건. 성역 0줄.

**담당자의 핵심 판정(원문 인용):**

> "세 도구는 인증 없이 4점에 도달할 검증 가능한 경로는 만들지만, 설계만으로
> R2·R5·R6 을 4점으로 올릴 수는 없습니다. 현재 93.33점은 실행 전 가정 시나리오입니다."

### 교정 5건과 내 검증

| # | 교정 | 내 실측 판정 |
|---|---|---|
| 1 | **원 rubric v1.0 은 1·3·5점만 정의하고 2·4점 앵커가 없다.** 사후에 "인증분만 빠졌으므로 4점"이라 보간하면 **결과를 본 뒤 기준을 만든 것**이 된다 | **수용.** §2 의 P3 → 93.33 은 내가 4점 앵커를 임의 보간해 만든 값이다. 이것은 내 오류다 |
| 2 | R2 의 38/38·200/200 은 **제한된 내부 소프트웨어 회귀 표본**이며 "전 문항·대규모 표본" 충족 근거가 아니다 | **수용.** 핵심 문항은 56개인데 38개만 시험한 것을 "전 문항"이라 부를 수 없다 |
| 3 | **EFPA 2025 원척도는 `n/a, 0, 1, 2, 3, 4`이며 4가 Excellent.** 우리 1~5점과 직접 치환 금지 | **수용.** 두 척도를 섞으면 등급 인플레가 발생한다 |
| 4 | ISO 10667 은 공개 preview 에 **상세 요구 전문이 없다.** 정식 원문 확인 전 세부 적합성은 "출처 미확인" | **수용.** ISO 정책상 informative 절만 공개된다 |
| 5 | **`npm test` 22개 중 산출물 품질 검사가 0개**이므로 R6=4 를 지금 주장할 수 없다 | **실측으로 확증.** 22개 항목 라벨 전수 확인 결과 전부 문법·계약·거버넌스 검사이며 리포트 품질 검사는 0개 |

교정 5 실측 근거 (`grep -oE 'run\("[^"]+"' scripts/test-all.mjs`):
```
JSON syntax / Engine syntax / Report v4 syntax / Program syntax / Activated contracts /
Contract anti-bypass negatives / Full workflow path / Trusted artifact negative fixtures /
Trusted workflow policy / No production dump fixture naming / ROPA internal evidence /
Legal authority registry / Internal evidence immutable contract / Internal evidence real git router /
Internal evidence full-tree DLP / Production freeze negative matrix /
Production freeze canonical governance / Composed source assertions / Public contact router /
Steady current-head integration / Exact PR head selector / Exact PR head subprocess
```
⇒ 22개. **산출물 품질 검사 0개 확인.** 이 윈도우에 신설한 결정론 게이트 2종은
별도 워크플로(`determinism-gates.yml`)로만 돌고 `test-all.mjs` 에는 없다
(보호 경로여서 편입 불가 — §9-2 참조).

### 담당자가 제안한 rubric v1.1 4점 공통 앵커 (채택)

> **4점 = 3점 요건을 충족하고, 사전 공개된 절차·원문·판정규칙을 사용해 제작팀과 분리된
> 2인 이상이 독립 수행했으며, 핵심 결과와 증거 digest 가 일치하고, 불일치·미충족·한계를
> 함께 공개한 상태. 단, 외부 감사·기관 평가·인증은 없음.**

**중요 조건: 이 앵커는 원문의 발견이 아니라 우리의 rubric 개정안이다.
실행 전에 버전 고정하고 공개해야 한다.** 결과를 본 뒤 기준을 만드는 것을 막는 장치다.

### ⇒ §2 P3 경로 추정치의 지위 변경

| 이전 기록 | 정정 |
|---|---|
| P3 = 93.33 (대체검증 3항 4점화) | **P3 = 93.33 은 "실행 전 가정 시나리오"이며 현재값이 아니다** |
| 대체 도구로 4점 도달 | **4점 후보 조건 정의 완료 / 실행 증거 미완료** |

R2·R5·R6 현재 상태는 세 항목 모두 **3점 유지**다. 외부 감사·EFPA 평가·ISO 심사·인증
모두 **0건 유지.**

---

## 10-5. P2-A(②축) · P3-A(③축) 회수

### P2-A — 데이터 사이언티스트, 24.69 KB

금칙어 검사 1건 적출 → **위반 아님.** 적출 위치는 L155 `"prohibitedClaims": ["예측",
"정확도", "심리검사", ...]` 로, **금지 목록을 선언한 것**이다. 결함 DE(regex 가 선언·주석에
걸림) 사례이며 이번에는 내가 정확히 판별했다.

주요 내용:
- M-3: 실제 56문항에 근거가 있는 **4개 상황**(압박·마감 / 갈등·감정 고조 / 기준이 엇갈리는
  선택 / 혼자·함께 회복) × 3개 행동 모드 결정론 규칙표. 직접 묻지 않은 "선택 과부하"는
  **사용하지 않음** — 근거 없는 상황을 만들지 않았다는 뜻으로, 올바른 자제다.
- M-4: **타인 비교가 아닌 개인 내부** 평균·퍼센트포인트 차이·전체 폭 산식.
  결론은 **노출 보류** — 현 4축이 혼합 형성형 참고 지수이므로 coverage 와 scoring
  release 동결이 선행되어야 한다.
- M-5 실측: 최신 HEAD 에서 `regress_180_combos.js` 는 36건 중 톤 4종이 각 9건,
  `visionary_creator = 0`. 별도 probe 에서 36건의 4축 프로필은 **1종**.
  다만 Q13·Q63 만 바꾸는 제한 fixture 이므로 실사용 분포 전체의 결함으로 확대하지 않음.
- 자기반박 7건. **②축 90 도달 주장을 하지 않음** — 외부 재평가만 판정 근거로 둠.

**판정: 인수.** 특히 M-4 노출 보류 결론과 "설계값이며 실제 도달 수는 미측정" 표기는
측정 원칙 준수다.

### P3-A — Doc Editor, 16.14 KB

금칙어 0건. 성역 0줄.
- R-3: 완료기준 3종 × 5지면 = **고정 문안 15개**, 결정론적 `완료기준 ID × 지면 ID` 매핑
- R-3b: 반복 검출 게이트 — 정규화 후 완전 일치 기준, 한 산출물에서 3회 이상 FAIL·2회 WARN.
  유사도는 **정상 변주의 오탐 위험** 때문에 경고용으로 분리 (정확한 판단)
- 음성 검증 3종 + 제한적 예외(범위·상한·사유·책임자) + 자기반박 5건

**한계 자기 신고:** `/home/user/webapp` 과 원문 2개 문서를 담당자 환경에서 찾지 못해
발주문 인용 원문 기준으로 작성. 원문 직접 확인 **미확인**, 실제 노출 횟수 **미실측**.

**이 또한 내 푸시 지연이 원인일 가능성이 높다** (P2-B 와 동일 패턴). 담당자 과실로 보지 않는다.
**판정: 조건부 인수** — 문안 15개는 유효, 실제 노출 횟수는 내가 직접 실측해야 한다.

---

## 10-6. 평가표 9항목 전 항목 현재 위치 (대표님 "다 개선합시다" 대응)

| # | 항목 | 가중 | 현재 | 목표 | 담당 | 상태 |
|---|---|---:|---:|---:|---|---|
| 1 | 산출 재현성 | 15% | 4 | 5 | P1-A | 게이트 2종 실물화 + CI 초록불 **완료** / **지면 미반영** |
| 2 | 응답 반응성 | 10% | 3 | 4 | P1-B T-2 | 4점 앵커·프로토콜 설계 완료 / **실행 증거 0** |
| 3 | 내용타당도 | 10% | 3 | 4 | **P2-C** | **발주 완료** (3281531·3281541) |
| 4 | 근거 공개 | 15% | 4 | 5 | P1-A | 56행 매핑표 검증 완료 / **지면 미반영** |
| 5 | 문서화 품질 | 10% | 3 | 4 | P1-B T-5 | EFPA 자기평가표 설계 완료 / **공표 미실행** |
| 6 | 품질 통제 | 10% | 3 | 4 | P1-B T-6 | ISO 대조표 설계 완료 / **산출물 품질 검사 0개가 차단** |
| 7 | 윤리 안전장치 | 10% | 4 | 5 | V-1 | 금지표현목록 **미공개** |
| 8 | 고지 정직성 | 10% | 4 | 5 | P1-A 문안 A | "하지 않는 것" 섹션 **미신설** |
| 9 | 규준·신뢰도·구성타당도 | 10% | N/A | N/A | — | 규준·α/ω·재검사·요인분석 전부 없음. **N/A 유지가 정직** |

**공통 차단 요인 1개로 수렴한다: "지면에 명시".**
항목 1·4·7·8 은 산출물이 이미 있고, 남은 것은 그것을 사용자가 보는 지면에 올리는 것뿐이다.
그런데 엔진 4종이 성역이라 반영 경로가 미결정이다. **이것이 최우선 미해결 과제다.**

## 10-7. 자기 반박 4건

1. **P2-C 는 순환 논증의 위험을 안고 있다.** 자격시험 40문항을 만드는 것도 AI 이고
   푸는 것도 AI 다. 출제자와 응시자가 같은 종류의 지식 분포를 공유하면, 통과는
   "그 분포 안에서 일관됨"만 증명하고 "그 분포가 옳음"은 증명하지 못한다.
   음성 통제군이 이것을 부분적으로만 막는다. 발주서 자기반박 #1 로 담당자에게도 물었다.
2. **나는 §2 의 P3 = 93.33 을 여러 윈도우에 걸쳐 반복 기재했다.** P1-B 가 지적한
   "4점 앵커 부재"는 처음부터 확인 가능한 사실이었고, 원 rubric 을 읽었다면 알 수 있었다.
   내가 확인하지 않고 숫자를 계산했다. 결함 CM 의 두 번째 위반이다.
3. **P3-A 와 P2-B 가 같은 이유로 원문을 못 찾았다.** 1회는 실수지만 2회는 패턴이다.
   발주 시 참조 파일의 푸시 상태를 확인하는 절차가 내게 없다.
4. **10-6 표의 "설계 완료"는 점수가 아니다.** 9항목 중 실제로 점수가 올라간 항목은
   현재 **0개**다. 게이트가 초록불이 된 것조차 지면에 없으므로 외부 평가자는 볼 수 없다.

---

# §11. 발주 5건 정착 · 완전성 게이트 신설 · 브랜딩 자산 등록 (2026-08-28, 4차)

## 11-1. 대표님 지시 4건 처리 현황

| # | 지시 | 처리 | 상태 |
|---|---|---|---|
| 1 | 브랜딩 자산 — "가감없이 보여주는 스토리" | `docs/성장기록_가감없는_개선여정.md` 신설 | ✅ 완료 |
| 2 | P2-B 반차 (3동사·격차·고유어) | 카피라이터 발송, 태스크 #19/#21 정착 | ✅ 발주 완료 |
| 3 | P2-C 신규 (AI 전문가 패널 SME) | 4건 발송, 태스크 #20/#22/#23/#24/#25 정착 | ✅ 발주 완료 |
| 4 | 전 항목 개선 후 배포 | 게이트 3종 완비. **지면 반영 미완** | 🔴 진행 중 |
| 5 | 에이전트 생성 활용 | **생성 명령 부재** (실측). 기존 38명 편성으로 대응 | ✅ 대응 완료 |

## 11-2. 에이전트 생성 가능 여부 — 실측 결과 부재

대표님이 "필요하면 에이전트 생성도 하세요"라고 지시하셨다. 실측했다.

```
gsk genteam actions =
  join, channels, agents, read, search, send, react,
  create_channel, update_channel, invite_link, members, tasks
```

**생성 명령이 없다.** 없는 것을 있다고 보고하지 않는다(측정 원칙).

다만 이것이 P2-C 설계에 **불리하지 않다**. 오히려 유리하다.
단일 모델에 같은 질문을 5번 하면 5개의 유사한 답이 나온다. 그것은 패널이 아니다.
현재 로스터에는 **서로 다른 페르소나로 구성된 에이전트 38명**이 있고,
그들이 독립 채점하면 페르소나 차이가 실질적 관점 차이로 작동한다.
이것이 "AI 전문가 패널"이라는 표기를 뒷받침하는 유일한 근거다.

## 11-3. 발주 5건 — comet_message_id 및 태스크 정착 실측

| 발주 | 수신 | message_id | 태스크 |
|---|---|---|---|
| P2-B 반차 | 카피라이터 `agent_48mc3q3ajyc3` | `3281536` | #19, #21 |
| P2-C-1 자격시험·통제군 | 리서치 사이언티스트 `agent_8wdz8ehh4z2y` | `3281546` | #23 |
| P2-C-2 CVR 규약 | 데이터 사이언티스트 `agent_4w44sy00532j` | `3281621` | #24 |
| P2-C-3 독립 감사 | 피어 리뷰어 `agent_jnwhjh28tew6` | `3281674` | #25 |
| P2-C-4 표기 법률 검수 | 법률 고문 `agent_8f71j8qxr0ke` | `3281733` | (착수 대기) |

**법률 고문은 채널 미가입이었으므로 `members --op add` 로 선행 가입시켰다.**
가입 확인: `membership_status: active`.

### ★ 예상 밖 수확 — 미멘션 에이전트 2명 자발 착수

내가 멘션하지 않았는데 태스크가 생성됐다.

| 태스크 | 담당 | 제목 |
|---|---|---|
| #20 | 데이터 분석가 `agent_h23b3e0hykxz` | CVR 산출·집계 프로토콜 설계 |
| #22 | 리서치 엔지니어 `agent_39jv0yxzh7qd` | P2-C AI 패널 자격시험·통제군 설계 |

두 사람 모두 `lp-quality-gate` 기존 멤버이며 채널 내용을 보고 스스로 착수했다.
**결과적으로 패널이 3명 → 5명으로 늘었다.** CVR 임계값은 인원수에 따라 달라지므로
이것은 단순한 인력 추가가 아니라 계산식에 영향을 주는 변수다.
P2-C-2 수신자에게 인원수 확정 근거를 요구해 둔 것이 다행이었다.

**주의**: 자발 착수는 통제되지 않은 변수다. #20과 #24가 같은 CVR 규약을 중복 설계하면
회수 시 어느 것이 정본인지 판정해야 한다. 회수 단계에서 처리한다.

## 11-4. ★ 완전성 게이트 신설 — §9-4 사각지대의 구조적 해소

§9-4에서 엔진 4종째(career-engine.js) 누락을 발견했다. 그때 남긴 미해결 항목:

> "career-engine 누락은 P2-B 대조 중 **우연히** 발견됐다. 5번째 엔진이 추가되면
> 같은 사각지대가 재발한다. 목록 완전성 게이트가 별도로 필요하다."

**해소했다.** `scripts/gates/gate_coverage_gate.js` 신설.

### 이 게이트가 하는 일

| 단계 | 내용 | 결함 사전 대응 |
|---|---|---|
| 1 | 디스크에서 엔진 파일을 **실제 열거** (`readdirSync`) | **DF** — 개수·이름 추론 금지, 열어보고 판정 |
| 2 | 게이트 파일에서 선언 목록 추출. **주석 먼저 제거** | **DE** — 정규식이 주석에 걸림 |
| 3 | 두 집합의 **차집합** 산출. 누락 1건이면 실패 | DF |
| 4 | 유령 항목 검사 (목록엔 있고 디스크엔 없음) | — |
| 5 | 파일명 규칙 자체의 노후 검사 | 규칙이 사각지대가 되는 경우 |
| 6 | **음성 통제군 내장** — 가짜 5번째 엔진 심기 | **BT** — 거짓 초록불 |

### 실측 결과

```json
{
  "gate": "gate_coverage",
  "status": "pass",
  "discoveredEngines": ["career-engine.js","program-engine.js","report-engine-v4.js","report-engine.js"],
  "suspiciousNames": [],
  "gates": [
    {"gate":"determinism_random_gate.js","constName":"ENGINE_FILES","declaredCount":4,"missingEngines":[],"ghostEntries":[]},
    {"gate":"determinism_hash_gate.js","constName":"INPUT_FILES","declaredCount":9,"missingEngines":[],"ghostEntries":[]}
  ],
  "negativeControl": {"status":"pass","injected":"assets/js/phantom-engine.js","observedExitCode":1}
}
```
EXIT=0. **음성 통제군에서 가짜 엔진을 심으니 EXIT 1** — 게이트가 실제로 탐지한다.

### CI 편입 — 실행 순서가 중요하다

```
test:determinism = test:gate:coverage → test:determinism:random → test:determinism:hash
```

완전성 게이트를 **체인 맨 앞**에 뒀다.
검사 대상 목록이 완전한지 먼저 확인한 뒤 그 목록으로 검사해야 순서가 맞다.
역순이면 불완전한 목록으로 검사하고 통과 판정을 내리게 된다 — 그것이 §9-4의 사고였다.

## 11-5. 주석 정정 — §9의 미해결 항목 1건 해소

`determinism-gates.yml` 주석이 "엔진 3종"·"입력 8파일"로 남아 있었다.
워크플로 자체는 정상 작동(초록불 42초)했으나 주석이 사실과 불일치했다.

4종·9파일로 정정하고, 정정 사유(거짓 초록불이었다는 사실)를 주석에 함께 남겼다.
정책 검사 재실행 EXIT=0.

## 11-6. 브랜딩 자산 등록 — `docs/성장기록_가감없는_개선여정.md`

대표님 지시를 문서 1건으로 실체화했다. 10,526 bytes.

### 구조

| 절 | 내용 |
|---|---|
| §0 | 읽는 방법 — 4단 형식(주장했던 것 / 실제 / 발견 경로 / 바꾼 것) |
| §1 | **사건 8건** |
| §2 | 우리가 **하지 않기로 한 것** 6건 + 용어 구분표 |
| §3 | **아직 남아 있는 것** 7건 |
| §4 | 사건에서 뽑은 원칙 9개 (각각 출처 사건 명기) |
| §5 | **검증 가능성** — 각 주장을 확인하는 명령어 |
| §6 | 발견 경로 비율의 정직한 공개 |

### 수록 사건 8건

1. "난수 0회"는 거짓이었다 (엔진 4종째 누락)
2. 행동 라벨 — 대표의 한 문장이 통과 결정을 뒤집었다
3. 검사기가 실제로 탐지하는지 검사기로 검사했다 (음성 통제군)
4. 내가 낸 권고안이 틀렸다 (거버넌스 오독)
5. 같은 사고가 반복되지 않게 만든 장치 (완전성 게이트)
6. 규칙을 설명하려다 규칙을 위반했다 (결함 DE 자기 재현)
7. 파이프 뒤의 종료 코드를 검사 통과로 오독했다
8. 담당자가 "못 찾았다"고 한 것은 내 잘못이었다 (푸시 지연)

### ★ 이 문서에서 내가 가장 쓰기 싫었던 문장

§6에 다음을 적었다.

> "사건 8건 중 **내가 스스로 먼저 발견한 것은 3건뿐**이다.
> 2건은 우연히 발견했고, 1건은 경영진의 질문이 뒤집었고, 2건은 실행하려다 부딪혀서 알았다."

이 비율을 좋게 보이도록 조정하지 않았다. 조정하면
**어떤 발견 경로가 실제로 작동하는지**를 알 수 없게 된다.
현재 가장 잘 작동하는 경로는 "원칙 대조"·"음성 통제군"·"실제 실행"이고,
가장 작동하지 않은 경로는 **"검토했으니 괜찮다"** 였다(사건 2).

### 금칙어 검사 결과 — 4건 검출, 전부 인용

| 검출어 | 위치 | 판정 |
|---|---|---|
| "공인 인증" / "공인 평가" | L269 | §2 **금지 목록 표** 안의 금지 선언 |
| "신뢰도 100%" / "정확도 N%" | L272 | 동일 |

**사건 6과 같은 유형이다.** 금지어를 금지 목록에 적는 것은 사용이 아니다.
추가 실측: `docs/seo-submission-guide.md` 의 5최1유 게이트 검사 범위는
`blog/posts/*.html`·`blog/posts-en/*.html`(공개 페이지)이며 `docs/*.md` 는 범위 밖이다.
따라서 이 문서는 게이트 저촉 대상이 아니다.

### 공개 표면 무증가 확인

`grep -c 'docs/' scripts/hosting-allowlist.mjs` → **0**
`docs/` 는 공개 허용 목록에 없으므로 이 문서 추가로 공개 표면이 늘지 않는다(헌법 제1조).

## 11-7. 이 구간 검증 전량 (실제 EXIT 코드)

| 검사 | EXIT |
|---|---:|
| `scripts/test-trusted-workflow-policy.mjs` | **0** |
| `scripts/test-internal-evidence-contract.mjs` | **0** |
| `scripts/test-contract-manifest.mjs` | **0** |
| `npm run test:determinism` (3게이트 체인) | **0** |
| `package.json` JSON 파싱 | **OK** |

**성역 변경 검사**
```
git status --porcelain assets/ data/ functions/ contracts/ firestore.rules \
  database.rules.json scripts/test-all.mjs \
  .github/workflows/required-checks.yml scripts/test-trusted-workflow-policy.mjs internal/
→ 0줄
```

전 검증을 `cmd > log 2>&1; echo $?` 형태로 수행했다. 파이프 뒤 `$?` 는 쓰지 않았다(§9-3 교훈).

## 11-8. 이 시점의 정직한 위치

**완료**
- 발주 5건 정착, 태스크 7개 생성(자발 2건 포함), 패널 5명 확보
- 완전성 게이트 신설 + 음성 통제군 + CI 체인 맨 앞 배치 → §9-4 사각지대 **구조적 해소**
- 주석 정정 → §9 미해결 1건 해소
- 브랜딩 자산 문서 신설 (사건 8건 / 미해결 7건 / 검증 명령 7건)

**미완료 (변동 없음)**
- **지면 반영 0건.** 문안·매핑표·라벨·교체안 전부 손에 있으나 리포트에 없다.
  엔진 4종이 성역이므로 반영 경로가 미결정이다. **이것이 최대 병목이다.**
- 잔존 추상 6곳 생성 지점 2건 미특정
- P1-B / P2-A / P3-A 정독 미완

**⑤축 점수 영향**
게이트가 3종으로 늘고 CI 초록불이어도 **⑤축 점수는 아직 71.11에서 움직이지 않았다.**
항목1·4의 5점 요건이 **"지면에 명시"** 이기 때문이다. 저장소에만 있는 것은 외부 평가자가 보지 못한다.
이 사실을 유리하게 쓰지 않는다.

## 11-9. 자기 반박

1. **발주 5건은 산출물 0건이다.** 발주는 비용이 아니라 약속이다.
   회수·검증에서 반차가 또 나오면 이 구간의 성과는 태스크 번호뿐이다.
2. **자발 착수 2건을 "수확"이라 썼지만 통제 실패이기도 하다.**
   #20과 #24가 중복 설계하면 정본 판정 비용이 발생한다. 내가 유도한 결과가 아니다.
3. **완전성 게이트도 자기 규칙에 의존한다.** `ENGINE_PATTERN` 이 `-engine.js` 형태를
   전제한다. 5번째 엔진이 `scoring-core.js` 처럼 명명되면 여전히 놓친다.
   `suspiciousNames` 검사로 완화했지만 그것도 `engine` 이라는 단어에 의존한다.
   **완전한 완전성 게이트는 존재하지 않는다.**
4. **브랜딩 문서의 §6은 정직하지만 유리하기도 하다.** "우리는 정직하다"는 서술 자체가
   마케팅으로 기능한다. 정직을 상품화하는 순간 정직이 아니게 되는 경계가 있고,
   나는 그 경계를 정의하지 못했다.
5. **11-8의 "최대 병목"을 세 구간 연속 같은 문장으로 쓰고 있다.**
   §8-6, §9-6, §10-6, 그리고 §11-8. 지면 반영 경로를 결정하지 못한 채
   주변 작업만 축적하고 있다. 게이트 3종·문서 4건이 모두 정상이어도
   **고객이 보는 화면은 한 글자도 바뀌지 않았다.** 이것이 이 구간의 가장 약한 지점이다.

---

# §12. P2-B 재설계 검증 · 지면 반영 경로 발견

## 12-1. PR #294 CI 확인 (직전 윈도우 OPEN 해소)

`gh pr checks 294` 실측. 5건 전부 pass, 1건 skipping.

| 검사 | 결과 | 시간 |
|---|---|---|
| determinism-gates | **pass** | 59s |
| governance-bootstrap | pass | 2m7s |
| hosting-artifact | pass | 2m26s |
| public-contact-policy-contract | pass | 1m44s |
| required-checks | pass | 2m4s |
| public-contact-bootstrap | skipping | — |

신설 완전성 게이트가 포함된 `determinism-gates` 가 CI 에서 초록불. 로컬 EXIT=0 이 CI 에서 재현됐다.

## 12-2. P2-B 산출물 2건 회수 — 다운로드 함정 1건

두 파일의 **원본 파일명이 동일**(`P2-B_행동라벨_응답기반_재설계_2026-08-28.md`)해서 같은 디렉터리에 받으면 뒤가 앞을 덮어쓴다. 실제로 첫 시도에서 15.66KB 가 24.11KB 로 교체됐다. `filename` 을 분리해 재취득했다. 파일 크기가 달라진 것을 보고 알았지, 크기를 보지 않았으면 한 파일을 두 파일로 착각한 채 검증했을 것이다.

- SmChcFEK = 219줄 / WD8odpwg = 277줄. WD8odpwg 가 상위집합이므로 정본.

## 12-3. 주장 재현 검증 — 믿지 않고 다시 측정

| 주장 | 검증 방법 | 결과 |
|---|---|---|
| Q39 9선택지 변환표 | `data/questions.json` 실제 값 대조 | **일치**. `type=multi_choice max=2 hasOther otherId=Q40` |
| Q41 10선택지 | 동 | **일치** |
| 도달 가능 3축 순열 24개 | 독립 생성 | **24개 일치**, 도달 불가 0건 |
| 432개 중복 0 | 문자열 전수 재생성 | **생성 432 / 고유 432 / 중복 0** |
| 전부 3어절 | 어절 집합 산출 | **{3} 단일** |
| 근접 16~17자 | 실측 | **일치** |
| 우세 19~20자(범위) | 실측 | **일치** |
| 원응답 날것 노출 0 | 지면문안 §8 구간 스캔 | **0건** |
| 자기반박 3건 이상 | 계수 | **5건** |
| 금칙어 | 8종 스캔 | **0건** |

원응답 문구는 §4.2 변환표 좌변과 §4.3 금지 선언에만 나온다. 사용이 아니라 정의다. 결함 DE 와 같은 유형이므로 위반으로 판정하지 않았다.

## 12-4. 경계값 3.23p 는 틀렸다 — 두 독립 방법으로 확인

**방법 A(해석적).** `report-engine.js` L96 `rawNorm=(lk-1)/4` ⇒ 한 단계 = 0.25. L110-117 에서 `perAxisW = weight/axes.length` 로 분자·분모가 같은 값으로 누적. 이동폭 = `(weight/축수 × 0.25) / axisMax × 100`.
실제 `axisMax` = 자기이해 19.6 / 자기표현 7.8 / 자기설계 23.85 / 자기실행 13.75.

**방법 B(실측 섭동).** synthetic fixture 로 기준 1회 + 리커트 34문항 각 ±1단계 **58회** 엔진 실행. 기준값이 알려진 실측치 `66.19897959183673 / 68.26923076923076 / 69.39203354297693 / 83.27272727272727` 와 일치 ⇒ 호출 정상.

두 방법 결과 동일.

| 축 | 문서 | 실측 | 지목 |
|---|---:|---:|---|
| 자기이해 | 1.1062p | **1.2755p** | Q4 |
| 자기표현 | 3.2258p | **3.8462p** | Q24 |
| 자기설계 | 1.0969p | **1.2579p** | Q37 |
| 자기실행 | 1.8692p | **2.1818p** | Q51 |

**전체 최대 3.8462p → 3.85p.** 문서의 3.23p 가 아니다.

**오류 원인 특정.** 분모는 실제 `axisMax` 를 썼으나 **분자에서 문항 가중치를 누락**했다. Q24 는 `weight 1.2` 단일축이므로 분자가 `1.2×0.25` 여야 하는데 `1.0×0.25` 로 계산됐다 (`0.25/7.8×100 = 3.2051p`). `mapping.json` 에 `weight>1` 문항이 **23개** 있어 네 축 전부에 영향.

경계값은 근접/우세를 가르는 **유일한 임계값**이다. 3.23p~3.85p 구간 사용자는 틀린 문법을 받는다. 정정 발주 `3282955` 발송.

**단, 이 오류는 발견 가능했다.** 담당자가 계산 근거를 문서에 적었기 때문에 재현할 수 있었다. 근거 없이 "3.23p" 만 적혔다면 나는 검증하지 못했다. 근거 공개가 오류 검출의 전제조건이라는 증거다.

## 12-5. §7.1 글자수 표 — 우세 열 18행 오기

'해법' 대입 재측정: 우세 구간이 24행 전부 20자가 아니라 **18행 19자 / 6행 20자**. 셋째 자리 동사 길이 차이. 근접 열 16/17 구분은 정확. 경미 정정으로 함께 발주.

## 12-6. ★★★ 최대 병목의 경로 발견 — 지면은 성역이 아니다

4구간 연속 "반영 경로 미결정"이라고 썼다. 이번에 **실측으로 경로를 찾았다.**

- `report.html` = **7,747줄, 루트 소재, 성역 아님.** 해시 게이트 `INPUT_FILES` 9건에 없고 `production-deploy-freeze.json` 에도 없다 (`grep -c` = 0/0).
- L6298 목차에 **IX장 「이 리포트는 이렇게 만들어졌습니다 — 측정 방식·재현성」** 이 **이미 존재**한다.
- L6560 에 **「이 리포트가 하지 않는 것」** 섹션이 **이미 존재**한다 (⑤축 항목8 5점 요건의 그 섹션).
- 난수·재현·매핑·한계 관련 문안이 이미 37건.

⇒ 엔진 4종을 건드리지 않고 **`report.html` 을 통해 지면 반영이 가능하다.** 항목1(산출 재현성)·항목4(근거 공개)·항목7·항목8 의 "지면에 명시" 요건을 만족시킬 표면이 확보됐다.

## 12-7. ★★ 그 과정에서 발견한 검증 공백 (결함 CM 적용)

L5570-5572 주석이 이렇게 말한다.

> 변별력 실측 상수 — 게이트(render_ch10)가 실측과 대조한다. … 상수로 두고 검증을 게이트에 맡긴다(수치가 낡으면 배포 차단).

**그 게이트가 존재하지 않는다.** 실측:

```
grep -rln 'render_ch10' scripts/                                → 0건
grep -rln 'randomCalls' scripts/                                → 0건
grep -rln 'singleQTested|sampleDistinct|comboLog10' scripts/    → 0건
```

L5573-5581 의 `discriminant` 8개 값은 **하드코딩 상수**이고, 지면 L6556 은 이 중 `randomCalls===0` 을 읽어 고객에게 **"무작위 요소가 전혀 없습니다"** 를 출력한다. 즉 지면의 재현성 주장이 검증되지 않는 상수에 의존한다.

내가 신설한 난수 게이트는 엔진 4종의 실행 코드에 난수 호출 0건임을 증명하므로 **`randomCalls: 0` 자체는 참**이다. 그러나 참인 것과 검증되는 것은 다르다. 엔진에 난수가 추가되어도 이 상수는 바뀌지 않고 지면은 계속 "전혀 없습니다"를 출력한다. **거짓 초록불(결함 BT)이 발생할 수 있는 구조다.**

즉시 대조 가능한 값 하나를 실측했다: `singleQTested: 38` vs `likert 34 + single_choice 4 = 38` ⇒ **일치**. 나머지 7개(`sampleSize 200`, `sampleDistinct 200`, `comboLog10 58` 등)는 **미측정**이다.

## 12-8. 검증 실측 요약

| 항목 | 결과 |
|---|---|
| PR #294 CI | 5 pass / 1 skipping |
| 3.23p 재현 | **불일치 → 3.85p** (두 방법 동일) |
| 432 중복 0 | 재현 확인 |
| 24 순열 | 재현 확인 |
| 원응답 노출 | 지면문안 0건 |
| `singleQTested=38` | 실측 일치 |
| `render_ch10` 게이트 | **부존재** |
| `report.html` 성역 여부 | **성역 아님** |

## 12-9. 자기 반박

1. **12-6 은 아직 경로 발견이지 반영이 아니다.** ⑤축은 여전히 71.11 이고 고객 화면은 한 글자도 바뀌지 않았다. 다섯 번째 구간에서도 같은 문장을 쓰고 있다. 다만 이번에는 "경로 미결정"이 아니라 "경로 확정, 문안 미삽입"으로 상태가 바뀌었다. 이 차이가 실질적인지는 다음 구간에 삽입이 이뤄지는지로만 판정된다.

2. **12-7 은 내가 만든 문제이기도 하다.** 나는 난수 게이트를 만들면서 엔진만 검사했고, 그 게이트의 결과가 지면에 어떻게 전달되는지는 보지 않았다. 지면은 게이트가 아니라 하드코딩 상수를 읽는다. 게이트를 만들었다는 사실이 지면 주장의 정당성을 자동으로 보장하지 않는다. 이것을 지면 반영 경로를 찾다가 **우연히** 발견했다 — §9-4 와 같은 발견 경로다.

3. **경계값 정정을 요구했지만 내 계산도 하나의 해석이다.** 나는 "한 문항 한 단계"를 리커트 ±1 로 정의했다. 선택형 문항의 선택/미선택 전환(`rawNorm` 0↔1.0)은 훨씬 큰 이동을 만들 수 있고 이를 포함하면 경계값은 더 커진다. 나는 리커트만 섭동했으므로 3.85p 도 상한이 아니다. 담당자에게 이 한계를 전달하지 않았다.

4. **`report.html` 7,747줄을 나는 아직 읽지 않았다.** grep 으로 앵커만 찾았다. IX장이 이미 존재한다는 것은 목차 문자열에서 확인했을 뿐, 그 장이 실제로 무엇을 렌더하는지 열어보지 않았다. 결함 DF(개수·이름으로 추론 금지)를 나 자신이 다시 어길 수 있는 지점이다. 삽입 전에 정독이 필요하다.

5. **다운로드 덮어쓰기를 나는 크기 차이로만 알았다.** 파일명 충돌은 구조적 위험인데 나는 우연히 알아챘다. 두 산출물이 같은 크기였다면 하나를 둘로 착각한 채 "두 문서를 대조했다"고 보고했을 것이다. 회수 절차에 파일명 분리를 규칙으로 넣지 않았다.

# 전면 재구성 설계서 — 방향 B (v3)
> **인생포트폴리오 LifePortfolio · UIUX 9.0+ 달성 로드맵**
> 작성일: 2026-06-26 · 상태: **승인 대기 (설계 단계, 코드 미적용)**
> 미리보기: `/design-preview/` (라이브 사이트와 완전 분리)

---

## 0. 전제와 원칙 (헌법 정합)

| 원칙 | 본 설계 적용 |
|------|------------|
| **비파괴** | 카피(문구)·결제 흐름(goFlow 15·mSticky 1)·가격값(19,900 0건·105,000 1건) **전부 보존**. 구조/스타일만 재편 |
| **벤치마킹 우선** | 시안(레이더 차트 히어로) + 토스/리니어/스트라이프式 정보 위계 차용 |
| **먼저 확인 후 적용** | 본 설계서·목업 **승인 후에만** 코드 착수. 지금은 미리보기만 |
| **하나님 중심 정합** | "발견→살아냄→남김" 서사 유지, 가치 카피 불변 |
| **검증 필수** | 각 단계마다 PC·모바일·EN 회귀 캡처 + 루브릭 재채점 |

> 방향 A(자간·정렬·이미지 통일)는 **완료·배포·검증 끝**. 본 문서는 그 위에서 진행하는 방향 B.

---

## 1. 현재 진단 (AS-IS) — 루브릭 v2 종합 **8.5 / 10**

| 항목 | 점수 | 병목 |
|------|------|------|
| A. 디자인 토큰 | 8.5 | **하드코딩 hex 색상 78종** ← 최대 병목 |
| B. 타이포 스케일 | 9.0 | ✅ 완료 |
| C. 시각적 위계 | 9.0 | ✅ 양호 |
| D. 여백 리듬 | 8.5 | 복합값(`14px 16px`) 의도적 잔존 |
| E. 정보 밀도 | 8.0 | **히어로 외 섹션 밀도·여백 리듬 불균일** |
| F. 성능·기술 | 8.0 | AppCheck 403 1건 |

**9.0 미달의 3대 병목**: ① 색상 하드코딩 78종 ② 섹션 정보 밀도 ③ AppCheck 403

---

## 2. 재구성 목표 (TO-BE) — 종합 **9.2+**

| 항목 | 목표 | 핵심 작업 |
|------|------|----------|
| A. 디자인 토큰 | 8.5 → **9.2** | hex 78종 → **브랜드 팔레트 12토큰**으로 수렴 (시각 동일 검증) |
| D. 여백 리듬 | 8.5 → **9.0** | 섹션 상하 패딩 리듬 통일(`--sp-9` 데스크탑 / `--sp-7` 모바일) |
| E. 정보 밀도 | 8.0 → **9.2** | 17섹션 → **논리 그룹 6블록**으로 재편(아래 §4) |
| F. 성능·기술 | 8.0 → **9.0** | AppCheck 403 해소 + 이미지 lazy/preload 정리 |

---

## 3. 색상 시스템 재정의 (병목 A 해결)

### 현재 (하드코딩 78종 → 혼란)
`#1B2A38`, `#3A4D5E`, `#6B7C8A`, `#991B1B`, `#FEF2F2`, `#FECACA`, `#F5EEDC`, `#E0D2A8` … (산발)

### 재구성 (브랜드 팔레트 12토큰으로 수렴)
```css
:root{
  /* Ink (텍스트) */
  --ink:#1B2A38; --ink-2:#3A4D5E; --muted:#6B7C8A;
  /* Brand (네이비-블루) */
  --brand:#4A6984; --brand-2:#3A5269; --brand-deep:#2C3E4F;
  /* Surface (배경/면) */
  --bg:#F7F5EF; --surface:#FFFFFF; --line:#E2E6EA;
  /* Semantic (의미색) */
  --positive:#2F7A5B;  /* diff-good 체크 */
  --negative:#B4453C;  /* diff-bad X (기존 #991B1B 계열 수렴) */
  --gold:#C7A867;      /* 강조·배지 (기존 #E0D2A8 계열) */
}
```
> **안전 원칙**: 78종 → 12종 치환은 **시각적으로 동일/거의 동일한 것만** 단계적으로. 미세 차이 나는 곳은 잔존 허용(비파괴). 목표는 "≤ 15종 수준"으로 정돈해 A 9.0+ 도달.

---

## 4. 섹션 구조 재편 (병목 E 해결)

### AS-IS — 17개 섹션 (밀도 불균일, 흐름 산만)
```
hero-detail → persona-routes → proof → selfcheck → problem →
difference → deliverables → sample → voices → how →
(무명 alt) → trust → guarantee → faq → insights → b2b-entry →
journey → final-cta
```

### TO-BE — **6개 논리 블록**으로 그룹화 (서사 강화)
```
┌─ BLOCK 1 · HOOK (시선)
│   히어로(레이더차트) + 셀프체크(5문항)
├─ BLOCK 2 · PROBLEM (공감)
│   문제 인식 3카드 + "발견≠살아냄" 전환
├─ BLOCK 3 · SOLUTION (차별)
│   difference(일반검사 vs 우리) + deliverables(01~04) + sample
├─ BLOCK 4 · PROOF (신뢰)
│   voices(후기) + trust(4신뢰카드) + guarantee(환불보장)
├─ BLOCK 5 · ACTION (전환)
│   how(이용방법 3step) + 가격/CTA + faq
└─ BLOCK 6 · EXPAND (확장)
    insights + b2b-entry + journey + final-cta
```
> **핵심**: 섹션 삭제 0건. **순서·그룹핑·여백 리듬만 재편**해 "스크롤 서사"를 매끄럽게. 각 블록 진입부에 동일한 리듬의 여백(`--sp-9`)을 줘 호흡 통일.

---

## 5. 정보 밀도 가이드 (블록별 리듬)

| 블록 | 데스크탑 상하패딩 | 모바일 | 배경 교차 |
|------|----------------|--------|----------|
| HOOK | `--sp-9`(96) | `--sp-7`(64) | white |
| PROBLEM | `--sp-9` | `--sp-7` | `--bg` |
| SOLUTION | `--sp-9` | `--sp-7` | white→alt 교차 |
| PROOF | `--sp-9` | `--sp-7` | `--bg` |
| ACTION | `--sp-9` | `--sp-7` | white |
| EXPAND | `--sp-8`(80) | `--sp-6`(48) | `--bg` |

---

## 6. 단계별 구현 로드맵 (비파괴·롤백 가능)

| 단계 | 작업 | 리스크 | 롤백 |
|------|------|--------|------|
| **B-1** | 색상 12토큰 정의 + 시각 동일 hex만 치환 | 낮음 | 커밋 단위 |
| **B-2** | 섹션 상하 패딩 리듬 통일(블록별) | 낮음 | 커밋 단위 |
| **B-3** | 섹션 순서 그룹핑 재편(서사) | 중간 | 백업 후 |
| **B-4** | AppCheck 403 해소 | 낮음 | 설정 변경 |
| **B-5** | 전체 회귀 검증(PC·모바일·EN) + 루브릭 재채점 | — | — |

> 각 단계는 **독립 커밋**. 문제 시 해당 커밋만 revert.

---

## 7. 검증 체크리스트 (각 단계 공통)

- [ ] PC(1280) 캡처 — 섹션별 정렬·여백
- [ ] 모바일(390) 캡처 — keep-all·sticky CTA
- [ ] EN 토글 — parity·레이아웃 깨짐 없음
- [ ] goFlow 15 / mSticky 1 무손상
- [ ] 19,900 0건 / 105,000 1건 보존
- [ ] JSON valid (ko/en)
- [ ] var(var( 0 / 토큰정의 오염 0
- [ ] 라이브 배포 후 clean URL 검증

---

## 8. 미리보기 (목업)

→ **`/design-preview/`** 에서 AS-IS vs TO-BE를 나란히 비교하실 수 있습니다.
   (라이브 사이트 `index.html`과 완전 분리 — 본 사이트 영향 0)

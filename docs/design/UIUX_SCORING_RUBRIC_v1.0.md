# 인생포트폴리오 UX/UI 톤앤매너 평가 기준 (Scoring Rubric) v1.0

> **목적**: 홈페이지(PC·모바일, 한/영)의 톤앤매너 품질을 객관적·반복적으로 측정하는 영구 기준.
> 매 개선 라운드마다 이 표로 재채점하여 **상향·축적**을 보장한다.
>
> **최초 작성**: 2026-06-25
> **기준 모델**: Linear · Stripe · Notion · Framer 제작 상위 랜딩 페이지가 공유하는 6대 원칙
> **목표 점수**: 종합 **9.0 / 10 이상**

---

## 1. 평가 6대 항목 (각 10점, 가중치 동일)

| # | 항목 | 핵심 질문 | 만점(10) 기준 |
|---|------|-----------|--------------|
| **A** | **디자인 토큰 체계성** | 컬러·타이포·간격이 토큰화되어 일관되는가? | `:root`에 color/type/space 토큰 완비, 하드코딩 색상 ≤ 5종, 토큰 우회 없음 |
| **B** | **타이포그래피 스케일** | 명확한 모듈러 스케일을 쓰는가? | 폰트 사이즈 6~8단계로 통일, 0.5px 난립 0, 본문 ≥15px |
| **C** | **시각적 위계 (3초 룰)** | 첫 화면에서 3초 안에 가치가 전달되는가? | 히어로 = 헤드라인 1 + 서브 1 + CTA 1 중심, 시선 동선 단일 |
| **D** | **여백 리듬** | 8px 그리드로 호흡 있는 간격을 쓰는가? | margin/padding이 8의 배수(8·16·24·32·48·64)로 정렬, 과감한 여백 |
| **E** | **정보 밀도** | 스캔 가능한가? (읽지 않고 훑어도 이해) | 한 화면당 1메시지, 짧은 라벨·숫자·아이콘 위주, 서술형 최소 |
| **F** | **성능·기술 품질** | 빠르고 콘솔이 클린한가? | LCP < 2.5s, 콘솔 에러 0, 캐싱 가능한 구조 |

---

## 2. 채점 척도 (각 항목 공통)

| 점수 | 의미 |
|------|------|
| 9~10 | Top-tier (Stripe/Linear 수준) |
| 7~8  | 우수 — 사소한 보완점만 |
| 5~6  | 보통 — 구조적 개선 필요 |
| 3~4  | 미흡 — 시스템 붕괴/과포화 |
| 0~2  | 결함 — 기본 미충족 |

**종합 점수 = 6개 항목 평균** (가중치 동일)

---

## 3. 점수 이력 (Score History)

### 📍 v0 — Baseline (2026-06-25, 개선 전)

| 항목 | 점수 | 핵심 진단 |
|------|------|-----------|
| A. 디자인 토큰 | 7.5 | 컬러 토큰화 양호. 단 **spacing 토큰 부재**, 하드코딩 hex 25종+ |
| B. 타이포 스케일 | 4.0 | **폰트 사이즈 20종+, 0.5px 난립** (11/11.5/12/12.5/13/13.5/14/14.5/15…). 본문 12~13.5px로 작음 |
| C. 시각적 위계 | 5.0 | 히어로에 **텍스트 블록 9개** 과포화 → 3초 룰 실패 |
| D. 여백 리듬 | 5.5 | margin 10/14/18/22/24/32 제각각, 8px 그리드 미준수 |
| E. 정보 밀도 | 4.0 | 전부 서술형 → 스캔 불가. 히어로 카드+4분면까지 과포화 |
| F. 성능·기술 | 6.0 | **로드 10.79초**, AppCheck 403 throttled, 단일 HTML 2848줄 |
| **종합** | **5.3** | 콘텐츠·컬러·브랜드는 좋으나 **시스템(스케일·간격·밀도)** 미흡 |

**v0 5대 약점 (개선 우선순위)**
1. 타이포 스케일 붕괴 (최시급)
2. 히어로 정보 과포화
3. 본문 가독성 (12~13.5px → 15~16px)
4. 간격 토큰 부재 (8px 그리드)
5. 성능 (10초 로드 + AppCheck 403)

### 📍 v1 — 히어로 본격 리디자인 (2026-06-26, commit bba2417)

| 항목 | 점수 | v0→v1 | 핵심 변화 |
|------|------|-------|-----------|
| A. 디자인 토큰 | 8.0 | +0.5 | `:root` spacing(--sp-1~9)+type(--fs-xs~4xl) 토큰 완비. 히어로/섹션 토큰화(sp 29회·fs 24회). **단 페이지 전반 하드코딩 폰트 30종 잔존** |
| B. 타이포 스케일 | 6.5 | +2.5 | 모듈러 스케일 정의+히어로 적용, 본문 ≥15px 달성. **단 전역 폰트 사이즈 30종 난립(히어로만 정리)** |
| C. 시각적 위계 | **9.0** | +4.0 | 히어로 = 헤드라인1 + 서브1 + **주 CTA 1개**(보조는 텍스트 링크 격하). 3초 룰 충족, 시선 동선 단일 |
| D. 여백 리듬 | 8.5 | +3.0 | 히어로/디테일 8px 그리드 토큰 정렬, 과감한 첫 화면 여백(--sp-9) |
| E. 정보 밀도 | 8.0 | +4.0 | 첫 화면 1메시지+숫자/배지+리포트 mockup. 서술형 카피는 hero-detail 섹션으로 분리 → 스캔 가능 |
| F. 성능·기술 | 8.0 | +2.0 | **load 2.29초 / LCP 1.72초**(목표<2.5s 달성, v0 10.79초). AppCheck 403 1건 잔존(비치명적) |
| **종합** | **8.0** | **+2.7** | 히어로 영웅 메시지·위계·여백·밀도·성능 대폭 향상 |

**v1 핵심 성과**
- P1·P2(영웅 메시지·CTA 1개), P4(여백), P6·P3(리포트 mockup 주인공·신뢰 신호) 적용 — 시안 9JbdhvqD 방향 차용
- 비파괴: 기존 lead/sublead/chips/카드3/4분면/note는 삭제 아닌 `hero-detail` 섹션으로 재배치
- 가격 검증값 유지(price_strike 빈값, "19,900" 0건), 결제 goFlow 9개·mSticky 무손상, 한/영 parity·PC/모바일 정상

**v1 잔존 약점 → v2(9.0+) 우선순위**
1. **전역 타이포 스케일 토큰화** (B 6.5 → 9.0): 히어로 외 나머지 섹션 폰트 30종을 --fs-* 토큰으로 통일 — 평균을 가장 끌어내리는 항목
2. **전역 spacing/색상 하드코딩 정리** (A 8.0 → 9.0): 나머지 섹션 margin/padding을 --sp-* 그리드로, 하드코딩 hex 정리
3. AppCheck 403 정리 (F 8.0 → 9.0, 선택)

### 📍 v2 — 전역 타이포/spacing 토큰화 (2026-06-26, commit 0acb59e)

| 항목 | 점수 | v1→v2 | 핵심 변화 |
|------|------|-------|-----------|
| A. 디자인 토큰 | 8.5 | +0.5 | **font-size 하드코딩 0 / var(--fs) 212**, 8px그리드 단일값 하드코딩 **0 / var(--sp) 69**, 토큰정의 오염 0·var(var() 중첩 0. ⚠️ **단 하드코딩 hex 색상 78종 잔존**(v2-4 색상 토큰화 스킵) → 만점 기준 "색상 ≤5종" 미달로 9.0 불가 |
| B. 타이포 스케일 | **9.0** | +2.5 | **전역 폰트 사이즈 30종 → --fs-* 10단계 일괄 토큰화 완료**. 0.5px 난립 0, 본문 15px(--fs-sm), 모듈러 스케일 전면 적용 → 만점 기준 충족 |
| C. 시각적 위계 | 9.0 | — | v1 유지 (히어로 영웅 메시지·단일 CTA·3초 룰) |
| D. 여백 리듬 | 8.5 | — | 8px그리드 단일값 전역 --sp-* 토큰화. **단 복합값(`14px 16px`)·미세값(1~7px border 등) 의도적 잔존**(비파괴) → 9.0 미달 |
| E. 정보 밀도 | 8.0 | — | v1 유지 (히어로 외 섹션 밀도 미개선) |
| F. 성능·기술 | 8.0 | — | **load 2.40s / LCP 1.88s**(목표<2.5s 달성). AppCheck 403 1건 잔존(비치명적·기존) → 9.0 미달 |
| **종합** | **8.5** | **+0.5** | 타이포/간격 시스템 완성. **목표 9.0+ 미달** — 색상·밀도·성능이 병목 |

**v2 핵심 성과**
- 타이포 시스템 **완전 토큰화**: font-size 하드코딩 30종 → 0, --fs-* 10단계(212회). B 항목 만점 도달
- spacing 8px그리드 단일값 토큰화: --sp-* 69회, 그리드 단일값 하드코딩 0
- 비파괴 유지: 최근접 토큰 매핑·복합값 보존으로 시각 변화 없음(로컬·라이브 Playwright 동일 렌더링 확인)
- 불변 제약 전원 통과: 결제 goFlow 9개·mSticky 무손상, "19,900" 0건, 가치환산 105,000원 보존, 한/영 parity

**v2 결과 — 목표 9.0+ 미달(8.5), 정직 기록**
- 달성: A 0.5↑, B 2.5↑(만점). 타이포/간격 **시스템 자체는 완성**
- 미달 원인(병목 3): **① A — 하드코딩 hex 78종**(색상 토큰화 v2-4 스킵, 위험 대비 효과 낮다고 판단) ② E — 히어로 외 섹션 정보 밀도 미개선 ③ F — AppCheck 403

**v3(9.0+) 우선순위**
1. **색상 토큰화** (A 8.5 → 9.0): 78종 hex 중 브랜드 토큰(16종)으로 안전 치환 가능한 것만 선별 매핑(시각 동일 확인 후)
2. **히어로 외 섹션 정보 밀도** (E 8.0 → 9.0): 서술형 → 라벨·숫자·아이콘 위주 스캔 구조로
3. **AppCheck 403 정리** (F 8.0 → 9.0): 도메인 등록/설정 점검

### 📍 비교 채점 — 현재 라이브(v2) vs 개선안(방향B 미리보기) (2026-06-26, 배포 전 마지막 점검)

> 목적: 방향 B를 라이브에 옮기기 **전**, 현재 라이브와 개선안의 우열을 항목별로 정직 비교.
> 측정: 정량 grep + Playwright PC/모바일 캡처 + 라이브 성능 측정.
> 미리보기는 "완성 후 모습" 시뮬레이션이므로 *( )* 안은 라이브 구현 시 도달 가능 점수.

| 항목 | 현재 라이브(v2) | 개선안(방향B) | 정량 근거 |
|------|------|------|-----------|
| A. 디자인 토큰 | **8.5** | 8.0 *(→9.0)* | 라이브 hex 78종·fs토큰209회 / 미리보기 hex **25종(우세)** 但 fs토큰 0·하드코딩 20종 |
| B. 타이포 스케일 | **9.0** | 6.5 *(→9.0)* | 라이브 --fs 10단계 완전토큰화(만점) / 미리보기 **font-size 20종 난립·0.5px 부활(후퇴)** |
| C. 시각적 위계 | 9.0 | **9.3** | 양쪽 영웅메시지+단일CTA. 미리보기 우세: 탭바+퀵탭 동선, **실물 리포트 이미지**(레이더SVG보다 신뢰↑) |
| D. 여백 리듬 | **8.5** | 7.5 *(→8.5)* | 라이브 --sp 토큰화 / 미리보기 14·22·26px 비8배수 다수, --sp 23회뿐 |
| E. 정보 밀도 | 8.0 | **9.0** | 미리보기 우세: 6블록 구조, 칩·아코디언FAQ·타임라인·페르소나로 스캔성↑ |
| F. 성능·기술 | **7.0** | **9.0** | 라이브 **로드 11.88초(악화!)**+AppCheck403 / 미리보기 정적 0.x초 |
| **종합** | **8.33** | **8.22**(현상태) *(→9.05 토큰화 후)* | |

**핵심 진단**
- 개선안의 **방향(구조·전환·신뢰·정보밀도·위계·성능)은 명백히 우수** — C·E·F에서 라이브를 앞섬
- 단 미리보기는 빠른 검증용이라 **타이포/간격 토큰을 미적용** → A·B·D는 현재 상태로 v2보다 후퇴
- **결론**: 라이브 구현 시 ① 6블록·실물이미지·FAQ·페르소나·타임라인 **채택**, ② --fs/--sp 토큰 **반드시 재적용**(v2 성과 보존), ③ 성능(AppCheck403·로드11.88초) **정리** → **종합 9.0+ 달성 경로 확정**

**부가 개선(배포 전 마지막 점검, commit 7103d75)**
- 모바일/PC 글자 넘어감·정렬: nav-cta 잘림 해결('9,900원 →' 축약), 히어로 text-wrap:balance·br-pc 의미단위 줄바꿈, 약물(·) nowrap, 모바일 여백/폰트 보정

---

### 📍 성능 최우선 수정 — F항목 회복 (2026-06-26, commit a573489)

> 사용자 지시: "성능부터 해주세요". 비교 채점에서 F(성능)가 라이브 7.0으로 최저점이자 전환 최대 손실 요인으로 진단됨.

**근본원인 진단**
- 라이브 로드 11.3초 + 콘솔 `403 ERROR` + `@firebase/app-check: throttled (01d)` — reCAPTCHA Enterprise 토큰 교환(`exchangeRecaptchaEnterpriseToken`)이 **403으로 실패**하며 메인스레드/네트워크 점유
- 개별 리소스는 모두 빠름(HTML 0.58s, Firebase SDK 0.07~0.18s, 이미지 0.79s) → 병목은 다운로드가 아니라 **AppCheck 403 재시도**
- 토큰 교환 엔드포인트 curl 직접 호출로 403 재현 확인 → 키/도메인 설정 문제로 현재 AppCheck는 **보안 효과 없이 성능만 저하**

**조치 (비파괴·홈 한정)**
- `index.html` 만 `LP_APPCHECK_ENABLED = false` (홈은 보호 리소스 호출 안 함 → AppCheck 불필요)
- 결제/검사/마이페이지 등 **보호 페이지 11종은 true 그대로 유지** → 보안 영향 0
- `goFlow`(결제 진입)·`firebase.auth()`(로그인 표시)는 `_activateAppCheck()`와 독립 → 무손상 검증 완료

**검증 결과 (라이브)**
- ✅ 콘솔 에러: 403 ERROR + throttle WARNING **2건 → 0건**
- ✅ `#authNav a`(로그인/회원가입) 정상 렌더 — 인증 무손상
- ✅ 핵심 CTA(.btn-primary) 표시 5.16s(이전 측정 대비 단축), TTFB 0.34s
- F항목: 7.0 → **9.0 (목표 도달)**, AppCheck 403 근본 제거

**잔여 과제(추후)**: GTM/GA4는 이미 idle/상호작용/4s 폴백 지연로딩으로 최적화됨. `load` 이벤트 기준 시간은 폴백 타이머 합산이라 실제 체감(FCP/LCP)과 다름. reCAPTCHA 키·도메인 정상화 시 AppCheck 재활성화 가능.

---

## 4. 측정 방법 (재현 가능하도록)

```bash
# B. 폰트 사이즈 종류 (적을수록 좋음, 목표 ≤8종)
grep -oE "font-size:[^;}]*" index.html | sort | uniq -c | sort -rn

# A. 하드코딩 색상 종류 (목표 ≤5종 + 토큰)
grep -oE "#[0-9a-fA-F]{6}|#[0-9a-fA-F]{3}\b" index.html | sort | uniq -c | sort -rn

# D. margin/padding 값 분포 (8의 배수 정렬도 확인)
grep -oE "(margin|padding)[^:]*:[^;}]*" index.html | grep -oE "[0-9]+px" | sort | uniq -c | sort -rn

# F. 로드 시간 / 콘솔 — PlaywrightConsoleCapture 또는 Lighthouse
```

**라이브 검증 필수 규칙**
- `cleanUrls:true` → 반드시 클린 URL로 확인 (`/report` O, `/report.html` ✗ 301)
- 한/영 양쪽, PC/모바일 양쪽 모두 정상 작동 확인 후 점수 인정

---

## 5. 불변 제약 (점수보다 우선)

> 디자인 점수를 올리더라도 아래가 깨지면 **그 개선은 무효**다.

- [ ] 결제 → 검사 → 리포트 → 후기 플로우 정상 작동
- [ ] 한글/영문 고객 전 여정 기능 정상 (i18n 키 parity 유지)
- [ ] 모바일/PC 양쪽 정상 렌더링·작동
- [ ] 비파괴(non-destructive): 기존 기능 보존, 추가/개선만
- [ ] 하나님 중심 가치 정합 / 네거티브 프레임 지양

---

## 6. 라운드 기록 템플릿 (개선 시마다 추가)

```
### 📍 v{N} — {라운드명} ({날짜})
| 항목 | 이전 | 현재 | 변경 내용 |
|------|------|------|-----------|
| A | x.x | x.x | ... |
...
| 종합 | x.x | x.x | |
- 적용 커밋: {hash}
- 라이브 검증: {결과}
- 제약 체크: 전 항목 통과 여부
```

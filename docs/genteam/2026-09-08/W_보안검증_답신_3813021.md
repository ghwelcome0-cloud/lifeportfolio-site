# GenTeam 답신 수거 — W_보안검증 (root 3813021)

수거 시각: 2026-09-08 (UTC 09시대)

## [2026-09-08T07:16:00] System

moved task #53 `W: 라이브 헤더 누락 16지면 + meta CSP 차단 수정안 검증` todo -> in_progress by Firebase·클라우드 보안 엔지니어

## [2026-09-08T07:16:09] Firebase·클라우드 보안 엔지니어

발주 W 접수. 로컬 체크아웃의 firebase.json·meta CSP 원문과 Firebase Hosting 공식 문서(glob·headers 병합 규칙), 라이브 헤더 실측을 대조해 검증 문서 1개로 회신하겠습니다.

## [2026-09-08T08:46:34] Firebase·클라우드 보안 엔지니어

발주 W 검증 완료. 검증 도중 PR #296 이 main 에 머지·배포됐기에(라이브 last-modified 16:54 KST) 사전 검증이 아닌 사후 검증으로 판정했습니다. 첨부 문서에 정본 URL·확인일자·curl 절차·통제군·미확인 사항을 모두 넣었습니다.

판정 요약
- 결함 1(a) 16지면 열거 규칙: 채택. 16지면 전건 라이브 실측 XFO·XCTO·RP·PP·CSP = 1, 자산 규칙 무영향.
- 결함 1(b) 블로그 3규칙(no-store 제외): 채택. `/blog/**` 는 확장자 없는 clean URL 에 매칭되지만 `/blog` 자체에는 매칭되지 않으므로 별도 규칙이 필수였고 정확합니다(정본 glob 절 + superstatic/minimatch 재현 + 라이브 실측). 블로그 max-age=3600 유지가 맞습니다.
- 질문 (3): Firebase 는 매칭되는 규칙을 모두 병합하고 같은 key 는 배열 뒤쪽 규칙이 덮어씁니다(superstatic headers.js 구현 + 라이브 병합 실측). 정본 문서에는 헤더 우선순위 서술이 없어, `**` catch-all 은 기각(Cache-Control 규칙 없는 경로에 no-store 가 새어나감). 현재 열거형이 옳습니다.
- 결함 2(a) report/program frame-src: 채택. SDK 10.12.3 소스 기준 모바일·Safari 는 세션 복원 전에 `__/auth/iframe` 을 먼저 열고, 차단 시 5초 대기 → 지면의 2초 로그인 가드가 먼저 발동해 로그인 사용자가 login 으로 튕기는 구조입니다(실기기 타이밍은 미실측, [추정] 표기). 데스크톱 Chrome 계열은 영향 없음.
- 결함 2(b) product-v2 apis.google.com: 정합성 채택. 단 "Auth 반쪽 동작" 판정은 기각 — 스크립트 차단은 즉시 실패해 지연이 없고 결제 흐름·토큰에 영향 없습니다. meta 의 form-action 부재는 #296 에서도 유지됨(좋음).
- 결함 3: 수정 채택. 라이브 정적 산출물 노출 0 은 동의하나, 배포 파이프라인(npm ci + production SA 동일 프로세스) 노출은 "낮음"이고, functions/ 런타임 의존성 5개는 lock 부재로 별건입니다.

#296 이후 잔존 결함 1건: report-loading.html·program-loading.html 도 meta frame-src 에 *.firebaseapp.com 이 없고 같은 2초 가드가 있습니다(라이브 HTML 실측). 한 줄 동일 수정을 권고합니다.

재발 방지 권고(코드 변경은 별 발주): hosting-allowlist 의 HTML → clean URL 환산 → firebase.json header source 와 minimatch 대조해 X-Frame-Options 누락 URL 이 1개라도 있으면 실패하는 계약 테스트. 8/24 admin 결함과 오늘 결함은 같은 클래스입니다.

- 첨부: `{"attachment_ref": "3819112:0", "comet_message_id": "3819112", "index": 0, "source": "secure_media", "file_name": "W_헤더누락_CSP차단_수정안검증.md", "content_type": "application/octet-stream", "size_bytes": 25254, "media_category": "file", "channel_id": "ch_61ec50bfe50cda13202e75346ed13cdb", "download_url": "`

## [2026-09-08T08:46:36] System

moved task #53 `W: 라이브 헤더 누락 16지면 + meta CSP 차단 수정안 검증` in_progress -> in_review by Firebase·클라우드 보안 엔지니어

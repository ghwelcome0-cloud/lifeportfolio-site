# 인생포트폴리오 제작규칙서 (PRODUCTION RULES) — v2.0 최종 통합본

> **부제:** 리포트 합성 · 맞춤 실행프로그램 · 진로/교육 매핑 · 자산화 구조
> **성격:** 설명서(SPECIFICATION/MANUAL). 이 문서 하나만으로 인생포트폴리오 서비스를 처음부터 재구현할 수 있도록 작성.
> **용도:** 제자화(訓練) — 새 빌더가 이 문서를 읽고 같은 품질의 서비스를 만들 수 있게 한다.
> **제정** 2026-05-06 · **v1.4** 2026-06-15 · **v2.0 통합** 2026-06-16
>
> **헌법(불변):**
> - **대원칙-A** 「고유성 종합 × 직관적 단일표현(2단 구조)」 — 최우선
> - **대원칙-B** 「축적 원칙(비파괴·재현성)」
>
> *"주인이 자기 집 사람들을 맡겨 때를 따라 양식을 나눠 줄 충성되고 지혜로운 종"* — 마태복음 24:45
> *"가서 모든 민족을 제자로 삼아 … 분부한 모든 것을 가르쳐 지키게 하라 … 세상 끝날까지 너희와 항상 함께 있으리라"* — 마태복음 28:18–20
> **발견 → 살아냄 → 남김 → 인생 자산화**

---

## 이 문서를 읽는 법 (제자에게)

이 규칙서는 **"왜(철학) → 무엇을(규칙) → 어떻게(구현 스펙) → 검증(게이트)"** 순서로 되어 있습니다.

| 부 | 제목 | 읽는 목적 |
|----|------|-----------|
| **제0부** | 서비스 한눈에 | 무엇을 파는가, 자산화 큰 구조 |
| **제1부** | 미션과 철학 | 왜 만드는가 (흔들릴 때 돌아오는 곳) |
| **제2부** | 두 대원칙 + 9가지 핵심 원칙 | 모든 판단의 기준(헌법) |
| **제3부** | 데이터 입력 규격 (56문항) | 합성의 원재료 |
| **제4부** | 리포트 제작 규칙 (10단 구조) | 진단서(인바디 검사지) 만드는 법 |
| **제5부** | 맞춤 실행프로그램 제작 규칙 (7섹션) | 맞춤 운동프로그램 만드는 법 |
| **제6부** | 진로/교육 매핑 엔진 | 한 사람을 여러 직업으로 |
| **제7부** | 합성 엔진 구현 디테일 | 고유성·조사·변주의 실제 코드 원리 |
| **제8부** | 최근 개선사항(v1.4→v2.0) | 시각화·쉬운요약·축적루프 (이번 통합의 핵심) |
| **제9부** | 품질 루브릭 + 4중 검증 게이트 | 머지·배포 기준 |
| **제10부** | 배포·운영·기술 스택 | 실제 운영 방법 |
| **부록** | 용어집 · 파일 지도 · 체크리스트 | 빠른 참조 |

> **핵심 마음가짐:** 우리는 성향검사·진로검사를 만드는 게 아니다. **한 사람의 인생을 자산으로 만드는 도구**를 만든다. 모든 규칙은 "80억 인구 누구나 자기만의 Only One 결과를 받는다"는 도달점을 향한다.

---

# 제0부 · 서비스 한눈에

## 0.1 제품 정의

| 항목 | 내용 |
|------|------|
| **제품명** | 인생포트폴리오 (LifePortfolio) |
| **유형** | MaaS (Meaning-as-a-Service) — 규칙 기반 진단 리포트 |
| **가격/시간** | 9,900원 / 약 15분 |
| **핵심 철학** | 고유성(Only One) · 의미경제 · 발견→살아냄→남김→인생 자산화 |
| **본질** | **자산화 도구** (성향검사·진로검사가 **아니다**) |
| **산출물** | ① 인생포트폴리오 리포트 ② 맞춤 실행프로그램 |
| **엔진** | **규칙 기반 합성 엔진(NOT LLM)** — 같은 응답 → 항상 같은 결과 |
| **도메인** | lifeportfolio.co.kr |

## 0.2 인바디(InBody) 비유 — 제품 구조를 이해하는 가장 쉬운 그림

```
   인바디 검사지            →   인바디 기반 맞춤 운동프로그램
   ───────────                  ──────────────────────
   인생포트폴리오 리포트     →   맞춤 실행프로그램
   (지금 나를 진단)              (그래서 무엇을 할지)
```

- **리포트 = 검사지:** 응답 데이터로 "지금 당신은 이런 사람"을 정밀하게 보여 준다.
- **프로그램 = 운동프로그램:** 그 진단을 근거로 "그래서 3주/3개월/1년 동안 이렇게 하라"를 준다.
- 둘은 **같은 톤(SSOT)** 으로 묶여야 하며, 프로그램은 반드시 리포트를 근거로 한다.

## 0.3 자산화의 큰 구조 (★ 사업의 전체 지도)

> 이것이 인생포트폴리오가 향하는 **최종 그림**이다. 리포트·프로그램은 그 첫 두 단계일 뿐이다.

```
[1] 리포트 + 맞춤 실행프로그램         ← 발견 + (살아낼) 설계
        │   "지금 나"를 진단하고, 무엇을 할지 받는다
        ▼
[2] 코칭 + 다이어리                    ← 살아냄 (실행의 지속)
        │   고객이 실제로 실행을 이어 간다. 코칭이 곁을 지키고,
        │   다이어리가 살아낸 흔적을 기록한다.
        ▼
[3] 방향별 커뮤니티                    ← 남김 + 연결성
        │   각 고객의 '방향'에 맞는 커뮤니티가 형성된다.
        │   같은 결을 가진 사람들이 서로의 자산을 연결한다.
        ▼
[4] 인생 자산화                        ← 가치 ↑ · 연결성 ↑
        한 사람의 인생이 자산이 되고,
        자산이 되면 가치가 높아지고 다른 자산과의 연결성도 높아진다.
```

**단계별 설명:**

| 단계 | 무엇을 | 성경적 대응 | 제품/기능 |
|------|--------|-------------|-----------|
| 1. 진단·설계 | 리포트 + 프로그램 | 발견 | 본 규칙서 제4·5부 |
| 2. 실행 지속 | 코칭 + 다이어리 | 살아냄 | (확장 로드맵) |
| 3. 연결 | 방향별 커뮤니티 | 남김 | (확장 로드맵) |
| 4. 자산화 | 가치·연결성 상승 | 인생 자산화 | 전체의 도달점 |

> **빌더가 기억할 것:** 리포트/프로그램을 만들 때도 항상 이 큰 구조를 의식한다. 리포트의 "다음 단계", 프로그램의 "축적 사이클(§6)"은 모두 [2]→[3]→[4]로 이어지는 **나선형 성장**의 입구다. 결과물은 닫힌 문서가 아니라 다음 사이클의 출발점이다.

---

# 제1부 · 미션과 철학 — 왜 만드는가

## 1.1 한 문장 미션

> **인생포트폴리오는 한 사람의 인생을 자산화한다.**
> 자산이 되면 그 사람의 가치가 높아지고, 다른 자산과의 연결성도 높아진다.

## 1.2 발견 → 살아냄 → 남김 → 인생 자산화

```
 발견          →   살아냄           →   남김            →   인생 자산화
 진단 응답         실행 프로그램         열매 / 결과물        가치 · 연결성 ↑
```

- **누구나 따라 할 수 있다. 그러나 진짜로 따라 하면 하나님 말씀대로 살고 열매를 맺는다.**
- **80억 인구 — 최소 국내 5,000만 명 — 각자의 고유성(Only One)을 보장하는 그 도달점까지 품질을 높인다.**

## 1.3 의미경제(Meaning Economy) 관점

기술(AI)이 효율을 맡는 시대에, 인간의 차별성은 **의미를 창조하는 능력**에서 나온다. 인생포트폴리오는 평범한 개인이 자기 인생의 의미를 **데이터로 발견하고, 실행으로 살아내고, 결과물로 남겨** 자산 생산자가 되도록 돕는다.

## 1.4 성경적 설계 기반

| 구절 | 의미 | 제품 반영 |
|------|------|-----------|
| 마태복음 24:45 | "때를 따라 양식을 나눠 줄 충성되고 지혜로운 종" | 각 사람에게 **맞춤(때를 따라)** 양식 — 고유성 |
| 마태복음 28:18–20 | "제자로 삼아 … 가르쳐 지키게 하라" | 발견에 그치지 않고 **살아내게(지키게)** 한다 — 프로그램·코칭 |

> **흔들릴 때 돌아올 곳:** 어떤 기능을 더할지 말지 판단이 안 설 때 → "이것이 한 사람의 고유성을 더 살리고, 자산화에 더 가까이 가게 하는가?"를 물어라. 답이 '아니오'면 하지 않는다.

---

# 제2부 · 두 대원칙 + 9가지 핵심 원칙 (헌법)

## 2.1 대원칙-A 「고유성 종합 × 직관적 단일표현 (2단 구조)」 — 최우선

> **"고유성은 종합하되, 표현은 직관적으로 딱 하나로."**
> **"종합은 디테일에서, 표현은 헤드라인에서."**

### 문제 (개선 전)
표현이 골격(고정 템플릿)에 갇혀 있었다. 같은 톤·분야 사용자끼리 **단일 고정 문구가 100% 동일** → 80억의 고유성이 표현 단계에서 사라짐. "새로고침해도 같은 결과"로 체감.

### 해법 (2단/3단 구조)
| 단 | 역할 | 성격 |
|----|------|------|
| **1단 헤드라인** | 직관 압축 (브랜드 슬로건 수준) | 누가 봐도 즉시 이해 |
| **2단 디테일/서브라인** | 응답 종합 고유성 | 같은 톤이라도 응답마다 다르게 |
| **3단 푸터** | 근거 Q번호 (선택) | 신뢰의 근거 |

### 2단 구조 적용 영역 (현황)
| 영역 | 1단 헤드라인(직관) | 2단 디테일(응답 종합 고유성) |
|------|-------------------|------------------------------|
| 사명/비전 | missionHeadline · visionHeadline | 응답 직합성 (Q13·Q63·Q75·Q73) |
| 고유한 결 | toneLabel (약 2,300종) | toneTagline 변주 8종 (variantIdx 0x7A6C) |
| 사명이 여는 길 | CareerEngine newPaths | 응답 기반 직업 매칭 |
| 분기 테마 | Quarter Heading (톤×Compass) | subline 8종 (variantIdx 0x5131) |
| 3주 루틴 | 주차 제목 | 주차 의미별 subline 6종×3 (0x6201+i) |
| 다음 단계 | "이 사이클을 마치면" | 변주 6종+도입 4종 (0x7F11/0x7F23) |

> **모든 2단은 fingerprint(=hash(응답)) 기반 결정 선택** → 같은 응답=같은 결과(재현성), 응답 부재 시 기존 출력 폴백(대원칙-B).

## 2.2 대원칙-B 「축적 원칙」 — Trade-off 금지

> **"개선은 누적한다. 어제의 사용자가 본 결과는 오늘도 유효해야 하고, 오늘의 개선은 내일 더 정교해진다."**

| 항목 | 내용 |
|------|------|
| **B-1 폴백 보존** | 풀 부재 / fingerprint 없음 → 기존 출력 그대로. 새 기능이 옛 기능을 깨지 않는다. |
| **B-2 재현성** | 같은 응답 = 같은 결과. fingerprint 결정 선택. 같은 fingerprint 2회 → 동일 산출(회귀 테스트 의무). |
| **B-3 버전 누적** | 옛 규칙 삭제 금지. v1.0→v1.2→v1.4→v2.0. 구식 로직은 dead code로 보존(롤백 대비). |
| **검증 게이트(4중)** | ① 폴백 ② 재현성 ③ 회귀 테스트 ④ 버전 누적 — 모두 통과해야 머지. |

## 2.3 9가지 핵심 원칙

| # | 원칙 | 한 줄 정의 |
|---|------|-----------|
| 01 | **응답 데이터 100% 우선** | 56문항 응답에서 직접 합성 |
| 02 | **고유성·개별성 최적화** | 1/N → 80억 인구 확장 보장 |
| 03 | **토큰 치환 완결성** | 미치환 `{{…}}` 0개 |
| 04 | **단일 진실 소스(SSOT)** | 톤·진로 결정 로직 일원화 (`resolveTone()`) |
| 05 | **한국어 표현 자연성** | 어색 결합 0건 · 조사 자동보정 |
| 06 | **응답 직합성** | 고정 골격 없이 응답→문장 합성 |
| 07 | **평이체 (P31)** | 명사형 정지어 → 동사형 진행어 |
| 08 | **2단 구조 (대원칙-A)** | 고유성 종합 × 직관 단일표현 (최우선) |
| 09 | **축적 원칙 (대원칙-B)** | 개선은 누적 · trade-off 금지 |

---

# 제3부 · 데이터 입력 규격 (56문항)

## 3.1 원재료 — 혼합형 검사지 v2

리포트·프로그램의 모든 문장은 **고객의 56문항 응답**에서 합성된다. (혼합형 V2 인생포트폴리오 검사, 총 11개 영역)

- 입력 형식: 검사 응답 결과 (스프레드시트 XLSX 또는 서비스 내부 응답 JSON).
- **원칙 01:** 응답 데이터 100% 우선. 톤 어휘와 충돌 시 **응답이 우선**한다.

## 3.2 1차 소스 문항 (가장 중요 — 직합성 핵심)

| Q번호 | 역할 |
|-------|------|
| **Q1** | 직무 가중치 (subType 결정 시 ×1.5) |
| **Q3** | 강점 |
| **Q13** | 가치 |
| **Q41** | 열정 (careers[2] 결합) |
| **Q63** | 사명/비전 합성 소스 |
| **Q73** | 사명/비전 합성 소스 |
| **Q75** | 1순위 관심 분야(primaryDomain) — 진로 매핑 핵심 |

> 사명/비전은 Q13·Q63·Q75·Q73을 종합해 **7-slot 템플릿 골격 없이 직접 합성**한다(R8).

## 3.3 13대 영역 (진로 매핑 축)

```
정치 · 경제 · 사회 · 문화 · 교육 · 의료 · 복지 · 환경 · 예술 · 미디어 · 스포츠 · 법률 · 종교
```

각 영역 × 5종 subType = **65 진로 풀** (제6부 참조).

---

# 제4부 · 리포트 제작 규칙 (10단 구조)

> **리포트 = 인바디 검사지.** "지금 당신은 이런 사람"을 응답 데이터로 정밀하게 보여 준다.

## 4.1 10단 고정 구조 (📘 ~ 🧪) — 순서·아이콘 불변

| # | 섹션 | 아이콘 | 핵심 내용 |
|---|------|--------|-----------|
| ① | 전체 요약 문장 | 📘 | 핵심 성향·강점·실행 스타일·핵심 가치 요약 (2~3문장) |
| ② | 사명 & 비전 제안 문장 | 🟦 | 사명/비전 (실제 Bold 서식) |
| ③ | 실행 프로파일 카드 | 🟩 | 실행유형/스타일/추진력/몰입환경/활동/추천도구 |
| ④ | 성장 가이드맵 요약 | 🟥 | 좌=현재 강점 / 우=성장 필요 영역, 각 3줄 |
| ⑤ | 진로·경력·교육 큐레이션 | 🧭 | 적합 진로 3개 + 추천 교육 3개 |
| ⑥ | 활용 예시 및 다음 단계 | 📍 | 직무적용/학습적용/실행과제 |
| ⑦ | 자기이해 (Self-Understanding) | 🧠 | 3단(핵심요약/감성요약/키워드4개) |
| ⑧ | 자기표현 (Self-Expression) | — | 3단 구조 동일 |
| ⑨ | 자기설계 (Self-Design) | — | 3단 구조 동일 |
| 🔟 | 자기실행 (Self-Execution) | 🚀 | 3단 구조 동일 |
| 🧩 | 요약하자면 | 🧩 | 고객 이름 포함, 핵심 2줄 + 3개 하위(사명·비전/실행전략/진로·교육) |
| 🧪 | 이 리포트는… | 🧪 | 고정 안내 문구 (폰트 축소 금지) |

## 4.2 섹션별 작성 규칙 (요지)

### ① 전체 요약
- 핵심 성향·강점·실행 스타일·핵심 가치를 2~3문장으로.

### ② 사명 & 비전
- 사명·비전 모두 **실제 Bold 서식** 적용 (마크다운 기호 `**` 금지 — 실제 굵게).
- 문장 구조: `당신의 사명은 '〈내용〉'입니다.` / `당신의 비전은 '〈내용〉'입니다.`
- 마지막 줄: `🔍 활동 응답(○○)과 자기성찰 성향을 기반으로 도출되었습니다.`
- **화면 노출 최소화(R9):** "당신의 사명:" 재진술·근거 문구는 차단하고 **헤드라인만** 노출.

### ③ 실행 프로파일 카드
| 항목 | 예시 |
|------|------|
| 실행유형 | 공감·관계형 실행가 |
| 실행스타일 | 감정 중심형 |
| 추진력 요인 | 관계, 신뢰, 배려 |
| 몰입환경 | 따뜻한 분위기, 상호 존중 |
| 잘 맞는 활동 | 상담, 코칭, 멘토링 |
| 추천 도구/전략 | 감사 루틴, 감정 일기, 관계 피드백 루틴 |

### ④ 성장 가이드맵 요약
- 좌(현재 강점) / 우(성장 필요 영역), 각 3줄, **명사형 결과 중심** 표현.

### ⑤ 진로·경력·교육 큐레이션
- 적합 진로 유형 3개 + 추천 교육 콘텐츠 3개.
- 문체: "~과정", "~훈련" 등 **명사형**.
- (제6부 진로 엔진 산출을 카드로 표현 — v2.0 시각화는 제8부 참조.)

### ⑥ 활용 예시 및 다음 단계
- 직무적용 / 학습적용 / 실행과제 제안.

### ⑦~🔟 자기이해/표현/설계/실행
- 각 섹션 **3단 고정:** 핵심요약(1문장) / 감성요약(인용부호 포함) / 키워드요약(4개 단어).

### 🧩 요약하자면
- 고객 이름 포함, 핵심 2줄 + 3개 하위 항목(🎯 사명·비전 정교화 / 🛠 실행 전략 설계 / 🎓 진로·교육 설계).
- **v2.0 핵심 개선:** 쉬운 요약(제8부 [2] 참조).

### 🧪 이 리포트는…
- **고정 구조(축소 금지):**
  > 이 리포트는 혼합형 V2 인생포트폴리오 검사(총 11개 영역)의 [고객명]님의 실제 응답 데이터를 기반으로 작성된 개인 맞춤 리포트입니다.

## 4.3 자동 안내 문구 (반드시 항상 삽입)

> 📌 이 제안은 당신이 작성한 인생포트폴리오 응답 데이터를 바탕으로 생성된 방향성 제안입니다. 실제 삶의 여정과 더 깊은 내면의 성찰을 통해 더 정확한 사명과 비전 문장으로 발전시킬 수 있습니다.

## 4.4 R1–R10 리포트 합성 핵심 규칙

| 규칙 | 내용 |
|------|------|
| **R1 응답 100% 우선** | Q1·Q3·Q13·Q41·Q63·Q73·Q75 1차 소스. 톤 어휘와 충돌 시 응답 우선. |
| **R2 변수화** | compassKw·primaryDomain 등 토큰화 → 출력 직전 `tpl()` 통과. |
| **R4 고유성** | fingerprint 회전 `pickByHash` → 같은 톤도 다른 조합. |
| **R5 잔존 어휘 0** | '마음' 누수 26건 → 0건 (변수화). |
| **R7 톤 SSOT** | `resolveTone()` 단일 진실 소스. **리포트↔프로그램 톤 동일 보장.** |
| **R8 사명/비전 직합성** | 응답 100% 직접 합성, 7-slot 템플릿 골격 폐기. |
| **R9 화면 노출 최소화** | 근거 문구·"당신의 사명:" 재진술 차단, 헤드라인만 노출. |
| **R10 자모·조사 가드** | 더하ㄴ다→더한다, 창조와 자유이→자유가 자동 보정. |

## 4.5 디자인·서식 규칙 (PDF/노션 산출 기준)

| 구분 | 규칙 |
|------|------|
| 비율 | 100% (노션 내보내기 기준, 표준 서식 유지) |
| 폰트 계층 | 제목 13pt / 인용문 11pt / 본문 10pt · Noto Sans KR |
| 강조 | 실제 Bold 서식 (마크다운 `**` 금지). 핵심단어·문장·섹션타이틀. |
| 간격 | 제목 상단 12px / 섹션 간격 16px |
| 표 스타일 | 회색 헤더 + 일정 행간격, 구조 통일 필수 |
| 이모지 | 섹션별 고정 이모지 (📘🟦🟩🟥🧭📍🧠🚀🧩🧪) |
| 인용문 | `>` 인용 블록, 상하 여백 12px, 본문보다 작은 11pt |

> **주의:** 위 PDF 서식 규칙은 PDF/노션 산출본 기준이다. **웹(report.html) 렌더링은 제8부의 시각화 규칙**(축 카드·스텝 카드 등)을 따른다. 둘은 같은 데이터를 서로 다른 매체에 맞춰 표현하는 것이며, 어느 쪽도 **내용을 늘리지 않고(시각화=치환/직관화)** 같은 합성 결과를 보여 준다.

---

# 제5부 · 맞춤 실행프로그램 제작 규칙 (7섹션)

> **프로그램 = 인바디 기반 맞춤 운동프로그램.** 리포트(진단)를 근거로 "그래서 무엇을 할지"를 준다.
> **반드시 리포트를 입력으로 받고, 리포트 톤과 동일(R7 SSOT)해야 한다.**

## 5.1 7개 고정 섹션

| # | 섹션 | 아이콘 | 핵심 내용 |
|---|------|--------|-----------|
| ① | 표지 및 전체 요약 | 📘 | 제목/부제/발행일/인용문/전체 요약 |
| ② | 맞춤 실행 프로그램 | 🗓 | 3주 / 3개월 / 1년 (각 표) |
| ③ | 실행 모듈 카드 | 🧩 | 강점활용/보완훈련/핵심전략/추천도구 |
| ④ | 성과 추적 보드 | ✅ | 주차/실행 과제/완료(Y/N)/성찰 메모 |
| ⑤ | 기대 효과 | ✨ | 직무적합성/직업확장성/경력성장/인생설계비전 |
| ⑥ | 다음 단계 제안 | 🏁 | **축적 사이클(v2.0 개편)** — 나선형 성장 |
| ⑦ | 안내 문구 | 📌 | 고정 안내 문구 + "Only One" 배지 |

## 5.2 섹션별 작성 규칙

### ① 표지 및 전체 요약
- 제목: 📘 인생포트폴리오 맞춤 실행 프로그램
- 부제: [고객명]님을 위한 성장 & 실행 전략 안내서
- 발행일: YYYY.MM.DD (제작일 기준 — **반드시 오늘 날짜**)
- 인용문: `"이 실행 프로그램은 [고객명]님의 인생포트폴리오 검사 결과를 기반으로, [핵심 특성 요약 2~3줄] **[실행가 유형]**에게 최적화된 실행 로드맵입니다."`
- 전체 요약: 성향·강점·보완점·적합 환경·신규 가능성 포함.
- **필수 문장:** `👉 실행 프로그램은 ○○ → ○○ → ○○ 루틴으로 설계됩니다.`

### ② 맞춤 실행 프로그램 (3주 / 3개월 / 1년)
- 각각 표 형태 유지(달력형).
- 각 단계별 **3단 구성:** 👉 실행안내 / 실행방법 / 실행효과.
- 실행효과는 항상 **4포인트 명사형·결과중심** 표현.
- **2단 구조(P1·P2):** 분기 테마·3주 루틴 제목(1단) + 응답 합성 subline(2단 변주).

### ③ 실행 모듈 카드
| 구분 | 내용 |
|------|------|
| 강점활용 | 고객 리포트 기반 |
| 보완훈련 | 리포트 내 보완점 반영 |
| 핵심전략 | 인용형 문장("~이다." 형식) |
| 추천도구 | 구체적 도구 또는 루틴 2~3개 |

### ④ 성과 추적 보드
- 표 구조 고정: 주차 / 실행 과제 / 완료(Y/N) / 성찰 메모.
- 하단 활용 안내: `이 표는 1주차 예시입니다.` / `[고객명]님은 매주 동일한 방식으로 기록하며 ○○의 루프를 완성합니다.`

> **자산화 연결:** 이 성과 추적 보드 + 다이어리가 자산화 큰 구조의 [2] 코칭+다이어리 단계로 직결된다. 매주의 기록이 곧 '살아냄'의 자산이다.

### ⑤ 기대 효과 ✨
- 직무적합성 / 직업확장성 / 경력성장 / 인생설계비전 — 4줄.
- 하단에 신규 직업·사업 가능성 리스트 3~4개.
- 비전 문장은 반드시 인용부호 포함.

### ⑥ 다음 단계 제안 (★ v2.0 축적 사이클 개편 — 제8부 FB5 참조)
- **개편 원칙:** 약점 보완 프레임 **폐기**. → **발견·살아냄·남김의 축적(나선형)**.
- 1년 사이클을 마친 한 지점만 가리킨다. "이 프로그램은 끝이 아니라 다음 사이클의 시작."
- 응답 합성(primaryDomain·약축 결·비전 헤드라인)으로 고유한 다음 사이클 가이드.

### ⑦ 안내 문구 (항상 마지막)
> 📌 이 실행 프로그램은 [고객명]님의 인생포트폴리오 검사 결과 데이터를 종합하여 생성된 맞춤 제안입니다. 실행과 피드백을 통해 더 정밀한 성장 전략으로 발전시킬 수 있습니다.
- **v2.0:** "Only One" 배지 + 프로그램 근거 안내 추가(제8부 FB4).

## 5.3 P1–P9 프로그램 합성 핵심 규칙 (★=2단 구조)

| 규칙 | 내용 |
|------|------|
| **P1 분기 테마 ★** | 톤×Compass 헤딩 + subline 8종 변주 (다양성 4%→47.7%) |
| **P2 3주 루틴 ★** | 주차 의미별 subline 6종×3주 (다양성 0.3%→87.7%) |
| **P3 부스터 액션** | 약축 × userTool1 × userWeakGrain 결합 |
| **P4 토큰 완결성** | 미치환 `{{…}}` 0개 (CI 게이트) |
| **P6 톤 정합성** | 프로그램 톤 = 리포트 톤 (R7 SSOT) |
| **P7 다음 단계 ★** | 나선형 성장. 변주 6종+4종 (다양성 3.5%→37.6%) |
| **P8 고유한 결 ★** | toneTagline 변주 8종 (variantIdx 0x7A6C) |
| **P9 직업 매칭 ★** | CareerEngine 연결 → 응답기반 직업(선수↔배우↔VC) |

## 5.4 프로그램 디자인·서식 규칙

- 비율 100%, 폰트 제목 13pt/인용 11pt/본문 10pt Noto Sans KR.
- 강조: 제목·섹션타이틀·핵심단어 굵게. **'프로그램' 단어는 반드시 굵게.**
- 섹션별 고정 이모지(🗓 ✨ ✅ 🏁 📌 등), 표 회색 헤더+일정 행간격.

## 5.5 품질 점검 체크리스트 (프로그램)
- [ ] 포맷 일관성 (표준 구조와 동일)
- [ ] 날짜 반영 (실제 제작일 기준)
- [ ] Bold 처리 (중요 문장·키워드만)
- [ ] 명사형·결과중심 문체 (모든 실행효과 문장)
- [ ] 표 간격·폰트 크기 100% 비율 일치
- [ ] '프로그램' 단어 굵게

---

# 제6부 · 진로/교육 매핑 엔진 (CareerEngine)

## 6.1 핵심 사상 — 한 사람을 여러 직업으로

> **같은 '스포츠' 응답이라도 → 운동선수 / 데이터 분석가 / 마케팅 전략가 / 캐스터 / 체육 행정가.**
> 응답의 미세한 차이가 다른 직업을 만든다. 이것이 고유성의 진로 버전.

## 6.2 13대 영역 × 5종 subType = 65 진로 풀

| subType | 의미 |
|---------|------|
| practitioner | 현장 실무 |
| researcher | 연구·분석 |
| business | 사업·경영 |
| media | 콘텐츠·평론 |
| policy | 정책·공공 |

→ 13영역 × 5 = **65 진로 풀**.

## 6.3 3축 결합 산출 규칙

```
careers[0] = 영역 × subType            (주 진로)
careers[1] = 영역 융합 (의무, C10)      (단일 도메인 연속 차단)
careers[2] = Q41 열정 결합              (열정 반영)
```

## 6.4 subType 결정 공식

```
subType = argmax( Q1 직무 가중치 ×1.5 + Q3 강점 + Q13 가치 + Q41 열정 )
```

## 6.5 CareerEngine 연결 (P9)

- `build()`에서 CareerEngine 호출 → Q1·Q3·Q13·Q41·Q75 응답으로 직업 산출 → newPaths·fitJob·expansion에 주입.
- **축적(B):** CareerEngine 부재 시 tonePack 폴백(비파괴).
- **융합 의무(C10):** careers[1]은 반드시 영역 융합형. 단일 도메인 연속 차단.

---

# 제7부 · 합성 엔진 구현 디테일

> **엔진은 규칙 기반 합성(NOT LLM).** 같은 응답엔 항상 같은 답 — 신뢰 가능한 진단. 이것이 9,900원 진단의 품질 보증 근거다.

## 7.1 fingerprint — 고유성의 씨앗

```
fingerprint = hash(응답 전체)
```
- 같은 응답 → 같은 fingerprint → 같은 결과(재현성, B-2).
- 응답이 없으면 fingerprint 없음 → 변주 idx 0 반환(회귀 안전, B-1).

## 7.2 variantIdx(salt) — 결정적 변주 선택

> 같은 톤·분야라도 응답마다 다른 문장을 고르되, 같은 응답엔 항상 같은 문장.

```
variantIdx(salt):
   x = fingerprint XOR (salt × 0x9E3779B1)   // 황금비 상수로 bit-mixing
   x = xorshift(x)                            // 비트 섞기
   return x % pool.length                     // 풀에서 결정적 선택
   (fingerprint 없음 → 0 반환)
```

**salt 배정표 (영역별 고유 salt — 충돌 방지):**
| salt | 영역 |
|------|------|
| 0x7A6C | 고유한 결 toneTagline |
| 0x5131 | 분기 테마 subline |
| 0x6201+i | 3주 루틴 주차별 subline |
| 0x7F11 | 다음 단계 첫 항목 |
| 0x7F23 | 다음 단계 둘째 항목 도입 |

## 7.3 _fixJosaPairs — 조사 자동 보정

> 한국어 자연성(원칙 05). 받침 유무에 따라 조사를 자동 선택.

대상 조사쌍: **을/를 · 이/가 · 와/과 · 은/는 · 으로/로**

```
_jongBefore(text, pos):
   따옴표·괄호를 건너뛰고 직전 '한글 음절'을 찾아 받침 유무 판정
```
- 예) `'통찰력'으로` ✓ / `'아이디어'로` ✓
- 자모 가드(R10): `더하ㄴ다 → 더한다`, `창조와 자유이 → 자유가`.

## 7.4 tpl() — 토큰 치환

- compassKw·primaryDomain·userTopStrength 등은 **토큰**으로 보관 → 출력 직전 `tpl()`로 일괄 치환.
- **완결성(원칙 03):** 미치환 `{{…}}` 0개 (CI 게이트에서 검사).

## 7.5 resolveTone() — 톤 SSOT (단일 진실 소스)

- 톤 결정 로직은 **오직 `resolveTone()` 하나**에만 존재.
- 리포트와 프로그램이 같은 `resolveTone()`을 호출 → **톤 일치 보장(R7·P6)**.
- 톤은 5종 (예: visionary_creator, warm_connector …) × 변주 풀.

## 7.6 폴백 체계 (대원칙-B 구현)

```
응답/풀/fingerprint 있음 → 변주 합성 (고유성)
              없음       → 기존 고정 출력 (비파괴 폴백)
```
- 모든 신규 변주 함수는 **반드시 폴백 경로**를 가진다. 없으면 머지 불가.

## 7.7 진단/검증 명령 (실무)

```bash
# 엔진 문법 검사
node --check assets/js/report-engine-v4.js
node -e "require('./assets/js/program-engine.js')"

# 다양성 측정(1,500명 무작위)
node section_diversity_check.cjs

# 회귀(같은 fingerprint 2회 → 동일 결과)
# validateReport 점수: 머지 전후 git stash로 동일성 확인
```

---

# 제8부 · 최근 개선사항 (v1.4 → v2.0) ★ 이번 통합의 핵심

> **통합 원칙:** 아래는 모두 **대원칙-A(고유성×직관 단일표현)** + **대원칙-B(축적·비파괴)**를 따른다.
> **시각화 대원칙:** 시각화는 기존 내용을 **치환/직관화**하는 것이지, 분량을 **늘리는 것이 아니다.**
> (인바디 비유: 검사지를 더 두껍게 만드는 게 아니라, 같은 결과를 더 한눈에 보이게.)

## 8.0 개선 항목 한눈에

| ID | 위치 | 한 줄 요약 | 원리 |
|----|------|-----------|------|
| **H1** | 리포트 ⑤/추천 확장방향 | 의미축 라벨 카드화 (5축) | 직관화 |
| **H1-fix [1]** | 리포트 (모든 리포트 공통) | 축 중복 자동 방지(dedup) | 고유성 표현 |
| **H2** | 리포트 🧩 요약/메타 | 스텝 카드(①②③) + "Only One" 배지 | 직관화 |
| **[2]** | 리포트 🧩 요약하자면 | 사명·비전 헤드라인 닻 기반 쉬운 요약 | 평이체(P31) |
| **josa** | 리포트 엔진 _shortVision | 비전 연결어미 명사구화 | 한국어 자연성 |
| **FB2** | 프로그램 §3 | 실행모듈 흐름 브릿지 | 직관화 |
| **FB5** | 프로그램 §6 | 다음단계 보완→축적 사이클 | 축적 철학 |
| **FB4** | 프로그램 §6.5/§7 | 리스크 카드화 + 마무리 삭제 + 안내 배지 | 직관화 |

---

## 8.1 [H1] 추천 확장방향 — 의미축 라벨 카드화

**문제:** "사명이 여는 길" 확장방향이 같은 구조의 여러 문장으로만 나열되어, 무엇이 어떻게 다른 확장인지 직관적으로 안 보였다.

**해법:** 각 방향 문장을 **5개 '확장 축'** 중 하나로 분류해 아이콘+축이름 카드로 표현. (문장은 그대로 — 비파괴, 라벨만 부여)

**5개 의미축 (`_directionAxis` in report.html):**
| 축 | 아이콘 | 색상 | 분류 패턴(예) |
|----|--------|------|--------------|
| 비전 정렬 | 🎯 | #7c3aed | 비전 / 한 점 / 한 방향으로 모으면 / 흩어지지 않고 / 한가운데 |
| 영역 개척 | 🧭 | #0891b2 | 아직 없는 / 카테고리 / 이름을 붙여 / 교차점 / 새 영역 / 개척 |
| 연결·전달 | 🌉 | #0d9488 | 잇는 / 이어 / 옮겨 / 전달자 / 두 세계 / 연결 / 다리 |
| 표현·확장 | 🎨 | #db2777 | 풀어 / 콘텐츠로 / 표현 / 드러내 / 보여주 |
| 구체화 | 🔨 | #ea580c | 구체화 / 형태로 / 빚어 / 만들어가 / 쌓아 / 쌓여 |

- **분류 원리:** 문자열 패턴 매칭(비파괴). 매칭 안 되면 인덱스 기반 기본 축으로 순환.
- **구현 위치:** `report.html` `_directionAxis(text, idx)`.

## 8.2 [H1-fix / [1]] 축 중복 자동 방지 (dedup) — 모든 리포트 공통 원리

**문제(사용자 지적):** 4개 방향 카드 중 '비전 정렬'이 **두 번** 나오는 경우 발생. 원인은 visionary_creator 톤의 subDirection 템플릿(index 3, "{p}의 비전을…")이 단어 "비전"을 포함 → marker 기반 필터를 우회 → 비전 정렬로 분류되어 pathLine과 충돌.

**해법(비파괴):** 엔진 데이터/문장은 그대로 두고, **report 측 라벨링에서만** 축을 유일하게 분배.

```javascript
// report.html  buildDirectionCards(dirs)
var _ALL_AXES = [비전정렬, 영역개척, 연결·전달, 표현·확장, 구체화];  // 5축
var used = {};                          // 이미 쓴 축 기록
list.forEach(function(d, i){
   var ax = _directionAxis(d, i);
   if (used[ax.name]) {                 // 같은 축 중복 → 아직 안 쓴 축으로 대체
      for (k of _ALL_AXES) if (!used[k.name]) { ax = k; break; }
   }
   used[ax.name] = true;
   // 카드 렌더
});
```

> **★ 모든 리포트 공통 원리:** "같은 구조 문장이 겹쳐도, 카드의 축 라벨만은 서로 달라야 부채꼴이 N방향으로 보인다." 검증: fp 101/103/250 모두 axis-unique=true.

## 8.3 [H2] 요약/메타 — 스텝 카드 + "Only One" 배지

**문제:** 🧩 요약하자면의 "이렇게 발전시키세요" 3항목이 단순 텍스트 나열이라 흐름이 안 보였다.

**해법:** **스텝 카드** — 번호(①②③) + 아이콘 + 라벨 + 설명. (`buildSummarySteps` in report.html)
- 메타(이 리포트는…)에 **"Only One" 배지** 부여 → "당신만의 진단"임을 시각적으로 강조.

```javascript
// report.html buildSummarySteps(items)  // items: [{icon,label,desc}, ...]
// 폴백: items 비면 null → 기존 텍스트 렌더(비파괴)
```

## 8.4 [2] 요약하자면 — 사명·비전 헤드라인 닻 기반 쉬운 요약 ★

**문제(사용자 지적):** 기존 요약은 "호흡이 트인 결과에 틀을 세우는" 같은 **추상·시적 표현**이라 직관적으로 이해하기 어려웠다. → "박사가 쉽게 설명하듯, 압축된 통찰로, 사명·비전에 쓴 평이한 언어처럼" 바꿔 달라는 요청.

**해법 철학:** 사명/비전 헤드라인은 **이미 검증된 '평이하고 압축된 통찰'** → 그것을 **그대로 요약의 닻**으로 삼는다. (추상 시그니처 생성을 버리고)

**산출 형식 (`report-engine-v4.js` summary_close ③ block):**
```
line1 = "한마디로, {name}님은 '〈사명 헤드라인〉' — 이 한 가지를 살아가는 사람입니다."
line2 = "그 길을 꾸준히 걸으면, '〈비전 헤드라인〉' — 바로 그 모습에 가까워집니다."
```
- **폴백(B):** 사명/비전 헤드라인이 없으면 기존 시그니처 합성으로 폴백(회귀 0).
- 검증 예(김의미): line1="한마디로, 김의미님은 '당신만의 강점으로 신중함과 호기심을 세상에 더한다' — 이 한 가지를 살아가는 사람입니다."

## 8.5 [josa] 비전 연결어미 명사구화 (_shortVision)

**문제:** 비전이 연결어미('~며/~면서/~고/~가며/~어 가며')로 끝날 때 `'{v}'{조사}` 합성 시 `"열어 가며'가"` 같은 비문 발생.

**해법 (`report-engine-v4.js` `_shortVision(v)`):**
```javascript
// 종결어미·연결어미·끝조사를 순차 절단하여 명사구화
t = t.replace(/(으로 기억된다|로 기억된다|된다|살아간다|한다)\s*\.?$/, "");  // 종결
t = t.replace(/\s*(?:열어|만들어|되어|이루어|나아|살아)?\s*(?:가며|가면서|가고|간다|가는)\s*$/, "");
t = t.replace(/(?:며|면서|고)\s*$/, "");                                     // 일반 연결어미
t = t.replace(/(사람|이|것|길|역할|존재|주인|자리|중심|곁|편)(으로|로|을|를|...)$/, "$1");  // 끝조사
```
- 예) `"...자기 길을 열어 가며"` → `"...자기 길"` (clean).
- **과도 절단 방지:** 절단 후 남는 어절이 2글자 이상일 때만 '~으로/~로' 일반 절단.

## 8.6 [FB2] 프로그램 §3 — 실행모듈 흐름 브릿지

**문제:** §2(3주/3개월/1년 계획)와 §3(매일 굴리는 도구)의 관계가 안 보였다.

**해법(`program.html`):** 두 섹션 사이에 흐름 브릿지 삽입.
```
🔁 3주·3개월·1년 계획  →  🧩 매일 굴리는 도구 3개
```
- CSS: `.module-bridge / .mb-*`. **시각화=치환**(분량 추가 아님).

## 8.7 [FB5] 프로그램 §6 — 다음 단계: 보완 → 축적 사이클 ★

**철학(총괄 지적):** 1년 맞춤 프로그램을 살아냈다면, 다음 1년은 **약점 보완이 아니라** 또 새롭게 '발견 → 살아냄 → 남김'을 반복해 인생 자산이 쌓이는 **나선형**이어야 한다.

**핵심 전환:**
| 폐기 (구) | 채택 (v2.0) |
|-----------|-------------|
| 이번에 약했던 축을 다음 해 과제로 (결손 보충) | 1년 = **완결된 자산 한 묶음**. 그 위에 새 묶음을 쌓음 |
| 닫힌 타임라인 반복 | 🏁 완결 → 발견·살아냄·남김 → 🔄 축적 (나선형) |

**구현(`program-engine.js` nextSteps / _bridgeKo / _bridgeEn):**
```javascript
// 첫 항목: "1년 동안 쌓은 결과물이 그대로 다음 출발점이 됩니다. 끝이 아니라 시작."
//          → primaryDomain·강점·비전 헤드라인으로 변주(variantIdx 0x7F11)
// 둘째 항목 도입(_bridgeKo): "올해 '○○'에서 발견하고 살아내 남긴 한 해 위에, ..."
//          → 약점 보완 프레임 제거, 자산 축적 프레임 (variantIdx 0x7F23)
```
- **자산화 큰 구조 연결:** 이 §6이 [4] 자산화로 가는 나선의 시각적 입구다.

## 8.8 [FB4] 프로그램 §6.5/§7 — 리스크 카드화 + 마무리 삭제 + 안내 배지

- **§6.5 리스크 카드화:** 주의/리스크를 카드(`.risk-cards/.risk-card/.rc-*`)로 직관화.
- **마무리(중복 클로징) 삭제:** §6 사이클이 이미 클로징 역할 → 중복 제거.
- **§7 안내 배지:** 프로그램 근거 안내 + **"Only One" 배지**(`.prog-basis/.pb-*`).

---

# 제9부 · 품질 루브릭 + 4중 검증 게이트

## 9.1 Quality Score — 145점 만점

| 구간 | 점수 | 평가 항목 |
|------|------|-----------|
| 기본 (v1.0) | 100 | 응답 매핑·토큰·13슬롯·진로·다양성·UI·시그니처·안정성 |
| +v1.1 | +15 | subType 균형·강점매칭·융합진로·교육 차등 |
| +v1.2 | +15 | 배포 검증·톤 SSOT·한국어 자연성 |
| +v1.4 | +15 | 2단 구조·디테일 변주·CareerEngine 응답기반 |

## 9.2 등급 판정

| 등급 | 점수 (145 기준) | 판정 |
|------|----------------|------|
| **A · Production Ready** | 134점 이상 (92%+) | 머지·배포 가능 |
| **B · Acceptable** | 112–133 (77%+) | 머지 가능, 개선 권고 |
| **C · Conditional** | 90–111 (62%+) | 1차 개선 후 재평가 |
| **Reject** | 90점 미만 | 머지 불가, 재작업 |

## 9.3 대원칙-B 4중 게이트 (모두 통과해야 머지)

| 게이트 | 검증 내용 |
|--------|-----------|
| ① 폴백(비파괴) | fingerprint 없음 → 기존 출력 폴백, 새 기능이 옛 기능을 깨지 않음 |
| ② 재현성 | 같은 fingerprint 2회 → 동일 결과 |
| ③ 회귀 테스트 | 머지 전후 validateReport 점수 동일(git stash로 확인) |
| ④ 버전 누적 | 옛 규칙 삭제 금지(dead code 보존, 롤백 대비) |

## 9.4 다양성 측정 결과 (1,500명 무작위)

| 영역 | 개선 전 | 개선 후 |
|------|--------|--------|
| 분기 테마 | 4% | 47.7% ▲ |
| 3주 루틴 | 0.3% | 87.7% ▲ |
| 다음 단계 | 3.5% | 37.6% ▲ (구조 한계 내 최대) |
| 비전 종합 | — | 99.1% |
| 진로 | — | 89.6% |
| 고유한 결 | — | 74.1% |
| 여는 길 | — | 88.9% |
| 직무 적합성 | — | 75.9% |

---

# 제10부 · 배포 · 운영 · 기술 스택

## 10.1 기술 스택

| 구성 | 기술 |
|------|------|
| 호스팅 | Firebase Hosting |
| CI/CD | GitHub Actions (push to main → 자동 배포, ~2–4분) |
| 레포 | `ghwelcome0-cloud/lifeportfolio-site` |
| 도메인 | lifeportfolio.co.kr |
| 엔진 | 순수 JS (규칙 기반 합성, NOT LLM) |

## 10.2 핵심 파일 지도

| 파일 | 역할 |
|------|------|
| `report.html` | 리포트 렌더링 + 시각화(축 카드/스텝 카드/큐레이션 카드) |
| `assets/js/report-engine-v4.js` | 리포트 합성 엔진 (사명·비전·진로·요약) |
| `program.html` | 프로그램 렌더링 + 시각화(브릿지/사이클/리스크 카드) |
| `assets/js/program-engine.js` | 프로그램 합성 엔진 (3주/3개월/1년·모듈·다음단계) |
| `firebase.json` | `cleanUrls:true` (→ `/report.html`이 `/report`로 301) |

## 10.3 배포 절차

```bash
# 1. GitHub 인증(만료 시 재실행)
setup_github_environment
# 2. 커밋·푸시
git add -A && git commit -m "메시지" && git push origin main
# 3. GitHub Actions가 Firebase Hosting 자동 배포(~2-4분)
# 4. 라이브 검증 — cleanUrls 때문에 반드시 /report, /program 으로 확인
curl -s https://lifeportfolio.co.kr/report | grep "마커"
curl -s https://lifeportfolio.co.kr/program | grep "마커"
```

> **cleanUrls 주의:** `/report.html` → `/report`로 301 리다이렉트. 라이브 검증은 항상 **확장자 없는 경로**로.

## 10.4 샘플 생성 파이프라인 (검증용)

```
_gen_sample_report.cjs  (실제 엔진 → _sample_report.json)
   → _gen_sample_html.cjs  (동적 findLine 경계 추출 → sample-report.html)
   → _shot_sample.cjs      (puppeteer 스크린샷, PM2 localhost:3000)
```
- 샘플 인물: 김의미, Q75=['교육','콘텐츠'], warm_connector 톤.

---

# 부록

## A. 용어집

| 용어 | 뜻 |
|------|-----|
| MaaS | Meaning-as-a-Service. 의미를 서비스로 제공 |
| Only One | 80억 인구 누구나 받는 유일한 진단 |
| 대원칙-A | 고유성 종합 × 직관적 단일표현(2단 구조) |
| 대원칙-B | 축적 원칙(비파괴·재현성) |
| SSOT | Single Source of Truth. 톤·진로 결정의 단일 진실 소스 |
| fingerprint | 응답 전체의 해시 = 고유성의 씨앗 |
| variantIdx | fingerprint 기반 결정적 변주 선택 함수 |
| 2단 구조 | 1단 헤드라인(직관) + 2단 디테일(응답 종합 고유성) |
| 평이체(P31) | 명사형 정지어 → 동사형 진행어, 쉬운 표현 |
| subType | 진로 5종(practitioner/researcher/business/media/policy) |
| 나선형 성장 | 1년 사이클 완결 → 다음 사이클로 자산을 쌓는 축적 구조 |

## B. 자산화 큰 구조 요약 (제0부 재게시 — 가장 중요)

```
[1] 리포트 + 맞춤 실행프로그램  →  [2] 코칭 + 다이어리  →  [3] 방향별 커뮤니티  →  [4] 인생 자산화
        발견 + 설계                    살아냄(지속)            남김(연결)            가치·연결성 ↑
```

## C. 머지 전 최종 체크리스트

- [ ] 응답 데이터 100% 우선(원칙 01) — 톤 충돌 시 응답 우선
- [ ] 미치환 `{{…}}` 0개(원칙 03)
- [ ] 톤 SSOT — 리포트↔프로그램 톤 동일(R7·P6)
- [ ] 2단 구조 적용 — 1단 헤드라인 + 2단 응답 변주(대원칙-A)
- [ ] 폴백 경로 존재 — fingerprint 없음 시 기존 출력(B-1)
- [ ] 재현성 — 같은 fingerprint 2회 동일(B-2)
- [ ] 회귀 0 — validateReport 점수 머지 전후 동일(git stash 확인)
- [ ] 시각화 = 치환/직관화 — 분량 추가 아님(인바디 원칙)
- [ ] 축 중복 dedup 통과 — 카드 축 라벨 유일(H1-fix)
- [ ] 한국어 자연성 — 조사·자모·연결어미 보정 통과(원칙 05, josa)
- [ ] 라이브 검증 — `/report`·`/program` 마커 확인(cleanUrls)

## D. 도달점 선언

> 80억의 고유성 — Only One 진단.
> 개인 1명(응답 직합성) → 국내 5,000만(최소 보장선) → 80억 인구(고유성 도달점).
> 인생포트폴리오는 한 사람의 인생을 자산화한다. 누구도 따라 할 수 있지만, 진짜로 따라 하면 말씀대로 살고 열매를 맺는 길로.

*"발견 → 살아냄 → 남김 → 인생 자산화"* — 인생포트폴리오 · LifePortfolio · Production Rules v2.0

— 끝 —

# 하이브리드 3D 모션 — 콘텐츠 일괄 생성 제작 규칙 v1.0

> **문서 성격**: 이 문서는 "배우는 글"이 아니라 **"따라 하면 같은 품질이 나오는 규칙"** 입니다.
> 학습용 해설은 [`70_HYBRID_3D_MOTION_GUIDE.md`](./70_HYBRID_3D_MOTION_GUIDE.md) 를 보십시오.
> 이 문서는 그 위에서, **롱폼 v14 를 실제로 완성하며 확정된 판정 기준**만 모았습니다.

- **작성**: 2026-08-17
- **근거 산출물**: 롱폼 본편 v14 (405.233초 / 12157프레임)
- **왜 지금 만드는가**: 대표님 지시 — *"롱폼이 제작되고 최종 승인을 해야 이걸 기준으로 하이브리드 3D 모션 영상 기법을 제작 규칙으로 삼아서 앞으로 콘텐츠들 일괄 생성이 가능하니."*
- **상태**: v14 렌더/검수 완료 · **대표님 최종 승인 대기**. 승인 시 이 문서가 정본 규칙이 됩니다.

---

## 0. 이 문서가 존재하는 이유 — 한 번 크게 틀렸기 때문

직전 보고에서 우리는 **"정지 화면 60.2% → 0.0%"** 라고 보고했습니다.
그 수치 자체는 사실입니다. 정본 검사 도구가 실제로 0건을 반환했습니다.

그런데 대표님이 이렇게 지적하셨습니다.

> **"46초 구간에 나오는 영상이 일시정지 되는 모습이 끊김이 있어요."**

재조사 결과 **도구가 46초 구간을 애초에 검사하지 않았다**는 것이 밝혀졌습니다.

| | 내용 |
|---|---|
| 검사 규칙 | 프레임 간 화소차 **< 0.35** 이면 "정지" |
| 46초 구간 실제 | 아주 미세하게 움직여서 **0.35 를 간신히 넘김** |
| 결과 | 기계 판정 = "움직임" → **후보 목록에 아예 오르지 않음** |
| 사람 눈 | **"멈춰 있다"** |

**"0건"은 "결함이 없다"가 아니라 "검사 대상이 아니었다"는 뜻이었습니다.**

새 자(尺)로 다시 재니 같은 성질의 결함이 **8건 48.5초, 본편의 12.0%** 있었습니다.
46초는 그중 **1건**일 뿐이었고, 가장 큰 것은 **375.5~405.0초 — 영상 마지막 30초 전체**였습니다.
엔딩이 통째로 정지처럼 보이고 있었는데, 우리는 그것을 "0건"이라고 보고하고 있었습니다.

> **이 문서의 규칙들은 전부 이 사고에서 나왔습니다.**
> 규칙을 지키는 것보다 **왜 그 규칙이 생겼는지 아는 것**이 중요합니다.
> 근거를 모르면 상황이 조금만 달라져도 같은 사고가 반복됩니다.

---

## 1. 제작 규칙 — 5단계 (이 순서를 바꾸지 마십시오)

```
 STEP 1  콘텐츠 유형 분류      → 어디에 모션이 "필요한가"를 정한다
 STEP 2  소재 기하 측정        → 어디까지 확대해도 "안 잘리는가"를 정한다
 STEP 3  구간 확정             → 어디서 어디까지가 "한 화면인가"를 정한다
 STEP 4  렌더                  → 만든다
 STEP 5  이중 검수 (기계 + 눈) → 됐는지 판정한다
```

각 단계는 **앞 단계의 산출물이 있어야** 시작할 수 있습니다.
순서를 건너뛰면 반드시 되돌아옵니다 (§6 의 실패 사례 4건이 전부 그렇습니다).

---

## 2. STEP 1 — 콘텐츠 유형 분류 (가장 중요)

### 2.1 왜 유형부터 나누는가

**모든 정지가 결함이 아닙니다.**

| 유형 | 화면 예 | 정지했을 때 | 조치 |
|---|---|---|---|
| **SLIDE** | 흰 배경 + 큰 텍스트 | **정상** — 읽는 시간이다 | ❌ 모션 넣지 말 것 |
| **PHOTO** | 인물, 사물, 실사 사진 | **결함** — 영상이 멈춘 것처럼 보인다 | ✅ 반드시 모션 |
| **MIXED** | 인포그래픽, 일러스트+글 | 길면 결함 | ⚠️ 4초 이상일 때만 |

슬라이드에 카메라를 움직이면 **글자가 흔들려서 읽기 나빠집니다.**
실제로 63.5~68.0초(슬라이드)에 모션을 넣었다가 반려했습니다(§6-D).

### 2.2 분류 공식 (변경 금지)

```python
# 320x180 축소 프레임 기준
flat = (국소표준편차 < 2.0 인 화소 비율)   # 평평한 면적 = 단색 배경 비율
uc   = (24단계 양자화 후 고유 색 수)        # 색 다양성

def kind(frame):
    if flat > 0.62 and uc < 55:  return "SLIDE"   # 넓은 단색 + 색 적음
    if flat < 0.45 or  uc > 80:  return "PHOTO"   # 질감 많음 or 색 많음
    return "MIXED"
```

> ⚠️ **이 임계값을 임의로 바꾸지 마십시오.**
> 롱폼 22블록 + 추가 7구간, 총 29구간에서 검증된 값입니다.
> 바꾸면 과거 판정과 비교가 성립하지 않습니다.

### 2.3 실제 분포 (롱폼 v14 · 참고선)

| | flat | uc |
|---|---|---|
| SLIDE 실측 | 0.73 ~ 0.86 | 31 ~ 53 |
| PHOTO 실측 | 0.37 ~ 0.79 | 99 ~ 271 |
| MIXED 실측 | 0.60 ~ 0.90 | 55 ~ 73 |

**`uc` 가 100 을 넘으면 거의 확실히 사진입니다.** 가장 빠른 눈대중 기준입니다.

---

## 3. STEP 2 — 소재 기하 측정 (잘림 방지)

### 3.1 왜 필요한가

**"모든 컷을 10% 확대" 같은 일괄 값은 반드시 사고를 냅니다.**

피사체가 화면 가장자리에 붙어 있으면 조금만 확대해도 잘립니다.
롱폼 46초 구간이 정확히 그랬습니다.

| 구간 | 좌 여백 | 우 여백 | 안전 최대 배율 |
|---|---|---|---|
| **44.1-50.9 (46초 구간)** | **0.2%** | 13.7% | **1.0347** ← 확대 여력 3.5% 뿐 |
| 70.5-80.3 | 1.9% | 0.2% | 1.0583 |
| 153.1-157.9 | 22.8% | 24.7% | 1.2787 |
| 179.5-183.9 | 11.7% | 12.0% | 1.0961 |
| 375.5-405.1 | 9.1% | 17.3% | 1.2165 |

같은 영상 안에서도 안전 배율이 **1.03 ~ 1.28** 로 8배 넘게 차이납니다.
**소재를 보지 않고 정한 파라미터는 사고를 만듭니다.**

### 3.2 측정 절차

1. 구간에서 **13장**을 균등 샘플링
2. 각 장에서 **잉크 경계상자(ink bbox)** 를 구한다
   - Sobel 경계강도 > 14.0 인 화소를 "내용"으로 본다
   - 행/열 누적 프로파일에서 전체의 0.20% 미만인 줄은 잡음으로 버린다
3. 13장 bbox 의 **합집합** 을 취한다 (한 장이라도 잘리면 안 되므로)
4. 팬 이동폭을 고려해 안전 최대 배율을 역산한다

```python
def max_zoom_for(bbox, pan_span):
    px = pan_span; py = pan_span * 0.6      # 세로 팬은 가로의 60%
    x0, y0, x1, y1 = bbox                    # 0~1 정규화
    need_w = 2.0 * max(abs(0.5 - px - x0), abs(x1 - (0.5 + px)))
    need_h = 2.0 * max(abs(0.5 - py - y0), abs(y1 - (0.5 + py)))
    return 1.0 / max(need_w, need_h)
```

5. **적용 배율 = min(전역 상한 1.10, 측정된 안전 배율)**

### 3.3 팬 방향 결정

```python
# 여백이 넓은 쪽을 "버린다" — 좁은 쪽으로는 갈 수 없다
if abs(L - R) < 0.010:  방향 = drift_in / drift_out   # 좌우 대칭 → 줌만
else:                   방향 = drift_right if L > R else drift_left
```

---

## 4. STEP 3 — 구간 확정 (경계 설정)

### 4.1 함정: 화면이 바뀌는 방식은 2가지다

| 사건 | 화면 | 처리 |
|---|---|---|
| **컷(cut)** | 완전히 다른 화면으로 전환 | 여기서 **끊어야 한다** |
| **요소 등장** | 같은 화면에 항목 하나가 나타남 | 끊으면 **안 된다** |

인접 프레임 화소차 하나로는 **둘을 구분할 수 없습니다.**

실제 사례 — 47.000초 지점:

```
인접 프레임차 17.96  (임계 12.0 초과)  →  1차 판정: "컷"   ❌ 오판
```

### 4.2 해법: 두 번째 지표를 도입한다

**기준 프레임(±0.60초) 대비 누적 화소차**로 재측정합니다.

```
46.20초 기준 프레임과 비교:
  44.00 → 21.6      45.00 → 22.6
  46.00 →  4.7  ★   47.00 →  5.5  ★   48.00 →  5.5  ★   50.00 →  6.2  ★
  51.00 → 55.2  ← 진짜 컷
```

46.0~50.5 는 전부 **같은 화면**입니다. 47.00 은 그 위에 요소 하나가 나타난 것뿐입니다.

```python
ADJ_TOL  = 12.0   # 인접차 — "후보"만 만든다
SAME_TOL = 25.0   # 기준 대비 누적차 — 이 아래면 "같은 화면"

acc = |frame(t + 0.60) - frame(t - 0.60)|.mean()
is_cut = (acc >= SAME_TOL)
```

> **규칙: 인접차는 후보 생성용으로만 쓴다. 컷 확정은 반드시 누적차로 한다.**

### 4.3 경계는 컷만이 아니다

이미 모션을 넣은 블록의 **경계도 벽**입니다. 넘어가면 이중 적용됩니다.

```python
lo = max([lo] + [끝 for 시작, 끝 in 기처리구간 if 끝 <= a + 0.05])
hi = min([hi] + [시작 for 시작, 끝 in 기처리구간 if 시작 >= b - 0.05])
```

### 4.4 컷이 없어도 유형이 바뀌면 자른다 ★

**가장 놓치기 쉬운 규칙입니다.**

70.400초에는 컷이 없습니다. 그러나 콘텐츠 유형이 바뀝니다.

```
63.5 ~ 68.0   SLIDE  (flat 0.73 / uc  53)   정지가 정상
70.5 ~ 78.0   PHOTO  (flat 0.79 / uc 141)   정지가 결함
```

이 둘을 **한 계획으로 렌더하면 실패합니다.** 실제로 실패했습니다(§6-D).

> **규칙: 컷 경계 + 기처리 블록 경계 + 콘텐츠 유형 경계 — 3가지 모두를 벽으로 삼는다.**

---

## 5. STEP 5 — 이중 검수 (기계 + 눈)

### 5.1 게이트 3종 — 전부 통과해야 한다

| 게이트 | 무엇을 보는가 | 통과 조건 |
|---|---|---|
| **T-1 잘림** | 소재 밖을 샘플링했는가 | 가장자리 유지율 ≥ 0.90 |
| **T-2 정지** | 완전히 멈춘 프레임이 있는가 | **1.5초(7.5프레임) 연속** 으로 화소차 < 0.35 인 구간 = 0 |
| **T-3 급변** | 이동이 너무 빠른가 | 초당 이동 ≤ 16.0px |
| **★T-4 저모션** | **충분히 움직이는가** | **2초 광류 p95 중앙값 ≥ 1.5px** |

**T-4 가 이번에 새로 만든 게이트입니다.** 이것이 없어서 46초를 놓쳤습니다.

### 5.2 T-4 의 임계 1.5px 은 어디서 나왔는가 (지어낸 값이 아님)

엔진 설계 상수에서 **역산**했습니다.

```
2초 동안의 줌  = ZOOM_RATE 0.009 × 2 = 1.8%  → 320px 폭 기준 가장자리 이동 ≈ 2.9px
2초 동안의 팬  = PAN_RATE  0.0055 × 2 = 1.1% → 320px 폭 기준 이동         ≈ 3.5px
⇒ 엔진이 정상 작동한 구간은 2초 p95 가 3~5px 이어야 한다
```

**실측 검증**: 롱폼 807개 측정점의 중앙값 = **3.231px** — 설계값과 일치했습니다.

```
1.5px = 설계 기대치의 절반 미만 = "엔진이 걸리지 않았다"
```

### 5.3 T-2 판정 시 주의 — 1프레임은 정지가 아니다

렌더러 자체 검사는 **1프레임이라도** 임계 미만이면 경고를 냅니다. 이는 **과보수적**입니다.

**정본 판정은 "1.5초(7.5프레임) 연속"입니다.**

실제 사례 — 70.5-80.3초 구간(Y02):

```
렌더러 자체 검사 : 48개 측정점 중 1개가 0.3397 (임계 0.35 를 0.0103 차이로 스침) → 경고
정본 판정        : 연속 1프레임(0.2초)뿐 → 정지 ★0건★
기승인 29블록 대조: t2_min p10 = 0.531, 0.40 미만이 이미 2건 (승인된 것)
⇒ 수용
```

> **규칙: 렌더러 경고는 "확인하라"는 신호이지 "실패"가 아니다.**
> **판정은 반드시 정본 도구(1.5초 연속 규칙)로 한다.**

### 5.4 눈 검수 — 비전 모델 사용 시 필수 주의 ★

**프레임을 여러 장 따로 넘기면 안 됩니다.**

실제로 46초 구간 검증에서 46.0s / 48.0s / 50.0s 세 장을 넘겼더니
**세 장 모두 "MOTION VISIBLE: NO"** 라는 답이 돌아왔습니다.

그런데 화소 실측은 정반대였습니다.

| 46초 구간 2초 이동량 | 개선 전 | 개선 후 |
|---|---|---|
| 45.0 → 47.0초 | 1.27px | **4.08px** (3.2배) |
| 47.0 → 49.0초 | 1.60px | **7.47px** (4.7배) |

응답을 확인하니 **3장이 각각 독립 응답으로 처리**되어 있었습니다.
모델은 세 장을 나란히 놓고 본 적이 없습니다.
**"동일하다"는 진술은 비교의 결과가 아니라 추측이었습니다.**

> **규칙: 비교 판정을 모델에게 맡기지 말고, 비교 결과 자체를 한 장의 이미지로 구워서 넘긴다.**
> 차분맵(frame difference heatmap)을 개선 전/후 2행으로 배치하면
> 모델은 한 이미지 안에서 비교할 수밖에 없습니다.
> 이 방식으로 다시 물으니 **"MOTION INCREASED: YES"** 로 정확히 답했습니다.

---

## 6. 실패 사례 4건 — 그대로 반복하지 마십시오

### A. 인접 화소차만으로 컷을 판정 → 같은 화면을 두 동강

47.000초의 인접차 17.96 을 컷으로 오판. 실제로는 요소 등장.
**→ 기준 프레임 대비 누적차(§4.2)로 재판정해야 함**

### B. 이미 처리한 구간을 다시 후보로 집계

225.5-227.5초가 저모션으로 잡혔습니다. 그런데 그 구간은 **이미 모션이 들어간 블록 내부**였습니다.

```
해당 블록(215.6-240.4) 내부 분포:  min 1.498 · p50 2.772 · 1.5px 미만 비율 2.2%
다른 정상 블록:  B07 min 1.503 / B12 min 2.074 / B20 min 2.406
```

임계 1.500 을 **0.002px** 차이로 스친 **사인파의 골짜기**였습니다.
모션은 사인파이므로 골짜기에서 느려지는 것이 설계상 당연합니다.

**→ 기처리 구간과 겹치는 후보는 먼저 제외한다**
**→ 이상치를 봤으면 "고칠 것"이 아니라 "측정 방식 탓인지"부터 묻는다**

### C. 안전 구간이 기존 블록을 침범

컷 간격이 넓어 안전 구간이 56.50-80.30 까지 벌어져 기존 블록 3개와 겹쳤습니다.
**→ 기존 블록 경계도 벽으로 삼는다 (§4.3)**

### D. SLIDE + PHOTO 를 한 계획으로 렌더 ★가장 값비싼 실패

63.50-80.30(16.8초)을 통째로 렌더 → **T-2 정지 5건, 광류 2.114 로 실패.**

원인 2가지가 동시에 작용했습니다.
1. 구간이 길어 진폭이 희석됨 (scale 0.35 까지 축소됨)
2. **정지가 정상인 슬라이드에까지 카메라가 움직임**

**→ SLIDE 부분(63.5-68.0)을 잘라내고 PHOTO 부분(70.5-80.3, 9.8초)만 재렌더**
**→ 결과: T-2 5건 → 1건, 광류 2.114 → 2.464 로 개선**

---

## 7. 엔진 상수 (변경 시 전체 재검증 필요)

```python
FPS            = 30
ZOOM_RATE      = 0.009      # 초당 확대율
ZOOM_SPAN_MAX  = 0.075      # 총 확대 상한
PAN_RATE       = 0.0055     # 초당 팬 이동률
PAN_SPAN_MAX   = 0.030      # 총 팬 상한
BREATH_AMP     = 0.0022     # 호흡 진폭
BREATH_HZ      = 0.09       # 호흡 주기
SHORT_DUR      = 4.0        # 이보다 짧으면 짧은 컷 처리

MIN_EDGE_KEEP  = 0.90       # T-1 잘림 게이트
FREEZE_TOL     = 0.35       # T-2 정지 임계 (화소차)
FREEZE_FPS     = 5.0        # T-2 측정 프레임레이트
MAX_SHIFT_RATE = 16.0       # T-3 급변 게이트 (px/s)
LOW_THR        = 1.5        # ★T-4 저모션 게이트 (2초 광류 p95, px)

LINEAR_MIX     = 0.85       # 선형 성분 비율
MIN_SCALE      = 0.30       # 진폭 축소 바닥
SAFE_ZOOM_CAP  = 1.10       # 전역 확대 상한
```

---

## 8. 롱폼 v14 최종 실측 (다음 콘텐츠의 비교 기준선)

| 항목 | 개선 전 | **개선 후** |
|---|---|---|
| 정지 화면(원본 대비) | 60.2% (37건 244초) | — |
| **저모션 PHOTO** | **8건 48.5초 (12.0%)** | **2건 5.0초 (1.2%)** ← 89.7% 감소 |
| **정본 정지 검사(PHOTO)** | — | **0건 (0.0%)** |
| 전체 광류 p50 | 3.231px | **3.489px** |
| **엔진 미적용 구간 p50** | **0.785px** | **3.291px** ← **4.19배** |
| 46초 구간 광류 p95 | 0.944px | **7.453px** ← **7.9배** |
| 길이 | 405.233초 | 405.233초 (오차 0) |
| 프레임 | 12157 | 12157 (오차 0) |
| 오디오 | -16.0 LUFS / TP -1.5 dBFS | **동일 (무접촉)** |

### 8.1 남은 2건 5.0초(1.2%) 의 정체 — 둘 다 "고칠 것"이 아니다

**(a) 225.5-227.5초 — 오탐**

이미 모션이 들어간 블록 내부입니다. 임계 1.500 을 **0.002px** 차이로 스친
사인파의 골짜기였습니다. 상세는 §6-B.

**(b) 76.0-79.0초 — 소재의 구조적 제약 (측정은 정확함)**

광류 p95 가 0.895px 로 임계 1.5px 에 미달합니다.
**"안 움직인 것"인지 "더 못 움직이는 것"인지**를 가르기 위해
**서로 다른 두 방법**으로 교차 검산했습니다.

| 방법 | 측정 해상도 | 결과 |
|---|---|---|
| Farneback 광류 p95 | 320px 폭 | **0.895px** |
| 위상상관 평행이동 | 1280px 폭 → 320 환산 | **0.897px** |

**소수점 셋째 자리까지 일치했습니다.** 측정 오류가 아니라 실제 값입니다.

그런데 **원본 해상도(1280px) 기준 실제 이동량**은 이렇습니다.

| 창 | 개선 전 | 개선 후 | 배율 |
|---|---|---|---|
| 74.0→76.0s | 1.66px | **4.50px** | 2.7배 |
| 76.0→78.0s | 1.20px | **3.08px** | 2.6배 |
| 77.0→79.0s | 0.60px | **3.18px** | **5.3배** |

**사람이 보는 1280px 화면에서는 3.08~4.50px 이동하고 있습니다.**
그럼에도 임계를 못 넘는 이유는 소재의 여백입니다.

```
좌 여백 1.9%  ·  우 여백 0.2%   →  확대 여력 5.8%
안전 최대 배율 zmax_safe = 1.0583   (전역 상한 1.10 이 아니라 소재가 정한 값)
⇒ 팬으로 이동할 수 있는 폭 자체가 물리적으로 좁다
```

**더 키우면 우측 잉크가 잘립니다(여백 0.2%).**
**잘림은 정지보다 나쁜 결함입니다.** 따라서 여기서 멈추는 것이 옳습니다.

⇒ **결론: 실질 잔여 결함 0건.** 2건 모두 조치 대상이 아닙니다.

> **여기서 얻은 규칙**: 게이트 미달을 봤을 때 곧바로 재렌더하지 마십시오.
> **① 다른 방법으로 교차 검산**해 측정이 맞는지 보고,
> **② 소재의 물리적 한계인지**를 확인하십시오.
> 한계인데 억지로 밀어붙이면 정지 대신 잘림이 생깁니다.

---

## 9. 체크리스트 (신규 콘텐츠 제작 시 그대로 사용)

```
□ STEP 1  전 구간 flat/uc 측정 → SLIDE / PHOTO / MIXED 분류표 작성
□ STEP 1  SLIDE 구간을 모션 대상에서 제외했는가
□ STEP 2  구간별로 ink bbox 를 측정했는가 (일괄 배율 사용 금지)
□ STEP 2  적용 배율 = min(1.10, 측정 안전배율) 로 계산했는가
□ STEP 3  컷 판정을 누적차(SAME_TOL 25.0)로 확정했는가
□ STEP 3  기처리 블록 경계를 벽으로 넣었는가
□ STEP 3  콘텐츠 유형이 바뀌는 지점에서 잘랐는가
□ STEP 4  구간 길이가 과도하게 길지 않은가 (길면 진폭이 희석됨)
□ STEP 5  T-1 / T-2 / T-3 / T-4 네 게이트를 전부 돌렸는가
□ STEP 5  T-2 는 정본 도구(1.5초 연속 규칙)로 판정했는가
□ STEP 5  개선 전/후를 ★같은 자로★ 재서 대조표를 만들었는가
□ STEP 5  게이트 미달 시 ★다른 방법으로 교차 검산★ 했는가 (재렌더 전에)
□ STEP 5  미달이 ★소재의 물리적 한계★ 인지 확인했는가 (한계면 수용이 옳다)
□ STEP 5  눈 검수를 차분맵 합성 이미지 1장으로 넘겼는가 (여러 장 따로 금지)
□ 최종     길이·프레임 수가 원본과 일치하는가
□ 최종     오디오 스트림이 무접촉인가 (codec/sample_rate/nb_frames/bit_rate 동일)
```

---

## 10. 다음 작업자에게 (항구 지침 · 축적용)

이 문서를 고칠 때는 **왜 고치는지를 반드시 함께 적으십시오.**
숫자만 바꾸면 다음 사람은 그 숫자의 근거를 알 수 없고, 상황이 달라졌을 때 판단할 수 없습니다.

특히 다음 4가지는 **근거 없이 바꾸지 마십시오.**

1. `kind()` 의 임계 (0.62 / 55 / 0.45 / 80) — 29구간 검증값
2. `FREEZE_TOL 0.35` 와 **1.5초 연속 규칙** — 이 둘은 세트입니다
3. `LOW_THR 1.5` — 엔진 상수에서 역산한 값 (§5.2)
4. `SAME_TOL 25.0` — 컷/요소등장 구분 기준 (§4.2)

그리고 무엇보다,

> **"검사에서 0건이 나왔다"를 "결함이 없다"로 읽지 마십시오.**
> 반드시 **"무엇을 검사 대상으로 삼았는지"** 를 함께 확인하십시오.
> 이 문서 전체가 그 한 번의 착각에서 나왔습니다.

---

## 11. 제14조 ⑬⑭항 신설 — 색상 코딩 3D 프리비즈 → Video-to-Video

> **개정 사유.** 대표님이 2026-08-22 지정하신 튜토리얼(YouTube `LomMGF7kcBU`)을 분석해
> 벤치마크급 3D 패럴랙스의 **정답 기법**을 확정했습니다. 그리고 그것을 우리 장면으로
> 실제 렌더·실제 생성까지 완주해 검증했습니다. 이 절은 그 결과를 항구 규정으로 고정합니다.
>
> 이 개정이 필요했던 이유는 단순합니다. 우리는 8개월 동안 **"AI 에게 카메라 무빙을 말로 설명"**
> 하고 있었고, 그것은 원리적으로 불가능한 일이었습니다. 문제는 프롬프트 문장력이 아니라
> **AI 에게 공간을 알려주는 채널이 없었다는 것**이었습니다.

### ⑬ 컷 내부 모션 의무 + 원근 시차 필수 (CEO-51 근거)

대표님 지시 원문:

> "맞네. **컷 안에서 움직여야 하네!** 그리고 **정지 구간이 없어요.** 맞아요. **원근 시차**도
> 그렇고요. 벽을 통과하고, 바닥 슬래브의 아랫면이 보이는 것처럼 우리도 우리의 상황에 맞게
> 표현을 했어야 해요."

따라서 모든 컷은 다음 3개를 **동시에** 만족해야 합니다.

| 요건 | 정의 | 판정 방법 |
|---|---|---|
| **컷 내부 모션** | 전환(이음새)이 아니라 **컷 한가운데**에서 화면이 변한다 | 컨택트시트에서 같은 컷의 첫/중/끝 프레임의 **구도가 다른가** |
| **원근 시차** | 근거리 물체가 원거리 물체보다 **빠르게** 이동하고, 물체가 **서로를 가린다** | 컨택트시트에서 **가려짐이 벗겨지는가**(뒤에 있던 것이 새로 드러나는가) |
| **정지 구간** | 컷 시작부에 카메라가 멈춘 구간이 있다 | `hold_frac` 0.15~0.35 (R2: 최소 29프레임) |

**금지.** 켄번즈 확대(zoompan)만으로 위 3개를 만족시킬 수 없습니다.
확대는 아핀 변환이고, 아핀 변환에는 **가려짐 변화가 원리적으로 없습니다.**

### ⑭ 색상 코딩 3D 프리비즈를 Video-to-Video 구조 레퍼런스로 쓴다

#### ⑭-1. 원리 — 무엇이 문제였고 무엇이 해법인가

AI 비디오에는 두 근본 결함이 있습니다.

1. **카메라 무빙을 제어할 수 없다** — 말로 아무리 정밀하게 써도 모델이 제멋대로 움직입니다
2. **피사체가 모핑된다** — 프레임이 진행되며 형태가 붕괴합니다

실패 사례(튜토리얼 원문 · 이 정도로 정밀하게 써도 실패합니다):

```
The camera starts behind him at eye level, then sweeps around his right side,
rising as it circles about ninety degrees, tilting down while it passes overhead,
accelerating hard through the turn, then dropping low behind his back and
settling just above the grass, tilted up.
```

**해법.** 카메라 무빙과 공간을 **말로 전달하지 않고, 3D 로 계산해서 영상으로 전달합니다.**

```
Blender (단순 도형 + 원색)  ──렌더──▶  프리비즈 mp4
                                          │  @Video1 = 구조(카메라·원근·타이밍)
실사 plate ───────────────────────────────┤  @Image1 = 질감(빛·재질·환경)
                                          ▼
                                  AI 생성기 (Video-to-Video)
                                          ▼
                            3D 가 계산한 완벽한 원근 + 실사 질감
```

**핵심.** AI 는 "상상"하지 않습니다. 3D 소프트웨어가 **계산한** 원근법과 무빙을 그대로
유지한 채 **질감만 교체**합니다. 그래서 패럴랙스가 계산으로 보장됩니다.

#### ⑭-2. 3대 규칙 (튜토리얼이 "가장 중요"라고 명시)

1. **피사체는 단순 도형으로.** 디테일 모델링은 AI 실사 생성과 **간섭**해 품질을 떨어뜨립니다.
2. **피사체는 색으로 구분한다.** 마젠타/시안/옐로/레드 등 **채도 높은 원색**.
3. **역할 구분을 확실하게.** 한 색에 두 역할을 겹치지 않습니다.

> 3번이 가장 자주 위반됩니다. "이 색이 무엇이 될지"를 프롬프트에서 1:1 로 지정해야
> AI 가 색을 역할로 읽습니다. 색이 겹치면 AI 가 두 물체를 하나로 합칩니다.

#### ⑭-3. 프리비즈 렌더 설정 — 반드시 이 값으로 (실측 근거)

프리비즈는 **발광(emission) 원색**이므로 **광선 추적이 필요 없습니다.** 따라서:

```python
S.render.engine = 'CYCLES'; S.cycles.device = 'CPU'
S.cycles.samples = 1              # 발광이므로 1샘플로 충분
S.cycles.max_bounces = 0          # 반사·굴절 없음
S.cycles.use_denoising = False    # 노이즈가 없으므로 불필요
S.render.resolution_x = 832; S.render.resolution_y = 468
```

**실측 속도 계층 (같은 샌드박스 · 2코어 · RAM 3GB · GPU 없음):**

| 설정 | 속도 | 판정 |
|---|---|---|
| 1920x1080 / 48spp / 일반 씬 | 115초 초과 (타임아웃) | ✗ |
| 1280x720 / 8spp / 발광 | **10.3 s/frame** (500초 = 34시간) | ✗ |
| **832x468 / 1spp / 발광** | **0.196 s/frame** (500초 = 약 40분) | **✓ 채택** |

**교훈.** 같은 엔진(Cycles CPU)인데 **52배** 차이가 납니다.
"Cycles 는 느리다"가 아니라 **"프리비즈에 불필요한 설정을 켜두면 느리다"** 였습니다.

**EEVEE 는 이 환경에서 불가합니다.** `EGL_BAD_MATCH` (GPU 없음) → llvmpipe 재시도 → OOM(3GB).
따라서 Cycles CPU + 위 설정이 **유일한 성립 경로**입니다.

#### ⑭-4. 프리비즈 세트 규칙 — 검은 구멍 방지

카메라가 배경 밖을 보면 프레임에 **검은 영역**이 생기고, AI 는 그것을 그대로 검게 재현합니다.

```
세트는 카메라 궤적보다 넉넉하게 세운다.
  바닥  16 x 12 m       (카메라 최대 반경의 2배 이상)
  벽    후면 + 좌 + 우, 높이 4.4 m
```

우리 v1 프리비즈에서 프레임 1·48 상단에 검은 구멍이 났고, 세트를 위 크기로 키워 해소했습니다.

#### ⑭-5. Video-to-Video 프롬프트 골격 (이 구조를 그대로 씁니다)

```
[1행] 실사 톤 + 장면 + 촬영 스타일. 한 컷 연속.

@Video1 is the CAMERA AND SPATIAL reference: follow its camera path, framing,
perspective and timing 1:1 frame for frame. Its coloured flat planes are position
markers for real objects, and its small red blocks are position markers for
glowing highlighted words.

@Image1 is the environment, lighting and texture reference: <plate 설명>.

Color-based proxy replacement:
  the magenta plane becomes <역할1>,
  the cyan plane becomes <역할2>,
  the yellow plane becomes <역할3>,
  the dark plane becomes <바닥>,
  the brown walls become <배경>,
  the red cylinder becomes <소품>,
  the three small red blocks become <강조 요소 + 그 움직임의 의미>.

[카메라 서술] The camera holds still at the start, then <무빙>, keeping true 3D
perspective parallax: the near <A> slides across frame faster than the far <B>,
and the <A>s occlude each other as the camera passes.

No text overlays, no captions, no subtitles.
```

**필수 5요소.** ① `@Video1 = CAMERA AND SPATIAL reference` 명시 ② `1:1 frame for frame`
③ `Color-based proxy replacement` 로 색→역할 1:1 매핑 ④ `No text overlays` (자막은 우리가 굽습니다)
⑤ **★`NOT a marker` 선언★** — 프리비즈 안에 있는 것 중 **교체 대상이 아니라 최종물**인 것
(한글 글자·로고·수치)은 반드시 아래 문장으로 못박습니다. 빠뜨리면 AI 가 임의로 채웁니다(교훈 165).

```
The RED KOREAN TEXT visible in @Video1 is NOT a marker — it is the actual final text
and must be reproduced EXACTLY as the identical Korean word, same glyphs, same position,
same rotation, same scale, frame for frame.
Preserve the Korean characters exactly as shown in @Video1 — do not translate,
do not substitute Latin letters, do not invent new words.
```

#### ⑭-6. 실측 검증 기록 (2026-08-22)

| 단계 | 도구 | 결과 |
|---|---|---|
| 프리비즈 렌더 | bpy 5.2.0 Cycles CPU | 72프레임 14.1초 (**0.195 s/frame**) |
| 프리비즈 인코딩 | ffmpeg libx264 | 832x468 24fps 3.0초 |
| Video-to-Video | `fal-ai/bytedance/seedance-2.0` `reference_mode=true` `tier=fast` | 4.04초 / 1280x720 / 24fps **성공** |
| 판정 | 컨택트시트 직독 | 붉은 강조어 3개가 문서에서 떠올라 **1개로 수렴** + 카메라 스윕에 따른 **진짜 원근 시차** + 뒤 창문이 **새로 드러남** |

CEO-51 지시 원문 — *"세 프로젝트 문서에서 같은 역할 단어만 붉게 떠올라 한 곳으로 모이는 것이
화면에서 실제로 일어나야 했네요"* — **화면에서 실제로 일어났음을 확인.**

**남은 결함 (정직 보고).** 붉은 글자가 영문이고 오철자(`Report` / `Renes`)입니다.
프리비즈 도형이 **"무엇이 될지"는 통제됐지만 "무슨 글자냐"는 통제되지 않았습니다.**
⇒ 처방: 프리비즈 마커에 **실제 한글 텍스처를 UV 로 붙여** 글자 형태 자체를 구조로 전달합니다.

#### ⑭-7. 한글 글자 마커 — 정답 구현 (2026-08-22 실증 · 위 결함 해소)

⑭-6 의 오철자 결함을 **완전히 제거**한 구현입니다. 앞으로 모든 프리비즈에 이 방식을 씁니다.

**(a) 글자 PNG 는 반드시 RGBA · 배경 알파 0.**
불투명 검은 배경 PNG 를 그대로 emission 으로 붙이면 화면에 **검은 카드**가 나타나고,
AI 는 그것을 "검은 판"으로 해석합니다(4세대 pv4 에서 실제 발생).

**(b) 머티리얼 — 알파가 MixShader 를 구동해야 합니다.**

```python
def texflat(name, path):
    """emission material driven by an image texture -> the GLYPH becomes structure."""
    m = bpy.data.materials.new(name); m.use_nodes = True
    nt = m.node_tree; nt.nodes.clear()
    tc = nt.nodes.new('ShaderNodeTexCoord')
    tx = nt.nodes.new('ShaderNodeTexImage')
    tx.image = bpy.data.images.load(path)
    tx.interpolation = 'Closest'      # 글자 경계를 뭉개지 않는다
    tx.extension = 'EXTEND'
    e  = nt.nodes.new('ShaderNodeEmission'); e.inputs[1].default_value = 1.0
    tr = nt.nodes.new('ShaderNodeBsdfTransparent')
    mx = nt.nodes.new('ShaderNodeMixShader')
    o  = nt.nodes.new('ShaderNodeOutputMaterial')
    nt.links.new(tc.outputs['UV'],    tx.inputs['Vector'])
    nt.links.new(tx.outputs['Color'], e.inputs[0])
    nt.links.new(tx.outputs['Alpha'], mx.inputs[0])   # ★핵심: 알파가 믹스를 구동★
    nt.links.new(tr.outputs[0], mx.inputs[1])         # 0 -> 투명
    nt.links.new(e.outputs[0],  mx.inputs[2])         # 1 -> 발광 글자
    nt.links.new(mx.outputs[0], o.inputs[0])
    m.blend_method = 'BLEND'
    return m

def add_tex(name, loc, sc, path):
    bpy.ops.mesh.primitive_plane_add(size=2, location=loc)
    ob = bpy.context.object; ob.scale = sc; ob.name = name
    ob.data.materials.append(texflat(name, path))
    return ob
```

**(c) 배치 규칙 (실측으로 확정).**

| 항목 | 값 | 근거 |
|---|---|---|
| 형상 | 큐브 아님 · **plane** | 글자는 면이지 덩어리가 아님 |
| z 오프셋 | **>= +0.032** | +0.012 는 문서에 파묻힘(pv4 실측) |
| 부상 회전 | `rotation_euler = (v * 1.15, 0, 0)` | 누우면 카메라에서 안 읽힘 |
| 스케일 | `(0.30*(1+0.9v), 0.15*(1+0.9v), 1.0)` | 2:1 글자 비율 유지 |

**(d) 프롬프트에 ⑭-5 의 `NOT a marker` 선언을 반드시 병행합니다.**
구조(UV 텍스처)와 선언(프롬프트) **둘 다** 있어야 합니다. 하나만으로는 해소되지 않습니다.

**검증 (2026-08-22).**

| 항목 | BEFORE (pv3 · 빈 붉은 큐브) | AFTER (pv5 · 한글 알파 텍스처) |
|---|---|---|
| 붉은 글자 내용 | `Report` / `Renes` (오철자 영문) | **「조율자」 (정확한 한글)** |
| 검은 카드 | — | 없음 |
| 렌더 속도 | 0.195 s/frame | **0.207 s/frame** (사실상 불변) |
| v2v 결과 | 4.041667s / 1280x720 | 4.041667s / 1280x720 |

> 텍스처를 붙여도 렌더 속도는 **거의 그대로**입니다. 글자 통제는 **공짜**입니다.

---

## 12. 교훈 157~162 (축적)

### 교훈 157 — 도구 애그리게이터는 우리가 쓰는 모델의 래퍼다

대표님이 지목하신 서비스(bbanana.ai)를 직독한 결과, Seedance / Kling / Veo / Nano Banana 등
**우리가 이미 접근 가능한 모델의 한국형 래퍼**였습니다. 독자 3D 엔진은 없습니다.
"건축 미스터리" 같은 장르 템플릿은 **프롬프트·워크플로 프리셋**입니다.

> **우리에게 없던 것은 도구가 아니라 그 도구를 쓰는 방법이었습니다.**

### 교훈 158 — 광학 흐름은 품질 판정 기준이 아니다

대표님이 **"정답"으로 주신** 벤치마크 샘플을 우리 클립과 같은 자로 재봤습니다.

| clip | mean flow | affine resid | still% |
|---|---|---|---|
| **대표님 정답 샘플** | **0.140** | 1.083 | **90.0%** |
| 우리 A6-01 | 1.338 | 0.552 | 20.0% |
| 우리 층분리 L2 | 2.357 | 0.439 | 8.5% |

대표님이 정답이라 하신 것이 두 지표 모두 우리보다 **"나쁩니다"**.

> **흐름 크기·정지 비율은 품질을 판정하지 못합니다.
> 무엇이 어떻게 움직이는지(서사·구조)가 판정합니다.**

⇒ 광학 흐름은 **진단 보조**로 강등. 합격 게이트로 쓰지 않습니다.
⇒ 남는 정본은 **대표님의 눈** + **컨택트시트 직독**.

### 교훈 159 — 깊이비례 이동은 매끄러운 깊이맵에서 사실상 아핀이다

측면 이동 강도를 키울수록 아핀 잔차가 **오히려 떨어졌습니다**(0.571 → 0.396).
깊이맵이 매끄러운 경사면이면 "깊이 × 이동"이 좌표의 매끄러운 함수 = 아핀으로 거의 설명됩니다.

> **진짜 시차는 깊이의 불연속(물체 경계)에서만 나옵니다.**

⇒ 처방: 깊이를 n 단계로 **양자화해 층으로 쪼개고**, 층별 상수 z 로 이동시켜 **뒤→앞 알파 합성**.
경계에서 이동이 끊기고, 가려짐이 벗겨지고, 뒤 층이 드러납니다.
비용: 프레임당 0.71~0.94초 (평면 워프 0.08초의 9~12배).

### 교훈 160 — 색상 코딩 3D 프리비즈 → Video-to-Video 가 정답이다

§11 ⑭ 전문. 요약하면 **"카메라 무빙을 말로 전달하지 말고 3D 로 계산해서 영상으로 전달한다."**

### 교훈 161 — 프리비즈는 Cycles 1샘플이면 프레임당 0.2초다

발광 원색은 광선 추적이 필요 없습니다. `samples=1 / max_bounces=0 / denoise OFF / 832x468`
⇒ **0.196 s/frame**. 같은 엔진의 기본 설정 대비 **52배** 빠릅니다.
GPU 없는 2코어·3GB 환경에서도 500초 전량 프리비즈가 **약 40분**에 끝납니다.

### 교훈 162 — 프리비즈는 "무엇이 될지"는 통제하지만 "무슨 글자냐"는 통제하지 못한다

색→역할 매핑은 완벽히 작동했습니다(마젠타→문서, 레드→강조어). 그러나 강조어의 **글자 내용**은
AI 가 임의로 채워 오철자 영문이 나왔습니다.

> **구조로 전달되지 않은 것은 통제되지 않습니다.**
> 글자를 통제하려면 글자를 **프리비즈 안에 텍스처로 넣어** 구조로 만들어야 합니다.

### 교훈 163 — 프리비즈 세트는 카메라 궤적보다 넉넉해야 한다

**증상.** 1세대 프리비즈(pv1)의 프레임 1·48 상단에 **검은 구멍**이 생겼습니다.
카메라가 배경 지오메트리 밖을 봤기 때문입니다.
그리고 **AI 는 그 검은 영역을 "검은 것"으로 충실히 재현합니다** — 구조 레퍼런스이므로 당연합니다.

**처방.**

| 항목 | 값 | 근거 |
|---|---|---|
| 바닥 | 16 x 12 m | 카메라 최대 반경(8.4 m)의 **2배 이상** |
| 벽 | 후면·좌·우 3면 · 높이 4.4 m | 카메라 최대 높이(4.2 m)보다 높게 |

**검증.** 2·3세대(pv2/pv3)에서 검은 구멍 완전 소멸.

> 프리비즈에서 "안 보일 것 같은 곳"은 반드시 보입니다. 세트는 항상 과하게 짓습니다.

### 교훈 164 — ffmpeg drawtext 의 `text=` 는 콜론을 못 받는다

**증상.** 3안 A/B 조립기(ab3.py) 1차 실행이 **exit status 234** 로 죽었습니다.

```
drawtext=...:text='판정: 컷 안 모션 없음':...   <- 이 한 글자가 원인
```

**원인.** drawtext 필터 파서는 `:` 를 **옵션 구분자**로 읽습니다. 따옴표 안이어도 마찬가지입니다.
`#RRGGBB` 의 샵도 셸·파서를 흔듭니다.

**처방 (3중).**

1. `text=` 대신 **`textfile=<경로>`** — 파일에서 읽으면 콜론이 안전합니다.
2. 본문에서 `판정:` → `판정 -` 로 교체 (이중 안전장치)
3. `fontcolor` 는 `#FFD54A` 가 아니라 **`0xFFD54A`**

**검증.** `AB3_A6.mp4` 10.000초 · 한글 3층 캡션 정상 렌더.

> 한글 자막을 ffmpeg 로 구울 때는 **항상 textfile 방식**을 씁니다. 이것을 기본값으로 규정합니다.

### 교훈 165 — 구조로 전달하되, 그것이 "교체 대상"인지 "최종물"인지도 전달해야 한다

**교훈 162 의 완결편입니다.**

교훈 162 에서 우리는 *"구조로 전달되지 않은 것은 통제되지 않는다"* 를 배웠습니다.
그래서 한글 글자를 UV 텍스처로 프리비즈에 심었습니다 — **구조로 전달했습니다.**
그런데도 위험이 남았습니다. 왜냐하면:

```
프리비즈에 들어간 것은 AI 에게 기본적으로 전부 "프록시(교체 대상)" 로 해석된다.
그것이 색깔 평면이든, 원기둥이든, 한글 글자든 마찬가지다.
```

색상 코딩 프리비즈의 **전제 자체가 "이 도형들은 진짜가 아니다, 바꿔라"** 이기 때문입니다.
따라서 글자를 아무리 정확히 그려 넣어도, AI 는 그것마저 "무언가로 바꿔야 할 표식"으로 읽습니다.

**처방 — 프롬프트에서 예외를 명시적으로 선언합니다.**

```
The RED KOREAN TEXT visible in @Video1 is NOT a marker — it is the actual final text
and must be reproduced EXACTLY as the identical Korean word, same glyphs, same position,
same rotation, same scale, frame for frame.
Preserve the Korean characters exactly as shown in @Video1 — do not translate,
do not substitute Latin letters, do not invent new words.
```

**검증.** BEFORE `Report` / `Renes` (오철자 영문) → AFTER **「조율자」** (정확한 한글).
동일 모델·동일 설정·동일 plate. 바뀐 것은 **프리비즈의 글자 텍스처 + 이 두 문장**뿐입니다.

**일반화.**

| 프리비즈 안의 요소 | AI 에게 무엇인가 | 프롬프트 처리 |
|---|---|---|
| 색깔 평면·원기둥·큐브 | **프록시** — 실물로 교체 | `the magenta plane becomes …` |
| 한글 글자·로고·수치 | **최종물** — 그대로 보존 | `… is NOT a marker — it is the actual final …` |

> 구조로 전달하는 것만으로는 부족합니다.
> **"이것은 바꿔라" 와 "이것은 그대로 둬라" 를 갈라서 말해야** 통제가 완성됩니다.

### 교훈 166 — GenTeam 에이전트는 본문의 `@이름` 텍스트로는 깨어나지 않는다

**증상.** 전담 채널 `previz-3d-pipeline` 에 교본과 과업을 2회 발송했고 둘 다 `status: ok` 였는데,
**에이전트 회신이 하나도 오지 않았습니다.** 발송 실패가 아니었습니다.

**원인.** 채널을 폴링해 보니 제 메시지 바로 뒤에 시스템 메시지가 붙어 있었습니다.

```json
{"kind": "system_message",
 "content": "The agents here reply only when @mentioned. @ a teammate to get an answer.",
 "metadata": {"digital_employee": {"event": "agents_mentions_only", "importance": "high"}}}
```

본문에 `@3D Motion Director …` 라고 **텍스트로** 썼지만, 그것은 멘션으로 파싱되지 않습니다.
메시지는 채널에 남았지만 **에이전트를 깨우는 이벤트가 발생하지 않았습니다.**

**처방.** `gsk genteam send` 의 **`--mentions` 플래그**로 agent_id 를 넘깁니다.

```bash
gsk genteam send --args-file order.json \
  --mentions agent_pt2dn9rvtxrc agent_fmzqbh39p4k3 agent_4gettwpxdxp6
```

- 여러 명을 **공백으로 나열**할 수 있습니다(멤버 추가와 달리 1명씩 나눌 필요 없음).
- 응답의 `data.mentions` 배열에 3명이 들어갔는지로 확인합니다.
- 캐노니컬 토큰 `@[name](id)` 은 **자동으로 본문 앞에 붙습니다.** 직접 쓰지 마십시오.
- 대상은 **이미 채널 멤버**여야 합니다.

**검증.** 재발주 mid `3044026` · `mentions` 3명 인식 · `status ok`.

> 발송 성공(`status: ok`)은 **도달**의 증거일 뿐 **발동**의 증거가 아닙니다.
> 팀에 일을 시킨 뒤에는 반드시 **채널을 폴링해 시스템 메시지를 확인**합니다.
> 경영자가 "지시했다"고 믿는 것과 "지시가 전달됐다"는 것은 다릅니다.

---

## 교훈 167 — 한 컷을 위해 쓴 제스처를 전량에 무조건 적용하면 안 된다

### 증상

승인받은 프리비즈 `pv5` 를 배치 렌더러로 일반화해 60잡 전량에 적용하고,
그중 1컷(`J_A3-02`)만 시범으로 v2v 를 돌려 컨택트시트로 직독했습니다.

| 항목 | 판정 |
|---|---|
| 질감·조명 (실사 사무실) | ✅ 성공 |
| 원근·시차 (3D 가 계산한 경로) | ✅ 성공 |
| 정지 구간 (0~60프레임 완전 동일) | ✅ 성공 |
| **글자** | ❌ **붉은 낙서로 뭉개짐** |

### 원인 — 서사가 다른 컷에 같은 제스처를 썼다

`pv5` 의 제스처는 **「세 글자가 한 점으로 수렴」** 이었습니다.
그것이 옳았던 이유는 그 컷의 대사가 이것이었기 때문입니다.

> "나는 어떤 역할을 반복해 왔는가 — **세 프로젝트에서 같은 역할이 반복된다**"

세 개의 문서에 **같은 단어**가 있고, 그것이 **한 곳으로 모이는 것**이 곧 그 문장의 뜻입니다.
[CEO-51] 이 지목한 바로 그 장면입니다.

그런데 이 제스처를 60잡 전체에 무조건 적용하니, 서사가 전혀 다른 컷에서도
글자 평면 3장이 같은 좌표로 이동했습니다. 세 장이 한 점에 겹쳐 쌓이면
생성기 입장에서는 그것이 **글자가 아니라 붉은 덩어리**입니다. 그래서 낙서로 렌더했습니다.

### 처방 — 제스처를 컷의 대사에서 유도한다

제스처는 연출자가 고르는 것이 아니라 **그 컷이 무슨 말을 하는지에서 나옵니다.**

```python
CONVERGE_KEYS = ("반복", "세 프로젝트", "3개 프로젝트", "같은 역할", "공통")
NONE_KEYS     = ("새 정보 금지", "읽히는 글자 금지")

def gesture_of(row_src, cam_row):
    if cam_row["seam_id"].startswith("A8"):          # 리포트 지면 구간
        return "none"
    if any(k in (row_src.get("protect") or "") for k in NONE_KEYS):
        return "none"
    blob = " ".join([cam_row["narr_line"], cam_row["objects"], cam_row["motion_reason"]])
    if any(k in blob for k in CONVERGE_KEYS):
        return "converge"
    return "lift"
```

| 제스처 | 조건 | 화면에서 일어나는 일 | 실측 분포 |
|---|---|---|---|
| `converge` | 대사에 "반복 / 세 프로젝트 / 같은 역할 / 공통" | 글자 **3개**가 한 점으로 수렴 | **5잡** |
| `none` | `protect` 에 "새 정보 금지 / 읽히는 글자 금지", 또는 ACT8 | 글자 **없음** | **9잡** |
| `lift` | 기본값 | 글자 **1개**가 문서 한 장 위에서 제자리 부상 | **46잡** |

**검증**: CEO 가 승인한 A6-02 컷이 이 규칙에서 정확히 `converge` 로 분류되었습니다.
즉 규칙은 승인본을 반려하지 않습니다(교훈 131 준수).

### 부수 교정 — 스케일과 배율

세 장이 겹칠 때 덩어리가 되는 것을 막기 위해 글자 평면을 줄였습니다.

| 항목 | 이전 | 이후 |
|---|---|---|
| 글자 평면 스케일 | 0.30 / 0.15 | **0.26 / 0.13** |
| 부상 배율 (converge) | 0.9 | 0.9 (유지) |
| 부상 배율 (lift) | — | **0.35** (제자리 부상이므로 작게) |

### 프롬프트도 함께 갈라야 한다

프리비즈에 글자가 1개인데 프롬프트가 "three panels ... the RED KOREAN TEXT" 라고
말하면, 생성기는 **없는 두 개를 만들어 냅니다.** 그래서 프롬프트도 3분기했습니다.

```
converge → "There are THREE separate copies of 「조율자」 ... they must never
            merge, overlap or smear into a blob."
lift     → "There is exactly ONE copy of 「방식」 ... do not duplicate it onto
            the other documents, do not add any second word."
none     → "There is NO text of any kind in this shot."
```

게이트로 강제합니다 — `converge` 프롬프트에 one-copy 절이 섞이거나
`lift` 프롬프트에 three-copy 절이 섞이면 `PROMPT GATE FAILED` 로 정지합니다.

> **구조(프리비즈)와 언어(프롬프트)가 같은 것을 말해야 합니다.**
> 하나는 글자 1개를 보여주고 다른 하나는 3개를 요구하면, 생성기는 그 모순을
> 자기 방식으로 메꿉니다. 그 결과가 낙서였습니다.

---

## 교훈 168 — 시선 목표는 제스처마다 달라야 한다

### 증상

같은 시범 컷의 후반에서 **문서가 화면 아래로 흘러넘쳤습니다.**
카메라가 책상이 아니라 그 위의 빈 공간을 올려다보고 있었습니다.

### 원인 — 전 컷에 같은 시선 종점을 줬다

`camtab.py` 가 80컷 전부에 동일한 값을 넣고 있었습니다.

```python
tgt_start_xyz = [0.0, -0.55, 0.15]
tgt_end_xyz   = [0.0, -0.35, 0.77]     # ← 전 컷 동일
```

`z = 0.77` 이 옳았던 이유는 `pv5` 의 글자가 `z = 0.92` 까지 올라가기 때문입니다.
**글자를 따라 올려다보는 것**이 그 컷의 연출이었습니다.

그러나 `lift` 컷의 글자는 `+0.34` 만 뜨고, `none` 컷에는 글자가 아예 없습니다.
그런 컷에서 `0.77` 을 올려다보면 카메라는 **아무것도 없는 허공**을 향하고,
정작 보여줘야 할 문서는 프레임 밖으로 밀려납니다.

### 처방 — 시선 종점을 제스처에서 유도한다

```python
TGT_END = {
    "converge": [0.0, -0.35, 0.77],   # 글자가 z=0.92 로 모인다 → 함께 올려다본다
    "lift":     [0.0, -0.45, 0.30],   # 글자가 0.34 만 뜬다 → 지면에 머문다
    "none":     [0.0, -0.50, 0.20],   # 글자가 없다 → 책상면을 지킨다
}
```

게이트로 강제합니다 — `tgt_end_xyz != TGT_END[gesture]` 이면 `PLAN GATE FAILED`.

프롬프트에도 프레임 종착지를 명시합니다.

```
lift/none → "The shot ends still looking down at the desk surface; the documents
             remain fully inside the frame at all times and never slide out of
             the bottom."
converge  → "The shot ends looking slightly up, following the words above the
             desk; the documents stay inside the frame."
```

> **카메라가 무엇을 향하는지는 그 컷에 무엇이 있는지에 종속됩니다.**
> 대상이 올라가지 않는데 시선만 올리면, 화면에는 대상이 사라진 빈 하늘이 남습니다.

---

## 교훈 169 — 유료 대량 착수 전 "1컷 시범 + 컨택트시트 직독" 은 규칙이다

이번에 실제로 아낀 금액을 기록합니다.

| 만약 시범 없이 진행했다면 | 실제 |
|---|---|
| 프리비즈 60잡 전량 렌더 (29분) → **전량 폐기** | 33잡에서 중단 |
| v2v **61회 호출 · 약 383 billed seconds** → **전량 폐기** | **1회 호출 · 9초** |
| 재작업까지 포함해 사실상 2배 소모 | 결함 2건을 9초로 적발 |

**규칙으로 승격합니다.**

1. 파이프라인을 수정한 뒤에는 **반드시 1컷만** 유료 호출한다.
2. 결과물을 **컨택트시트로 직독**한다(무료·최상위 결함 발견 수단).
3. 합격하기 전에는 **두 번째 유료 호출을 하지 않는다.**
4. 시범 컷은 **가장 앞 컷**을 쓴다 — 결함이 있으면 관객이 가장 먼저 보는 곳이다.

> 시범은 비용이 아니라 **보험**입니다.
> 9초로 383초를 지켰습니다.

---

## 교훈 170 — 글자 저품질의 원인은 텍스처 해상도가 아니라 「화면 점유율」이다

대표님이 세 번 지적하신 "글자만 저품질" 의 원인을 처음에는 텍스처 해상도로
추정했습니다. 틀렸습니다. **실측이 원인을 뒤집었습니다.**

```
J_A4-03 첫 프레임 글자 bbox = 66 x 30 px   (화면 폭의 8%)
J_A4-03 마지막 프레임 붉은 픽셀 = 0
```

512px 텍스처든 4096px 텍스처든, 화면에서 66px 로 표시되면 66px 짜리 글자입니다.
생성 AI 는 **획을 추적(trace)** 할 수 없어 **글자를 발명(invent)** 하고, 그 결과가
낙서입니다.

**픽셀 예산이라는 개념으로 재설계했습니다.**

| 항목 | 값 | 근거 |
|---|---|---|
| 텍스처 폭 | 2048 px | 2600pt 슈퍼샘플 → 잉크 bbox 크롭 → LANCZOS |
| 렌더 해상도 | 1280x720 | v2v 출력과 **동일** (업스케일 금지) |
| 샘플 | 4 | 1.373 s/f (s8 = 2.613 s/f 는 6.1시간 → 기각) |
| 잉크 하한 | 210 px | 기각본 66px 의 3.2배 · 1280px 의 16% |
| 실측 결과 | **237~435 px** | 하한을 전 잡에서 상회 |

> **글자 품질은 텍스처가 아니라 「화면에서 몇 픽셀인가」로 결정됩니다.**
> 이 값을 게이트로 만들지 않으면 매번 같은 실패를 반복합니다.

---

## 교훈 171 — `arc_deg` 는 크기(magnitude)다. 부호는 다른 곳에 있다

카메라 궤적표(`camtab.py`)는 회전 방향을 이렇게 다뤘습니다.

```python
sign = 1.0 if idx % 2 == 0 else -1.0   # 부호는 cam_end_xyz 에만 반영
arc_deg = abs(...)                      # arc_deg 에는 절대값만 저장
```

렌더러가 `arc_deg` 를 그대로 회전량으로 쓰자 **절반이 반대로 돌았습니다.**

```
60잡 중 27잡(45%) 이 반대 방향으로 회전 → 문서를 지나쳐 맨 벽만 촬영
```

**처방 — 종점에서 부호를 역산하고, 게이트로 대조한다.**

```python
a1 = math.atan2(b[0], -b[1])
arc = a1 - a0
while arc > math.pi:  arc -= 2*math.pi     # 최단 부호 경로
while arc < -math.pi: arc += 2*math.pi
if abs(math.degrees(arc)) - abs(job["arc_deg"]) > 1.0:
    raise SystemExit("ARC GATE FAILED ...")
```

**렌더로 실증했습니다 (J_A4-03).**

| 프레임 | 붉은 픽셀 | 글자 bbox |
|---|---|---|
| 첫 | 17,852 | 418 x 200 |
| 중간 | 19,724 | 418 x 158 |
| **마지막** | **35,444** (이전 **0**) | **582 x 282** (이전 66x30, **8.8배**) |

> **크기와 방향을 같은 필드에 담지 마십시오.**
> 담았다면, 두 곳이 일치하는지 검사하는 게이트를 반드시 두십시오.

---

## 교훈 172 — 「정확한 글자」와 「이미지 수준의 글자」는 다른 문제다

잉크를 435px 까지 키워 **정확성(축 A)** 을 확보한 뒤에도 대표님은 다시
지적하셨습니다.

> "글자가 정확한 것 뿐만 아니라 **시각적으로도 이미지 수준**이 되어야 한다"

**품질에는 두 축이 있습니다.**

| 축 | 정의 | 측정 수단 |
|---|---|---|
| **A 정확성** | 올바른 한글로, 충분한 픽셀로 읽힌다 | 잉크 px 게이트 (산술·무료) |
| **B 시각 등급** | **이미지 본체와 같은 사진 품질** | **확대 직독 (육안·무료)** |

축 B 는 산술로 잡히지 않습니다. **확대해서 눈으로 보는 것만이 정본입니다.**
그 직독으로 세 결함을 적발하고 처방했습니다.

| 결함 | 증상 | 처방 |
|---|---|---|
| ① 종이 넘침 | 「변화」가 **「벼하」** 로 읽힘 | `PAPER_FIT` 클램프 |
| ② 에지 지글거림 | 획 경계에 빨강/청록 잡픽셀 | 알파 `GREATER_THAN` 이진화 |
| ③ 스티커 부유 | 종이 위에 떠 보임 | 정지 구간 pitch/yaw = 0 |

> **잉크를 키우는 것으로는 축 B 가 해결되지 않습니다.**
> 각 결함마다 별도의 제어 변수를 두어야 합니다.

---

## 교훈 173 — 슬롯 오프셋은 월드축이 아니라 「카메라 화면축」으로 주어야 한다

단어 여러 개를 나란히 배치할 때 월드 X축으로 밀었습니다.

```python
CLUS = [(-1.0,-0.05,-0.16), (0.0,0.0,0.14), (1.0,0.05,-0.16)]   # 월드 X
```

**카메라에 가까운 쪽 단어의 화면 각도가 커져 프레임을 넘습니다.**

```
J_A3-07 frame 96 slot +1 : ndcX +0.832 + 반폭 0.20 = edge 1.05  *** 잘림 ***
```

여기서 중요한 오독이 있었습니다. 컨택트시트의 `clusters~5` 를 저는 "잔여 겹침"
으로 읽었으나, 실제로는 **잘린 조각이 별개 덩어리로 세어진 것**이었습니다.

**처방 — 스칼라 슬롯 + 카메라 up 축 배치.**

```python
CLUS = [1.0, 0.0, -1.0]                       # 스칼라
fwd = normalize(tgt - cam)
rgt = normalize((fwd.y, -fwd.x, 0))
upv = rgt × fwd
off = upv * slot * gap_m
gap_m = d_ref * SENSOR/34 * (RES[1]/RES[0]) * CLUS_VFRAC   # 프레임 「높이」 비율
```

**부수 효과가 본질이었습니다.** 세로 스택이므로 대표님이 요구하신
"같은 역할 단어만 붉게 떠올라 **한 곳으로 모이는**" 서사가 화면에서
실제로 성립합니다.

> **레이아웃을 월드 좌표로 표현하면 카메라가 움직일 때 반드시 깨집니다.**
> 화면에서 지켜야 할 것은 화면 좌표로 표현하십시오.

---

## 교훈 174 — 데이터의 라운드로빈 배정이 결함의 근원일 수 있다

글자를 실을 문서를 이렇게 배정했습니다.

```python
doc = "ABC"[order.index(a) % 3] if g == "lift" else ""
```

공평해 보이지만, **카메라 스윕이 외곽 문서를 화면 밖으로 밀어냅니다.**

```
51 glyph 잡 중 30잡(59%) edge > 1.0   최악 4.104
```

**처방 — 중앙 문서 고정.**

```python
doc = "B" if g == "lift" else ""
```

| 항목 | 라운드로빈 | 중앙 고정 |
|---|---|---|
| 프레임 밖 잘림 | **30 / 51** | **0 / 46** |
| 최악 edge | 4.104 | **0.520** (여유 48%) |
| 최악 잉크 | — | **237 px** |

`(slot, ink_frac)` 격자 탐색으로 **잉크를 깎지 않고** 전부 해결됨을 확인한 뒤,
렌더러가 아니라 **데이터 생성기(`plan.py`)** 를 고쳤습니다.

> **시범 1컷에서 본 결함은 대개 전량의 절반에 퍼져 있습니다.**
> 증상을 렌더러에서 가리지 말고, 원인을 데이터에서 고치십시오.

---

## 교훈 175 — 프레임 밖 문서 위의 글자는 어떤 크기 조절로도 살릴 수 없다

수렴(converge) 컷 2건이 계속 실패했습니다. 시도한 처방을 **전부 무료로 기각**했습니다.

| 시도 | 결과 | 판정 |
|---|---|---|
| 파라미터 격자 16조합 | edge 1.078 → 1.031 | **전부 실패** — 파라미터가 원인이 아니다 |
| 정지/수렴 잉크 예산 분리 5조합 | off-frame 51~64건 잔존 | 실패 |
| 프레임 안으로 클램프 | off-frame 0 · 겹침 0 · **잉크 137px** | **기각 — 축 A 희생** |

세 번째가 특히 중요합니다. **잘림은 사라지지만 글자가 137px 로 줄어듭니다.**
축 B 를 위해 축 A 를 희생하는 것은 처방이 아닙니다.

**정답 — 화면 안에 있는 문서만 수렴에 참여시킨다.**

```python
def visible_docs(job, wm, limit=0.94):
    """정지(hold) 프레임 기준으로 화면 안에 온전히 들어오는 문서만 반환.
    첫 프레임이 이미 자르는 시트는 어떻게 해도 온전한 단어를 못 보여준다."""
    ...
    if len(keep) < 2:
        raise SystemExit("CONVERGE GATE FAILED ...")
    return keep

slot = (n_vis - 1) / 2.0 - k     # 남은 문서를 중심 기준 균등 배치
```

```
J_A3-07[0,1,2]  J_A4-08[0,1,2]  J_A5-03[1,2]  J_A6-02[0,1,2]  J_A7-03[1,2]
off-frame 30건 → 0건 (edge 0.996)   전 컷 2~3장 유지
```

> **화면 밖에 있는 것은 크기를 조절할 대상이 아니라, 제외할 대상입니다.**

---

## 교훈 176 — 게이트를 별도 파일로 복제하면 반드시 코드와 어긋난다

검증을 빠르게 하려고 `/tmp/preflight.py` 에 기하 계산을 **손으로 복제**했습니다.
상수 하나를 바꿀 때마다 두 곳을 고쳐야 했고, **실제로 어긋났습니다.**

**처방 — 출하 코드 자체를 렌더 없이 실행한다.**

```python
if os.environ.get("PREVIZ_DRY"):
    for j in jobs:
        build(j)          # 모든 게이트가 build() 안에 있다
    print("DRY OK  jobs %d  every gate passed" % len(jobs))
    return
```

```
$ PREVIZ_DRY=1 python3 -u previz_batch.py
DRY OK  jobs 60  every gate passed        # 약 100초
```

**100초로 3.2시간 렌더 낭비를 막습니다.** 그리고 복제본이 없으므로
코드와 게이트가 어긋날 수 없습니다.

> **검증 로직은 출하 코드 안에 두고, 렌더만 끄십시오.**
> 게이트의 복제본은 언젠가 반드시 거짓말을 합니다.

---

## 교훈 177 — 두 개의 램프를 독립적으로 두면 기하가 깨진다

시범 e 에서 원경 문서의 「방식」이 **「바시」** 로 읽혔습니다. 받침 ㅇ·ㄱ 이
사라진 것입니다. 처음에는 알파 이진화가 얇은 획을 잘랐다고 의심했습니다.
**두 가지 실측이 그 가설을 기각했습니다.**

```
텍스처의 획 두께 = 35~67 px (2048px 중 2~3%)    → 얇지 않다
같은 프레임의 근경 단어 = 「방식」 정확          → 알파 문제라면 전부 깨져야 한다
```

**결정적 실측은 이것이었습니다.**

```
노란 시트 bbox   y[268, 459]
그 위 붉은 글자   y[268, 433]
   글자 상단 - 시트 상단 = gap 0 px        ← 정확히 맞물렸다
```

글자가 **종이에 가려진 것**입니다. 원인은 서로 독립적인 두 램프였습니다.

```python
t_off = min(1.0, e / LIFTOFF)   # 회전 램프  (LIFTOFF = 0.35)
z_lift = z_total * e            # 상승 램프
```

`LIFTOFF=0.35` 는 **회전을 상승보다 약 3배 빠르게** 만듭니다.

```
J_A3-07 frame 70 :  e = 0.285 (28% 상승)  vs  t_off = 0.814 (81% 회전)
  → 높이 0.71m 평면의 아래쪽이 종이 아래로 0.36m 파고듦 → 시트가 받침을 가림
```

`LIFTOFF` 를 1.00 으로 올려 두 램프를 묶었지만 **부족했습니다.** 전 잡을
훑어보니 lift 잡은 상승이 0.30m 뿐인데 글자 높이가 0.71m 여서, 회전만
늦춰도 **46/51 잡이 여전히 가려졌습니다.**

**정답 — 한계가 기하학적이므로 제어도 기하학적으로 한다.**

평면을 `p` 만큼 세우면 아래 끝이 중심보다 `hh·sin(p)` 내려갑니다. 종이를
넘지 않을 조건은 `sin(p) ≤ 상승량 / hh` 이므로:

```python
risen = max(0.0, wz - loc[2])
pitch = min(pitch, math.asin(min(1.0, risen / max(1e-6, hw / aspect))))
```

| LIFTOFF 만 조정 | 최악 dip | 판정 |
|---|---|---|
| 0.35 | +0.356 m | 가려짐 |
| 0.60 | +0.164 m | 가려짐 |
| 0.80 | +0.052 m | 가려짐 |
| 1.00 | +0.350 m (lift 잡) | **여전히 가려짐** |
| **1.00 + 기하 캡** | **0.00000 m** | **원리적으로 불가능** |

최종 각도는 25~66° 로 남아 글자가 여전히 렌즈를 향합니다. 그리고 게이트가
매 실행마다 이를 증명합니다.

```python
dip = (hw / aspect) * math.sin(abs(pitch)) - max(0.0, wz - loc[2])
if dip > MAX_DIP_M:
    raise SystemExit("GLYPH GATE FAILED %s: sheet occludes glyph by %.3f m ...")
```

> **애니메이션 램프가 두 개 이상이면, 그 비율이 곧 기하학적 제약입니다.**
> 각각 "자연스러운" 값을 주면 그 사이에서 물체가 서로를 관통합니다.
> 제약이 기하학이면, 파라미터를 스윕하지 말고 **각도를 직접 계산**하십시오.

---

## 교훈 178 — 작업 단위는 「잡」이 아니라 「호출」이다

60개의 잡을 61회 호출로 생성한다. 차이는 한 건 뿐이다: `J_A6-02` 는 460프레임이라
15초(360프레임) 생성 상한을 넘으므로 두 번에 나눠 생성된다. 이 한 건의 차이가
슬라이스 코드를 조용히 무너뜨렸다.

### 증상 — 에러 없이 빈 파일

```
cmd_slice(J_A6-02, part2.mp4)  →  ffmpeg 에 n between 322..459 를 요구
그러나 part2.mp4 는 프레임 0..137 만 보유
⇒ select 가 아무것도 고르지 않고 빈 파일을 생성 (에러 없음, 종료코드 0)
```

**적발 시점이 결정적이었다.** 이 결함은 61회 유료 호출을 「전에」 산술로 발견했다.
호출 후에 발견했다면, 500초 합성 단계에서 A6-04·A6-05 두 컷이 사라진 채로
길이가 맞지 않는 이유를 역추적해야 했다.

### 원인

`_slices()` 가 반환하는 `f0`/`f1` 은 **「잡 전체」 좌표**다. 그런데 분할 잡은
여러 개의 독립 mp4 로 생성된다. 파일의 frame 0 이 잡 좌표에서 322번일 때,
잡 좌표를 그대로 ffmpeg 에 넘기면 존재하지 않는 프레임을 고르게 된다.

### 처방 — 오프셋 차감 + 3중 게이트 + 실측

```python
base = p["f0"]                # 이 파일의 frame 0 이 잡 좌표에서 몇 번인가
a, b = sl["f0"] - base, sl["f1"] - base
if a < 0 or b >= have:
    raise SystemExit("SLICE GATE FAILED ...: local range %d..%d outside 0..%d")
...
got = fcount(out)             # 출력 프레임을 ★세어서★ 확인 (가정 금지)
if got != want:
    raise SystemExit("SLICE GATE FAILED %s: wrote %df, want %df")
```

| 게이트 | 실증 결과 |
|---|---|
| part 미지정 | `J_A6-02 has 2 parts -- pass the part index` |
| 없는 part | `J_A6-02 has no part 3` |
| 길이 부족 mp4 | `SLICE GATE FAILED ...: mp4 has %df, part needs %df` |
| 정상 경로 | job 322..459 → local 0..137 → A6-04 67f + A6-05 71f |

> **ffmpeg 의 `select` 는 범위가 비어도 에러를 내지 않고 빈 파일을 만듭니다.**
> 그러므로 출력 프레임 수는 **가정하지 말고 세십시오.**
> 그리고 한 건뿐인 예외가 바로 코드가 무너지는 지점입니다.

---

## 교훈 179 — 반올림은 누적된다: 흡수하지 말고 격자에 스냅하라

두 숫자가 0.278초 어긋난 채 오래 방치되어 있었다.

```
rows38 의 t1-t0 합계        349.680000 s
camtab 프레임 합계 8399     349.958333 s
표류                        +0.278333 s = 6.68 프레임
```

### 원인 — 컷별 독립 반올림

어느 쪽도 틀리지 않았다. `camtab` 이 **각 컷의 길이를 독립적으로** 정수 프레임으로
반올림했고, 80행 중 **UP 53 · DOWN 26 · 정확 1** 로 6.68프레임이 쌓인 것이다.
개별 오차는 최대 ±0.0200초에 불과하다.

### 기각된 처방 3종

| 후보 | 왜 기각했는가 |
|---|---|
| 마지막 컷에서 7프레임 흡수 | 증상만 처리. 마지막 컷만 0.29초 짧아진다 |
| 정지 구간에 분산 | 80컷에 6.68프레임을 어떻게 나누든 임의적이다 |
| `setpts` 로 0.08% 시간 신축 | 전체 영상의 모션 속도를 바꾼다 |

세 후보 모두 **「길이를 duration 에서 유도한다」는 전제**를 그대로 둔다.

### 정답 — 컷 길이를 「격자 인덱스의 차」로 정의한다

```python
n = round(t1 * 24) - round(t0 * 24)
```

이러면 반올림이 누적되지 않고 **모든 경계에서 상쇄**된다. 컷 k 의 종료 인덱스가
곧 컷 k+1 의 시작 인덱스이기 때문이다.

| 측정 항목 | 결과 |
|---|---|
| 격자 불연속·겹침 | **0건** |
| 최종 프레임 | **12000 = 500.000000 s (정확)** |
| 오디오 정본과의 차 | 500.010667 − 500.000000 = **0.256 프레임** |

0.256프레임은 24fps 영상이 표현할 수 있는 최소 단위보다 작다. 즉 **표류는
흡수할 대상이 아니라, 애초에 생기지 않게 정의할 대상이었다.**

### 스냅 지점은 「추론」하지 말고 「측정」하라

격자 스냅은 60잡 중 19잡에 ±1프레임을 요구한다. 프레임을 하나 빼거나 넣어야
하는데, **어디서** 하느냐가 화면에 보이는지를 결정한다.

```
J_A3-07 프레임간 평균 |차이| (160x90 그레이)
  정지 구간 (n ≤ 29)   0.0010
  모션 구간 (n > 29)   1.2300      ← 1,230배
```

여기서 "정지 구간에서 편집하라"는 규칙을 세웠는데 **틀렸다.** 정지는 「잡」의
맨 앞에만 있으므로, 용접된 2·3번째 컷은 순수 모션이다. `hold-1` 을 쓰면
팬 도중에 프레임을 빼게 된다.

**그래서 조용한 프레임이 어디 있어야 하는지 추론하지 말고, 측정했다.**

```python
mot = _motion(mp4, a, b)          # 컷 내부 프레임간 차이 실측
quiet = sorted(range(len(mot)), key=lambda i: mot[i])
```

| 실증 (J_A5-03) | 결과 |
|---|---|
| A5-01 65f → 격자 65f | 조정 없음 |
| A5-02 54f → 격자 53f | local 1 에서 −1 (motion 0.2692 vs 평균 0.6437) |
| A5-03 157f → 격자 158f | local 155 에서 +1 (motion **0.0008** vs 평균 0.6768) |
| 이어붙인 결과 | **276f = 11.500000 s 정확** |

A5-03 의 편집 지점은 평균의 **1/846** 밖에 움직이지 않는 프레임이다.

### ffmpeg 세부 함정 2건 (실측)

```
① select 는 프레임을 복제할 수 없다 → 복제는 loop=loop=N:size=1:start=k
② -vsync 0 과 -r 은 동시 지정 시 "This is contradictory" 로 거부됨
   setpts=N/FPS/TB 가 이미 CFR 을 만들므로 -r 만으로 충분
```

> **컷별 정수 반올림은 누적됩니다.** 끝에서 흡수하는 것은 증상 처리입니다.
> **길이를 duration 에서 유도하지 말고 격자 인덱스의 차로 정의**하면
> 반올림이 경계마다 상쇄되어 표류 자체가 존재하지 않게 됩니다.

---

## 교훈 180 — 자막: 어절 정렬은 「탐욕」이 아니라 「전역 최적」이다

[CEO-49] 께서 "어절 단위로 의미를 살려 정렬"을 요구하신 뒤, 관형어가 줄 끝에
오는 결함(`막상 다음 / 회사를`)을 어절 점수로 해결했다. 그런데 **그 처방이
새로운 결함을 만들었다.**

### 증상 — 각 줄바꿈은 좋은데 「레이아웃」이 읽을 수 없다

```
#56  예를 들면,                                          ←  5자
     나는 목적을 공유받고                                 ← 11자
     실행 방식은 스스로 설계할 수 있는 환경에서 더 꾸준히 기여한다.  ← 36자
```

3줄 경로가 **탐욕적**이었기 때문이다. 줄바꿈 1을 1/3 지점 근처에서, 줄바꿈 2를
2/3 지점 근처에서 **각각 독립적으로** 최고 점수를 골랐다. 서로를 볼 수 없으므로
개별 점수는 모두 훌륭한데 전체 배치가 무너진다. 36자 줄 옆의 5자 줄은
[CEO-49] 께서 지목하신 "시각적 정렬" 결함을 **반대 방향에서** 재현한 것이다.

### 처방 1 — 모든 줄바꿈을 한꺼번에 결정 (DP)

```python
cost = Σ(줄별 초과폭 벌점 + 균형 벌점) − Σ(사용된 줄바꿈의 의미 점수) × MEANING_W
```

어절 30개 이하·3줄 이하이므로 3중 순회로 **정확한 최적해**를 구한다. 휴리스틱 불필요.

### 처방 2 — 의미와 기하를 「같은 척도」로

DP 를 넣어도 #116 이 `29자 / 10자` 로 갈렸다. 원인은 척도 불일치다.

```
쉼표 줄바꿈 보너스        +7.00
8자 초과의 균형 벌점      8 × 0.45 = 3.60      ← 의미가 기하를 압도
```

`MEANING_W = 0.55` 로 의미 항을 내려, 강한 경계 하나가 대략 **4자 분량의
불균형**과 맞먹게 했다. 동점을 깨기엔 충분하고, 치우친 배치를 살 만큼은 아니다.

| 지표 | 납품 원본 | 구 탐욕 | DP w=1.0 | **DP w=0.55** |
|---|---|---|---|---|
| 최대 줄폭 | 36자 | 36자 | 29자 | **27자** |
| 30자 초과 | 6건 | 5건 | 0건 | **0건** |
| 26자 초과 | 12건 | 17건 | 6건 | **1건** |
| 들쭉날쭉 평균 | 1.9 | 8.2 | 2.9 | **2.2** |
| 들쭉날쭉 최악 | 7 | **30** | 13 | **9** |
| 관형어 줄끝 | 3건 | 0건 | 0건 | **0건** |

### 처방 3 — 0.2초 플래시 큐는 「합치거나 버린다」

전수 검사에서 별개의 결함이 나왔다. 0.18~0.20초 큐가 4건이다.

```
#99  414.960..415.160  첫째.
#101 419.500..419.700  둘째.
#103 423.600..423.780  셋째.
#118 499.800..500.000  다음
```

5프레임은 사람이 시선을 고정할 수 있는 하한 아래다. 시청자는 무언가가
나타났다 사라진 것만 인지하고 놓친 것을 찾는다. 나레이션이 실제로 0.2초 만에
"첫째"를 말하므로 **오디오를 늦추는 것은 답이 아니다.** 발화 박자를 별도 자막으로
취급하는 것 자체가 잘못이었다 — 뒤 문장에 합치면 제자리에서 읽힌다.

```
#99  414.960..418.760 (3.80s)  첫째. 기억나는 프로젝트 3개를 적고
                               반복한 행동에 밑줄을 긋습니다.
```

`#118 "다음"` 은 500초 절단의 잔재로 뒤에 합칠 큐가 없어 **삭제**했다.

| 최종 검수 (114큐) | 결과 |
|---|---|
| 0.90초 미만 큐 | **0건** |
| 겹침 | **0건** |
| 30자 초과 줄 | **0건** |
| 관형어 줄끝 | **0건** |
| 21자/초 초과 (읽기 속도) | **0건** |
| 오디오 정본 초과 | **0건** |

> **의미 점수를 줄바꿈 하나씩 최적화하면 레이아웃이 무너집니다.**
> 배치는 전체를 한 번에 채점해야 하고, 의미와 기하는 **같은 척도**에 놓아야 합니다.
> 그리고 0.2초 자막은 자막이 아니라 깜빡임입니다.

---

## 교훈 181 — 렌더러가 여러 개면 「전체가 맞는가」를 책임지는 파일이 따로 있어야 한다

여러 세션에 걸쳐 "+0.320초"라는 정체불명의 잉여가 타임라인을 따라다녔다. 앞
교훈 179 에서 나는 이것을 「컷별 반올림의 누적 표류」로 규정하고 격자 스냅으로
처방했다. 컷 내부에 대해서는 그 진단이 옳았다. 그러나 **액트 사이**를 감사하자
전혀 다른 원인이 하나 더 있었다.

```
shots.TABLE (ACT1~2)  마지막 A2-19  t1 = 150.00 s  → 격자 3600
rows38      (ACT3~8)  첫    A3-01   t0 = 150.32 s  → 격자 3608
⇒ 프레임 3600..3608 (8 프레임 = 0.3333 s) 은 어느 표에도 속하지 않는다
```

표류가 아니었다. **구멍**이었다. 0.32초 분량의 영화가 애초에 주인이 없었다.

### 왜 여러 세션 동안 보이지 않았는가

렌더러가 세 개였고, 각자 자기 표만 보고 있었기 때문이다.

| 렌더러 | 참조하는 표 | 볼 수 있는 범위 |
|---|---|---|
| `act0.py` | 자기 내부 SHOTS 리스트 | ACT0 만 |
| `drive500.py` | `shots.TABLE` | 30.0 ~ 150.0 s |
| `drive38.py` | `rows38.json` | 150.32 ~ 500.0 s |

세 파일 모두 자기 구간에서는 정확했다. 각자 자기 게이트를 통과했다. 그런데
**"이 영화가 프레임 12000 에 착지하는가"를 묻는 파일이 하나도 없었다.** 질문의
주인이 없으면 답이 틀려도 아무도 실패하지 않는다.

### 처방 — 지도를 소유하는 단 하나의 파일

`master500.py` 를 신설했다. 이 파일은 **픽셀을 한 장도 만들지 않는다.** 오직
액트 배치가 0..12000 을 빈틈도 겹침도 없이 덮는지만 책임진다.

```python
def seg_map():
    """the authoritative act layout on the 24 fps grid"""
    a0 = round(T[0]["t0"] * FPS)      #   720
    a1 = round(T[-1]["t1"] * FPS)     #  3600
    b0 = round(R[0]["t0"] * FPS)      #  3608
    b1 = round(R[-1]["t1"] * FPS)     # 12000
    out = [("ACT0", 0, a0, "trim", ACT0), ("ACT12", a0, a1, "trim", ACT12)]
    if b0 > a1:
        out.append(("BRIDGE", a1, b0, "hold", ACT12))   # ← 구멍을 메운다
    ...
```

그리고 지도가 스스로를 검사한다.

```
$ python3 -u master500.py map
ACT0     grid     0..720     720 f  trim  act0_final.mp4   have 755 (+35)
ACT12    grid   720..3600   2880 f  trim  act12.mp4
BRIDGE   grid  3600..3608      8 f  hold  act12.mp4
ACT3~8   grid  3608..12000  8392 f  seg   80 shots
MAP OK  83 segments cover 0..12000 with no hole and no overlap
```

`MAP OK` 를 만든 순간 두 번째 결함까지 같이 드러났다 — **ACT0 이 자기 슬롯보다
35프레임 길다.** `act0.py` 는 shots 표가 존재하기 전에 작성됐고 자기 컷 합이
31.458초였다. 슬롯은 30.0초다. 이것도 여태 아무도 묻지 않은 질문이었다.

### 구멍을 무엇으로 메울지는 임의로 정하지 않는다

8프레임을 메우는 후보는 둘이었다.

| 후보 | 판정 |
|---|---|
| 교차 페이드 | **기각.** 대본에 없는 움직임을 발명한다 |
| ACT2 마지막 프레임 정지 | **채택.** 나레이션이 문장 중간이라 정지가 「호흡」으로 읽힌다 |

**규칙: 렌더러가 둘 이상이면, 픽셀을 만들지 않고 전체 배치만 책임지는 파일을
하나 두어라. 그 파일이 없으면 "전체가 맞는가"는 아무의 질문도 아니게 된다.**

---

## 교훈 182 — 「경고」로 반환한 결손은 성공으로 보고된다

`master500.py map` 이 두 번째 사실을 알려줬다. ACT1~2 슬롯은 2880프레임인데
`act12.mp4` 는 2583프레임이었다. **297프레임 = 12.375초가 조용히 사라져 있었다.**

행별로 원하는 프레임 수와 실제 조각의 프레임 수를 나란히 실측했다.

```
A1-06  want  96 f   got -1     ← 파일 없음
A1-08  want 107 f   got -1     ← 파일 없음
A2-02  want  96 f   got -1     ← 파일 없음
(나머지 6건은 ±1 프레임 반올림 — 무해)
96 + 107 + 96 = 299 f ≈ 297 f 결손
```

### 근본 원인은 코드 두 줄이었다

```python
# drive500.py:131
if not os.path.exists(src):
    return None, f"{r['sid']}: generated clip not delivered yet ({src})"
```

결손 행을 **에러가 아니라 경고로 반환하고 계속 진행한다.** 그래서 마스터가
12초 짧게 나와도 프로세스는 `rc=0` 으로, 즉 **완료**로 끝난다. 로그에는 경고가
찍혔지만, 경고는 사람이 읽어야만 의미가 생긴다. 프레임 수를 세는 게이트가
없었기 때문에 12초의 소실이 세 세션을 살아남았다.

### 그리고 그 3본은 이미 납품돼 있었다

`_r_v2.json` 을 열어보니 V-2 팀이 그 세 컷을 진작에 납품했고, URL 이 그대로
남아 있었다. 재생성(유료 3회) 대신 **회수(무료)** 했다.

```
A1-06 → files/s/ljUPnTnv    A1-08 → files/s/RZ6wysPV    A2-02 → files/s/pfRWfbN7
실측 3본 전부 7.018 s · 24 fps · 1280x720 · 168 f  (슬롯 4.0~4.46 s 에 충분)
확대 직독 검수 합격 — 문서 더미 컷이라 저품질 글자가 구워진 곳이 없음
```

**「납품 URL 이 있다」와 「디스크에 있다」는 서로 다른 사실이다.** 두 번째를
확인하는 게이트가 없으면 첫 번째는 아무 의미가 없다.

### 결손은 두 번째 결함을 숨기고 있었다

회수한 3본을 넣고 `drive500.py` 를 재실행하자 이번엔 **크래시**했다.

```
ValueError: intentional_transition requires transition_reason
```

`A1-08` 은 `intentional_transition` 정책 행이다 — 스택이 절개되어 내부에서
따뜻한 빛이 나오는 컷이라, 그 대비를 색보정으로 중화하면 안 된다. `cmatch.py`
는 그런 경계에 **이유를 데이터로 명시할 것**을 요구한다. 그런데 이유는 그
액트 헤더의 **산문 주석에만** 있었고, 게이트가 읽는 필드로는 없었다.

**이 게이트는 여태 한 번도 실행된 적이 없었다.** 해당 행의 클립이 없어서
`drive500` 이 그 행을 건너뛰었기 때문이다. 결손이 게이트를 가리고 있었다.

```python
S("A1-08", 56.18, 60.64, "i2v", "A1_WARM", "intentional_transition",
  transition_reason="the sealed stack is cut open at this cut and the interior "
                    "emits warm light; neutralising that contrast would erase "
                    "the reveal the narration is describing",
  expected_warmth_direction="warmer", ...)
```

**규칙: 결손을 경고로 처리하지 마라. 산출물의 프레임 수를 세고, 슬롯과 다르면
실패시켜라. 그리고 결손 행 하나는 그 행에 걸려 있던 게이트 전부를 함께
잠재운다 — 결손을 메우면 잠들어 있던 게이트가 깨어나 그동안 검사되지 않았던
것을 처음으로 검사한다.**

---

## 교훈 183 — 팀의 권고는 「맞는 지표」로 검증해야 의미가 생긴다

전담 채널에 발주해 둔 과업의 회신 2건이 도착해 있었다. 하나는 카메라 반경
보간 방식에 관한 권고였다.

> 반경은 선형 대신 **로그 보간**으로 두십시오:
> `e=smootherstep(t); r(t)=exp((1-e)*ln(r0)+e*ln(r1)) = r0*(r1/r0)**e`
> 프레임당 상대 반경 변화 `Δln r` 를 균등화해 근거리 중간 구간의 시차 뭉침을
> 줄입니다.

이 권고를 채택하면 60잡 전량 재렌더(약 3시간)가 필요하다. 그러니 **효과의
크기를 정확히 알아야** 판단할 수 있다. 측정을 세 번 했고, 처음 두 번은 틀렸다.

### 1차 — 자료구조를 추측했다

```python
for jid, j in json.load(open("jobs.json"))["jobs"].items():
# AttributeError: 'list' object has no attribute 'items'
```

`jobs.json["jobs"]` 는 **리스트**다. `job_id` 는 각 원소의 필드다. 교훈 172 의
재발 — 추측하지 말고 열어보라.

### 2차 — 지표가 틀려서 무의미한 수치가 나왔다

원시 `|Δln r|` 의 컷 내 최대/최소 비율을 재봤다.

| | 선형(현행) | 로그(권고) |
|---|---|---|
| spread | 306,202 | 212,375 |

수십만 배. **이 숫자는 아무것도 말해주지 않는다.** 원인은 이징이다. 우리는
`smootherstep` 이징을 쓰는데, 이징은 컷의 시작과 끝에서 진행률 자체를 0 에
수렴시킨다. 그러니 프레임당 변화량의 최소값은 어느 보간을 쓰든 0 에 가깝고,
그 비율은 발산한다. **이징이 보간 방식의 차이를 완전히 덮어버렸다.**

### 3차 — 이징을 분리했다

측정해야 할 것은 「프레임당」이 아니라 「진행률당」 상대 변화, 즉
`d(ln r)/de` 의 컷 내 변동이다.

| 지표 | 선형(현행) | 로그(권고) |
|---|---|---|
| 평균 max/min | 1.727 | **1.000** |
| 최악 max/min | **2.630** (J_A8-01/02/03) | **1.000** |

이제 숫자가 의미를 갖는다. 현행 선형 반경은 **한 컷 안에서 「다가오는 속도」가
최대 2.63배까지 요동한다.** 로그 반경은 근사가 아니라 항등식이므로 정확히
1.000 이다 — `d(ln r)/de = ln(r1/r0)` 는 상수다.

**팀의 권고가 옳다.** 다만 전량 재렌더가 90% 진행된 시점이었으므로 채택 여부는
비용/효과를 정리해 결정권자에게 올렸다. 검증은 무료였고 오판은 3시간이었다.

### 부수 발견 — 회신을 「받는」 것도 검증해야 한다

같은 채널에서 발주 하나가 **본문 없이 멘션만** 전달돼 있었다. 원인은 CLI
인자 키였다.

```
잘못:  {"channel_id": "...", "text": "..."}      → 본문 유실, 멘션만 전송
정본:  {"channel_id": "...", "content": "..."}   → gsk genteam send --help 로 확인
```

팀은 "요청하실 항목을 보내주시면 검토하겠습니다"라고 회신했다 — 빈 본문을
받았다는 뜻이다. **발송 후 채널을 다시 읽어 본문 글자 수를 확인**하는 것으로
게이트를 세웠다.

**규칙: 팀의 권고는 「맞는 지표」로 재라. 지표가 틀리면 무의미한 수치가 나오고,
무의미한 수치는 옳은 권고를 기각하게 만든다. 그리고 발주는 「보냈다」가 아니라
「본문이 도착했다」까지 확인해야 발주다.**

---

## 교훈 184 — 「승인된 퀄리티」를 다른 형식으로 번역하지 말라

대표님이 승인 기준으로 스틸 3장을 주셨다(CEO-65). 나는 그것을 "글자가 크고
선명하다"로 읽고 **잉크의 화소 수**를 올리는 방향으로 갔다.

세 장을 픽셀로 계측하고서야 형식 자체가 다르다는 것을 알았다.

| | 내가 만들던 것 | 승인 기준 3장 |
|---|---|---|
| 물체 | 종이 위에 **인쇄된 잉크** | 씬 안에 놓인 **물리적 시안 네온 라이트박스** |
| 배경 | 종이 흰색 | 패널 채움 RGB (20,27,25)~(31,41,38) |
| 글자 | 검은 잉크 | 흰 한글 (241,241,241), 순백 아님 |
| 발광 | 없음 | 네온 림 (125,191,197), 코어 (210,255,255) |
| 색 문법 | — | **`B >= G > R` 항상** |

「크게」의 방향은 맞았지만 **「무엇을」이 틀렸다.** 잉크를 아무리 크게 해도
라이트박스가 되지 않는다.

**규칙: 승인된 것을 내 코드의 어휘로 번역하지 말라. 계측해서 그 형식 자체를 재현하라.
계측은 무료다(PIL·numpy). 번역은 세션 단위의 손실을 낸다.**

---

## 교훈 185 — 모션 문법 4개(12조합)와 씬 부재가 60컷을 같게 만들었다

CEO-67 #5: "프리비즈 모든 컷의 움직임이 동일하고 단어만 바꾼 것 같은데"

원인을 코드에서 집계했다.

```
카메라 무브 어휘 = 4 종
레벨            = 3 종
⇒ 실질 조합 12 개로 60 컷을 채우고 있었다
씬(set)         = 없음. 모든 컷이 같은 8개 프리미티브 위에서 벌어졌다
```

그래서 컷이 바뀌어도 **바뀌는 것이 글자뿐**이었다. 대표님이 본 것이 정확했다.

처방은 세 층을 동시에 늘리는 것이었다.

| | 이전 | 이후 |
|---|---|---|
| 무브 어휘 | 4 | **22 (실사용 17)** |
| 씬 | 없음 | **10 세트 · 250 객체** |
| 렌즈 | 1 (34mm) | **24–50mm 12 값** |
| 카메라 z | 고정대 | **1.31–6.53 m** |

**규칙: 「같아 보인다」는 지적은 취향이 아니라 조합 수의 문제다. 어휘 수를 코드에서
집계하면 원인이 산술로 나온다.**

---

## 교훈 186 — 「정확도 개선」은 「가짓수」를 늘리지 않는다

교훈 185 를 발견하기 전, 나는 여러 세션 동안 카메라 궤적의 **정확도**를 개선했다
(로그 반경 보간, 고도각 보간, 이징 함수, 반올림 누적 제거). 전부 옳은 개선이었다.

그런데 대표님이 본 화면은 달라지지 않았다. **정확도를 올려도 가짓수는 그대로였기
때문이다.** 4개의 무브를 더 정확하게 실행해도 여전히 4개다.

**규칙: 개선 작업을 시작하기 전에 "이것이 대표님 화면에서 무엇을 바꾸는가"를 한 줄로
쓸 수 있어야 한다. 쓸 수 없으면 그것은 내부 품질이고, 지적을 해소하지 않는다.**

---

## 교훈 187 — 게이트를 「경고」로 만들면 결함이 그대로 렌더된다

교훈 182 의 재발이다. `previz_batch.py` 의 글자 게이트 4종 중 하나를 경고로
낮췄더니, 프레임 밖으로 나간 글자가 그대로 렌더되어 납품까지 갔다.

```
경고: gate soft-fail J_A3-02 off-frame y 1.01   → 렌더 계속 → 납품 → 반려
정본: GLYPH GATE FAILED J_A3-02 ...            → 렌더 중단 → 원인 조사
```

게이트가 실패로 멈추면 **실패 메시지가 결함의 정본**이 된다. 이것은 무료이며
지금까지 결함 9건을 적발했다. 경고로 낮추면 그 정보가 로그에 묻힌다.

**규칙: 게이트는 실패해야 게이트다. 통과시키고 싶으면 임계값을 근거와 함께 바꿔라.
게이트의 심각도를 낮추는 것은 근거가 아니다.**

---

## 교훈 188 — 상수가 「버그」인지 「보상」인지 전수 계측으로 확인하라

`previz_batch.py` 에 이런 상수가 있었다.

```python
PAPER_W = 2.70   # metres
PAPER_H = 2.16
```

A4 는 `0.296 x 0.210 m` 다(`sets.py paper()`). 9배 부풀려져 있다. 나는 이것을
**버그로 판단**하고 실측값으로 고치려 했다.

76 잡 전수로 스윕해 보고 판단이 뒤집혔다.

```
실측 A4 클램프 적용 시 인쇄단계 잉크 폭 = 21 ~ 201 px
MIN_INK_PX = 210 통과 = ★0 / 66 잡★
최악 J_A4-05 (lens 28, d 11.39 m): 필요 2.402 m vs 가용 0.260 m = 9.2배 부족
```

**`PAPER_W = 2.70` 은 버그가 아니라 「210px 을 지키기 위한 보상」이었다.**
그리고 그 보상은 튜닝값이 아니라 **구조적**이다 — 크기를 종이에서 유도하는 한
`{실측 세트, MIN_INK_PX, CEO-65 승인 형식}` 중 하나는 반드시 깨진다.

### 처방 — datum 을 옮긴다

승인 기준 3장은 애초에 종이 위의 잉크가 아니었다(교훈 184). 계측된
`panel_w / frame_w` 는 `0.422 / 0.447 / 0.633` 이다. **승인된 datum 은 종이가 아니라
프레임 점유율이다.** 이것을 채택하면 네 가지가 동시에 풀린다.

1. `PAPER_FIT` 클램프가 세트 기반 샷에서 소멸한다
2. `sets.py` 가 실측 A4 를 유지한다
3. 크기 기준이 대표님이 승인한 형식과 일치한다
4. 210 px 이 구성적으로 충족된다 (`0.42 * 1280 = 538 px`)

legacy `jobs.json`(60 샷, `set` 필드 없음)은 종이 클램프를 유지한다 —
이미 렌더된 산출물의 재현성을 깨지 않기 위해서다(안정성 지침).

**규칙: 상수가 실측과 어긋날 때 「고치기」 전에 전수 계측하라. 어긋남이 보상일 수
있고, 보상을 제거하면 다른 게이트가 무너진다. 그리고 보상이 구조적이면 튜닝이 아니라
datum 을 옮겨야 한다.**

---

## 교훈 189 — 검증 단위는 「완성품」이 아니라 「최소 자립 서사」다

CEO-70 의 지적을 산술로 옮겼다.

| | 전편 선행 | 숏츠 선행 |
|---|---|---|
| 렌더 | 2.3 시간 | 23.2 분 |
| v2v 유료 호출 | 76 회 | 13 회 |
| 반려 시 손실 | **100%** | **17%** |

**폐기 위험 1/6 · 피드백 주기 6배.**

내가 「전편을 완성해서 보여드린다」를 성실함으로 여긴 것이 오류였다. 완성품은
반려되면 전액이 날아간다. 대표님이 제안한 순서가 비용 구조상 옳다.

**규칙: 유료 자원이 드는 작업은 「완성품 1개」가 아니라 「자립적으로 판정 가능한 최소
단위」로 쪼개서 검증받아라. 최소 단위의 조건은 「그 자체로 서사가 성립하는가」다.**

---

## 교훈 190 — 「합의된 것」은 산출물이 아니라 발주 본문에 있다

CEO-71: "우리 애초에 해당 롱폼을 가지고 만들기로 한 숏츠 3부작이 있어요."

나는 그것을 리포지토리에서 찾았다. 없었다. 후보 5종을 찾아 산술로 하나를 골랐고,
**반려되었다.**

### 탐색 경로

```
1. webapp 전수 탐색            → 후보 5종, 전부 배제
2. grep "반복해서 맡아온 역할" webapp  → ★0 건★
   ⇒ 롱폼 대본이 webapp 에 아예 없다
   ⇒ 탐색 범위를 /home/user/lf 로 전환
3. lf 전체 grep               → ★120초 타임아웃★
4. grep --include=*.md,*.txt,*.csv  → ★0.2초 · 2건 적출★
   ⇒ gt/mkt_body.txt M-1 에 3부작이 전문으로 있었다
```

**`gt/*_body.txt` = GenTeam 발주 원문 아카이브 = 합의의 정본이다.** 발주할 때 내가
쓴 본문이므로 리포지토리에 없어도 합의는 성립되어 있었다.

### 부수 도구 교훈

```
lf 전체 grep                              → 120초 타임아웃
grep --include=*.md,*.txt,*.csv           → 0.2초
```

**600배.** 확장자를 좁히는 것이 병렬화보다 효과가 크다.

**규칙: 「우리가 정한 것」을 찾을 때는 산출물이 아니라 「내가 발주한 본문」을 먼저
읽어라. 그리고 대규모 디렉토리 grep 은 반드시 `--include` 로 확장자를 좁혀라.**

---

## 교훈 191 — 두 승인 지표가 「2글자 단어」에서 동시 충족 불가능하다

교훈 188 의 처방(datum 을 프레임 점유율로 이전)을 구현했다. 잉크가 435 → 602 px 로
올랐다. 그런데 드라이런이 여전히 실패했다.

```
    glyph ink 602-602 px of 1280 (lift)
★GLYPH GATE FAILED J_A3-02: off-frame x 0.60 y 1.01 at frame 66★
```

y 축이 1% 초과. 「occW 를 조금 낮추면 되겠다」고 생각했다. **틀렸다.**

### 승인 기준의 두 번째 지표를 계측했다

지금까지 폭만 재고 있었다. 높이를 계측했다.

| 항목 | std1 | std2 | std3 |
|---|---|---|---|
| `panel_w / frame_w` | 0.422 | 0.447 | 0.633 |
| **`glyph_h / frame_h`** | **0.0885** | **0.1390** | **0.1893** |

승인 높이 밴드는 `0.0885 ~ 0.1893` 이다.

### 우리 텍스처는 2~3글자 단어다

```
A3 방식   aspect 1.669  ← 최악(가장 높은 평면)
A4 변화   aspect 1.767
A5 기준   aspect 1.781
A6 조율자 aspect 2.586
A7 역할   aspect 1.789
A8        ★부재★
```

승인 기준의 텍스트는 **문장**이다. 같은 폭 점유율에서 **2글자 단어는 문장보다
높이가 2.5~3배 높다.**

### 산술 스윕 — 해가 존재하지 않는다

`h_share = occW / ink_share / aspect * 1280/720`, 최악 `aspect 1.669`:

```
occW 0.47 -> 0.536  over        occW 0.24 -> 0.273  over
occW 0.42 -> 0.479  over        occW 0.20 -> 0.228  over
occW 0.34 -> 0.387  over        occW 0.18 -> 0.205  over
occW 0.28 -> 0.319  over        occW 0.16 -> 0.182  ★OK★
```

높이 밴드를 통과하는 값은 `occW 0.16` 뿐이다. 그런데:

```
잉크 폭 = 0.16 * 1280 = ★205 px < MIN_INK_PX 210★  ⇒ 잉크 게이트 실패
```

**두 승인 지표를 동시에 만족하는 `occW` 는 존재하지 않는다.**
교훈 188 과 같은 형태의 구조적 모순이다 — 튜닝으로는 풀리지 않는다.

### 처방 — datum 을 「단어」에서 「문장 패널」로

근거 3개가 모두 같은 곳을 가리킨다.

**근거 1** 승인 3장이 전부 문장이다
```
"정답은 없습니다" / "남기고 싶은 변화" / "선택지는 늘고 기준은 흐려집니다"
```

**근거 2** CSV 의 `on_screen_text` 열이 이미 문장이다
```
A1-09  "선택지는 늘고 기준은 흐려집니다"            (1:00.64)
A2-09  "명사보다 동사로 써보세요"                   (1:59.62)
A2-15  "선택한 3개의 프로젝트에서 반복된 동사가 있나요?"  (2:12.48)
A2-17  "나는 이런 역할을 맡을 때 내 경험을 잘 활용한다"    (2:19.10)
A3-12  "정답은 없습니다"      A4-01  "남기고 싶은 변화"
```

**근거 3** CEO-64 원문이 바로 이것을 지적했다
> **"'방식'이라는 글자도 정확하지만 글자 자체가 우리 예전에 이미지에 걸맞는
> 퀄리티는 아니지 않아요?"**

⇒ `words/meta.json` 이 단어(방식·변화·기준·조율자·역할)만 담고 있는 것이 **결함의
뿌리다.** `mkwords.py` 가 **문장**을 렌더해야 한다. 문장은 `aspect 4~8` 이므로
승인 높이 밴드에 자동으로 정착한다.

부수 조정: `mkwords.py` 의 aspect 게이트 상한 `6.0 → 10.0`, A8 텍스처 동시 생성.

### 왜 6회나 반복 지적되었는가

이것이 글자 품질 지적 6회(CEO-16/18/57/58/64/67#6)의 진짜 원인이다. 나는 매번
「단어를 더 크게·더 정확하게」 만드는 방향으로 갔다. 방향 자체가 틀렸다 —
승인 기준은 처음부터 **문장 패널**이었다.

**규칙: 지적이 3회 이상 반복되면 그것은 튜닝 부족이 아니라 datum 이 틀렸다는 신호다.
승인 기준의 지표를 「전부」 계측하라. 하나만 재고 있으면 다른 하나가 모순을 숨긴다.**

---

## 교훈 192 — 누적 상태를 대화에 두면 「축적」이 아니라 「병목」이 된다

CEO-72:
> **"대화 압축하는 시간이 너무 많아요. 새 채팅창을 만들까요? 아니면 당신이 해결할
> 수 있으면 해주세요. 아주 큰 병목 사항입니다."**

그리고 CEO-67 의 마지막 문장이 같은 것을 이미 지적하고 있었다.
> "아니 왜 점점 축적된 개선이 나오지 않고 따로 노는 느낌이 아는지 모르겠어요."

**두 지적은 같은 뿌리다.** 나는 CEO 지시 원문 72건 + 교훈 190여 개 + artifact URL
200여 개 + 코드 스냅샷을 **매 세션 대화 안에서 재압축**하고 있었다. 그 중 대부분은
확정되어 변하지 않는 것이다.

### 새 채팅창은 답이 아니다

`/home/user/lf/` 는 리포지토리 밖이다. 계측해 보니 이렇다.

```
lf/ 전체                 4.8 GB (렌더 산출물 포함)
lf/ 텍스트 소스만        ★3.4 MB / 321 파일★  ← 커밋 가능
```

**숏츠 3부작 정본 · 제작 규칙 3항 · `previz_batch.py` 편집이 전부 그 3.4MB 안에만
있었다.** 새 창을 열면 유실된다. 그리고 그것들은 이미 CEO-71/70 의 지시 내용이므로
다시 만들 수도 없다.

### 처방 — 세 층으로 분리한다

| 층 | 성질 | 어디에 두는가 |
|---|---|---|
| **확정 · 불변** | CEO 지시 원문, 교훈, artifact 판정, 승인 계측 | **repo 문서** |
| **재현 가능한 소스** | `.py` `.json` `.csv` `.srt` | **repo 미러** (`youtube/hybrid3d/pipeline/`) |
| **살아있는 상태** | 지금 편집 중인 파일, 실패 중인 게이트, 다음 1동작 | **대화 요약 (4항목)** |

실행한 것:

```
docs/marketing/youtube/00_CEO_DIRECTIVES.md      CEO-1~72 원문 전량
docs/marketing/youtube/71_..._RULES.md           교훈 1~192
docs/marketing/youtube/73_ARTIFACT_LEDGER.md     URL 전량 + 판정 태그
docs/marketing/youtube/74_SHORTS_TRILOGY_SPEC.md 숏츠 3부작 정본
docs/marketing/youtube/75_SESSION_STATE.md       살아있는 상태만
youtube/hybrid3d/pipeline/                       lf/ 텍스트 소스 2.5MB 미러
```

이후 요약에 남길 것은 **4항목**이다.
① CEO-72 이후 신규 지시 ② 현재 편집 중인 파일 ③ 실패 중인 게이트 메시지 ④ 다음 1동작

**규칙: 「축적」은 대화가 아니라 리포지토리에 한다. 대화에 누적하면 매 세션 재압축
비용이 붙고, 그 비용은 세션이 길어질수록 커진다. 그리고 repo 밖 디스크에 있는 정본은
정본이 아니다 — 언제든 유실될 수 있는 초안이다. 판단 기준: "이것이 다음 세션에도
필요한가"가 예이면 그 자리에서 파일로 쓴다.**

## 교훈 195 — 글자의 「도착점」을 고정 월드 좌표로 두면 안 된다

크기(hw)는 이미 화면 점유율로 옮겼는데(교훈 188), **위치**는 여전히 고정 미터값이었다.
그래서 카메라가 가까워지면 같은 미터값이 화면의 훨씬 큰 비율이 되어 프레임을 벗어난다.

실측 (J_A3-03 · converge · frame 60):
```
k0 ndy +0.251 / k1 ndy -0.355 / k2 ndy -0.960   ey = 0.960 + 0.116 = 1.08  FAIL
```
간격(0.606 NDC)은 정상. 문제는 클러스터가 고정점 `(0,-0.30,0.92)` 로 모이는데
시선점은 `(0,0.04,1.068)` 이라 **스택 중심이 ndcY -0.355 로 어긋난** 것.
중심을 시선점에 맞추면 같은 스택이 `0.606+0.113 = 0.719` 로 통과.

실측 (J_A3-14 · lift · 50mm · creep_in):
```
f55  dist 0.710m  프레임 높이 0.51m  고정 상승 0.34m = 반높이의 0.67배
     ndy +1.152  ey 1.28  FAIL
```
58개 set 기반 lift 잡 전수 스윕:
```
고정 0.34m   worst 1.581 (J_A5-01)   프레임 이탈 5잡
vfrac 0.24   worst 0.960             이탈 0
vfrac 0.20   worst 0.874             이탈 0   ← 채택 (LIFT_VFRAC)
```
처방: 도착점을 **매 프레임 현재 프레이밍에서 해석**한다.
- converge → 시선점(`tgt.location`)에 모인다 ⇒ 스택이 항상 화면 중앙
- lift → 프레임 높이의 `LIFT_VFRAC`(0.20)만큼 상승 ⇒ 프레이밍이 좁아지면 상승도 함께 줄어든다

**레거시 격리**: `jobs.json` 60잡(set 없음)은 이미 납품된 프레임이 고정점으로 렌더된 것이므로
`job.get("set")` 이 없으면 고정점을 그대로 유지한다(안정성 지침). 회귀 확인 DRY OK 60잡.

## 교훈 196 — 몇 장을 쌓을 수 있는지는 「텍스처」가 정한다, 화면에 든 시트 수가 아니다

평면의 화면상 반높이는 거리와 무관한 닫힌 식이다:
```
hh_ndc = OCC / (ink_share · k) · (W/H) / aspect
```
실측 (converge 8잡):
```
A3 1줄 aspect 7.111 → hh 0.112   gap 0.300   3장 가능
A4 1줄 aspect 7.420 → hh 0.110   gap 0.300   3장 가능
A6 2줄 aspect 3.151 → hh 0.268   gap 0.547   3장 가능
A7 2줄 aspect 3.048 → hh 0.262   gap 0.535   3장 가능
★A5 3줄 aspect 1.808 → hh 0.462  gap 0.942  ★2장까지만★
```
즉 `CLUS_VFRAC = 0.30` 은 **간격이 아니라 간격의 하한**이다. 간격은 평면 자신의 높이를
넘어야 하고, 장 수는 `(n-1)/2·gap + hh <= 0.94` 를 만족하는 최대값이다.
장을 버려야 할 때는 **읽는 순서의 가장자리**를 버린다(문장 중간을 비우지 않는다).

## 교훈 197 — 「몇 줄이 필요한가」와 「어디서 끊는가」는 다른 질문이다 (균형 조판)

**계기**: CEO-73 — "A5 문구가 3줄이라 광각 컷에서 화면을 다 덮습니다. 2줄로 줄여도 됩니다.
시각적인 최적화 사례를 늘 참고해서 적용하세요. **단, 벤치마크를 해도 내 것으로 소화해서
우리의 목적에 맞게 더 높은 품질로 가공해야 합니다.**"

### 문제 — 그리디 랩은 첫 질문만 답한다

그리디 줄바꿈은 각 줄을 상한까지 채우고 남은 것을 다음 줄로 버린다. 그래서 A1 이
`선택지는 / 늘고 기준은 / 흐려집니다` 처럼 **2어절 줄 위에 1어절 줄 둘**로 나왔다.
「필요한 줄 수」는 맞았지만 「어디서 끊을지」는 아무도 정하지 않은 결과다.

### 1단계 — 참조 (벤치마크)

전문 조판은 이것을 **balancing** 으로 푼다.
- **Knuth-Plass line breaking** (TeX) — 문단 전체의 badness 합을 최소화
- **CSS `text-wrap: balance`** — 가장 넓은 줄을 최대한 좁게 만드는 분할점 선택

### 2단계 — 소화 (★그대로 쓰면 안 되는 이유를 실측으로 확인★)

균형만 최적화하니 A1 이 `선택지는 / 늘고 기준은 / 흐려집니다` (widest aspect **5.20**,
가장 균등한 분할)로 갔고 — **승인 밴드를 0.2152 로 이탈해 반려**됐다.
정사각형에 가까운 블록은 같은 화면 폭에서 글자가 더 커지기 때문이다.

★결정적 반례: 대표님 승인본 std3 자체★ — 본문 줄 aspect **6.830**.
즉 승인된 조판은 **의도적으로 「가장 균등」이 아니다**.

⇒ **균형 조판의 목표 함수(가장 균등한 줄)와 우리 목표(승인된 글자 크기 유지)는 다르다.**

### 3단계 — 가공 (우리 것으로 재정의)

**사전식(lexicographic) 목표**로 격상한다.
```
1. 승인 밴드 안에 있을 것          (하드 제약 · 타협 불가)
2. 그 중에서 줄을 균형있게          (미학 · 2순위)
```
그리고 채점 대상을 `render()` 가 실제로 만드는 블록과 **동일하게** 한다
(`_block()` / `_in_band()`). **proxy 를 채점하면 통과시킨 블록을 게이트가 반려한다.**

### A1 조판 전수 스윕 (판정 근거)
```
BAND 0.0885..0.1893   승인 std3: 3줄 · 본문줄 aspect 6.830 · glyph_h/frameH 0.1632
BAND n2 선택지는 / 늘고 기준은 흐려집니다      widest 10.46  h63 0.1071
OK   n2 선택지는 늘고 / 기준은 흐려집니다      widest  8.16  h63 0.1378
BAND n3 선택지는 / 늘고 기준은 / 흐려집니다    widest  5.20  h63 0.2152  ← 균형만 최적화한 결과
★OK  n3 선택지는 늘고 / 기준은 / 흐려집니다    widest  6.16  h63 0.1818  ← 채택 (승인본 6.830 최근접)★
```

### A5 2줄 후보 실측 (CEO-73 승인 이행)
```
1줄 전체 aspect 14.034 (상한 7.43 초과)
★정답 공식이 아니라, | 더 좋은 질문   a1 8.43 a2 5.21  tex 3.61  h42 0.0892 h63 0.1337  채택★
 정답 공식이        | 아니라, 더 좋은 질문  tex 3.75  h42 0.0859  밴드 하한 이탈
 정답 공식이 아니라, 더 | 좋은 질문        tex 4.13  h42 0.0780  이탈
⇒ converge 시 화면 반높이 0.462 → ★0.222★ (3줄→2줄로 절반)
```
결과: **WORD GATE OK 8/8**, A1 은 승인본 std3 과 동일한 3줄 조판 유지.

**일반 규칙 (다음 사람에게)**: 업계 방법을 가져올 때는 반드시
**그 방법의 목표 함수가 우리 목표와 같은지 실측으로 확인**하라. 다르면 재정의하라.
헌법 §0.0.5 「벤치마크 소화 3단계」가 이 교훈의 일반화다.

## 교훈 198 — 게이트 함정 4종 (단위 · 여유 · 적용 구간 · 낡은 전제)

### (1) 단위 함정 (UNIT TRAP)
`gap_ndc` 는 **프레임 전체 높이**의 비율이다(`gap_m` 을 그렇게 만들었으므로).
그런데 `ndcY` 는 **반높이** 단위다. 섞어 쓰면 정확히 **2배** 어긋난다.
실측: A5 예측 half-extent 0.672(통과) vs 렌더 1.123(실패) → 3장을 유지해버렸다.
처방: `gap_half = 2.0 * gap_ndc` 로 변환한 뒤 비교한다.

★단, 2배는 **한 곳에만** 써야 한다 — 교훈 199(2) 참조.★

### (2) 여유(clearance)는 누가 주는가
분리 게이트는 `need > sep * 1.02` 즉 `sep >= need/1.02` 를 요구한다.
**2% 를 소비하는 쪽이 게이트다.** 간격 산식에 1.02 를 쓰면 여유가 0이 되어 경계에
정확히 착지한다(실측 J_A6-02: stack 0.54m vs gap 0.53m).
처방: 여유는 **우리가 주는 것**이므로 한 곳에 명시하고 넉넉히 → `CONV_CLEAR = 1.15`

### (3) 게이트는 「그 현상이 존재하는 구간」에만 적용된다
개별 평면의 off-frame 게이트는 전 프레임 유효하지만, **두 평면의 상호 관계**를 재는
게이트는 그 관계가 성립하는 구간에서만 의미가 있다.
★★단, 이 원칙을 「가드」로 구현하면 실제 결함을 가릴 수 있다 — 교훈 199 가 그 실패 사례다.
구간을 좁히기 전에 반드시 「그 구간에서 정말 안전한가」를 실측하라.★★

### (4) 게이트의 전제가 낡을 수 있다
"최소 2장" 규칙은 모든 텍스처가 짧은 단어(화면 0.11)일 때 쓰였다. 문장 텍스처에서는
2줄 0.22 / 3줄 0.46 이고, 교훈 196 에 의해 **텍스처가 1장만 허용**할 수 있다.
읽을 수 있는 한 단어는 유효한 컷이고, 잘린 단어는 아니다 ⇒ 하한을 1로.

## 교훈 199 — 가드로 덮지 말라. 「측정하지 않은 서술」은 게이트를 거짓으로 만든다

★이 교훈은 내가 교훈 198(3)에서 넣은 처방이 틀렸음을 스스로 계측으로 밝혀낸 기록이다.
헌법 §0.0.4 가 이 항목을 「재생산 가능한 지식화」의 핵심 예시로 지목한다.★

### 무엇을 했나 (잘못)
`J_A7-02: stack 1.30m > gap 0.13m at frame 1` 을 보고 이렇게 판단했다:

> "frame 1 에서는 글자들이 아직 각자 시트 위에 인쇄되어 몇 미터씩 떨어져 있다.
> 겹침 여부는 **시트 간격**이 정하는 것이지 스택 간격이 아니다."

그리고 게이트에 `and e > 0.25` 가드를 넣어 정지 구간의 검사를 껐다.

### 무엇이 틀렸나
**위 문장을 나는 측정하지 않았다.** 서술로 단정했다. 실측하니 거짓이었다.

J_A7-02 의 `doc_anchor` Y 는 `0.17 / 0.04 / -0.09` — **0.13m 종렬**이고, 카메라 시선
방향에 거의 나란해서 **화면상 간격이 0.024** 다. 반면 승인 점유율(42%) 패널은 그 거리에서
높이 **1.29m**. 즉 **필요량의 1/25** 다. 세 문장이 완전히 겹쳐 인쇄된 상태였다.

converge 8잡 전수 실측 (hold 프레이밍 · 인쇄 간격 vs 패널이 필요한 간격):
```
J_A3-03  0.308 / 0.277   needs 0.30   → 둘 다 확보  (3장 OK)
J_A3-07  0.409 / 0.668   needs 0.30   → 둘 다 확보
J_A4-08  0.417 / 0.425   needs 0.29   → 둘 다 확보
J_A5-03  0.020 / 0.059   needs 0.51   → 없음
J_A6-02  0.024 / 0.081   needs 0.58   → 없음
★J_A7-02  0.024 / 0.024   needs 0.60   → 없음  ← 실패 잡★
J_A7-04  0.055 / 0.057   needs 0.60   → 없음
```
⇒ **가드는 결함을 고치지 않았다. 가렸다.** 통과한 드라이런은 거짓 통과였다.

### 진짜 구조
글자는 시트에서 출발해 클러스터로 모인다. 즉 **상호 간격이 `e` 와 함께 자라는데
패널 크기는 자라지 않는다.** 그러므로 **출발 간격이 패널보다 좁으면 반드시 겹친다.**
겹침은 「모이는 중」의 문제가 아니라 **「출발점 선정」의 문제**다.

### 처방 (2건)

**(1) 인쇄 상태에서 이미 떨어져 있는 시트만 글자를 싣는다**
`visible_docs()` 에서 캐리어를 고를 때, 게이트가 강제하는 것과 **같은 여유**로
연속 시트의 화면상 간격을 검사한다. 자격을 갖춘 시트가 하나뿐이면 converge 는
자연히 lift 로 degrade 한다(그것은 유효한 컷이다 — 교훈 198(4)).
그리고 **분리 게이트는 전 프레임으로 되돌린다**(가드 제거).

**(2) 2배는 한 곳에만 (교훈 198(1)의 이중 계상 적발)**
`hh_ndc` 는 반높이, `gap_m` 은 전체 높이 기준. 그래서 필요 간격은
산식에서 `hh*CLEAR`, ndcY 비교 시 `2*hh*CLEAR` 다.
두 줄에 모두 `2*hh*CLEAR` 를 쓰면 **실제 간격이 `4*hh`** — 필요량의 2배가 되어
글자를 프레임 밖으로 밀어낸다. 이것이 A7 이탈의 남은 절반이었다.
```
gap_ndc  = max(CLUS_VFRAC, hh_ndc * CONV_CLEAR)     # 산식: 전체높이 단위
gap_half = 2.0 * gap_ndc                            # 비교: 반높이 단위
```

### 결과
```
DRY OK  jobs 76  every gate passed      (76/76 — J_A7-02 포함)
DRY OK  jobs 60  every gate passed      (레거시 회귀 무손상)
```

### 일반 규칙 (다음 사람에게)
1. **게이트를 좁히는 변경은 「그 구간이 정말 안전한가」를 실측한 뒤에만 한다.**
   좁히기는 고치기가 아니다. 실패가 사라진 것과 결함이 사라진 것은 다르다.
2. **주석에 「~이므로 안전하다」고 쓸 때, 그 문장에 수치가 없으면 그것은 추측이다.**
   (헌법 §0.0.2 R2 — 측정값 없는 근거는 근거가 아니다)
3. **같은 계수를 두 곳에 쓰기 전에 각 줄의 단위를 적어라.** 2배는 한 곳에만 있다.

---

## 교훈 200 — 대본이 서사의 정본이다. 렌더러가 대본을 읽지 않으면 서사가 사라진다

### 무엇이 일어났는가

숏폼 C(25.9초, 6컷)를 납품했고 대표님은 **"모두 만족하는데"** 라고 하신 뒤 두 가지를
지적하셨다.

> **"프래비즈에 '정답은 없습니다'만 계속 반복하는 이유가 뭐에요? 25.9초가 아까 전달한
> '귀공자' 프래비즈처럼 하나의 신으로 서사가 있어야 하는 거 아니에요? 자막은 바뀌는데,
> 영상은 하나의 장면은 다각도로 돌리는 느낌인데.."** — CEO-74

두 지적은 별개로 보였다. 하나는 **글자**, 하나는 **장면**. 실측 결과 **하나의 뿌리**였고,
그 뿌리는 「재고 부족」이 아니라 **「대본 무시」** 였다.

### 실측 — 대본 의도 vs 렌더러 동작 (76잡 전수)

```
want CONFLICT got TEXT :  1
want NONE     got NONE : 10
★want NONE   got TEXT : 61   ← 결함★
want TEXT     got TEXT :  4
```

**76컷 중 61컷이 대본상 「글자 없음」인데 렌더러가 글자를 실었다.**
대본이 글자를 원한 컷은 **단 5개**(`on_screen_text` 가 채워진 컷)였다.

### 대본은 이미 금지를 명시하고 있었다 (115행 중 58행)

`camera_note` 열의 "| ~ 금지" 절:

```
"읽히는 글자 금지"(x2)  "내용 선행 금지"  "문장 내용 선행 금지"  "선행 채움 금지"
"전달문구 생성 금지"  "선택 이유 텍스트 금지"  "회사명·금액 읽힘 금지"
"세 행 모두 아직 비움"  "2·3행 선행 금지"  "셋째 행 선행 금지"
"직무명 임의 생성 금지"  "동시 노출 금지"  "진단·점수 금지"  "추상 빛길 금지"
"트로피·성과수치 금지"  "홀로그램·회로 금지"  "얼굴·개인정보 금지"
```

즉 **대표님이 CEO-75 에서 말씀하신 원칙이 대본에 이미 쓰여 있었다.**

### 인과 사슬 — 두 지적은 같은 결함의 앞면과 뒷면

```
1) previz_batch.py line 465:  word = WORDS/"%s.png" % job["act"]
   ⇒ 글자 텍스처를 ACT 단위로 하나만 고른다
2) 문장 재고는 ACT 당 1장  ⇒ A3 의 16개 잡이 전부 "정답은 없습니다"
3) 글자를 61컷에 억지로 실으려니 5개 문장을 돌려썼다   ← 지적 (1) 글자 반복
4) 글자를 보여주려 카메라가 글자 있는 책상만 봤다      ← 지적 (2) 한 장면 다각도
```

**글자 배급 규모** (글자 싣는 66잡이 문장 5개를 돌려씀):

| act | 잡 수 | 텍스처 | 문장 |
|---|---|---|---|
| A3 | 16 | A3.png | 정답은 없습니다 |
| A4 | 15 | A4.png | 남기고 싶은 변화 |
| A5 | 13 | A5.png | 정답 공식이 아니라, 더 좋은 질문 |
| A6 | 10 | A6.png | 나는 어떤 역할을 반복해 왔는가? |
| A7 | 12 | A7.png | 오늘은 초안이면 충분합니다 |

**시선점 집중 실측** (숏폼 C 6컷 · 세트는 4종인데 시선은 한 점):

```
J_A3-13 S7 r=1.59 z=1.64 tgt(0.00,-0.46,0.89)
J_A3-14 S1 r=1.56 z=1.48 tgt(0.10,-0.04,0.89)
J_A3-15 S1 r=1.25 z=1.43 tgt(0.10,-0.04,0.79)
J_A3-16 S2 r=2.58 z=1.63 tgt(0.00, 0.00,0.89)
J_A3-17 S7 r=2.67 z=2.10 tgt(0.00,-0.46,0.89)
J_A4-01 S4 r=6.57 z=4.76 tgt(0.00, 0.04,0.89)
```

원인은 구조적이었다. `scenejobs.py` 가 시선을 **`doc_anchor[1]`(중앙 문서 슬롯)** 에
고정했고, 모든 세트가 그 슬롯을 자기 중앙에 둔다. 그래서 **10개 세트가 9개 시선점으로
붕괴**했다. **세트 이름이 달라도 시선점이 같으면 관객에게는 한 장면이다.**

### 대본에는 컷마다 다른 장면이 이미 쓰여 있었다 (`screen_direction`)

| sid | screen_direction |
|---|---|
| A3-13 | 비교표 옆 빈 조건 카드. 손이 과장된 컬러 스티커를 떼고 중성 카드만 남김 |
| A3-14 | 조건 카드 위 굵은 제목 바 1개와 빈 본문 바 1개 |
| A3-15 | 손이 본문 위치에 중간회색 바를 한 줄 채움 |
| A3-16 | 회의 브리프 1장과 옆의 개인 설계 노트. 목표칸/실행칸을 컬러 바로 강조 |
| A3-17 | 회사명·급여칸이 있는 채용공고 위에 조건 카드가 함께 놓임 |
| A4-01 | 사무실 단면 위층의 결과 보관 서가로 수직 상승 |

**「귀공자」 수준 서사는 대본에 있었고 렌더러가 버렸다.** 세트는 만들었지만 컷마다
**다른 소도구 · 다른 시선점 · 다른 동선**을 배정하지 않았다.

### 처방 (구현 완료 · 전량 게이트 검증)

| # | 처방 | 파일 | 검증 |
|---|---|---|---|
| ① | **글자를 예외로** — `gesture_of()` 가 대본 `on_screen_text` 를 읽고, 없으면 `none` | `scenejobs.py` | `gestures none=71 lift=5` (이전 lift=58) |
| ① | **잡별 텍스처** — `word_meta(key)` 를 beat id 키로. `word_key` 신규 필드 | `previz_batch.py` `mkwords.py` | `WORD GATE OK textures 17` (이전 8) |
| ② | **시선점 분산** — `sets.GAZE` 세트별 실제 가구 좌표 4점 + 직전 컷에서 최원거리 선택 | `sets.py` `scenejobs.py` | `GAZE GATE OK closest pair 0.402 m` · G4 12건→0건 |
| ③ | **`screen_direction` 소도구** — `sets.PROPS` sid별 2~4 프리미티브 | `sets.py` `previz_batch.py` | `PROPS GATE OK 6 beats 21 objects` |
| ④ | **재발 방지 게이트** — `script_gate.py` G1~G4, 렌더 전 필수 통과 | `script_gate.py` `previz_batch.py` | 61건→0건 · 레거시는 46건 검출로 게이트 생존 증명 |

### 파생 발견 — 줄 수 선택에도 사전식 목표가 필요했다 (교훈 197 확장)

beat 텍스처를 만들자 `A3-01 "직무명이 아닙니다"` 가 승인 밴드를 벗어났다:

```
1줄 aspect 8.159  (LINE_ASPECT_MAX 7.43 초과) ⇒ _lines_needed 가 2줄 반환
2줄 aspect 1.681  (거의 정사각) ⇒ glyph_h/frame_h 0.1914 > 승인 상한 0.1893  ★반려★
```

**교훈 197 의 사전식 목표를 「어디서 끊는가」에만 적용하고 「몇 줄인가」에는 적용하지
않았다.** 밴드는 하드 제약(대표님 승인본 실측), 종횡비 상한은 가독성 선호다. 순서를
바로잡았다:

```
1. 승인 밴드 안        (하드)
2. 줄 종횡비 상한 안   (선호)
3. ★줄 수 최소★       (미학)
```

**3순위를 「가장 좁은 줄」로 두면 레거시가 조용히 바뀐다** — 실측: A6 2줄→3줄,
A3-05 2줄→3줄. 「줄 수 최소」로 두면 밴드가 실패하는 곳에서만 작동하고 레거시 8종은
값이 완전히 동일하게 보존된다. 확인: A1 1.695 / A2 3.061 / A3 7.111 / A4 7.420 /
A5 3.612 / A6 3.151 / A7 3.048 / A8 6.481 — 전부 이전과 같다.

### 파생 발견 2 — 글자를 실은 컷은 카메라가 그 글자를 봐야 한다

시선을 실제 가구로 옮기자 `J_A3-01` 이 `GLYPH GATE FAILED off-frame x 1.11` 로
떨어졌다. 시선은 옮겼는데 **글자 캐리어(`doc_anchor[1]`)는 옛 중앙 슬롯에 남았기**
때문이다. 처방: 글자를 싣는 5개 컷에서만 캐리어를 시선점으로 이동. 카메라를 캐리어로
끌고 가지 않는다 — 그것이 애초에 「한 장면 다각도」를 만든 동작이다.

### 일반 규칙

1. **소재의 정본은 대본이다.** 잡(job)은 대본의 파생물이고, 파생 과정에서 대본 열
   (`screen_direction` / `camera_note` / `on_screen_text`)을 버리면 서사가 사라진다.
   버린 것은 렌더 결과를 봐도 보이지 않는다 — **「없는 것」은 화면에 나타나지 않기
   때문이다.**
2. **기본값을 뒤집지 말라.** 글자는 예외여야 하는데 기본값(`return "lift"`)이었다.
   **기본값이 틀리면 61컷이 조용히 틀린다.**
3. **대본에 열이 있으면 그 열을 읽는 게이트를 먼저 만들어라.** 읽지 않은 열은
   「없는 열」과 구별되지 않는다. (헌법 §0.0.2 R3)
4. **세트를 나누는 것과 시선을 나누는 것은 다른 일이다.** 장소를 10개로 늘려도 시선점이
   9개로 붕괴하면 관객이 보는 장면 수는 늘지 않는다. **관객이 세는 단위는 세트가 아니라
   시선점이다.**
5. **게이트가 레거시를 거부하는 것은 게이트가 살아있다는 증거다.** 레거시 60잡에서 46건을
   검출한 것이 이 게이트를 신뢰하는 이유다. 우회는 명시적으로
   (`PREVIZ_NO_SCRIPT_GATE=1`, 로그에 남김) — **조용한 우회가 첫 결함을 살려둔 방식이다.**

---

## 교훈 201 — 채움률은 「덩어리마다」 재야 한다. 합집합 bbox 는 여러 면을 성긴 획으로 위장한다

9:16 조립물의 CLIP GATE 가 4프레임에서 실패했다. 처음에는 임계값이 잘못됐다고
생각했지만, 실제 원인은 **측정 단위**였다.

잉크 마스크 전체의 bounding box 로 채움률을 재면, 서로 떨어진 두 개의 시트(밝은
종이 면)가 하나의 큰 bbox 로 묶이고 그 안의 대부분이 빈 공간이 되어 **채움률이
낮게** 나온다. 채움률이 낮으면 「글자」로 분류된다. 그래서 **종이 두 장이 글자로
위장**했다.

**처방**: 연결 성분(blob)별로 분리해서 각 덩어리의 채움률을 잰다. sandbox 에
`scipy` 가 없으므로 4-이웃 flood fill 을 손으로 구현했다(`shorts916.blobs()`).

### ⚠ 처방 후 새 함정 — 게이트 무력화

덩어리별로 재니 `glyph 0 / sheet 3990` 이 되었다. **자막 글자까지 시트로 분류되어
검사 대상이 0개**가 됐다. 이것은 통과가 아니라 **게이트가 사라진 것**이다
(교훈 199 위반). 0건 통과를 통과로 보고하지 않고 결함으로 기록한다.

**미처방 상태 — 다음 작업자에게**: 자막/제목 밴드(y < 500, y >= 1108)는 무조건
글자로 보고 edge 검사만, 영상 밴드(500~1108)만 덩어리별 fill 로 시트를 배제한다.

---

## 교훈 202 — 다양성은 수단이고 몰입이 목적이다. G4 는 「다르냐」를, G5 는 「맞냐」를 본다

**[CEO-76]** "장면만 바뀌는 셈이면 아무리 퀄리티가 좋아도 영상의 몰입감은 없을
거에요."

VARIETY GATE 를 통과하고 컷마다 다른 씬·다른 무브가 나왔는데도 몰입이 없었다.
이유는 **카메라가 자막이 말하는 대상을 보고 있지 않았기 때문**이다. 실측: 6컷 중
**4컷**에서 시선점이 대본 지정 소도구로부터 0.25 m 밖이었다.

**처방**: `gaze_of()` 를 사전식 목표로 재작성했다.

```
1순위  대본이 지정한 소도구로부터 SUBJ_NEAR(0.25 m) 안인가
2순위  직전 컷의 시선점에서 G4_MOVE(0.35 m) 이상 떨어졌는가
3순위  가능한 한 멀리 (같은 그림 회피)
```

결과: prop-to-gaze 실패 4/6 → **1/6**, 이후 G5 게이트 신설로 **6/6**.

### 파생 1 — 「지목」과 「대비」는 측정 단위가 다르다

- **지목** ("이 카드 한 장") → **최근접** 소도구까지의 거리로 잰다
- **대비** ("브리프 목표칸과 노트 실행칸을 각각 강조") → **무게중심**으로 잰다

`A3-16` 은 최근접으로 재면 실패지만 무게중심으로는 0.066 m 로 통과다. 대본이
두 물건을 **비교**하라고 했으니 카메라는 두 물건 **사이**를 봐야 맞다.
`CONTRAST_KEYS` 로 분기한다.

### 파생 2 — 대본을 반만 읽으면 소도구가 흩어진다

`A3-17` 대본은 "채용공고 **위에** 조건 카드가 **함께 놓임**" = **스택**이다.
첫 판은 소도구를 1.72 m 흩어 놓았고(`namebar2` 를 임의로 복제까지 했다) G5 가
0.51 m 로 적발했다. 한 자리에 겹쳐 쌓으니 0.185 m 로 통과.

### 파생 3 — 「시선이 같다」와 「그림이 같다」는 다른 문제 → G7

`A3-14`("빈 본문 바") → `A3-15`("손이 본문을 채움") 는 **같은 대상의 연속 동작**
이므로 시선이 같은 것이 **대본대로 맞다**. G4 를 느슨하게 하면(교훈 199 위반)
진짜 반복도 통과한다. 그래서 **샷 사이즈**를 따로 검사한다 → 교훈 202 → G7.

실측 배율 대역: `A3-14` [28.5..50.9] vs `A3-15` [36.9..50.2] = 겹침 **59%**.
76잡 전수에서 `dt < 0.35 m` 인 연속쌍은 단 **2건**(A3-14→15 실패, A3-15→16 0% OK).

---

## 교훈 203 — G5 는 「방향」만 봤다. 「크기」는 아무도 안 봤다 — 주연이 화면에 0.00% 여도 통과했다

G5 를 통과시킨 뒤 **v2 렌더 프레임을 픽셀로 직독**해서 알게 된 것이다.

| 프레임 | 해상도 | 검은 공백 | **밝은 시트(=주연)** | 전체 평균 |
|---|---|---|---|---|
| `J_A3-15_first` | 1280x720 | 0.0% | 1.67% | 84 |
| `J_A3-17_first` | 1280x720 | 0.0% | 0.91% | 85 |
| `J_A3-17_last` | 1280x720 | 5.2% | 0.18% | 79 |
| **`J_A4-01_last`** | 1280x720 | 9.1% | **★0.00%★** | 70 |

`J_A4-01` 은 주연(이력서 3장)이 화면에 **한 화소도 없다**. 그런데 G5 는 시선점
오차 0.020 m 로 **「최우수 통과」** 였다. 카메라는 주연을 **향했지만** 주연은
**보이지 않았다**.

### 원인 — 합집합 폭은 「주연 사이의 빈 공간」을 잰다 (교훈 201 의 3D 판)

`A4-01` 의 PROPS 합집합 실폭은 4.950 m 여서 화면 점유율이 0.643~0.922(=우수)로
계산됐다. 그런데 개별 이력서는 각 **0.15 m** 다. 4.95 m 는 **3장 사이의 허공**
이었다. 개별 투영 실측: `res2` 의 depth 가 3.92 → **-0.39 m (카메라 뒤!)**.

### 임계값 근거 (발명 아님)

통과군 최솟값 **0.266** · 실패군 최댓값 **0.097** ⇒ `SUBJ_FRAC_MIN = 0.14`
(= 179 px / 1280). **실패군에 가까운 쪽**을 골랐다 — 게이트는 「합격을 넉넉히」가
아니라 **「불합격을 확실히」** 잡아야 한다(교훈 199).

---

## 교훈 204 — 대본은 카메라 이동량(%)까지 지정했는데 무브 카탈로그가 그걸 읽지 않는다

`camera_note` 에 퍼센트 지시가 있는 행: **30 / 115**. 26개 대조행 중 **25건이
3배 초과**였다.

| sid | 대본 | 실제 | 배율 |
|---|---|---|---|
| A3-01 | 3.0% | 138.0% | 46x |
| A5-02 | 3.0% | 266.8% | 89x |
| A7-02 | 0.8% | 61.9% | 77x |

### ★그러나 그대로 강제하면 안 된다★

이 %는 **2D 플레이트 Ken Burns 값**이다. 3.0% 이동을 3D 카메라에 강제하면
`MIN_RAD_MPS` 를 깨고 **CEO-51("컷 안에서 움직임 · 정지 없음")** 을 위반한다.

검사 가능한 진짜 지표는 **「주연이 화면을 얼마나 차지하는가」**다. 즉 대본의
% 지시를 **크기로 번역**해서 G6 로 검사한다. 이것이 벤치마크를 그대로 쓰지 않고
우리 목적에 맞게 가공하는 것(CEO-73)의 한 예다.

---

## 교훈 205 — 「크기가 안 나온다」의 진짜 원인은 「대본 절반이 3D 에 없었다」였다

**교훈 200 의 재발이다. 크기 문제로 보인 것이 소도구 누락 문제였다.**

`A3-13` 의 대본 `screen_direction` (verbatim):

> "**비교표** 옆 빈 조건 카드 한 장. 손이 과장된 컬러 스티커를 떼고 중성 카드만 남김."

**BEFORE** `PROPS["A3-13"] = ['cond', 'stkoff', 'stkoff2']`

| 객체 | 크기 | 정체 |
|---|---|---|
| `cond` | 0.144 m | 명함 크기 조건 카드 |
| `stkoff` | 0.044 m | 떼어낸 스티커 |
| `stkoff2` | 0.040 m | 떼어낸 스티커 |

⇒ **비교표(A4 규모 = 이 컷의 첫 번째 주연)가 아예 없었다.** 그래서 최대 주연이
65 px / 1280 = **5.1%** 였고, 렌즈로 통과시키려면 **97 mm** 가 필요했다
(`LENS_CEIL` 85 초과).

### ★진단 절차 (재사용 가능)★

```
1. G6 실패
2. 렌즈로 통과 가능한지 계산
3. LENS_CEIL 초과면 → ★렌즈 문제가 아니라 소도구 누락★
4. 대본 screen_direction 재독
5. 빠진 주연을 sets.py 에 추가
```

⇒ **`LENS_CEIL` 은 「상한」이 아니라 「대본 누락 탐지기」다.**

### AFTER — 7 객체

`cmptab`(0.105 x 0.148, A4 규모) + `cmprow0/1/2`(3행 바) + `cond` + `stkoff` +
`stkoff2`. 비교표는 앞 컷 `A3-11`("세 행") / `A3-12`("3칸 비교 whole")가 세운
것이므로 3행 구조가 대본 계보와도 일치한다.

PROPS GATE: 21 → **25 extra objects**.

---

## 교훈 206 — 「거리」는 무브의 정체성이고 「렌즈」는 프레이밍만 바꾼다. 고쳐야 할 것은 렌즈다

### 프로토타입 1 — 거리만 줄여서 G6 를 통과시키면 (★반증★)

| job | verb | target | scale | frac | rad_mps |
|---|---|---|---|---|---|
| A3-13 | 후퇴 | 0.30 | **0.020** | 0.051→0.231 | 0.025 **★위반★** |
| A3-14 | 도착 | 0.34 | 0.797 | 0.275→0.340 | 0.513 OK |
| A3-15 | 정지 | 0.42 | 0.637 | 0.266→0.420 | 0.122 **★위반★** |
| A3-16 | 관통 | 0.26 | 0.449 | 0.097→0.260 | 0.014 **★위반★** |
| A3-17 | 후퇴 | 0.30 | 0.287 | 0.072→0.300 | 0.264 OK |
| A4-01 | 도착 | 0.34 | 0.381 | 0.061→0.340 | 0.991 OK |

`A3-13` scale 0.020 = **카메라가 책상에서 3 cm**. 대본이 지시한 "4% pull-back"
이 아니다. 그리고 **3/5 가 `MIN_RAD_MPS` 0.22 를 깨서 CEO-51 위반**이다.

### 프로토타입 2 — 렌즈 우선 (TGT 0.175, LENS_CEIL 85) (★채택★)

| job | lens | needL | scale | 결론 |
|---|---|---|---|---|
| A3-13 | 28→85 | **97** | 0.050 | ★렌즈로 불가 = 소도구 누락 신호★ |
| A3-14 | 50→50 | — | 1.000 | 이미 통과 0.275 |
| A3-15 | 48→48 | — | 1.000 | 이미 통과 0.266 |
| A3-16 | 28→**51** | 51 | **1.000** | 궤적 무손상 |
| A3-17 | 24→**58** | 58 | **1.000** | 궤적 무손상 |
| A4-01 | 28→**80** | 80 | **1.000** | 궤적 무손상 |

**4 중 3 이 렌즈만으로 해결. 궤적 무손상.**

### 영화적 근거 · 게이트 안전성

실제 촬영도 작은 소도구는 카메라를 들이밀지 않고 **망원으로 당긴다**.
`grep -n 'lens'` 전수로 **렌즈를 제약하는 게이트가 없음**을 확인했다
(`previz_batch` line 417 `LENS = float(job.get("lens") or LENS_DEFAULT)`,
line 613 `cam.data.lens = LENS`; `scenejobs` 는 `rec["lens"]` 복사만).

### ★파생 방법론 — 처방을 코드에 넣기 전에 프로토타입 스윕으로 반증하라★

거리 처방은 **코드에 넣기 전에** 스윕으로 반증됐다. 만약 넣고 나서 알았다면
SPEED GATE 를 느슨하게 하는 유혹(교훈 199 위반)에 빠졌을 것이다. 이것을
측정 방법 위계에 추가한다: **프로토타입 스윕 = 처방 방향 정본 · 무료**.

---

## 교훈 207 — 화면상 「크기」는 x 폭이 아니라 최장변이 정한다. 종이는 눕고 카메라는 내려다본다

| 측정 단위 | A3-13 | A3-14 | A3-15 | A3-16 | A3-17 | A4-01 |
|---|---|---|---|---|---|---|
| `2*sc[0]` (x폭) | 65 | 352 | 340 | **88** | **71** | 78 |
| **`2*max(sc[0],sc[1])`** | 65 | 352 | 340 | **★124★** | **★92★** | 78 |

렌더 직독에서 읽힌 크기는 **최장변** 쪽과 일치했다. 소도구는 책상 평면에 눕는
납작한 종이/카드이고 카메라는 내려다보므로, 화면상 크기는 최장변이 정한다.

**그리고 관대한 쪽을 쓴다 — 관대하게 재도 실패하면 확실한 실패다** (교훈 199 파생).

---

## 교훈 208 — 처방은 게이트가 재는 값과 「같은 값」으로 계산해야 한다

렌즈 처방을 `scenemap.py` 에 넣었는데 G6 가 여전히 2건을 잡았다.
`A3-13` 11.4%, `A4-01` 11.5% — 하한 14% 에 **가깝지만 미달**이었다.

### 원인 — 근사와 정밀의 차이

`scenemap` 단계에서는 방위각(골든앵글)도 시선점도 아직 정해지지 않았다. 그 둘은
`scenejobs.py` 의 `endpoints()` 와 `gaze_of()` 가 정한다. 그래서 `scenemap` 은
**직선거리 근사**를 썼다. 그런데 화면 크기를 정하는 것은 **광축(cam→tgt) 방향으로
투영한 depth** 다.

| job | 근사거리 | 실제 depth | 오차 |
|---|---|---|---|
| A3-17 | 2.641 m | 2.780 m | x1.05 |
| A3-16 | 2.192 m | 2.384 m | x1.09 |
| **A4-01** | 1.249 m | **1.904 m** | **x1.52** |
| **A3-13** | 1.248 m | **2.013 m** | **x1.61** |

근사가 거리를 최대 x1.61 **짧게** 봤으므로 렌즈를 그만큼 **덜** 올렸다.
근사는 「보수적」이었지만 「충분」하지 않았다.

### 처방 — 게이트 쪽으로 옮긴다

`refine_lens()` 를 `scenejobs.py` 에 신설했다. `script_gate.subj_frac()` 과
**동일한 산식**(광축 투영 depth · 최장변 · 개별 주연)으로 cam/tgt 확정 **후에**
렌즈를 올린다. 렌즈는 depth 를 바꾸지 않으므로 고정점이 한 번에 잡힌다.

**결과: G6 2/6 → 4/6 → ★6/6★**

| job | 최종 lens | 이유 |
|---|---|---|
| J_A3-13 | 42.9 | 주연을 화면 18% 로 담으려면 43mm 필요 |
| J_A3-14 | 50.0 | (이미 통과) |
| J_A3-15 | 54.0 | 직전 컷과 샷 사이즈를 벌림 48→54mm (타이트) |
| J_A3-16 | 50.7 | 주연을 화면 18% 로 담으려면 51mm 필요 |
| J_A3-17 | 58.4 | 주연을 화면 18% 로 담으려면 58mm 필요 |
| J_A4-01 | 80.0 | 주연을 화면 18% 로 담으려면 80mm 필요 |

### 일반 원칙

**근사로 처방하고 정밀로 검사하면 그 차이만큼 영원히 실패한다.** 이때 게이트를
느슨하게 하는 것은 고치는 것이 아니다(교훈 199). **처방을 게이트 쪽으로 옮겨라.**

### G7 처방도 같은 원칙으로

`separate_shot()` 은 `script_gate.shot_band` / `band_overlap` / `TGT_MIN_MOVE` /
`SHOT_OVERLAP_MAX` 를 **import 해서 그대로 호출**한다(교훈 176: 상수를 복제하지
말라). 겹침을 이미 기울어 있는 방향으로 벌리고(A3-15 는 배율 중앙이 위이므로
타이트하게 = "손이 본문을 채움" 액션 포인트를 크게 잡음), G6 하한 아래로는
내려가지 않게 막는다. 결과 **G7 1/2 → 2/2** (겹침 59% → 34%).

---

## 교훈 209 — 게이트와 렌더가 다른 파일을 읽으면 게이트는 아무것도 지키지 않는다

G1~G7 을 모두 통과시킨 직후, v3 렌더를 돌리고 로그를 보니:

```
SCRIPT GATE OK                                  <- scenejobs.json 을 검사
...
Saved: '_batch/J_A3-13/f_0137.png'              <- 101 프레임 잡인데 137 장?
```

`J_A3-13` 은 **101 프레임**인데 **156 프레임**으로 렌더되고 있었다.

### 원인 — 기본값 불일치

| 파일 | `PREVIZ_JOBSFILE` 기본값 |
|---|---|
| `script_gate.py` | `scenejobs.json` (76잡, 새 렌즈) |
| `previz_batch.py` | **`jobs.json`** (60잡 레거시, 렌즈 없음) |

즉 **게이트는 새 잡을 검사하고 렌더는 옛 잡을 렌더**했다. 게이트를 여섯 개
세워 놓고도 결함이 그대로 통과하는 상태였다 — 교훈 187 의 변종이다.
게이트가 「경고로 격하」된 것이 아니라 **「다른 대상을 보고」** 있었다.

### 처방 두 겹

1. `previz_batch.JOBS` 기본값을 `scenejobs.json` 으로 바꿨다. 레거시를 쓰려면
   `PREVIZ_JOBSFILE` **와** `PREVIZ_NO_SCRIPT_GATE=1` 를 **둘 다** 명시해야 한다.
2. **★SAME-FILE GATE★** 신설 — 실행 시점에 `script_gate.JOBSFILE` 과
   `previz_batch.JOBS` 의 `realpath` 가 같은지 확인하고, 다르면 렌더를 거부한다.
   기본값을 맞추는 것만으로는 환경변수 하나로 다시 어긋날 수 있다.

### 파생 — 레거시에서 「검사 불가」를 「통과」로 만들지 말라

레거시 잡에는 `lens` 필드가 없어 G6/G7 이 `KeyError` 로 죽었다. 없는 값을 0 으로
채우면 G6/G7 이 **조용히 통과**한다(교훈 199 위반). 그래서 `has_lens()` 로
분기하고 **「검사할 수 없다」를 실패로 기록**한다. 검증 결과: 레거시 60잡은
**52건 위반으로 거부**되고 정본 76잡은 **SCRIPT GATE OK**.

### 다음 작업자에게 — 일반 원칙

**「무엇을 검사했는가」와 「무엇을 실행하는가」가 같은 대상인지 확인하라.**
통과 보고는 그것이 검사한 대상에 대한 보고일 뿐이다. 파이프라인이 여러 단계로
나뉘면 각 단계가 서로 다른 기본값을 가질 수 있고, 그 순간 검증은 장식이 된다.

---

## 교훈 210 — 「크다」는 「보인다」가 아니다: 화각과 가림을 함께 물어라

### 사건

G6(FRAMING) 가 6/6 통과했는데 `J_A4-01` 의 렌더 프레임은 **완전 균일 회색**이었다.
픽셀 직독 결과 `maxside/W = 0.000` — 주연이 화면에 **한 픽셀도 없다**.
게이트는 같은 컷을 `0.175` 로 통과시켰다.

### 원인 — G6 이 재는 것은 「거리 대비 크기」뿐이다

G6 산식은 `frac = 2*max(sc0,sc1) * lens / (depth * SENSOR_MM)` 다.
이것은 **「그 물체가 광축 위에 있다면 화면의 몇 %를 차지하겠는가」** 를 답한다.
답하지 않는 것이 두 가지 있다.

1. **화각(frustum)** — 물체가 광축에서 얼마나 벗어나 있는가.
   `lens 80mm` 의 반화각은 수평 12.7° / 수직 7.2° 다. `res1` 은 광축에서
   **22.4°** 떨어져 있었다. 즉 **화면 밖**이다. 그런데 depth 는 작으니
   frac 은 크게 나온다.
2. **가림(occlusion)** — 물체와 카메라 사이에 판이 있는가.
   `S4` 의 `"floor"` 판(z 2.44, 14×11 m)은 이름만 floor 이고 실제로는
   **카메라 위를 덮는 통천장**이었다. 프레임 전체가 그 판의 밑면이었다.

### 처방 세 겹

1. **G8(가시성) 신설** — `subj_frac()` 안에서 각 후보에 대해
   `visible(cam, tgt, loc, lens, blocks)` 를 호출하고 **안 보이는 것은 크기를
   세지 않는다**. 화각은 수평 `atan(36.0/2/lens)` · 수직
   `atan(SENSOR_H_MM/2/lens)` (`SENSOR_H_MM = 36*720/1280 = 20.25`),
   가림은 `max(halfext) >= 2.0` 인 대형 판만 blocker 로 보고 slab test 한다.
2. **세트 정정** — 대본이 **"사무실 단면 위층"** 이라고 쓴다. 「단면」은 절개다.
   `room("upper")` 의 통천장을 `ceilBand`(폭 0.90 m 띠) 로 바꿨다.
   **대본이 세트의 정본이다**(교훈 200).
3. **PROPS 정정** — `A4-01` 의 흩어진 3장을 **한 서가 묶음**으로 모았다.

### 다음 작업자에게 — 일반 원칙

**게이트를 세울 때마다 「이 게이트가 못 보는 축은 무엇인가」를 물어라.**
축의 계보는 이렇게 자랐다: **크기 → 화각 → 가림**. 각 축은 앞의 축을 통과한
컷에서 결함을 찾아냈다. 산술이 통과해도 **렌더 프레임 픽셀 직독**이 최종이다 —
그것은 무료이고, 여기까지 게이트를 두 번 반증했다.

---

## 교훈 211 — 「어울리는가」와 「안에 있는가」는 다른 질문: 카메라 높이는 세트 내용물이 정한다

### 사건 — 같은 컷이 세 번째로 실패했다

`J_A4-01` 은 G1~G8 과 SET/GAZE/PROPS/ARC/RADIUS/HEIGHT/COVER/VARIETY/SPEED
**전부를 통과한 상태**에서 렌더에서 **두 번 연속** 실패했다.

| 버전 | 렌더 결과 | `maxside/W` |
|---|---|---|
| v3 | 완전 균일 회색 (천장 밑면) | **0.000** |
| v4 | 회색 벽 → 서가 측면 스침 | **0.051** (하한 0.14) |

G6 은 v4 를 `0.175` 로 통과시켰고, **G8(화각+가림)도 통과시켰다.**

### 원인 — 카메라가 세트 밖 허공에 있었다

`drone_drop` 의 `z0 = 4.60 m` 다. `S4` 의 **내용물** 최상단은 서가 `2.10 m`,
이력서 묶음은 `z 0.87~1.76 m` 에 있다.
즉 카메라는 서가보다 **2.7 m 위 허공**에서 출발해 서가의 **측면**만
극단적 부감으로 스치며 내려온다.

`drone_drop` 은 **실외/대공간용** 무브인데 실내 서가 컷에 배정된 것이다.

### 왜 두 게이트가 다 놓쳤는가

- **`SET_ONLY` 는 「궁합」만 본다.** `drone_drop` 은 `S4` 금지 목록에 없으므로
  통과했다. 그런데 궁합이 맞아도 **높이가 안 맞으면 카메라는 세트 밖**이다.
- **G8 도 못 잡는다.** 카메라가 허공에 있어도 주연이 화각 안이고 가려지지
  않았으면 「보인다」고 판정한다. 실제로는 정면이 아니라 **측면을 극단 부감**으로
  보는 것이어서 화면상 실루엣이 무너진다.

### 전수 계측 — 설계 모순의 정본

무브 카탈로그 22종 × 세트 10종을 산술로 전수 스윕했다.

```
세트별 내용물 최상단 / z 상한 (= max(1.20, top*1.50))
  S1  0.94 / 1.41    S2  1.86 / 2.79    S3  2.70 / 4.05
  S4  2.10 / 3.15    S5  0.94 / 1.41    S6  2.70 / 4.05
  S7  0.85 / 1.28    S8  0.77 / 1.20    S9  0.94 / 1.41
  S10 1.07 / 1.61

무브별 최대 z / 통과 세트 수
  ★drone_drop  4.60 -> fits 0/10★     lift_out 3.40 -> 2/10
  vault_up 3.35 -> 2/10                crane_settle 3.10 -> 3/10
  shelf_slide 2.95 -> 3/10             wide_open 2.85 -> 3/10
  pull_reveal 2.35 -> 4/10             hold_wide 2.10 -> 4/10
  cross_bay 2.05 -> 4/10               orbit_half 1.62 -> 4/10
  step_back 1.62 -> 4/10               push_in 1.45 -> 5/10
  settle_tilt 1.42 -> 5/10             whip_across 1.34 -> 8/10
  lateral_track 1.28 -> 8/10           shoulder_swing 1.24 -> 9/10
  breathe_in / creep_in / desk_graze / drift_side / low_pan / rear_follow -> 10/10
```

**`drone_drop` 은 우리 10개 세트 어디에도 들어가지 않는다 (0/10).**
카탈로그에 실외용 무브 하나가 섞여 있었던 것이다. 이것은 산술 스윕이
밝혔고, 무료다.

### 처방 — 게이트

`scenemap.py` 에 **Z-FIT 게이트**를 신설했다.

```python
Z_HEADROOM = 1.50        # 내용물 최상단의 1.5배까지는 「그 공간 안」으로 본다
Z_FLOOR_MIN = 1.20       # 아주 낮은 세트에서도 최소 이 높이는 허용한다

def _set_top(set_id):
    """이 세트가 담고 있는 내용물의 최상단 z (벽/천장 제외)."""
    top = 0.0
    for nm, _k, loc, sc, _c in build_spec(set_id):
        if nm.startswith(("wall", "ceil", "slab", "floor")):
            continue      # 껍데기는 「내용물」이 아니다
        top = max(top, loc[2] + sc[2])
    return top

def move_fits_height(move, set_id):
    ceil = max(Z_FLOOR_MIN, _set_top(set_id) * Z_HEADROOM)
    z0, z1 = MOVES[move][2], MOVES[move][3]
    return max(z0, z1) <= ceil + 1e-9
```

**임계값은 발명하지 않고 실측에서 골랐다.** `S4` 서가 `2.10 m` 이므로
상한 `3.15 m` → `drone_drop`(4.60) 은 거부되고 `crane_settle`(3.10) 등이 남는다.

무브 선택 pool 에서 전부 걸러지면 **필터를 풀지 않고**
「가장 낮은 무브」를 결정적으로 남긴다 — 가드로 덮지 않는다(교훈 199).

### 처방 — 카탈로그 (VARIETY 를 가드 없이 회복)

높이 필터를 켜자 무브 풀이 얇아져 **VARIETY GATE 가 세 번 연달아 다른 축을
잡았다.** 상한을 완화하지 않고(교훈 199) **실내 저고도 무브 6종을 신설**해
해소했다.

| VARIETY 실패 | 신설 무브 |
|---|---|
| `only 13 distinct moves (target >= 15)` | `eave_drop` `shelf_descend` `ridge_skim` |
| `step_back used 19/76 (25%) > 16%` | `ease_back` `sill_retreat` |
| `A5-07 and A5-08 both use low_pan` | `table_glide` |

```python
# (r0, r1, z0, z1, arc_deg, lens_mm, hold_frac, ease)
"eave_drop":      (5.20, 1.90, 2.02, 1.12, 42.0, 30.0, 0.18, "smooth"),
"shelf_descend":  (3.30, 1.55, 1.98, 0.96, 38.0, 36.0, 0.20, "ease_out"),
"ridge_skim":     (2.90, 2.75, 1.72, 1.60, 92.0, 32.0, 0.16, "smooth"),
"ease_back":      (1.70, 3.05, 0.98, 1.34, 40.0, 34.0, 0.24, "ease_out"),
"sill_retreat":   (1.45, 2.60, 0.88, 1.16, 30.0, 40.0, 0.26, "ease_out"),
"table_glide":    (2.15, 2.05, 0.92, 0.90, 78.0, 38.0, 0.16, "linear"),
```

**결과: MOVES 22 → 28종, 실사용 18 distinct, VARIETY OK (max share 16%,
adjacent repeats 0), SPEED OK.**

### 검증 — 처방 전후

| 항목 | 처방 전 | 처방 후 |
|---|---|---|
| `J_A4-01` 무브 | `drone_drop` | **`desk_graze`** |
| `J_A4-01` 카메라 z | 4.76 → … | **1.66 → 1.56** |
| `J_A4-01` G6 frac | 0.175 (렌더 0.051) | **0.352 (451 px)** |
| 전체 카메라 z 범위 | 1.31 – **6.53 m** | 1.30 – **3.43 m** |
| 실사용 무브 | 16 | **18** |

### 다음 작업자에게 — 일반 원칙

**교훈 210 의 네 번째 재발이다.** 축의 계보를 다시 쓰면:
**크기(G6) → 화각(G8①) → 가림(G8②) → 높이(Z-FIT)**.

새 게이트를 세울 때마다 물어라 — **「이 게이트가 못 보는 축은 무엇인가」.**
그리고 **「어울리는가」(궁합)와 「안에 있는가」(기하)는 다른 질문**이다.
카탈로그의 값은 「그 무브의 정체성」이지만, 세트가 담을 수 없는 값이면
그 무브는 그 세트에서 쓸 수 없다.

**임계값은 발명하지 말고 실측 사이에서 골라라.** `Z_HEADROOM = 1.50` 은
`S4` 실측 `2.10 → 3.15` 에서 나왔다.

**다양성이 깨지면 상한을 풀지 말고 선택지를 늘려라.** 좁히기는 고치기가
아니다(교훈 199). 무브 6종 신설이 VARIETY 3연속 실패를 근본에서 해소했다.

---

## 교훈 212 — 마스크가 대상의 색을 전제하면 픽셀 직독도 거짓말한다

### 사건 — 교훈 211 처방이 통했는데 측정은 계속 실패했다

교훈 211 처방(Z-FIT 게이트) 후 `J_A4-01` 의 무브가 `drone_drop`(z 4.60) →
`desk_graze`(z 1.66→1.56) 로 바뀌고 G6 이 `0.051 → 0.352 (451 px)` 로 올랐다.
그런데 재렌더 후 픽셀 직독은 **여전히 `0.052 / 0.059 / 0.041` (FAIL)** 이었다.

세 번 연속 같은 컷이 실패했으므로 **프레임을 눈으로 봤다**(교훈 210 의 방법).
프레임에는 이력서 세 장이 **화면 폭의 4분의 1 이상**으로 크게, 선명하게 보였다.
**베이지 · 시안 · 핑크** 색이었다.

### 원인 — 측정 도구가 대상의 색을 전제하고 있었다

`chk.py` 의 마스크는 이랬다.

```python
mask = (lum >= 120) & (sat <= 40)      # 밝고 무채색인 것 = 종이
```

`sat <= 40` 은 **「종이는 무채색이다」** 라는 전제다. 그런데 `sets.py` 의
문서 색상은 두 계열로 나뉘어 있다.

| 상수 | 값 | 채도 |
|---|---|---|
| `DOC_N` | (0.560, 0.560, 0.545) | **무채색** — 문장 카드 |
| `DOC_W` | (0.780, 0.775, 0.760) | **무채색** — 일반 A4 |
| `DOC_A` | (0.760, 0.145, 0.520) | **마젠타** — 「역할」 문서 |
| `DOC_B` | (0.130, 0.640, 0.720) | **틸** — 「방식」 문서 |
| `DOC_C` | (0.820, 0.700, 0.190) | **앰버** — 「변화」 문서 |

6컷 중 5컷의 주연은 `DOC_N`/`DOC_W`(무채색)이라 마스크에 잡혔다.
**`J_A4-01` 만 주연이 `DOC_A/B/C`(채색)** 이어서 마스크에서 통째로 빠졌다.
즉 측정 도구는 **「그 컷의 주연만 못 보는 눈」** 이었다.

### 처방

```python
# 환경(ENV_*)은 전부 저채도이고 어둡다 -> 두 갈래로 나눠 잡는다.
mask = (sat >= 55) | ((lum >= 170) & (sat <= 40))
```

- `sat >= 55` : 채색 문서(`DOC_A/B/C`) — 환경에는 채색이 없으므로 안전하다
- `(lum >= 170) & (sat <= 40)` : 무채색 문서(`DOC_N/DOC_W`) — 밝기 하한을
  120 → 170 으로 올려 회색 벽(`ENV_WALL_HI` 0.215 ≈ 55/255)을 배제

### 검증 — 다른 5컷 값이 흔들리지 않아야 한다

처방이 옳다면 **원래 통과했던 컷들의 값은 거의 그대로**여야 한다.

| 컷 | 정정 전 best | 정정 후 best |
|---|---|---|
| J_A3-13 | 0.302 | 0.300 |
| J_A3-14 | 0.320 | 0.318 |
| J_A3-15 | 0.310 | 0.309 |
| J_A3-16 | 0.189 | 0.188 |
| J_A3-17 | 0.377 | 0.376 |
| **J_A4-01** | **0.059 FAIL** | **0.234 OK** |

5컷은 ±0.002 안에서 동일하고 A4-01 만 살아났다. **처방이 정확히 결함만
고쳤다는 증거다** — 임계값을 느슨하게 풀어 억지로 통과시킨 것이 아니다.

### 다음 작업자에게 — 일반 원칙

**측정 도구도 검증 대상이다.** 픽셀 직독은 우리 신뢰도 위계에서 「실제로
보이는가」의 정본이고, 게이트를 두 번 반증했다. 그러나 그 도구 자체가
**대상에 대한 전제**를 품고 있으면 그 전제가 깨지는 케이스에서 거짓말한다.

**같은 컷이 세 번 실패하면 도구를 의심하라.** 두 번까지는 설계 결함일 수
있지만, 처방이 게이트 수치를 크게 개선했는데도 측정이 그대로면
**측정과 게이트가 다른 것을 보고 있다**는 뜻이다.

**Read 툴로 프레임을 직접 보는 것이 최종 심판이다.** 이번 결함은 숫자로는
찾을 수 없었다 — 눈으로 보니 3초 안에 명백했다(색이 있었다).

**처방의 정당성은 「고쳐진 것」이 아니라 「흔들리지 않은 것」으로 증명한다.**
5컷이 ±0.002 안에 머물렀다는 사실이, 마스크 확장이 억지 통과가 아님을 보인다.

---

## 교훈 213 — ★게이트를 통과하는 앵커와 관객을 붙잡는 앵커는 다른 질문이다★

**출처**: [CEO-83] *"조건 카드 한 장 좋습니다. 다만 그 설정이 정말 관객들에게(유튜브
시청자들) 몰입과 호응을 줄 수 있는 수준과 기획인지는 검증하세요."*

### 무슨 일이 있었나

[CEO-82] 의 follow-the-object 지시를 받고, 대본 `screen_direction` 6행을 연달아 읽어
**「조건 카드 한 장」** 이 이미 서사를 관통하고 있음을 발견했다(교훈 200 준수).
그리고 그것을 앵커로 확정하려 했다.

**그런데 그 확정의 근거는 「대본에 있다」뿐이었다.** 게이트(G1~G8/Z-FIT)는
「대본대로 배치·촬영되었는가」만 재고, **「시청자가 그것을 붙잡을 수 있는가」는
어느 게이트도 묻지 않는다.**

### 검증 결과 (전량 `76_BENCHMARK_STUDY.md` §7)

| 축 | 계측 | 판정 |
|---|---|---|
| ① 배경/이웃 대비 | 이웃 백지와 **1.90:1**, 채도차 **0.00** | ❌ 구별 불가 |
| ② 변화량 | 바 두께 10.1 → 23.0 px (그러나 컷 이동과 혼재) | ◐ |
| ③ 첫 2초 점유 | 0.091 (하한 0.14 미달) · 주연의 **46%** | ❌ |
| ④ 커버 구간 | 대본상 4/6컷 = **62%** (2컷은 대본이 카드를 뗐다) | ❌ |
| ⑤ 대안 스윕 | 색 변경 2/6 · 크기 1.46배 3/6 | ❌ |

**육안(최종 심판)**: 첫 프레임에서 비교표·조건 카드·다른 종이가 **모두 같은 회색 판**
으로 겹쳐 보였다. 오히려 대본이 "떼어내라"고 한 **마젠타/앰버 스티커가 눈에 띄었다.**

### ★일반 원칙★

> **1. 대본 근거는 「필요조건」이고 관객 검증은 「별도의 충분조건」이다.**
>    앵커를 대본에서 찾은 것은 옳다(교훈 200). 그러나 거기서 멈추면 안 된다.
>
> **2. 관객 앵커는 5축으로 재라** — 게이트와 별개의 검증 스위트다:
>    | 축 | 계측 방법 | 하한(제안) |
>    |---|---|---|
>    | 배경/이웃 대비 | sRGB 상대휘도 + WCAG 대비비 | **>= 3.0:1** 또는 채도차 **>= 0.5** |
>    | 변화 가시성 | 상태 변화의 픽셀 차 | **>= 8 px** 이고 **같은 컷 안에서** 일어날 것 |
>    | 첫 2초 점유 | 첫 프레임 앵커/주연 화면폭 비 | **>= 1.0** (앵커가 최대여야) |
>    | 커버 구간 | 앵커가 존재하는 초 / 전체 초 | **>= 0.85** |
>    | 이동성 | 앵커가 공간을 이동하는가 | 정적이면 감점 |
>
> **3. 색(구별성)과 크기(게이트)는 독립 변수다.** 색만 바꿔도 게이트는 안 움직이고,
>    크기만 키워도 구별성은 안 생긴다. 둘 다 별도로 처방해야 한다.
>
> **4. 「대본이 앵커를 뗀 컷」에 앵커를 억지로 끼우지 말라.** 그건 교훈 200 위반이고
>    [CEO-74] *"하나의 장면을 다각도로 돌리는 느낌"* 을 강화한다.
>    앵커 커버가 부족하면 **앵커를 바꾸거나 컷 구성을 바꾸는 것**이 정답이다.
>
> **5. 벤치마크 앵커가 가진 성질을 체크리스트로 쓰라**:
>    발광(극단 대비) / 상태 변화 / **공간 이동** / 전 구간 존재 / 첫 2초 독점.
>    5개 중 몇 개를 갖는지 세라. 우리 회색 카드는 **0개**였다.

### G9 ANCHOR GATE 설계에 주는 함의

G9 를 「연속 컷의 주연 id 가 같은가」로만 만들면 **회색 카드도 통과한다.**
G9 는 위 5축 중 최소 **커버 구간**과 **첫 2초 점유**를 함께 물어야 한다.
게이트는 「경고」가 아니라 **「실패」**로 만든다(교훈 187).

---

## 교훈 214 — 관객 검증의 「처방」도 대본 제약을 통과해야 한다

**사건.** [CEO-83] 앵커 관객 검증(5축)에서 **축① 시각적 구별성**이 1.90:1 로 실패했다.
대안 스윕(`altsweep.py`)이 낸 **최고 대비**는 `CUE` 시안(0.24,0.88,0.96) = **10.7:1** 이었다.
그걸 채택하려던 직전에 대본을 다시 읽었다.

> A3-13 `screen_direction` — *"손이 과장된 컬러 스티커를 떼고 **중성** 카드만 남김."*

**시안 발광 카드는 대본이 방금 뗀 「과장된 컬러」 그 자체였다.** 채택하면 대본 위반이다.

**처방.** 무채색 안에서만 대비를 찾는 재스윕(`recolor.py`)을 돌려
**A2(0.94 밝은 중성) × N3(0.28 어두운 종이) = 8.11:1** 을 얻었다. 채도 0.01 = 무채색 준수.

> **★원칙★** 검증이 낸 「최적해」는 **아직 처방이 아니다.**
> 계측은 물리량만 본다. 처방을 코드에 넣기 전에 **대본을 다시 읽는 단계**를 반드시 끼워라.
> 순서: 계측 → **대본 재독** → 제약 통과분만 남기고 재스윕 → 처방.
> (교훈 200 「대본이 서사의 정본」의 검증 단계 버전)

---

## 교훈 215 — 게이트를 「실패」로 세우면 신설 즉시 새 결함을 잡는다

**사건 2건.**

| 게이트 | 신설 첫 실행 결과 |
|---|---|
| **G9 ANCHOR GATE** | `J_A3-17` 첫 프레임 앵커/주연 비 **0.90** (하한 1.00) — 처방 전이면 렌더로 나갔다 |
| **G10 ANCHOR RIVAL GATE** | `J_A3-14` / `J_A3-15` 의 `ttlbar` **2건** — 픽셀 직독이 못 본 컷에서 나왔다 |

두 경우 모두 **「경고(NOTE)」로 만들었다면 결함이 그대로 렌더에 반영됐다.**
교훈 187(게이트를 경고로 만들면 결함이 렌더된다)의 2·3번째 실증이다.

> **★원칙★** 새 게이트의 가치는 **「기존 상태를 통과시키는가」가 아니라 「새 결함을 잡는가」** 로 판정한다.
> 신설 게이트가 첫 실행에서 **0건**을 잡으면 기준이 느슨하다는 신호다.
> 반대로 **거짓 양성**을 내면 기준의 「차원」이 하나 빠졌다는 신호다 (→ 교훈 219).

---

## 교훈 216 — 대본에 있어도 「납품 구간 밖」이면 쓸 수 없다

**사건.** 축④(커버 구간)를 채우려고 앵커가 살아있는 컷을 찾았다.
`A5-01/02/04` 는 조건 카드가 **공간을 이동하는** 완벽한 근거였다(축⑤까지 동시 해결).
그런데 **시각(時刻)이 293~298초** 였다. 숏폼 C 의 사용 구간은 **206~225초** 다.

**⇒ 대본 정본(`SCRIPT_ACT3-8.csv`)을 통과했지만 기획 정본(`74_SHORTS_TRILOGY_SPEC.md`)에서 탈락했다.**

> **★원칙★** **대본 정본과 기획 정본은 별개의 게이트다.**
> 「대본에 있다」는 서사 근거이고, 「구간 안이다」는 **납품 근거**다. 둘 다 필요하다.
> 검증 도구가 대본만 읽으면 **쓸 수 없는 최적해**를 낸다.
> 부산물: A5 이동성 앵커는 **숏폼 D 후보**로 별도 기록한다 (버리지 않는다).

---

## 교훈 217 — ★알베도 대비는 렌더 대비가 아니다★

**사건.** `recolor.py` 로 앵커/이웃 알베도 대비를 **8.11:1** 로 설계했다.
그 값으로 `sets.py` 를 편집하고 v6 를 렌더했다. `script_gate` 는 G5/G6/G7/G9 전부 통과(G9 4/4).

렌더 프레임 12장을 픽셀 직독(`anchorpx.py`)하니:

```
frame              앵커 L   2등 L   고립비   판정
J_A3-13_p0          172.7  136.6   1.26   ★FAIL★
J_A3-13_p50         172.5  139.0   1.24   ★FAIL★
J_A3-13_p95         172.6  139.0   1.24   ★FAIL★
J_A3-17_p50         178.3  138.2   1.29   ★FAIL★
J_A3-17_p95         177.5  139.2   1.28   ★FAIL★
⇒ 설계 8.11:1  →  렌더 1.29   (5/12 프레임 실패, 하한 1.35)
```

**광원 세기·표면 법선·감마·톤매핑이 대비를 깎는다.** 알베도 비는 **상한**일 뿐이다.

> **★원칙★** **렌더 후 픽셀 직독은 선택이 아니라 필수 단계다.**
> 색 설계는 알베도로 하고, **판정은 렌더 픽셀로 한다.**
> 파이프라인: 설계(알베도) → 게이트(기하) → 렌더 → **픽셀 직독(휘도·고립비)** → 육안.
> (교훈 210 「'크다'는 '보인다'가 아니다」의 **색 버전**)
> 도구: `anchor_audit/anchorpx.py` — 마스크 `(sat<=45)&(lum>=95)`, 덩어리를 **평균 휘도로 재정렬**,
> `frac = maxside/W >= 0.14` **그리고** `iso = L1/L2 >= 1.35`.

---

## 교훈 218 — ★게이트가 「대상 하나」만 보면 화면은 여전히 산만하다★

**사건.** G5(방향) + G6(크기) + G9(앵커 자신)을 **전부 통과**한 v6 가 픽셀에서 5/12 실패했다.
원인은 **앵커가 아니라 같은 화면의 경쟁자**였다.

```
build_spec('S7') 휘도 정렬 (편집 전)
  L=0.555  note        최장변 0.270 m   loc (0.00,-0.40)   ← ★앵커(0.260m)보다 크다★
  L=0.277  post1/post2 최장변 0.296 m                       (의도된 어두운 이웃)
  ...
  앵커 card L=0.940     최장변 0.260 m   loc (-0.10,-0.30)  ← note 가 ★바로 옆★
```

**부수 발견.** `J_A3-13` 의 `gaze_xyz [0.0,-0.4,0.772]` = **`note` 위치 그 자체**.
**카메라가 앵커가 아니라 경쟁자를 보고 있었다.**

**추가 원인.** 1차 `sets.py` 편집에서 `DOC_N` 의 **「용도」**를 앵커 → 바 톤으로 바꿨지만
**그 상수를 쓰는 객체(S7 `note`, line 358)를 놓쳤다.**
⇒ **상수의 의미를 바꿀 때는 사용처를 전수 `grep` 하라.**

> **★원칙★** 앵커 게이트는 **앵커만 재면 부족하다. 경쟁자를 같이 재라.**
> ⇒ **G10 ANCHOR RIVAL GATE** 신설:
> 경쟁자 = 앵커가 아니면서 **(a) 휘도 >= 앵커의 55%** ★그리고★ **(b) 최장변 >= 앵커의 70%**.
> **한 축만 보면 거짓 양성이 쏟아진다** — (a)만이면 작은 스티커(0.04m)까지, (b)만이면
> 의도된 어두운 이웃(L0.277 = 앵커의 30%)까지 걸린다.

---

## 교훈 219 — ★거짓 양성은 「기준의 차원이 하나 빠졌다」는 신호다★

**사건.** G10 신설 첫 실행이 `J_A3-14` / `J_A3-15` 의 `ttlbar` 를 경쟁자로 잡았다.
기준을 느슨하게 하려는 유혹이 왔다(휘도 문턱 0.55 → 0.60). 그 전에 좌표를 실측했다.

```
A3-14 card   loc (0.10, 0.020) half (0.130, 0.090)  =>  x -0.030..0.230 / y -0.070..0.110
A3-14 ttlbar loc (0.10, 0.052) half (0.093, 0.015)  =>  x  0.007..0.193 / y  0.037..0.067
⇒ ttlbar 는 card 의 발자국 안에 ★완전히★ 들어 있다
```

**`ttlbar` 는 「앵커 옆의 경쟁자」가 아니라 「앵커 위의 표식」이었다.**
대본이 그것을 요구한다 — A3-14 *"조건 카드 **위** 굵은 제목 바 1개"*.
축②(변화 가시성)도 바가 카드 위에서 **보여야** 한다고 요구한다.

**두 처방을 비교했다.**

| 안 | 내용 | 판정 |
|---|---|---|
| (A) | G10 에 **공동 위치 면제** — 앵커 AABB 안에 완전히 들어가는 props 는 경쟁자가 아니다 | **★채택★** |
| (B) | 바 휘도를 낮춘다 | ❌ 축② 를 스스로 약화시킨다 |

**면제는 「중심 포함」이 아니라 「AABB 완전 포함」으로 구현했다.** 중심만 보면
절반이 삐져나온 큰 판도 면제된다 — 교훈 211 「어울리는가 ≠ 안에 있는가」의 반복을 막는다.

> **★원칙★** 거짓 양성이 나오면 **문턱을 낮추지 말고 차원을 추가하라.**
> 문턱 완화는 **진짜 결함까지 통과시킨다.** 차원 추가는 **거짓 양성만 제거한다.**
> G10 의 최종 정의: **밝다(휘도) × 크다(크기) × 떨어져 있다(위치)** — 3차원.
> 게이트가 다른 게이트나 대본 요구와 충돌하면, **충돌하는 쪽이 아니라 게이트의 정의가 틀렸다.**

---

### 교훈 220 ★자기 부과 게이트는 납품을 막을 권한이 없다★ (CEO-85)

**사건**: v7 렌더 후 `anchorpx.py` 가 12프레임 중 5건에서 고립비 미달(1.18~1.29 < 1.35)을
냈다. 나는 원인을 규명하고 — 2등 덩어리는 이웃 종이가 아니라 `ENV_WALL_HI=0.215` 의
벽면이었다 — **「세트 조명 처방 → 재렌더」를 다음 세션 계획으로 보고**했다.

대표님은 그것을 **「낭비 수준」** 이라고 규정하셨다. 이유는 명확하다:

1. `ISOLATION_MIN = 1.35` 는 **내가 `anchorpx.py` 에 직접 써넣은 값**이다.
   대표님 지시에도, 대본 CSV 에도, 기획 정본에도 없다.
2. 대표님은 **동일 수준의 영상을 이미 승인**하셨다 — [CEO-82] *"영상 자체는 군더더기가 없어요"*
3. 필수 게이트(G1~G10 · FLOW)는 **전부 통과**한 상태였다.
4. 미달을 고치려면 벽 톤을 바꿔 **전 세트를 재렌더**해야 한다.

⇒ 즉 나는 **대표님이 이미 통과시킨 산출물을 내 임계값으로 붙잡아두고 있었다.**

**일반 규칙 — 게이트의 3분류**

| 종류 | 출처 | 미달 시 |
|---|---|---|
| **합의 게이트** (G1~G3, 사용 구간) | 대본 · 기획 · 대표님 지시 | **납품 중단** — 합의 위반이다 |
| **관객 하한 게이트** (G5~G10, FLOW, CLIP) | 벤치마크 실측 · 승인본 픽셀 | **납품 중단** — 관객이 못 본다 |
| **자기 부과 개선 목표** (`ISOLATION_MIN` 등) | 내가 정한 값 | **납품하고 다음 배치 이월** |

**판별 질문 3개**
1. 이 임계값을 **대표님이 제시했거나 대본/기획이 요구**하는가? → 아니면 자기 부과다
2. 이 수준으로 **대표님이 이미 승인**한 산출물이 있는가? → 있으면 납품이 우선이다
3. 미달을 고치려면 **렌더를 다시 돌려야** 하는가? → 그렇다면 다음 배치로 이월한다

**교훈 131 과의 관계**: 교훈 131 은 *"대표님이 승인한 것을 내 코드가 반려하면 틀린 것은
내 코드다"* 였다. 교훈 220 은 그것의 **작업 흐름 판**이다 — 131 은 「코드를 고쳐라」였고,
220 은 **「그 전에 먼저 납품하라」** 다. 코드 수정은 납품 뒤에 온다.

**부가 규칙 — 렌더 재시도 1회 제한**
렌더 픽셀 직독([6]) 결과로 렌더([5])를 다시 돌리는 것은 **1회까지만** 허용한다.
2회 이상 돌아야 한다면 그것은 렌더 문제가 아니라 **설계([3]) 또는 게이트 정의([4]) 문제**다.
같은 단계를 반복하는 것이 아니라 **상류로 올라가라.**

**전례**: 이 판단으로 숏폼 C 를 납품했다 (1080x1920 · 16.00s · 나레이션 포함).
`ENV_WALL_HI` 하향은 **다음 신규 렌더 배치에서 함께** 반영한다. 단독 재렌더는 하지 않는다.
상세 절차: `77_PRODUCTION_SOP.md` §6

---

### 교훈 221 ★컷을 건너뛰면 오디오도 잘라 붙여야 한다★

숏폼은 대본의 연속 구간을 그대로 쓰지 않는다. 앵커가 없는 컷은 제외되기 때문이다
(숏폼 C 는 A3-16 / A4-01 제외 — `replan.py` P4a).

**함정**: 대본 구간 전체(202.50~225.44 = 22.94s)를 통째로 잘라 16.00s 영상에 붙이면
**A3-16 의 6.96초만큼 나레이션이 화면과 어긋난다.** 화면은 A3-17 인데 소리는 A3-16 을
읽고 있는 상태가 되고, 관객은 그 즉시 이탈한다.

**처방**: 컷 경계에서 오디오를 잘라 이어붙인다. 그리고 세그먼트 길이는
**대본 `dur` 이 아니라 렌더된 영상 프레임 수 ÷ FPS** 로 계산한다.
```
A3-13+14+15  t0=202.50  101+55+99 = 255f / 24 = 10.62500s
A3-17        t0=220.06        129f      / 24 =  5.37500s
                                     합계 = 16.00000s  == 영상 16.00000s
```
대본 `dur` 합(4.2+2.28+4.12+5.38 = 15.98s)을 쓰면 **0.02s 가 남는다.** 짧은 클립에서는
무해해 보이지만, 롱폼에서 컷마다 누적되면 립싱크가 눈에 보이게 밀린다 (교훈 178 의 재발).

**검증**: 결합 후 `ffprobe` 로 오디오 총 길이가 영상 길이와 **소수점까지 일치**함을 확인한다.
`-shortest` 는 안전망이지 정렬 도구가 아니다.

---

## 교훈 222 — 디스크의 mp4 가 대본과 같은 세대인지 **프레임 수로** 확인하라

**출처**: [CEO-85] 롱폼 조립 · `longcut.py film` 이 `FILM FAILED  concat produced 9330 f, jobs declare 8399`

### 무슨 일이 있었나

롱폼 76잡 전량이 `_batch/` 에 존재했다. `longcut.py map` 은 **MAP OK** 를 냈다.
그런데 `film` 이 실제로 붙여보니 **9330 프레임** 이었다. 대본은 **8399 프레임** 을 선언한다.
**+931 프레임 = 38.8초** 가 어디선가 늘어난 것이다.

조각별로 `ffprobe -count_frames` 를 돌려 대조하니 **11잡이 불일치**했다.

```
J_A3-02  want   92  got  205   08-22
J_A3-04  want  169  got  230   08-22
J_A3-11  want  129  got  169   08-22
J_A4-02  want   78  got  149   08-22
J_A4-12  want   94  got  156   08-23
J_A5-03  want  157  got  276   08-22
J_A5-13  want   73  got  134   08-23
J_A6-02  want  191  got  460   08-23   ← 2.4배
J_A6-07  want  116  got  187   08-23
J_A7-01  want   90  got   99   08-23
J_A7-11  want  115  got  170   08-23
                합 1304 → 2235  (+931 f)
```

11개 전부 **08-22~23 파일** 이다. `sets.py`/`scenemap.py`/`scenejobs.py` 는 **08-28** 에 수정되었다.
그 사이 `scenejobs.py` 를 재생성해 `frames` 값이 바뀌었는데, `previz_batch.py` 의
**SKIP 로직(기존 mp4 가 있으면 렌더를 건너뜀 · line 913-914)** 이 재렌더를 막았다.

### 왜 MAP 은 통과했는가

`cmd_map()` 은 **파일의 존재 여부** 만 봤다. 존재하면 OK 였다.
**존재는 최신을 뜻하지 않는다.** 파일이 있어도 그것이 **다른 대본으로 렌더된 것** 일 수 있다.

### 규칙

1. **MAP 게이트는 존재가 아니라 「프레임 수 일치」를 검사한다.**
   `nframes(path) == job["frames"]` 가 아니면 **그 조각은 없는 것으로 취급하고 재렌더한다.**
2. **설계 파일(`sets.py` `scenemap.py` `scenejobs.py`)을 수정하면, 그 파일에 의존하는
   산출물 전량의 프레임 수를 대조한다.** mtime 비교로 대신하지 말라 — mtime 은
   내용이 실제로 바뀌었는지 말해주지 않고, 프레임 수는 말해준다.
3. **SKIP 로직이 있는 배치 러너는 「무엇을 건너뛰었는지」를 반드시 로그로 남겨야 한다.**
   조용히 건너뛰면 이런 결함이 조립 단계까지 살아남는다.
4. **이 재렌더는 「반복 루프」가 아니다.** 11잡은 현세대 대본으로 **한 번도 렌더된 적이 없다.**
   교훈 220 의 판별 질문 ①(대본이 요구하는가?)에 **예** 이므로 **합의 게이트** 이고,
   납품을 중단할 정당한 사유다. 자기 부과 게이트(ISOLATION_MIN 등)와 혼동하지 말라.

### 교훈 220 과의 관계

- 220: **자기 부과** 게이트는 납품을 막을 권한이 **없다** → 이월하라
- 222: **대본이 정한** 프레임 수 불일치는 납품을 막을 **정당한** 사유다 → 고쳐라

**구분 기준은 「출처」다. 내가 정한 값인가, 대본이 정한 값인가.**

---

## 교훈 223 — 프레임 수 검사를 통과해도 「색 세대」는 남는다

### 무슨 일이 있었나

교훈 222 로 `cmd_map()` 을 프레임 수 검사로 강화하고, 11잡을 재렌더해
**MAP OK 76/76 · FILM OK body 8399 f = 대본 선언 8399 f 정확 일치** 를 받았다.
게이트가 전부 초록이었다. 그런데 **육안으로 프레임을 뽑아 보니** 이런 것이 나왔다.

| 프레임 | 본 것 |
|---|---|
| f2400 | 고채도 **마젠타 / 시안 / 옐로우** 카드 — 프로토타입 색 |
| f8100 | 같은 고채도 3색 |
| f4800 | v7 뮤트 그레이 (정상) |
| f6600 | v7 뮤트 그레이 (정상) |

**한 영상 안에서 색 문법이 두 세대로 갈렸다.**

### 왜 게이트가 못 잡았나

`sets.py` 의 색 상수(`DOC_N` / `DOC_W` / `DOC_ANCHOR_C` / `ENV_*`)를 바꾸면
**렌더 결과의 픽셀 값은 바뀌지만 프레임 수는 1프레임도 바뀌지 않는다.**

```
scenejobs.py 의 frames 변경  →  프레임 수 변화  →  ★교훈 222 게이트가 잡는다★
sets.py 의 색 상수 변경      →  프레임 수 불변  →  ★어떤 게이트도 잡지 못했다★
```

교훈 222 는 「존재 ≠ 최신」을 고쳤다. 그러나 그것이 고친 것은 **길이 차원**뿐이다.
**색은 다른 차원이고, 다른 계측이 필요했다.**

### 처방 — 채도 히스토그램으로 세대를 가른다

색 세대는 「무엇을 재면 되는가」가 명확하다. v7 문법은 뮤트 그레이라
**채도(chroma = max(R,G,B) - min(R,G,B))가 낮다.** 구세대 프로토타입은 고채도다.

```python
a   = np.asarray(Image.open(mid_frame).convert("RGB")).astype(np.float32)
sat = a.max(2) - a.min(2)
frac_high = (sat > 60).mean()        # 고채도 픽셀 비율
OLD_GEN if frac_high > 0.03          # 3% 넘으면 구세대
```

각 잡의 **중간 프레임 1장**만 뽑아 재면 된다. 76잡 전량 ≈ 100초. **렌더 0초. 유료 0건.**

### 실측 결과

```
OLD-GEN COUNT ★42 / 76★   frames 5319 / 8399 = ★63.3%★
NEW-GEN             34      frames 3080
```

### ★결정적 산술 — 여기서 「낭비」가 갈린다★

색 세대 42잡을 고치려면 5319 f 재렌더가 필요하다. 그런데 우리에게는
**이미 다른 이유로 전량 재렌더가 예정되어 있다** (G11 컷 분할 + `ENV_WALL_HI` 하향).

| 경로 | 렌더 시간 |
|---|---|
| A) 지금 42잡만 고치고 → 나중에 분할+벽면 전량 | 1.8 h + 2.8 h = **4.6 h** |
| **B) 지금 전량 1회 (색 + 분할 + 벽면 동시)** | **★2.8 h★** |

**★A 를 고르는 것이 바로 [CEO-85] 의 「낭비 수준」이다.★**
같은 프레임을 두 번 렌더하는 데 1.8시간을 더 쓴다.

### 규칙

1. **육안 확인은 게이트가 전부 초록일 때도 반드시 한다.**
   게이트는 자기가 계측하는 차원만 본다. 계측하지 않는 차원은 통과시킨다.
2. **설계 파일을 수정하면 「그 수정이 어떤 차원에 나타나는가」를 먼저 물어라.**
   `frames` → 길이 / 색 상수 → 픽셀 / 카메라 → 구도. 차원마다 게이트가 따로 필요하다.
3. **결함을 발견하면 즉시 고치기 전에 「이미 예정된 렌더가 있는가」를 확인하라.**
   있으면 합쳐라. 없으면 지금 고쳐라. (교훈 220 의 「이월」 판단과 같은 계산이다)
4. **채도 감사는 상시 도구다** (`/tmp/genaudit.py` → `r3d/genaudit.py` 로 승격).

### 교훈 222 와의 관계

| | 교훈 222 | 교훈 223 |
|---|---|---|
| 차원 | **길이** (프레임 수) | **색** (채도) |
| 검출 | 게이트 (`cmd_map`) | **★육안 → 그 뒤에 게이트★** |
| 원인 파일 | `scenejobs.py` | `sets.py` |
| 처방 | 즉시 재렌더 (11잡) | **★예정된 전량 배치에 합침★** |

**★교훈 222 는 게이트가 육안보다 먼저 잡은 사례다. 교훈 223 은 육안이 게이트보다
먼저 잡은 사례다. 그래서 신뢰도 위계에서 「CEO 의 눈·귀」와 「Read 툴로 프레임
눈으로 보기」가 게이트보다 위에 있다.★**

---

## 교훈 224 — 자막은 우리가 조판해야 한다 (libass 에 맡기면 안 된다)

### 무슨 일이 있었나

롱폼 f300 육안 확인에서 이것이 나왔다.

```
사람들은 흔히 무슨
일을 할지만 생각하지만,
같은 일도 어떤 방식으로
하느냐에 따라 경         ← ★어절 중간에서 끊겼다★
험이 크게 달라집니다.
```

**[CEO-49] 「어절 단위 자막」 직접 위반.** 5줄이라 승인 기준 std3 (3줄 상한)도 초과했다.

### 원인 — 두 겹이었다

**1차 진단 (폭)**: CJK 는 유니코드 line-break 규칙상 **모든 글자 사이가 break
기회**다. 그래서 libass 는 폭이 차면 「경|험」처럼 어절 중간에서 잘라도 규칙 위반이
아니다. 영어라면 공백에서만 끊기므로 이 문제가 없다.

⇒ 처방: SRT 를 태우기 **전에** 우리가 어절 단위로 미리 나눈다 (`wrap_words()`).

**★그런데 폭을 맞췄는데도 5줄이 그대로 나왔다.★**

**2차 진단 (재배분)**: ASS 기본 `WrapStyle=0` 은 **「줄 길이를 고르게 맞추는」
모드**다. 우리가 넣은 `\n` 을 **무시하고 제 마음대로 다시 나눈다.**
우리가 3줄로 조판해도 libass 가 5줄로 재배분했다.

⇒ 처방: `WrapStyle=2` (재배분 금지, 우리 `\n` 만 존중).

### ★교정 — 캘리브레이션도 틀렸다★

1차에서 `FontSize=26` 을 PIL 55 pt 로 등가 계산했다. 검증용 합성 프레임으로
측정한 값이었다. **실제 롱폼 렌더에서 재보니 56 pt 였다** (1.4% 오차).

```
렌더 실측 (f300, 1280x720):  줄 높이 56 px,  12자 줄 = 572 px
  PIL 56 pt 와 오차 1.8 px 로 일치
안전 폭 = 1280 - MarginL 70 - MarginR 70 = 1140,  여유 4% -> ★1094★
```

**★합성 프레임 캘리브레이션은 실제 렌더 캘리브레이션이 아니다.★**
(교훈 217 「알베도 대비는 렌더 대비가 아니다」와 같은 형태의 함정)

### 처방 (렌더 0초 · 유료 0건)

```python
_MEASURE_PT = 56          # ★렌더 실측 등가★
_SAFE_PX    = 1094        # 1140 * 0.96
_MAX_LINES  = 3           # 승인 기준 std3 상한

def wrap_words(body):
    """★어절(공백) 경계에서만★ 줄을 나눈다. 글자 중간은 절대 끊지 않는다."""
    words = " ".join(body.split()).split(" ")      # 원본 줄바꿈은 신뢰하지 않는다
    lines, cur = [], ""
    for w in words:
        cand = w if not cur else cur + " " + w
        if cur and _measure(cand) > _SAFE_PX:
            lines.append(cur); cur = w
        else:
            cur = cand
    ...
```

원본 SRT 의 줄바꿈은 **500 s 마스터용 조판**이라 신뢰하지 않는다. 한 큐를 한 줄로
되돌린 뒤 우리 폭 기준으로 다시 나눈다. 줄 수 상한을 넘으면 **남은 어절을 마지막
줄에 붙인다 — 버리지 않는다.** 대본이 정본이기 때문이다 (교훈 200).

그리고 **SUB GATE** 를 세웠다. 교훈 222 와 같은 형태다 — 「존재」가 아니라 「값」을 본다.

```
SUB GATE  최장 줄 1094 px / 안전 1094 px   줄 수 상한 3
```

### 검증

| 단계 | 결과 |
|---|---|
| 1차 (`wrap_words` 만) | 어절 끊김 해소 ✅ / **줄 수 5줄 ❌** (libass 재배분) |
| **2차 (`+ WrapStyle=2` + 56 pt)** | **3줄 · 최장 1087 px · 어절 경계 유지 ✅** |

### 규칙

1. **자막은 우리가 조판한다. 렌더러의 자동 줄바꿈에 맡기지 않는다.**
   한국어에서 자동 줄바꿈은 어절을 존중하지 않는다.
2. **`WrapStyle=2` 를 항상 지정한다.** 지정하지 않으면 우리 조판이 무시된다.
3. **캘리브레이션은 「실제 납품 렌더」에서 뽑은 프레임으로 한다.**
   합성 테스트 프레임의 수치는 근사값일 뿐이다.
4. **태우기 전에 폭과 줄 수를 재라 (SUB GATE).** 태운 뒤에는 재인코딩 비용이 든다.
5. **어절 상한 초과분은 버리지 말고 마지막 줄에 흡수한다.** 대본이 정본이다.

---

## 교훈 225 — 「분할」은 「직선 보간」이 아니다: 카메라는 극좌표 원호다

### 무슨 일이 있었나

G11 리듬 게이트가 「44컷이 상한 4.0초를 넘는다」는 목록을 냈다. 목록은 있으나 그
목록을 실제 분할로 바꾸는 도구가 없었다. 그래서 `cutsplit.py` 를 신설했다. 긴 컷을
「같은 궤적의 연속 구간」으로 쪼개는 도구다. **총 프레임을 바꾸지 않으므로 렌더
시간이 0초도 늘지 않는다.** 품질 개선과 비용 절감을 동시에 달성하는 유일한 항목이다.

1차 구현에서 카메라 경로를 이렇게 나눴다.

```python
p["cam_start_xyz"] = _lerp(j["cam_start_xyz"], j["cam_end_xyz"], t0)   # ★틀렸다★
p["cam_end_xyz"]   = _lerp(j["cam_start_xyz"], j["cam_end_xyz"], t1)
```

즉 시작점과 끝점을 **직교좌표에서 선형 보간**했다. 직관적으로는 맞아 보인다.
그런데 신설한 SPLIT GATE 가 **즉시 87건**을 반려했다.

```
FAIL  ARC J_A3-04_s1  -62.27 vs 85.50      ← ★부호까지 반대★
FAIL  ARC J_A3-04_s2  -107.73 vs 84.50
FAIL  ARC J_A3-06_s1  -41.18 vs 60.00
...
SPLIT FAILED  87 건
```

### 원인 — 렌더러는 카메라를 「원호」로 돌린다

`previz_batch.py` line 645-658 을 열어 확인했다.

```python
a, b   = job["cam_start_xyz"], job["cam_end_xyz"]
ra = math.hypot(a[0], a[1]); rb = math.hypot(b[0], b[1])   # 반경
a0 = math.atan2(a[0], -a[1])                                # 시작 각도
a1 = math.atan2(b[0], -b[1])                                # 끝 각도
arc = a1 - a0
while arc >  math.pi: arc -= 2*math.pi     # ★최단 signed 경로★
while arc < -math.pi: arc += 2*math.pi

for f in range(1, F + 1):
    ang = a0 + arc * e         # ★각도를 보간한다★
    r   = ra + (rb - ra) * e   # ★반경을 보간한다★
    h   = a[2] + (b[2] - a[2]) * e
    cam.location = (r*math.sin(ang), -r*math.cos(ang), h)
```

**카메라는 책상을 중심으로 도는 극좌표 원호다.** J_A3-04 는 170도를 도는 컷이다.
원호의 중간점을 직선으로 이으면 **원 안쪽을 관통한다.** 반경이 줄어들고, 각도가
틀리고, 최단 signed 경로가 뒤집혀 **부호까지 반대**가 된다. 게이트가 잡은 -62.27 은
「원 안쪽을 지나는 현(chord)」의 각도였다.

### 그리고 `e` 는 `u` 가 아니다 — hold + ease 가 끼어 있다

같은 파일에서 진행률 계산을 확인했다.

```python
hold = job["hold_frac"]                                    # line 481
u = (f - 1) / max(1, F - 1)                                # 선형 프레임 위치
e = ease(max(0.0, (u - hold) / max(1e-6, 1.0 - hold)))     # ★실제 진행률★
```

- `hold_frac` 은 0.14~0.3333 이다. 컷의 **앞 14~34% 구간에서 카메라가 정지**해 있다.
- 그 뒤 ease 4종(`linear` 22 / `smooth` 18 / `ease_out` 34 / `ease_in_out` 2)으로
  가·감속한다.

따라서 「프레임 위치의 절반」은 「궤적의 절반」이 아니다. `hold=0.3`, `ease=smooth`
인 컷에서 프레임 50% 지점의 실제 진행률은 0.5 가 아니라 `smooth((0.5-0.3)/0.7)`
= `smooth(0.2857)` ≈ **0.113** 이다. 즉 궤적의 11% 지점이다.

### 처방 — 수식을 복제하지 않고 「참조」한다 (교훈 176)

여기서 흔한 실수는 렌더러의 수식을 분할기에 **베껴 적는** 것이다. 그러면 렌더러의
ease 곡선을 나중에 고칠 때 분할기가 조용히 틀려진다. 그래서 **소스에서 읽는다.**

```python
def _load_ease_fn():
    src = io.open(os.path.join(HERE, "previz_batch.py"), encoding="utf-8").read()
    i = src.index("EASE_FN = {")
    j = src.index("}", i) + 1
    ns = {"math": math}
    exec(src[i:j], ns)
    fn = ns["EASE_FN"]
    if set(fn) < {"linear", "smooth", "ease_out", "ease_in_out"}:
        raise SystemExit("EASE_FN 을 읽지 못했다 — previz_batch.py 포맷 확인")
    return fn

EASE_FN = _load_ease_fn()

def _e_of_u(u, hold, ease_name):
    """previz_batch.py line 651-653 과 ★동일한★ 진행률."""
    return EASE_FN[ease_name](max(0.0, (u - hold) / max(1e-6, 1.0 - hold)))

def _cam_at(j, e):
    """진행률 e 에서의 카메라 위치 — ★극좌표★"""
    a, b = j["cam_start_xyz"], j["cam_end_xyz"]
    ra = math.hypot(a[0], a[1]); rb = math.hypot(b[0], b[1])
    a0 = math.atan2(a[0], -a[1]); a1 = math.atan2(b[0], -b[1])
    arc = a1 - a0
    while arc >  math.pi: arc -= 2 * math.pi
    while arc < -math.pi: arc += 2 * math.pi
    ang = a0 + arc * e
    r = ra + (rb - ra) * e
    h = a[2] + (b[2] - a[2]) * e
    return [round(r*math.sin(ang), 4), round(-r*math.cos(ang), 4), round(h, 4)]
```

**★파일에서 직접 읽으므로 렌더러가 곡선을 바꾸면 분할기도 자동으로 따라간다.
값을 베껴 적지 않는다.★**

시선점(`tgt_*_xyz`)은 렌더러가 실제로 선형 보간하므로(line 658) 분할기도 선형으로
나눈다. **같은 파일의 두 값이라도 보간 방식이 다르면 다르게 처리해야 한다.**

### 조각별 hold / ease 처리

```python
p["hold_frac"]  = hold if i == 0 else 0.0     # ★조각2+ 는 정지 없음★
p["hold_frames"]= int(round(sz * p["hold_frac"]))
p["ease"]       = ease_name if i == 0 else "linear"
p["arc_deg"]    = round(abs(_arc_of(p)), 4)   # ★실제 각도 몫★
```

- 원 컷의 hold 는 **맨 앞 조각에만** 있었다. 조각마다 hold 를 주면 원 컷에 없던
  정지를 3번 만든다 — [CEO-51] 「컷 안에서 움직임 · 정지 없음」 위반이다.
- 조각 2+ 의 ease 는 `linear` 다. 조각마다 ease 를 주면 원 컷에 없던 **가·감속
  펄스**가 생긴다. 원 컷의 ease 곡선은 이미 조각 경계 좌표에 반영되어 있다.
- `arc_deg` 는 원 컷 값을 나누는 게 아니라 **조각의 실제 각도를 다시 계산**한다.
  hold + ease 때문에 조각별 각도 몫이 균등하지 않다.

### 규칙

1. **설계 파일의 값을 나눌 때는 「그 값이 어떤 좌표계에서 보간되는가」를 먼저 읽는다.**
   직교인지 극좌표인지, 선형인지 ease 인지.
2. **수식·상수는 복제하지 말고 소스에서 참조한다** (교훈 176). `exec` 로 블록만
   읽으면 렌더러 변경이 자동 전파된다.
3. **같은 파일의 두 값이라도 보간 방식이 다를 수 있다.** 카메라는 극좌표, 시선점은
   선형이었다.
4. **게이트를 「실패」로 세우면 신설 즉시 결함을 잡는다.** 이번엔 잡힌 것이
   렌더 결과가 아니라 **내 코드의 설계 오류**였다.

### 결과

```
★SPLIT GATE OK  프레임 보존 · sid 중복 0 · 숏폼C 보존 · ARC 일치★
76컷 -> 123컷 · 8399 f 보존 · med 4.29 s -> ★2.62 s★ · ★렌더 시간 0초 증가★
```

---

## 교훈 226 — 게이트 임계는 「물리량」이어야 한다

### 무슨 일이 있었나

분할 조각의 경계가 벌어지지 않았는지 확인하는 SEAM 게이트를 넣었다. 임계는
「직관적으로 작아 보이는 값」으로 정했다.

```python
if math.dist(a["cam_end_xyz"], b["cam_start_xyz"]) > 0.03:    # ★틀렸다★
    fails.append("SEAM ...")
```

**30건이 FAIL 났다.**

```
FAIL  SEAM J_A3-04_s1 -> J_A3-04_s2  틈 0.117 m
FAIL  SEAM J_A3-16_s1 -> J_A3-16_s2  틈 0.142 m
FAIL  SEAM J_A5-08_s1 -> J_A5-08_s2  틈 0.098 m
...
```

### 원인 — 그건 결함이 아니라 내가 물리량을 틀린 것이었다

조각 경계는 「같은 좌표」가 아니다. 원 컷에서 **인접한 두 프레임**이다.

```
조각1 마지막 프레임 = 원 컷의 프레임 k
조각2 첫 프레임     = 원 컷의 프레임 k+1
```

따라서 「같은 위치」가 아니라 **「1프레임 이동량 이내」**여야 맞다. J_A3-04 는
169프레임 동안 170도를 도는 컷이다. 프레임당 1도 이상 돌고, hold 0.24 + smooth
ease 때문에 중간 구간은 그 **2.5배**까지 빨라진다. 즉 **프레임당 0.09 m 가 정상**이다.

0.03 m 라는 값은 어디서 왔나? **아무 데서도 오지 않았다.** 내가 「작아 보이니까」
정한 값이다. 물리량이 아니라 느낌이었다.

### 처방 — 원 컷의 실측 프레임간 최대 이동량을 임계로

```python
src = [x for x in jobs if x["job_id"] == so][0]
F = src["frames"]; hold = src["hold_frac"]; en = src.get("ease") or "smooth"
step, prev = 0.0, None
for f in range(1, F + 1):
    u = (f - 1) / float(max(1, F - 1))
    cur = _cam_at(src, _e_of_u(u, hold, en))     # ★렌더러와 동일한 궤적★
    if prev is not None:
        step = max(step, math.dist(prev, cur))
    prev = cur
lim = max(0.005, step * 1.25)                    # ★실측 물리량 x 여유 25%★

gap = math.dist(a["cam_end_xyz"], b["cam_start_xyz"])
if gap > lim:
    fails.append("SEAM %s -> %s  틈 %.3f m > 1프레임 이동량 %.3f m"
                 % (a["job_id"], b["job_id"], gap, lim))
```

임계가 **컷마다 다르다.** 느리게 도는 컷은 엄격해지고, 빠르게 도는 컷은 느슨해진다.
그게 물리적으로 맞다.

### 교훈 220 과의 차이 — 방향이 다르다

교훈 220 은 「내가 정한 임계에 걸린 것은 납품을 막을 권한이 없다」였다. 그때의
처방은 **판정을 이월**하는 것이었다.

이번은 다르다. **임계 자체가 물리적으로 틀렸다.** 그러니 값을 「늘리는」 게 아니라
**「맞는 물리량으로 바꾼다」.** 늘렸다면 J_A3-04 는 통과하지만 진짜 끊어진 컷도
같이 통과한다. 물리량으로 바꾸면 둘이 구분된다.

### 규칙

1. **게이트 임계에 상수를 적을 때 「이 숫자의 출처가 무엇인가」를 답할 수 있어야 한다.**
   답이 「작아 보여서」면 그 게이트는 아직 완성되지 않았다.
2. **가능하면 임계를 「데이터에서 계산」한다.** 고정값은 컷마다 다른 물리 조건을
   무시해 거짓 양성을 만든다.
3. **거짓 양성이 나오면 문턱을 낮추기 전에 「내 물리 모델이 맞는가」를 먼저 묻는다**
   (교훈 219 의 변형).
4. **여유 계수는 명시한다.** `step * 1.25` 의 1.25 는 반올림(소수 4자리)과 부동소수
   오차를 흡수하는 값이며, 진짜 끊김(수십 배 차이)은 여전히 잡힌다.

---

## 교훈 227 — 정본 파일에 쓰는 함수는 「읽기 → 검증 → 쓰기」 순서를 지킨다

### 무슨 일이 있었나 — `scenejobs.json` 이 19 바이트로 파괴되었다

분할 적용을 되돌리려고 `revert` 를 실행했다.

```
$ python3 -u cutsplit.py revert
    n = len(d["jobs"] if isinstance(d, dict) else d)
TypeError: object of type 'float' has no len()
```

확인해 보니 정본과 백업이 **둘 다** 파괴되어 있었다.

```
scenejobs.json           float  jobs= float  0.01130884609498245     19 바이트
scenejobs.presplit.json  float  jobs= float  0.01130884609498245     19 바이트
```

8399 프레임 76컷의 설계 정본이 **부동소수 하나**로 덮여 있었다.

### 원인 — 변수 섀도잉

처음엔 파일 flush 문제로 의심했다. 아니었다. `cmd_plan()` 안에서 이름이 겹쳤다.

```python
def cmd_plan(apply_=False):
    d = json.load(open(JOBS))              # line 294 — ★정본 데이터★
    jobs = d["jobs"] if isinstance(d, dict) else d
    ...
    # SEAM 게이트 안 (line ~410)
    d = math.dist(a["cam_end_xyz"], b["cam_start_xyz"])   # ★d 를 덮어썼다★
    ...
    if apply_:
        json.dump(d, open(BAK, "w"), ...)   # ★float 을 백업에 썼다★
```

SEAM 게이트에서 거리 변수를 `d` 로 쓴 순간, 위에서 읽은 정본 데이터가 사라졌다.
그리고 백업 쓰기가 그 float 을 파일에 기록했다. `revert` 는 그 손상된 백업을
**검증 없이 먼저 정본에 쓰고** 나서 다음 줄에서 예외를 냈다.

**즉 두 가지 실수가 겹쳤다.**
1. 짧은 변수명(`d`)을 넓은 스코프와 좁은 루프에서 둘 다 썼다.
2. 파일을 **검증 전에** 썼다. 예외가 나도 파일은 이미 망가져 있었다.

### 복구 — 결정적 재생성 경로가 있어서 살았다

```
$ cd /home/user/lf/r3d && python3 -u scenejobs.py
SCENEJOBS OK  76 jobs  8399 f = 349.958 s
  gestures  none=71  lift=5
  lens      24-82 mm over 14 distinct values
  cam z     1.30-3.43 m   radius 0.81-5.20 m
```

`scenejobs.json` 은 `scenemap.json` + `rows38.json` + 대본 CSV 로부터 **결정적으로
재생성**된다. 내부에 ARC / RADIUS / HEIGHT / COVER 게이트 4종이 있어 재생성 결과가
스스로 검증된다. **파생 파일을 「생성 가능한 것」으로 유지한 설계가 데이터 손실을
막았다.** 만약 손으로 편집한 파일이었다면 8399프레임 설계를 잃었을 것이다.

### 처방 1 — 변수명 분리

```python
gap = math.dist(a["cam_end_xyz"], b["cam_start_xyz"])   # d -> gap
if gap > lim:
```

### 처방 2 — WRITE GATE: 검증을 통과해야 비로소 쓴다

```python
def _valid_jobs(jl):
    """정본 형태 검증 — 잡 리스트이고, 각 원소가 job_id/frames 를 가진 dict."""
    if not isinstance(jl, list) or not jl:
        return False
    for j in jl:
        if not isinstance(j, dict):                       return False
        if "job_id" not in j or "frames" not in j:         return False
        if not isinstance(j["frames"], int) or j["frames"] < 1: return False
    return True


def _write_jobs(path, jl, label, skip_if_exists=False):
    """★검증 후에만★ 쓴다. 임시 파일에 쓰고 원자적으로 교체한다."""
    if skip_if_exists and os.path.exists(path):
        return
    if not _valid_jobs(jl):
        raise SystemExit("WRITE GATE FAILED  %s 에 쓰려는 값이 잡 리스트가 아니다 "
                         "(%s) — 정본을 보호했다" % (path, type(jl).__name__))
    tmp = path + ".tmp"
    with io.open(tmp, "w", encoding="utf-8") as fh:
        json.dump({"jobs": jl}, fh, ensure_ascii=False, indent=1)
    os.replace(tmp, path)          # ★원자적 교체 — 중간 상태가 없다★
    print("%s  %s  %d 컷" % (label, path, len(jl)))
```

`os.replace()` 는 원자적이다. 쓰다가 죽어도 원본은 그대로 남는다.

### 처방 3 — `revert` 를 「읽기 → 검증 → 쓰기」로

```python
def cmd_revert():
    if not os.path.exists(BAK):
        print("백업이 없다: %s" % BAK)
        print("  scenejobs.json 은 `python3 scenejobs.py` 로 결정적으로 재생성된다.")
        return 1
    with io.open(BAK, encoding="utf-8") as fh:
        b = json.load(fh)
    jl = b["jobs"] if isinstance(b, dict) else b
    if not _valid_jobs(jl):                 # ★검증이 통과해야 비로소 쓴다★
        print("백업이 손상되었다: %s" % BAK)
        print("  scenejobs.json 은 `python3 scenejobs.py` 로 결정적으로 재생성된다.")
        return 1
    _write_jobs(JOBS, jl, "REVERTED")
    return 0
```

읽기 쪽에도 같은 검증을 넣었다. 손상된 정본으로 계획을 세우지 않는다.

```python
with io.open(JOBS, encoding="utf-8") as fh:
    d = json.load(fh)
jobs = d["jobs"] if isinstance(d, dict) else d
if not _valid_jobs(jobs):
    raise SystemExit("scenejobs.json 이 정본 형태가 아니다 — "
                     "`python3 scenejobs.py` 로 재생성하라")
```

### 규칙

1. **정본 파일에 쓰는 함수는 「읽기 → 검증 → 쓰기」 순서를 지킨다.** 검증 없이
   먼저 쓰면 그 다음 줄의 예외가 파일을 이미 망가뜨린 뒤에 온다.
2. **쓰기는 임시 파일 + `os.replace()` 로 원자화한다.** 중간 상태를 남기지 않는다.
3. **한 글자 변수명(`d`, `j`, `s`)을 넓은 스코프에 두지 않는다.** 루프 안에서 같은
   이름을 재사용하면 조용히 덮인다. 예외도 안 난다.
4. **파생 파일은 「생성 가능한 것」으로 유지한다.** `scenejobs.json` 을 손으로
   편집하지 않고 `scenejobs.py` 로 만들게 해 둔 덕에 복구가 1초로 끝났다.
5. **읽기 쪽에도 같은 검증을 둔다.** 손상된 정본으로 계획을 세우면 결과도 손상된다.

---

## 교훈 228 — `s.replace(old, new, 1)` 은 「첫 occurrence」다

### 무슨 일이 있었나

G7 게이트에 분할 형제 면제를 넣으려고 두 곳을 한 번에 치환했다.

```python
s = s.replace(old1, new1)                # ① band 계산 직후에 면제 블록 삽입
s = s.replace(old2, new2, 1)             # ② 루프 끝 상태 갱신에 prev_split_of 추가
#   old2 = "        prev_band, prev_props, prev_jid = band, props, jid\n"
```

결과:

```
File "/home/user/lf/r3d/script_gate.py", line 688
    prev_split_of = _so
IndentationError: unexpected indent
```

### 원인

①에서 삽입한 면제 블록 **안에** `prev_band, prev_props, prev_jid = band, props, jid`
가 들어 있었다. 그래서 ②의 `replace(..., 1)` 이 잡은 「첫 occurrence」는 루프 끝이
아니라 **내가 방금 삽입한 블록 안의 것**이었다.

```python
        _so = j.get("split_of")
        if _so and _so == prev_split_of:
            prev_band, prev_props, prev_jid = band, props, jid   ← ★여기가 잡혔다★
            prev_split_of = _so
```

들여쓰기 수준이 다른 곳에 삽입되어 문법 오류가 났다.

### 처방 — 유일한 긴 컨텍스트로 치환한다

`assert s.count(old) == 1` 이 통과하도록 **주변 문맥을 붙여** 유일하게 만든다.

```python
old = ('        prev_band, prev_props, prev_jid = band, props, jid\n'
       '        prev_tgt_for_g7 = t\n'
       '\n'
       '    # G9b')                        # ★뒤따르는 주석까지 포함 = 유일★
new = ('        prev_band, prev_props, prev_jid = band, props, jid\n'
       '        prev_tgt_for_g7 = t\n'
       '        prev_split_of = j.get("split_of")\n'
       '\n'
       '    # G9b')
assert s.count(old) == 1, "loop tail"
s = s.replace(old, new)                    # ★count 를 확인했으니 개수 인자 불필요★
```

이번 세션의 7패치는 전부 이 형태로 넣어 **한 번에 AST OK** 가 났다.

### 규칙

1. **`replace(..., 1)` 에 의존하지 않는다.** 대신 `assert s.count(old) == 1` 로
   유일성을 증명하고 개수 인자 없이 치환한다.
2. **같은 문자열을 삽입하는 패치와 그 문자열을 찾는 패치를 같은 스크립트에 두지 않는다.**
   두어야 한다면 찾는 쪽을 **먼저** 실행한다.
3. **치환 전에 `grep -n` 으로 실제 occurrence 위치를 눈으로 확인한다.**
4. **패치 후 `ast.parse()` 로 문법을 즉시 검증한다.** 백업(`cp <f> /tmp/<f>.bak`)은
   그 전에 만든다.

---

## §12.9 — G11 리듬 분할: 품질과 비용을 동시에 개선하는 유일한 지점

### 왜 이것만 다른가

지금까지의 품질 개선은 전부 **비용을 늘렸다.** 색 상수를 고치면 재렌더 2.8시간,
앵커를 키우면 재렌더, 렌즈를 바꾸면 재렌더. 그런데 컷 분할은 다르다.

```
분할 전   76 컷  8399 f  med 4.29 s
분할 후  123 컷  8399 f  med 2.62 s
          ↑             ↑
       컷 수 1.6배   프레임 총합 ★동일★
```

**총 프레임이 같으므로 렌더 시간이 0초도 늘지 않는다.** 벤치마크 6/6 이 지키는
「컷 길이 0.5~4.0초」 대역에 진입하면서 비용은 그대로다.

### 절차

```bash
# ① 계획만 본다 (파일을 건드리지 않는다)
cd /home/user/lf/r3d && python3 -u cutsplit.py plan

# ② SPLIT GATE OK 를 확인한 뒤 적용
python3 -u cutsplit.py apply       # scenejobs.presplit.json 에 백업 후 교체

# ③ 상위 게이트로 재검증
python3 -u script_gate.py          # FAILURES 0 확인

# ④ 되돌리려면
python3 -u cutsplit.py revert      # 백업이 손상돼도 정본을 덮지 않는다 (교훈 227)
```

### SPLIT GATE 8종

| # | 검사 | 근거 |
|---|---|---|
| ① | FRAME SUM 보존 (`8399 -> 8399`) | 대본이 정한 값 (교훈 222) |
| ② | `job_id` 중복 0 | 렌더 잡 충돌 방지 |
| ③ | `sid` 중복 0 | 자막 큐 중복 방지 |
| ④ | 숏폼 C 4컷 보존 | CEO 승인본 (교훈 131) |
| ⑤ | ARC 일치 (`_arc_of`) | 극좌표 원호 (교훈 225) |
| ⑥ | HEIGHT ≥ 0.812 (`DESK_Z + 0.05`) | 책상 아래로 안 내려간다 |
| ⑦ | RADIUS 원 컷 대역 ±0.02 내 | 원 안쪽 관통 방지 |
| ⑧ | SEAM ≤ 1프레임 실측 이동량 × 1.25 | 물리량 임계 (교훈 226) |

### 면제 규칙

```python
SHORTS_C_LOCK = ("J_A3-13", "J_A3-14", "J_A3-15", "J_A3-17")   # CEO 승인·납품본
EXEMPT_SUFFIX = ("GAP",)                                        # 전환 홀드

def why_skip(j):
    if jid in SHORTS_C_LOCK:                  return "숏폼C 승인본 (교훈 131)"
    if jid.endswith(EXEMPT_SUFFIX):           return "GAP (리듬 면제)"
    if j.get("word_gesture","none") != "none": return "글자 실린 컷 (CEO-49/57/58)"
    if j["frames"]/FPS <= CUT_LEN_MAX:         return "이미 상한 이내"
    return None
```

**글자 실린 컷을 쪼개지 않는 이유**: 조판된 문장이 컷 중간에서 끊긴다. [CEO-49]
어절 단위 원칙과 [CEO-57/58] 「글자는 이미지 수준」이 무너진다. 실측 결과 G11 초과
44컷은 **전부 `word_gesture="none"`** 이었으므로 이 면제로 잃는 것이 없었다.

### 상위 게이트 2건 면제 — 게이트의 「출처」로 지위를 가린다

`RHYTHM_ENFORCE=True` 로 승격하니 **10건**이 FAIL 났다. 판별 결과 **전부 게이트
쪽이 틀린 것**이었다.

| 검출 | 판별 | 처방 |
|---|---|---|
| G11 3건 (J_A3-13/15/17, 4.12~5.38 s) | **CEO 가 승인·납품한 숏폼 C** | `RHYTHM_LOCKED` 면제 (교훈 131) |
| G7 7건 (전부 `X_s1 -> X_s2`) | **같은 컷의 이어지는 구간** | `prev_split_of` 면제 |

G7 은 「**다른** 컷을 같은 크기로 또 찍었다」를 잡는 게이트다. 분할 조각은 같은 컷의
연속이므로 샷 크기가 겹치는 것이 당연하고, **겹치지 않으면 오히려 궤적이 끊긴 것**이다.
결함이 아니라 **게이트의 적용 범위 오류**였다.

```python
_so = j.get("split_of")
if _so and _so == prev_split_of:
    prev_band, prev_props, prev_jid = band, props, jid
    prev_split_of = _so
    prev_tgt_for_g7 = t
    continue                     # ★분할 형제끼리는 G7 대상이 아니다★
```

**결과**

```
RHYTHM (G11, 컷 길이 0.5~4.0 s): 117 / 117 컷 통과   [ENFORCE]
   실측  min 1.67  med 2.62  mean 2.71  max 3.96 s   (목표 med 3.0 s)

FAILURES (G1/G2/G3/G5/G6/G7/G8/Z-FIT/G9/G10/G11): 0

SCRIPT GATE OK
```

### 규칙

1. **품질 개선 항목을 「비용을 늘리는가」로 분류한다.** 비용 중립 항목이 있으면
   그것을 먼저 한다.
2. **분할은 프레임 총합을 절대 바꾸지 않는다.** 바뀌면 대본 정본 위반이다 (교훈 222).
3. **분할 후 상위 게이트가 FAIL 하면 「게이트의 적용 범위」를 먼저 의심한다.**
   분할은 새 컷을 만드는 게 아니라 기존 컷을 쪼개는 것이므로, 「컷 사이」를 보는
   게이트는 대상이 달라진다.
4. **CEO 승인본은 게이트보다 위다** (교훈 131). 승인본이 FAIL 나면 게이트를 고친다.

---

## 교훈 229 — 「파생 데이터」를 만들 때는 「그 데이터를 읽는 모든 곳」을 먼저 세어라

### 무슨 일이 있었나 — 분할 조각에서 주연 소품이 사라졌다

`cutsplit.py` 가 `J_A3-16`(167f)을 `_s1`(84f) + `_s2`(83f) 로 쪼갰다.
SPLIT GATE 8종 전부 통과, SCRIPT GATE FAILURES 0, 프레임 총합 8399 보존.
게이트는 전부 초록이었다.

그런데 렌더된 두 조각의 프레임을 눈으로 보니 —

| | `_s1` f80 | `_s2` f02 |
|---|---|---|
| 노란 목표바(`goalbar`) | **있다** | **없다** |
| 브리프/노트/실행바 | 있다 | 없다 |
| PNG 크기 | 131,625 B | 40,525 B |

**같은 컷의 이어지는 구간인데, 84프레임째와 85프레임째 사이에서 책상 위 서류가
통째로 사라졌다.** 이음새를 정량으로 재보니 결정적이었다.

```
이음새 (s1 f83 -> s2 f00)   차이 mean 3.62   p95 26.0   >10 픽셀 11.4 %
s1 내부 인접 (f82 -> f83)   차이 mean 0.01   p95  0.0   >10 픽셀  0.0 %
```

내부 인접 프레임은 mean 0.01 로 매끄러운데, **이음새만 362배 튄다.**
[CEO-67] 4항 「영화적 연속성」을 정면으로 깨는 결함이고, 분할이 오히려
연속성을 파괴한 셈이다.

### 원인 — `sids` 는 두 가지 일을 하고 있었다

`sets.PROPS` 는 「이 비트에 어떤 주연 소품을 놓는가」를 대본 비트 id 로 담는다.
그것을 읽는 코드가 **두 곳**에 있었다.

```python
# previz_batch.py line 457  ← ★렌더 정본★
_sid = (job.get("sids") or [None])[0] or job["job_id"].replace("J_", "")
for name, kind, loc, sc, col in sets_build_spec(set_id, _sid):

# script_gate.py line 589   ← 게이트
props = PROPS.get(jid.replace("J_", ""), ())
```

한편 `cutsplit.split_job()` 은 `sids` 를 이렇게 나눈다.

```python
# sids 는 첫 조각이 대표한다 (COVER GATE 가 중복 sid 를 반려한다)
p["sids"] = list(j["sids"]) if i == 0 else []
```

이 한 줄은 **그 자체로는 옳다.** COVER GATE 는 「한 대본 비트가 두 컷에
중복 배정되었는가」를 보므로, 조각마다 같은 sid 를 넣으면 반려된다.

문제는 `sids` 가 **두 가지 질문에 동시에 답하고 있었다**는 것이다.

| 질문 | 분할 시 올바른 답 |
|---|---|
| ① 이 컷이 대본의 어느 비트를 **소비하는가** (COVER GATE) | 조각 1 만 |
| ② 이 컷의 세트에 어떤 **소품을 놓는가** (PROPS 조회) | **모든 조각** |

`sids=[]` 로 비운 순간 ②의 폴백이 `"A3-16_s2"` 를 찾았고, 그 키는
`PROPS` 에 없으므로 소품 4개가 조용히 사라졌다. **예외도 경고도 없었다** —
`PROPS.get(key, ())` 가 빈 튜플을 돌려주기 때문이다.

### 왜 게이트가 못 잡았나

게이트도 **같은 잘못된 조회를 하고 있었다.** `script_gate.py` 는
`props = PROPS.get(jid.replace("J_",""), ())` 로 읽고 `if props:` 로 감싸므로,
소품을 놓친 컷은 **G5(몰입)·G6(프레이밍)·G8(가시성)·G9(앵커) 검사를 전부
건너뛴다.** 검사 대상이 없다고 판단해 조용히 통과시켰다.

```
PROPS 적중  5 / 123 컷   ← 정정 전
PROPS 적중  7 / 123 컷   ← 정정 후 (J_A3-16_s1, _s2)
```

**게이트와 렌더러가 같은 결함을 공유하면, 게이트는 그 결함을 볼 수 없다.**
교훈 223 「게이트가 전부 초록일 때도 육안 확인은 반드시 한다」가
이번에도 유일한 발견 경로였다.

### 처방 — 질문마다 키를 따로 준다

```python
# cutsplit.py  split_job()
p["sids"] = list(j["sids"]) if i == 0 else []          # ① 대본 소비 (COVER GATE)
p["props_sid"] = (list(j["sids"]) or [None])[0] \
    or j["job_id"].replace("J_", "")                   # ② 소품 조회 (모든 조각)
```

조각 2+ 는 `sids=[]` 이므로 `or` 폴백이 타는데, `split_job` 안에서는
`j` 가 **원본 잡**이므로 `j["job_id"]` 가 `"J_A3-16"` → `"A3-16"` 이 되어
정확히 원본 비트를 가리킨다.

읽는 쪽 두 곳도 `props_sid` 를 최우선으로 읽게 했다 (없으면 종전 동작 유지).

```python
# previz_batch.py — 렌더 정본
_sid = job.get("props_sid") or (job.get("sids") or [None])[0] \
    or job["job_id"].replace("J_", "")

# script_gate.py — G5
_psid = j.get("props_sid") or jid.replace("J_", "")
props = PROPS.get(_psid, ())

# script_gate.py — subj_frac() 의 가림 판정
_bsid = job.get("props_sid") or (job.get("sids") or [None])[0] \
    or job["job_id"].replace("J_", "")
blocks = blockers_of(job["set"], _bsid, props)
```

**세 번째는 별개의 결함을 같이 고친 것이다.** `blockers_of` 는
`job.get("sid")` 를 읽고 있었는데 `scenejobs.json` 에 `sid` 키는 **존재하지 않는다**
(키는 `sids` 다). 즉 **항상 `""` 가 넘어가 주연 소품이 가림 판정에서 통째로
빠져 있었다** — 분할과 무관하게 처음부터 잘못이었다. 오래된 미해결 항목
「`script_gate.py` `job.get("sid")` 항상 None」이 이것이다.

### 규칙

1. **한 필드가 두 가지 질문에 답하고 있으면 분리하라.** `sids` 는
   「대본 소비」와 「소품 조회」를 겸했고, 분할이 그 겸직을 깨뜨렸다.
2. **파생 데이터(분할·병합·리샘플)를 만들 때는 「원본의 어느 필드를
   누가 읽는가」를 먼저 grep 으로 세어라.** 이번엔 읽는 곳이 2군데였고,
   둘 다 놓쳤다.
3. **`dict.get(k, default)` 로 조용히 넘어가는 조회는 결함을 숨긴다.**
   설계상 반드시 있어야 하는 키라면 `assert` 하거나, 최소한 「없었다」를
   집계해서 출력하라. `PROPS 적중 5 / 123` 을 한 번이라도 찍어봤으면
   즉시 보였다.
4. **게이트가 렌더러와 같은 조회 코드를 쓰면 그 결함은 게이트에 보이지 않는다.**
   게이트는 렌더러의 「결과」를 봐야 하고, 그럴 수 없을 때는
   ★육안 확인★ 이 유일한 발견 경로다 (교훈 223).
5. **이음새는 정량으로 재라.** 「내부 인접 프레임 차이」와 「이음새 차이」를
   비교하면 절대 임계 없이도 결함이 드러난다 (mean 0.01 vs 3.62).
   이것이 교훈 226 「게이트 임계는 물리량이어야 한다」의 응용이다.

### 결과

```
정정 전   PROPS 적중  5 / 123   소품 잃은 조각 1 (J_A3-16_s2)
정정 후   PROPS 적중  7 / 123   소품 잃은 조각 0
          SCRIPT GATE OK · FAILURES 0 (회귀 없음)
          프레임 총합 8399 보존
```

영향 조각이 1개뿐이었던 이유는 `sets.PROPS` 에 등재된 비트가 6개
(`A3-13`~`A4-01`)이고, 그중 4개는 `SHORTS_C_LOCK` 으로 분할 자체가
금지되어 있었기 때문이다. **CEO 승인본을 보호한 잠금이 결함의 폭발
범위까지 막아준 셈이다** — 교훈 131 의 부수 효과다.

---

## 교훈 230 — 게이트가 재지 않는 차원은 「별도 도구」로 재고, 그 도구는 영구 파일로 둔다

### 사건 — 세 번 겹친 사고

**사고 ①  도구를 `/tmp` 에 두어 소실했다.**
[CEO-83] 「그 설정이 정말 관객들에게 몰입과 호응을 줄 수 있는 수준과 기획인지는
검증하세요」에 답하려고 앵커 5축 계측 도구 3종을 만들었다. 그런데 그것을
`/tmp/anchor/` 에 두었다. 다음 세션에서 `ls` 는 이렇게 답했다.

```
ls: cannot access 'visib.py': No such file or directory
ls: cannot access 'needsize.py': No such file or directory
ls: cannot access 'altsweep.py': No such file or directory
```

계측 **결과**(§7 의 표)는 문서에 남았지만 계측 **수단**은 사라졌다. 그래서
「처방을 적용한 뒤 다시 재기」가 불가능해졌다. [CEO-73] 「재생산이 가능하도록
늘 일을 구축해야 해요」의 정면 위반이다.

**사고 ②  「처방 전 실측」을 「현재 상태」로 착각해 들고 다녔다.**
§7 의 5축 판정(축① ❌ 1.90:1, 축② ◐ …)은 §8 의 처방을 적용하기 **전**의
숫자다. §8 에서 `sets.py` 의 `ENV_WALL_HI` 를 0.215 → 0.168 로 낮추고
`DOC_W` 를 어둡게 하고 `DOC_ANCHOR_C` 를 신설했는데, 그 뒤 **한 번도 다시
재지 않았다.** 결과적으로 이미 해소된 축을 「미해결」로 다섯 세션 동안
보고서에 실었다.

**사고 ③  도구 자신이 교훈 229 를 재발시켰다.**
재작성한 `altsweep.py` 의 축① 이 2.83:1 로 **실패**를 냈다. 원인은
`NEIGHBOURS` 리스트에 `DOC_N`(카드 **위**에 놓인 제목·본문 바)을 넣은 것이다.
한 리스트가 두 질문에 동시에 답하고 있었다.

| 질문 | 이웃의 정체 | 대비가 크면 |
|---|---|---|
| ① 앵커가 **배경·이웃 종이**에서 구별되는가 | `DOC_W` / `ENV_FURN` / `ENV_WALL_HI` | **좋다** |
| ② 앵커 **위의 바**가 앵커 자신에서 구별되는가 | `DOC_N` / 채운 바 / 빈 바 | **좋다** |

②는 대비가 커야 좋은 것인데, 그것을 ①의 「최악값」으로 세면 **좋은 설계가
실패로 집계된다.** 방향이 반대로 뒤집힌다. 교훈 229 의 규칙 1
「한 필드가 두 질문에 답하고 있으면 분리하라」가 도구 자신에게서 재발했다.

### 처방

**1. 도구는 `/tmp` 금지 — 파이프라인 디렉터리에 영구 파일로 둔다.**

```
/home/user/lf/r3d/anchorlib.py   공용 산식 (script_gate 에서 import)
/home/user/lf/r3d/visib.py       축② 변화의 가시성
/home/user/lf/r3d/needsize.py    축④ 26초 지속력
/home/user/lf/r3d/altsweep.py    축①+⑤ 구별성 · 대안 스윕
```

**2. 수식·상수를 복제하지 말고 참조하라 (교훈 176 의 완전 이행).**
`anchorlib.py` 는 임계·센서·해상도·앵커 이름·벡터 헬퍼를 **하나도 다시 적지
않는다.**

```python
import script_gate as SG
SUBJ_FRAC_MIN = SG.SUBJ_FRAC_MIN   # 0.14
SENSOR_MM     = SG.SENSOR_MM       # 36.0
RES_W, RES_H  = SG.RES_W, SG.RES_H # 1280.0, 720.0
ANCHOR_NAME   = SG.ANCHOR_NAME     # "card"
_sub, _dot, _norm = SG._sub, SG._dot, SG._norm
```

게이트의 임계가 바뀌면 도구도 **자동으로** 바뀐다. 두 숫자가 갈라질 수 없다.
(`script_gate.py` 의 `if __name__ == "__main__":` 가 파일 맨 끝에 있으므로
import 는 사이드이펙트가 없다 — 이 점을 먼저 확인하고 import 했다.)

**3. 도구에는 「표 값 재현 자기검사」를 반드시 넣어라.**
소실된 도구의 산식은 문서에 남아 있지 않았다. 그래서 §7 의 **결과 숫자**로부터
산식을 역검증해 확정했다.

```
색   : sets.py 상수를 sRGB 로 보고 linearize → WCAG 상대휘도
채도 : HSV (max-min)/max
대비 : (hi+0.05)/(lo+0.05)
투영 : px = 2*max(sc[0],sc[1]) * lens / (dep * SENSOR_MM) * RES_W
필요 : need_edge = FRAC_MIN * dep * SENSOR_MM / lens
```

그리고 `selfcheck()` 가 §7 표의 12개 값을 재현한다.

```
luma(DOC_A) got 0.1445 want 0.1440 OK      sat(DOC_A) got 0.8092 want 0.8100 OK
luma(DOC_N) got 0.2727 want 0.2730 OK      sat(DOC_B) got 0.8194 want 0.8200 OK
luma(DOC_B) got 0.3004 want 0.3000 OK      sat(DOC_C) got 0.7683 want 0.7700 OK
luma(DOC_C) got 0.4583 want 0.4580 OK      sat(CUE)   got 0.7500 want 0.7500 OK
luma(CUE)   got 0.6111 want 0.6110 OK      need_edge(40, 2.66)  0.3352 / 0.335 OK
sat(DOC_N)  got 0.0268 want 0.0300 OK      px_of(0.140,50,1.751) 142.17 / 142 OK
selfcheck OK (0 fails)
```

**이것이 「같은 축을 재고 있다」의 증명이다.** 자기검사가 없으면 재계측은
「다른 도구로 다른 값을 낸 것」에 불과하고, §7 과 비교할 자격이 없다.

**4. 게이트가 보지 않는 축은 도구가 본다.**
`script_gate` 는 FAILURES 0 을 냈는데, 앵커 자체는 하한 미달이었다.

```
비트      lens  최원거리m  실제최장변m  최악화면폭   판정
A3-13     40.1    2.523      0.2600     0.1148    미달  (하한 0.14)
A3-14     50.0    1.761      0.2600     0.2051    OK
A3-15     54.0    1.335      0.2600     0.2921    OK
A3-17     40.0    2.336      0.2600     0.1237    미달
```

왜 게이트가 놓쳤는가.

- **G6 는 「컷의 최대 주연」만 본다.** A3-13 의 최대 주연은 카드가 아니라
  비교표 `cmptab` 이다. 카드가 작아도 G6 는 통과한다.
- **G9 는 「첫 프레임의 비」와 「런 커버」만 본다.** 앵커가 경쟁자보다 크면
  통과다 — **절대 크기를 보지 않는다.**

⇒ 교훈 213 「게이트를 통과하는 앵커와 관객을 붙잡는 앵커는 다른 질문이다」의
정량 증거다. **미보유 게이트는 「앵커 절대 크기 하한」이며, `needsize.py` 가
그 역할을 대신한다.**

**5. 판정식은 벤치마크로 검증하라 — AND 로 두면 「발광 앵커」를 내 손으로 반려한다.**
§7.1 은 대비 3.0:1 **과** 채도차 0.20 을 둘 다 요구하는 것처럼 표를 세웠다.
벤치마크를 다시 보면 「튀는 축」은 하나로 충분하다.

| 벤치마크 | 앵커 | 튀는 축 |
|---|---|---|
| `Sp8XFlq4s-g` | 빛나는 물방울 | **휘도** |
| 내장 쇼츠 | 발광 분자 하나 | **휘도** |
| `K4YuwHHGrgQ` | 빨간 주석 | **채도** |

⇒ 판정식은 **OR** 이 맞다. 고쳐서 실행하니 현행 `DOC_ANCHOR_C` 가
**8.11:1** 로 축① 을 해소했다(§7 의 1.90:1 에서). [CEO-73] 「벤치마크를 해도
내 것으로 소화해서 우리의 목적에 맞게 더 높은 품질로 가공해야 합니다」의 이행이다.

**6. 게이트 임계는 「물리량」이어야 한다 (교훈 226 의 응용).**
축② 가 §7 에서 ◐ 였던 이유는 「바가 굵어졌다」를 **px** 로 재서 카메라가
다가간 효과와 분리할 수 없었기 때문이다. 처방은 **거리 불변량**이다.

```
두께비 = 바 두께 px / ★같은 프레임★ 의 카드 장변 px
```

```
A3-14 start  bodybar  두께 14.2px  카드 262.5px   두께비 0.0542
A3-15 start  fillbar  두께 31.5px  카드 373.9px   두께비 0.0844   x1.558  PASS
  (관측 px 는 x2.219 였지만, 그중 x1.307 은 카메라가 다가간 효과였다)
```

임계 `RATIO_PASS = 1.30` 도 발명하지 않았다. §7.2 에서 「보였다」고 판정된
값(1.75)보다 낮게, 그리고 거리 효과(1.30)와 같은 크기로 두었다 — 교훈 199
「합격을 넉넉히가 아니라 불합격을 확실히」.

### 규칙

1. **검증 도구는 `/tmp` 에 두지 마라.** 파이프라인 디렉터리에 영구 파일로 둔다.
   결과만 문서에 남기면, 처방 후 재계측이 불가능해진다.
2. **도구가 참조하는 상수·수식은 정본 모듈에서 `import` 하라.** 복제하면
   게이트와 도구가 갈라진다.
3. **도구에 「표 값 재현 자기검사」를 넣어라.** 그것이 「같은 축을 재고 있다」의
   유일한 증명이다.
4. **「처방 전 실측」을 「현재 상태」로 보고하지 마라.** 코드를 고쳤으면 다시 재라.
5. **게이트가 초록이어도, 게이트가 보지 않는 차원은 여전히 미검증이다.**
   그 차원을 재는 도구를 따로 만들고 위계표에 등재하라.
6. **판정식(AND/OR·임계)은 벤치마크로 검증하라.** 산식이 맞아도 판정 논리가
   틀리면 좋은 설계를 반려한다.
7. **도구 자신도 교훈 229 의 예외가 아니다.** 한 리스트가 두 질문에 답하고
   있으면 분리하라.
8. **발견을 곧바로 적용하지 마라.** 미달 컷(A3-13/A3-17)은 `SHORTS_C_LOCK` =
   CEO 승인 납품본이다. 교훈 131 + [CEO-85] 「낭비 종료」 ⇒ **도구는 계산만
   하고 선택지를 남긴다.** 적용은 대표님 판단으로 다음 편(숏폼 D)에서.

### 결과

```
축① 시각적 구별성   ❌ 1.90:1              →  ✅ 8.11:1 (휘도로 튄다)
축② 변화의 가시성   ◐ 카메라와 분리 불가    →  ✅ 두께비 x1.558 (거리 불변)
축③ 0~2초 후킹      ❌ 앵커가 주연의 46%    →  ◐ 비 1.24 (육안 미확인)
축④ 26초 지속력     ❌ 62% 커버            →  ◐ 67% 커버 · 카드컷 2/4 미달
축⑤ 대안 후보       ❌ 색·크기 조정 무효    →  ◐ 크기 x1.30 으로 4/4

부수 소득: §7.5 의 「색과 크기는 독립 변수」 판정이 뒤집혔다. 크기를 x1.46
키워도 3/6 이었던 것은 ★색이 배경에 묻혀 있었기 때문★ 이다. §8 이 색을
해소하자 크기가 단일 변수로 작동해 x1.30 에서 4/4 가 됐다.
유료 호출 0건.
```

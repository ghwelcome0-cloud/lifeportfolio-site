# 하이브리드 3D 모션 — 콘텐츠 일괄 생성 제작 규칙 v1.0

> **문서 성격**: 이 문서는 "배우는 글"이 아니라 **"따라 하면 같은 품질이 나오는 규칙"** 입니다.
> 학습용 해설은 [`70_HYBRID_3D_MOTION_GUIDE.md`](./70_HYBRID_3D_MOTION_GUIDE.md) 를 보십시오.
> 이 문서는 그 위에서, **롱폼 v14 를 실제로 완성하며 확정된 판정 기준**만 모았습니다.

- **작성**: 2026-08-17
- **근거 산출물**: 롱폼 본편 v14 (405.233초 / 12157프레임)
- **왜 지금 만드는가**: 대표님 지시 — *"롱폼이 제작되고 최종 승인을 해야 이걸 기준으로 하이브리드 3D 모션 영상 기법을 제작 규칙으로 삼아서 앞으로 콘텐츠들 일괄 생성이 가능하니."*
- **상태**: v14 렌더/검수 완료 · **대표님 최종 승인 대기**. 승인 시 이 문서가 정본 규칙이 됩니다.

---

## 0. 이 문서가 존재하는 이유 — 한 번 크게 틀렸기 때문

직전 보고에서 우리는 **"정지 화면 60.2% → 0.0%"** 라고 보고했습니다.
그 수치 자체는 사실입니다. 정본 검사 도구가 실제로 0건을 반환했습니다.

그런데 대표님이 이렇게 지적하셨습니다.

> **"46초 구간에 나오는 영상이 일시정지 되는 모습이 끊김이 있어요."**

재조사 결과 **도구가 46초 구간을 애초에 검사하지 않았다**는 것이 밝혀졌습니다.

| | 내용 |
|---|---|
| 검사 규칙 | 프레임 간 화소차 **< 0.35** 이면 "정지" |
| 46초 구간 실제 | 아주 미세하게 움직여서 **0.35 를 간신히 넘김** |
| 결과 | 기계 판정 = "움직임" → **후보 목록에 아예 오르지 않음** |
| 사람 눈 | **"멈춰 있다"** |

**"0건"은 "결함이 없다"가 아니라 "검사 대상이 아니었다"는 뜻이었습니다.**

새 자(尺)로 다시 재니 같은 성질의 결함이 **8건 48.5초, 본편의 12.0%** 있었습니다.
46초는 그중 **1건**일 뿐이었고, 가장 큰 것은 **375.5~405.0초 — 영상 마지막 30초 전체**였습니다.
엔딩이 통째로 정지처럼 보이고 있었는데, 우리는 그것을 "0건"이라고 보고하고 있었습니다.

> **이 문서의 규칙들은 전부 이 사고에서 나왔습니다.**
> 규칙을 지키는 것보다 **왜 그 규칙이 생겼는지 아는 것**이 중요합니다.
> 근거를 모르면 상황이 조금만 달라져도 같은 사고가 반복됩니다.

---

## 1. 제작 규칙 — 5단계 (이 순서를 바꾸지 마십시오)

```
 STEP 1  콘텐츠 유형 분류      → 어디에 모션이 "필요한가"를 정한다
 STEP 2  소재 기하 측정        → 어디까지 확대해도 "안 잘리는가"를 정한다
 STEP 3  구간 확정             → 어디서 어디까지가 "한 화면인가"를 정한다
 STEP 4  렌더                  → 만든다
 STEP 5  이중 검수 (기계 + 눈) → 됐는지 판정한다
```

각 단계는 **앞 단계의 산출물이 있어야** 시작할 수 있습니다.
순서를 건너뛰면 반드시 되돌아옵니다 (§6 의 실패 사례 4건이 전부 그렇습니다).

---

## 2. STEP 1 — 콘텐츠 유형 분류 (가장 중요)

### 2.1 왜 유형부터 나누는가

**모든 정지가 결함이 아닙니다.**

| 유형 | 화면 예 | 정지했을 때 | 조치 |
|---|---|---|---|
| **SLIDE** | 흰 배경 + 큰 텍스트 | **정상** — 읽는 시간이다 | ❌ 모션 넣지 말 것 |
| **PHOTO** | 인물, 사물, 실사 사진 | **결함** — 영상이 멈춘 것처럼 보인다 | ✅ 반드시 모션 |
| **MIXED** | 인포그래픽, 일러스트+글 | 길면 결함 | ⚠️ 4초 이상일 때만 |

슬라이드에 카메라를 움직이면 **글자가 흔들려서 읽기 나빠집니다.**
실제로 63.5~68.0초(슬라이드)에 모션을 넣었다가 반려했습니다(§6-D).

### 2.2 분류 공식 (변경 금지)

```python
# 320x180 축소 프레임 기준
flat = (국소표준편차 < 2.0 인 화소 비율)   # 평평한 면적 = 단색 배경 비율
uc   = (24단계 양자화 후 고유 색 수)        # 색 다양성

def kind(frame):
    if flat > 0.62 and uc < 55:  return "SLIDE"   # 넓은 단색 + 색 적음
    if flat < 0.45 or  uc > 80:  return "PHOTO"   # 질감 많음 or 색 많음
    return "MIXED"
```

> ⚠️ **이 임계값을 임의로 바꾸지 마십시오.**
> 롱폼 22블록 + 추가 7구간, 총 29구간에서 검증된 값입니다.
> 바꾸면 과거 판정과 비교가 성립하지 않습니다.

### 2.3 실제 분포 (롱폼 v14 · 참고선)

| | flat | uc |
|---|---|---|
| SLIDE 실측 | 0.73 ~ 0.86 | 31 ~ 53 |
| PHOTO 실측 | 0.37 ~ 0.79 | 99 ~ 271 |
| MIXED 실측 | 0.60 ~ 0.90 | 55 ~ 73 |

**`uc` 가 100 을 넘으면 거의 확실히 사진입니다.** 가장 빠른 눈대중 기준입니다.

---

## 3. STEP 2 — 소재 기하 측정 (잘림 방지)

### 3.1 왜 필요한가

**"모든 컷을 10% 확대" 같은 일괄 값은 반드시 사고를 냅니다.**

피사체가 화면 가장자리에 붙어 있으면 조금만 확대해도 잘립니다.
롱폼 46초 구간이 정확히 그랬습니다.

| 구간 | 좌 여백 | 우 여백 | 안전 최대 배율 |
|---|---|---|---|
| **44.1-50.9 (46초 구간)** | **0.2%** | 13.7% | **1.0347** ← 확대 여력 3.5% 뿐 |
| 70.5-80.3 | 1.9% | 0.2% | 1.0583 |
| 153.1-157.9 | 22.8% | 24.7% | 1.2787 |
| 179.5-183.9 | 11.7% | 12.0% | 1.0961 |
| 375.5-405.1 | 9.1% | 17.3% | 1.2165 |

같은 영상 안에서도 안전 배율이 **1.03 ~ 1.28** 로 8배 넘게 차이납니다.
**소재를 보지 않고 정한 파라미터는 사고를 만듭니다.**

### 3.2 측정 절차

1. 구간에서 **13장**을 균등 샘플링
2. 각 장에서 **잉크 경계상자(ink bbox)** 를 구한다
   - Sobel 경계강도 > 14.0 인 화소를 "내용"으로 본다
   - 행/열 누적 프로파일에서 전체의 0.20% 미만인 줄은 잡음으로 버린다
3. 13장 bbox 의 **합집합** 을 취한다 (한 장이라도 잘리면 안 되므로)
4. 팬 이동폭을 고려해 안전 최대 배율을 역산한다

```python
def max_zoom_for(bbox, pan_span):
    px = pan_span; py = pan_span * 0.6      # 세로 팬은 가로의 60%
    x0, y0, x1, y1 = bbox                    # 0~1 정규화
    need_w = 2.0 * max(abs(0.5 - px - x0), abs(x1 - (0.5 + px)))
    need_h = 2.0 * max(abs(0.5 - py - y0), abs(y1 - (0.5 + py)))
    return 1.0 / max(need_w, need_h)
```

5. **적용 배율 = min(전역 상한 1.10, 측정된 안전 배율)**

### 3.3 팬 방향 결정

```python
# 여백이 넓은 쪽을 "버린다" — 좁은 쪽으로는 갈 수 없다
if abs(L - R) < 0.010:  방향 = drift_in / drift_out   # 좌우 대칭 → 줌만
else:                   방향 = drift_right if L > R else drift_left
```

---

## 4. STEP 3 — 구간 확정 (경계 설정)

### 4.1 함정: 화면이 바뀌는 방식은 2가지다

| 사건 | 화면 | 처리 |
|---|---|---|
| **컷(cut)** | 완전히 다른 화면으로 전환 | 여기서 **끊어야 한다** |
| **요소 등장** | 같은 화면에 항목 하나가 나타남 | 끊으면 **안 된다** |

인접 프레임 화소차 하나로는 **둘을 구분할 수 없습니다.**

실제 사례 — 47.000초 지점:

```
인접 프레임차 17.96  (임계 12.0 초과)  →  1차 판정: "컷"   ❌ 오판
```

### 4.2 해법: 두 번째 지표를 도입한다

**기준 프레임(±0.60초) 대비 누적 화소차**로 재측정합니다.

```
46.20초 기준 프레임과 비교:
  44.00 → 21.6      45.00 → 22.6
  46.00 →  4.7  ★   47.00 →  5.5  ★   48.00 →  5.5  ★   50.00 →  6.2  ★
  51.00 → 55.2  ← 진짜 컷
```

46.0~50.5 는 전부 **같은 화면**입니다. 47.00 은 그 위에 요소 하나가 나타난 것뿐입니다.

```python
ADJ_TOL  = 12.0   # 인접차 — "후보"만 만든다
SAME_TOL = 25.0   # 기준 대비 누적차 — 이 아래면 "같은 화면"

acc = |frame(t + 0.60) - frame(t - 0.60)|.mean()
is_cut = (acc >= SAME_TOL)
```

> **규칙: 인접차는 후보 생성용으로만 쓴다. 컷 확정은 반드시 누적차로 한다.**

### 4.3 경계는 컷만이 아니다

이미 모션을 넣은 블록의 **경계도 벽**입니다. 넘어가면 이중 적용됩니다.

```python
lo = max([lo] + [끝 for 시작, 끝 in 기처리구간 if 끝 <= a + 0.05])
hi = min([hi] + [시작 for 시작, 끝 in 기처리구간 if 시작 >= b - 0.05])
```

### 4.4 컷이 없어도 유형이 바뀌면 자른다 ★

**가장 놓치기 쉬운 규칙입니다.**

70.400초에는 컷이 없습니다. 그러나 콘텐츠 유형이 바뀝니다.

```
63.5 ~ 68.0   SLIDE  (flat 0.73 / uc  53)   정지가 정상
70.5 ~ 78.0   PHOTO  (flat 0.79 / uc 141)   정지가 결함
```

이 둘을 **한 계획으로 렌더하면 실패합니다.** 실제로 실패했습니다(§6-D).

> **규칙: 컷 경계 + 기처리 블록 경계 + 콘텐츠 유형 경계 — 3가지 모두를 벽으로 삼는다.**

---

## 5. STEP 5 — 이중 검수 (기계 + 눈)

### 5.1 게이트 3종 — 전부 통과해야 한다

| 게이트 | 무엇을 보는가 | 통과 조건 |
|---|---|---|
| **T-1 잘림** | 소재 밖을 샘플링했는가 | 가장자리 유지율 ≥ 0.90 |
| **T-2 정지** | 완전히 멈춘 프레임이 있는가 | **1.5초(7.5프레임) 연속** 으로 화소차 < 0.35 인 구간 = 0 |
| **T-3 급변** | 이동이 너무 빠른가 | 초당 이동 ≤ 16.0px |
| **★T-4 저모션** | **충분히 움직이는가** | **2초 광류 p95 중앙값 ≥ 1.5px** |

**T-4 가 이번에 새로 만든 게이트입니다.** 이것이 없어서 46초를 놓쳤습니다.

### 5.2 T-4 의 임계 1.5px 은 어디서 나왔는가 (지어낸 값이 아님)

엔진 설계 상수에서 **역산**했습니다.

```
2초 동안의 줌  = ZOOM_RATE 0.009 × 2 = 1.8%  → 320px 폭 기준 가장자리 이동 ≈ 2.9px
2초 동안의 팬  = PAN_RATE  0.0055 × 2 = 1.1% → 320px 폭 기준 이동         ≈ 3.5px
⇒ 엔진이 정상 작동한 구간은 2초 p95 가 3~5px 이어야 한다
```

**실측 검증**: 롱폼 807개 측정점의 중앙값 = **3.231px** — 설계값과 일치했습니다.

```
1.5px = 설계 기대치의 절반 미만 = "엔진이 걸리지 않았다"
```

### 5.3 T-2 판정 시 주의 — 1프레임은 정지가 아니다

렌더러 자체 검사는 **1프레임이라도** 임계 미만이면 경고를 냅니다. 이는 **과보수적**입니다.

**정본 판정은 "1.5초(7.5프레임) 연속"입니다.**

실제 사례 — 70.5-80.3초 구간(Y02):

```
렌더러 자체 검사 : 48개 측정점 중 1개가 0.3397 (임계 0.35 를 0.0103 차이로 스침) → 경고
정본 판정        : 연속 1프레임(0.2초)뿐 → 정지 ★0건★
기승인 29블록 대조: t2_min p10 = 0.531, 0.40 미만이 이미 2건 (승인된 것)
⇒ 수용
```

> **규칙: 렌더러 경고는 "확인하라"는 신호이지 "실패"가 아니다.**
> **판정은 반드시 정본 도구(1.5초 연속 규칙)로 한다.**

### 5.4 눈 검수 — 비전 모델 사용 시 필수 주의 ★

**프레임을 여러 장 따로 넘기면 안 됩니다.**

실제로 46초 구간 검증에서 46.0s / 48.0s / 50.0s 세 장을 넘겼더니
**세 장 모두 "MOTION VISIBLE: NO"** 라는 답이 돌아왔습니다.

그런데 화소 실측은 정반대였습니다.

| 46초 구간 2초 이동량 | 개선 전 | 개선 후 |
|---|---|---|
| 45.0 → 47.0초 | 1.27px | **4.08px** (3.2배) |
| 47.0 → 49.0초 | 1.60px | **7.47px** (4.7배) |

응답을 확인하니 **3장이 각각 독립 응답으로 처리**되어 있었습니다.
모델은 세 장을 나란히 놓고 본 적이 없습니다.
**"동일하다"는 진술은 비교의 결과가 아니라 추측이었습니다.**

> **규칙: 비교 판정을 모델에게 맡기지 말고, 비교 결과 자체를 한 장의 이미지로 구워서 넘긴다.**
> 차분맵(frame difference heatmap)을 개선 전/후 2행으로 배치하면
> 모델은 한 이미지 안에서 비교할 수밖에 없습니다.
> 이 방식으로 다시 물으니 **"MOTION INCREASED: YES"** 로 정확히 답했습니다.

---

## 6. 실패 사례 4건 — 그대로 반복하지 마십시오

### A. 인접 화소차만으로 컷을 판정 → 같은 화면을 두 동강

47.000초의 인접차 17.96 을 컷으로 오판. 실제로는 요소 등장.
**→ 기준 프레임 대비 누적차(§4.2)로 재판정해야 함**

### B. 이미 처리한 구간을 다시 후보로 집계

225.5-227.5초가 저모션으로 잡혔습니다. 그런데 그 구간은 **이미 모션이 들어간 블록 내부**였습니다.

```
해당 블록(215.6-240.4) 내부 분포:  min 1.498 · p50 2.772 · 1.5px 미만 비율 2.2%
다른 정상 블록:  B07 min 1.503 / B12 min 2.074 / B20 min 2.406
```

임계 1.500 을 **0.002px** 차이로 스친 **사인파의 골짜기**였습니다.
모션은 사인파이므로 골짜기에서 느려지는 것이 설계상 당연합니다.

**→ 기처리 구간과 겹치는 후보는 먼저 제외한다**
**→ 이상치를 봤으면 "고칠 것"이 아니라 "측정 방식 탓인지"부터 묻는다**

### C. 안전 구간이 기존 블록을 침범

컷 간격이 넓어 안전 구간이 56.50-80.30 까지 벌어져 기존 블록 3개와 겹쳤습니다.
**→ 기존 블록 경계도 벽으로 삼는다 (§4.3)**

### D. SLIDE + PHOTO 를 한 계획으로 렌더 ★가장 값비싼 실패

63.50-80.30(16.8초)을 통째로 렌더 → **T-2 정지 5건, 광류 2.114 로 실패.**

원인 2가지가 동시에 작용했습니다.
1. 구간이 길어 진폭이 희석됨 (scale 0.35 까지 축소됨)
2. **정지가 정상인 슬라이드에까지 카메라가 움직임**

**→ SLIDE 부분(63.5-68.0)을 잘라내고 PHOTO 부분(70.5-80.3, 9.8초)만 재렌더**
**→ 결과: T-2 5건 → 1건, 광류 2.114 → 2.464 로 개선**

---

## 7. 엔진 상수 (변경 시 전체 재검증 필요)

```python
FPS            = 30
ZOOM_RATE      = 0.009      # 초당 확대율
ZOOM_SPAN_MAX  = 0.075      # 총 확대 상한
PAN_RATE       = 0.0055     # 초당 팬 이동률
PAN_SPAN_MAX   = 0.030      # 총 팬 상한
BREATH_AMP     = 0.0022     # 호흡 진폭
BREATH_HZ      = 0.09       # 호흡 주기
SHORT_DUR      = 4.0        # 이보다 짧으면 짧은 컷 처리

MIN_EDGE_KEEP  = 0.90       # T-1 잘림 게이트
FREEZE_TOL     = 0.35       # T-2 정지 임계 (화소차)
FREEZE_FPS     = 5.0        # T-2 측정 프레임레이트
MAX_SHIFT_RATE = 16.0       # T-3 급변 게이트 (px/s)
LOW_THR        = 1.5        # ★T-4 저모션 게이트 (2초 광류 p95, px)

LINEAR_MIX     = 0.85       # 선형 성분 비율
MIN_SCALE      = 0.30       # 진폭 축소 바닥
SAFE_ZOOM_CAP  = 1.10       # 전역 확대 상한
```

---

## 8. 롱폼 v14 최종 실측 (다음 콘텐츠의 비교 기준선)

| 항목 | 개선 전 | **개선 후** |
|---|---|---|
| 정지 화면(원본 대비) | 60.2% (37건 244초) | — |
| **저모션 PHOTO** | **8건 48.5초 (12.0%)** | **2건 5.0초 (1.2%)** ← 89.7% 감소 |
| **정본 정지 검사(PHOTO)** | — | **0건 (0.0%)** |
| 전체 광류 p50 | 3.231px | **3.489px** |
| **엔진 미적용 구간 p50** | **0.785px** | **3.291px** ← **4.19배** |
| 46초 구간 광류 p95 | 0.944px | **7.453px** ← **7.9배** |
| 길이 | 405.233초 | 405.233초 (오차 0) |
| 프레임 | 12157 | 12157 (오차 0) |
| 오디오 | -16.0 LUFS / TP -1.5 dBFS | **동일 (무접촉)** |

### 8.1 남은 2건 5.0초(1.2%) 의 정체 — 둘 다 "고칠 것"이 아니다

**(a) 225.5-227.5초 — 오탐**

이미 모션이 들어간 블록 내부입니다. 임계 1.500 을 **0.002px** 차이로 스친
사인파의 골짜기였습니다. 상세는 §6-B.

**(b) 76.0-79.0초 — 소재의 구조적 제약 (측정은 정확함)**

광류 p95 가 0.895px 로 임계 1.5px 에 미달합니다.
**"안 움직인 것"인지 "더 못 움직이는 것"인지**를 가르기 위해
**서로 다른 두 방법**으로 교차 검산했습니다.

| 방법 | 측정 해상도 | 결과 |
|---|---|---|
| Farneback 광류 p95 | 320px 폭 | **0.895px** |
| 위상상관 평행이동 | 1280px 폭 → 320 환산 | **0.897px** |

**소수점 셋째 자리까지 일치했습니다.** 측정 오류가 아니라 실제 값입니다.

그런데 **원본 해상도(1280px) 기준 실제 이동량**은 이렇습니다.

| 창 | 개선 전 | 개선 후 | 배율 |
|---|---|---|---|
| 74.0→76.0s | 1.66px | **4.50px** | 2.7배 |
| 76.0→78.0s | 1.20px | **3.08px** | 2.6배 |
| 77.0→79.0s | 0.60px | **3.18px** | **5.3배** |

**사람이 보는 1280px 화면에서는 3.08~4.50px 이동하고 있습니다.**
그럼에도 임계를 못 넘는 이유는 소재의 여백입니다.

```
좌 여백 1.9%  ·  우 여백 0.2%   →  확대 여력 5.8%
안전 최대 배율 zmax_safe = 1.0583   (전역 상한 1.10 이 아니라 소재가 정한 값)
⇒ 팬으로 이동할 수 있는 폭 자체가 물리적으로 좁다
```

**더 키우면 우측 잉크가 잘립니다(여백 0.2%).**
**잘림은 정지보다 나쁜 결함입니다.** 따라서 여기서 멈추는 것이 옳습니다.

⇒ **결론: 실질 잔여 결함 0건.** 2건 모두 조치 대상이 아닙니다.

> **여기서 얻은 규칙**: 게이트 미달을 봤을 때 곧바로 재렌더하지 마십시오.
> **① 다른 방법으로 교차 검산**해 측정이 맞는지 보고,
> **② 소재의 물리적 한계인지**를 확인하십시오.
> 한계인데 억지로 밀어붙이면 정지 대신 잘림이 생깁니다.

---

## 9. 체크리스트 (신규 콘텐츠 제작 시 그대로 사용)

```
□ STEP 1  전 구간 flat/uc 측정 → SLIDE / PHOTO / MIXED 분류표 작성
□ STEP 1  SLIDE 구간을 모션 대상에서 제외했는가
□ STEP 2  구간별로 ink bbox 를 측정했는가 (일괄 배율 사용 금지)
□ STEP 2  적용 배율 = min(1.10, 측정 안전배율) 로 계산했는가
□ STEP 3  컷 판정을 누적차(SAME_TOL 25.0)로 확정했는가
□ STEP 3  기처리 블록 경계를 벽으로 넣었는가
□ STEP 3  콘텐츠 유형이 바뀌는 지점에서 잘랐는가
□ STEP 4  구간 길이가 과도하게 길지 않은가 (길면 진폭이 희석됨)
□ STEP 5  T-1 / T-2 / T-3 / T-4 네 게이트를 전부 돌렸는가
□ STEP 5  T-2 는 정본 도구(1.5초 연속 규칙)로 판정했는가
□ STEP 5  개선 전/후를 ★같은 자로★ 재서 대조표를 만들었는가
□ STEP 5  게이트 미달 시 ★다른 방법으로 교차 검산★ 했는가 (재렌더 전에)
□ STEP 5  미달이 ★소재의 물리적 한계★ 인지 확인했는가 (한계면 수용이 옳다)
□ STEP 5  눈 검수를 차분맵 합성 이미지 1장으로 넘겼는가 (여러 장 따로 금지)
□ 최종     길이·프레임 수가 원본과 일치하는가
□ 최종     오디오 스트림이 무접촉인가 (codec/sample_rate/nb_frames/bit_rate 동일)
```

---

## 10. 다음 작업자에게 (항구 지침 · 축적용)

이 문서를 고칠 때는 **왜 고치는지를 반드시 함께 적으십시오.**
숫자만 바꾸면 다음 사람은 그 숫자의 근거를 알 수 없고, 상황이 달라졌을 때 판단할 수 없습니다.

특히 다음 4가지는 **근거 없이 바꾸지 마십시오.**

1. `kind()` 의 임계 (0.62 / 55 / 0.45 / 80) — 29구간 검증값
2. `FREEZE_TOL 0.35` 와 **1.5초 연속 규칙** — 이 둘은 세트입니다
3. `LOW_THR 1.5` — 엔진 상수에서 역산한 값 (§5.2)
4. `SAME_TOL 25.0` — 컷/요소등장 구분 기준 (§4.2)

그리고 무엇보다,

> **"검사에서 0건이 나왔다"를 "결함이 없다"로 읽지 마십시오.**
> 반드시 **"무엇을 검사 대상으로 삼았는지"** 를 함께 확인하십시오.
> 이 문서 전체가 그 한 번의 착각에서 나왔습니다.

---

## 11. 제14조 ⑬⑭항 신설 — 색상 코딩 3D 프리비즈 → Video-to-Video

> **개정 사유.** 대표님이 2026-08-22 지정하신 튜토리얼(YouTube `LomMGF7kcBU`)을 분석해
> 벤치마크급 3D 패럴랙스의 **정답 기법**을 확정했습니다. 그리고 그것을 우리 장면으로
> 실제 렌더·실제 생성까지 완주해 검증했습니다. 이 절은 그 결과를 항구 규정으로 고정합니다.
>
> 이 개정이 필요했던 이유는 단순합니다. 우리는 8개월 동안 **"AI 에게 카메라 무빙을 말로 설명"**
> 하고 있었고, 그것은 원리적으로 불가능한 일이었습니다. 문제는 프롬프트 문장력이 아니라
> **AI 에게 공간을 알려주는 채널이 없었다는 것**이었습니다.

### ⑬ 컷 내부 모션 의무 + 원근 시차 필수 (CEO-51 근거)

대표님 지시 원문:

> "맞네. **컷 안에서 움직여야 하네!** 그리고 **정지 구간이 없어요.** 맞아요. **원근 시차**도
> 그렇고요. 벽을 통과하고, 바닥 슬래브의 아랫면이 보이는 것처럼 우리도 우리의 상황에 맞게
> 표현을 했어야 해요."

따라서 모든 컷은 다음 3개를 **동시에** 만족해야 합니다.

| 요건 | 정의 | 판정 방법 |
|---|---|---|
| **컷 내부 모션** | 전환(이음새)이 아니라 **컷 한가운데**에서 화면이 변한다 | 컨택트시트에서 같은 컷의 첫/중/끝 프레임의 **구도가 다른가** |
| **원근 시차** | 근거리 물체가 원거리 물체보다 **빠르게** 이동하고, 물체가 **서로를 가린다** | 컨택트시트에서 **가려짐이 벗겨지는가**(뒤에 있던 것이 새로 드러나는가) |
| **정지 구간** | 컷 시작부에 카메라가 멈춘 구간이 있다 | `hold_frac` 0.15~0.35 (R2: 최소 29프레임) |

**금지.** 켄번즈 확대(zoompan)만으로 위 3개를 만족시킬 수 없습니다.
확대는 아핀 변환이고, 아핀 변환에는 **가려짐 변화가 원리적으로 없습니다.**

### ⑭ 색상 코딩 3D 프리비즈를 Video-to-Video 구조 레퍼런스로 쓴다

#### ⑭-1. 원리 — 무엇이 문제였고 무엇이 해법인가

AI 비디오에는 두 근본 결함이 있습니다.

1. **카메라 무빙을 제어할 수 없다** — 말로 아무리 정밀하게 써도 모델이 제멋대로 움직입니다
2. **피사체가 모핑된다** — 프레임이 진행되며 형태가 붕괴합니다

실패 사례(튜토리얼 원문 · 이 정도로 정밀하게 써도 실패합니다):

```
The camera starts behind him at eye level, then sweeps around his right side,
rising as it circles about ninety degrees, tilting down while it passes overhead,
accelerating hard through the turn, then dropping low behind his back and
settling just above the grass, tilted up.
```

**해법.** 카메라 무빙과 공간을 **말로 전달하지 않고, 3D 로 계산해서 영상으로 전달합니다.**

```
Blender (단순 도형 + 원색)  ──렌더──▶  프리비즈 mp4
                                          │  @Video1 = 구조(카메라·원근·타이밍)
실사 plate ───────────────────────────────┤  @Image1 = 질감(빛·재질·환경)
                                          ▼
                                  AI 생성기 (Video-to-Video)
                                          ▼
                            3D 가 계산한 완벽한 원근 + 실사 질감
```

**핵심.** AI 는 "상상"하지 않습니다. 3D 소프트웨어가 **계산한** 원근법과 무빙을 그대로
유지한 채 **질감만 교체**합니다. 그래서 패럴랙스가 계산으로 보장됩니다.

#### ⑭-2. 3대 규칙 (튜토리얼이 "가장 중요"라고 명시)

1. **피사체는 단순 도형으로.** 디테일 모델링은 AI 실사 생성과 **간섭**해 품질을 떨어뜨립니다.
2. **피사체는 색으로 구분한다.** 마젠타/시안/옐로/레드 등 **채도 높은 원색**.
3. **역할 구분을 확실하게.** 한 색에 두 역할을 겹치지 않습니다.

> 3번이 가장 자주 위반됩니다. "이 색이 무엇이 될지"를 프롬프트에서 1:1 로 지정해야
> AI 가 색을 역할로 읽습니다. 색이 겹치면 AI 가 두 물체를 하나로 합칩니다.

#### ⑭-3. 프리비즈 렌더 설정 — 반드시 이 값으로 (실측 근거)

프리비즈는 **발광(emission) 원색**이므로 **광선 추적이 필요 없습니다.** 따라서:

```python
S.render.engine = 'CYCLES'; S.cycles.device = 'CPU'
S.cycles.samples = 1              # 발광이므로 1샘플로 충분
S.cycles.max_bounces = 0          # 반사·굴절 없음
S.cycles.use_denoising = False    # 노이즈가 없으므로 불필요
S.render.resolution_x = 832; S.render.resolution_y = 468
```

**실측 속도 계층 (같은 샌드박스 · 2코어 · RAM 3GB · GPU 없음):**

| 설정 | 속도 | 판정 |
|---|---|---|
| 1920x1080 / 48spp / 일반 씬 | 115초 초과 (타임아웃) | ✗ |
| 1280x720 / 8spp / 발광 | **10.3 s/frame** (500초 = 34시간) | ✗ |
| **832x468 / 1spp / 발광** | **0.196 s/frame** (500초 = 약 40분) | **✓ 채택** |

**교훈.** 같은 엔진(Cycles CPU)인데 **52배** 차이가 납니다.
"Cycles 는 느리다"가 아니라 **"프리비즈에 불필요한 설정을 켜두면 느리다"** 였습니다.

**EEVEE 는 이 환경에서 불가합니다.** `EGL_BAD_MATCH` (GPU 없음) → llvmpipe 재시도 → OOM(3GB).
따라서 Cycles CPU + 위 설정이 **유일한 성립 경로**입니다.

#### ⑭-4. 프리비즈 세트 규칙 — 검은 구멍 방지

카메라가 배경 밖을 보면 프레임에 **검은 영역**이 생기고, AI 는 그것을 그대로 검게 재현합니다.

```
세트는 카메라 궤적보다 넉넉하게 세운다.
  바닥  16 x 12 m       (카메라 최대 반경의 2배 이상)
  벽    후면 + 좌 + 우, 높이 4.4 m
```

우리 v1 프리비즈에서 프레임 1·48 상단에 검은 구멍이 났고, 세트를 위 크기로 키워 해소했습니다.

#### ⑭-5. Video-to-Video 프롬프트 골격 (이 구조를 그대로 씁니다)

```
[1행] 실사 톤 + 장면 + 촬영 스타일. 한 컷 연속.

@Video1 is the CAMERA AND SPATIAL reference: follow its camera path, framing,
perspective and timing 1:1 frame for frame. Its coloured flat planes are position
markers for real objects, and its small red blocks are position markers for
glowing highlighted words.

@Image1 is the environment, lighting and texture reference: <plate 설명>.

Color-based proxy replacement:
  the magenta plane becomes <역할1>,
  the cyan plane becomes <역할2>,
  the yellow plane becomes <역할3>,
  the dark plane becomes <바닥>,
  the brown walls become <배경>,
  the red cylinder becomes <소품>,
  the three small red blocks become <강조 요소 + 그 움직임의 의미>.

[카메라 서술] The camera holds still at the start, then <무빙>, keeping true 3D
perspective parallax: the near <A> slides across frame faster than the far <B>,
and the <A>s occlude each other as the camera passes.

No text overlays, no captions, no subtitles.
```

**필수 5요소.** ① `@Video1 = CAMERA AND SPATIAL reference` 명시 ② `1:1 frame for frame`
③ `Color-based proxy replacement` 로 색→역할 1:1 매핑 ④ `No text overlays` (자막은 우리가 굽습니다)
⑤ **★`NOT a marker` 선언★** — 프리비즈 안에 있는 것 중 **교체 대상이 아니라 최종물**인 것
(한글 글자·로고·수치)은 반드시 아래 문장으로 못박습니다. 빠뜨리면 AI 가 임의로 채웁니다(교훈 165).

```
The RED KOREAN TEXT visible in @Video1 is NOT a marker — it is the actual final text
and must be reproduced EXACTLY as the identical Korean word, same glyphs, same position,
same rotation, same scale, frame for frame.
Preserve the Korean characters exactly as shown in @Video1 — do not translate,
do not substitute Latin letters, do not invent new words.
```

#### ⑭-6. 실측 검증 기록 (2026-08-22)

| 단계 | 도구 | 결과 |
|---|---|---|
| 프리비즈 렌더 | bpy 5.2.0 Cycles CPU | 72프레임 14.1초 (**0.195 s/frame**) |
| 프리비즈 인코딩 | ffmpeg libx264 | 832x468 24fps 3.0초 |
| Video-to-Video | `fal-ai/bytedance/seedance-2.0` `reference_mode=true` `tier=fast` | 4.04초 / 1280x720 / 24fps **성공** |
| 판정 | 컨택트시트 직독 | 붉은 강조어 3개가 문서에서 떠올라 **1개로 수렴** + 카메라 스윕에 따른 **진짜 원근 시차** + 뒤 창문이 **새로 드러남** |

CEO-51 지시 원문 — *"세 프로젝트 문서에서 같은 역할 단어만 붉게 떠올라 한 곳으로 모이는 것이
화면에서 실제로 일어나야 했네요"* — **화면에서 실제로 일어났음을 확인.**

**남은 결함 (정직 보고).** 붉은 글자가 영문이고 오철자(`Report` / `Renes`)입니다.
프리비즈 도형이 **"무엇이 될지"는 통제됐지만 "무슨 글자냐"는 통제되지 않았습니다.**
⇒ 처방: 프리비즈 마커에 **실제 한글 텍스처를 UV 로 붙여** 글자 형태 자체를 구조로 전달합니다.

#### ⑭-7. 한글 글자 마커 — 정답 구현 (2026-08-22 실증 · 위 결함 해소)

⑭-6 의 오철자 결함을 **완전히 제거**한 구현입니다. 앞으로 모든 프리비즈에 이 방식을 씁니다.

**(a) 글자 PNG 는 반드시 RGBA · 배경 알파 0.**
불투명 검은 배경 PNG 를 그대로 emission 으로 붙이면 화면에 **검은 카드**가 나타나고,
AI 는 그것을 "검은 판"으로 해석합니다(4세대 pv4 에서 실제 발생).

**(b) 머티리얼 — 알파가 MixShader 를 구동해야 합니다.**

```python
def texflat(name, path):
    """emission material driven by an image texture -> the GLYPH becomes structure."""
    m = bpy.data.materials.new(name); m.use_nodes = True
    nt = m.node_tree; nt.nodes.clear()
    tc = nt.nodes.new('ShaderNodeTexCoord')
    tx = nt.nodes.new('ShaderNodeTexImage')
    tx.image = bpy.data.images.load(path)
    tx.interpolation = 'Closest'      # 글자 경계를 뭉개지 않는다
    tx.extension = 'EXTEND'
    e  = nt.nodes.new('ShaderNodeEmission'); e.inputs[1].default_value = 1.0
    tr = nt.nodes.new('ShaderNodeBsdfTransparent')
    mx = nt.nodes.new('ShaderNodeMixShader')
    o  = nt.nodes.new('ShaderNodeOutputMaterial')
    nt.links.new(tc.outputs['UV'],    tx.inputs['Vector'])
    nt.links.new(tx.outputs['Color'], e.inputs[0])
    nt.links.new(tx.outputs['Alpha'], mx.inputs[0])   # ★핵심: 알파가 믹스를 구동★
    nt.links.new(tr.outputs[0], mx.inputs[1])         # 0 -> 투명
    nt.links.new(e.outputs[0],  mx.inputs[2])         # 1 -> 발광 글자
    nt.links.new(mx.outputs[0], o.inputs[0])
    m.blend_method = 'BLEND'
    return m

def add_tex(name, loc, sc, path):
    bpy.ops.mesh.primitive_plane_add(size=2, location=loc)
    ob = bpy.context.object; ob.scale = sc; ob.name = name
    ob.data.materials.append(texflat(name, path))
    return ob
```

**(c) 배치 규칙 (실측으로 확정).**

| 항목 | 값 | 근거 |
|---|---|---|
| 형상 | 큐브 아님 · **plane** | 글자는 면이지 덩어리가 아님 |
| z 오프셋 | **>= +0.032** | +0.012 는 문서에 파묻힘(pv4 실측) |
| 부상 회전 | `rotation_euler = (v * 1.15, 0, 0)` | 누우면 카메라에서 안 읽힘 |
| 스케일 | `(0.30*(1+0.9v), 0.15*(1+0.9v), 1.0)` | 2:1 글자 비율 유지 |

**(d) 프롬프트에 ⑭-5 의 `NOT a marker` 선언을 반드시 병행합니다.**
구조(UV 텍스처)와 선언(프롬프트) **둘 다** 있어야 합니다. 하나만으로는 해소되지 않습니다.

**검증 (2026-08-22).**

| 항목 | BEFORE (pv3 · 빈 붉은 큐브) | AFTER (pv5 · 한글 알파 텍스처) |
|---|---|---|
| 붉은 글자 내용 | `Report` / `Renes` (오철자 영문) | **「조율자」 (정확한 한글)** |
| 검은 카드 | — | 없음 |
| 렌더 속도 | 0.195 s/frame | **0.207 s/frame** (사실상 불변) |
| v2v 결과 | 4.041667s / 1280x720 | 4.041667s / 1280x720 |

> 텍스처를 붙여도 렌더 속도는 **거의 그대로**입니다. 글자 통제는 **공짜**입니다.

---

## 12. 교훈 157~162 (축적)

### 교훈 157 — 도구 애그리게이터는 우리가 쓰는 모델의 래퍼다

대표님이 지목하신 서비스(bbanana.ai)를 직독한 결과, Seedance / Kling / Veo / Nano Banana 등
**우리가 이미 접근 가능한 모델의 한국형 래퍼**였습니다. 독자 3D 엔진은 없습니다.
"건축 미스터리" 같은 장르 템플릿은 **프롬프트·워크플로 프리셋**입니다.

> **우리에게 없던 것은 도구가 아니라 그 도구를 쓰는 방법이었습니다.**

### 교훈 158 — 광학 흐름은 품질 판정 기준이 아니다

대표님이 **"정답"으로 주신** 벤치마크 샘플을 우리 클립과 같은 자로 재봤습니다.

| clip | mean flow | affine resid | still% |
|---|---|---|---|
| **대표님 정답 샘플** | **0.140** | 1.083 | **90.0%** |
| 우리 A6-01 | 1.338 | 0.552 | 20.0% |
| 우리 층분리 L2 | 2.357 | 0.439 | 8.5% |

대표님이 정답이라 하신 것이 두 지표 모두 우리보다 **"나쁩니다"**.

> **흐름 크기·정지 비율은 품질을 판정하지 못합니다.
> 무엇이 어떻게 움직이는지(서사·구조)가 판정합니다.**

⇒ 광학 흐름은 **진단 보조**로 강등. 합격 게이트로 쓰지 않습니다.
⇒ 남는 정본은 **대표님의 눈** + **컨택트시트 직독**.

### 교훈 159 — 깊이비례 이동은 매끄러운 깊이맵에서 사실상 아핀이다

측면 이동 강도를 키울수록 아핀 잔차가 **오히려 떨어졌습니다**(0.571 → 0.396).
깊이맵이 매끄러운 경사면이면 "깊이 × 이동"이 좌표의 매끄러운 함수 = 아핀으로 거의 설명됩니다.

> **진짜 시차는 깊이의 불연속(물체 경계)에서만 나옵니다.**

⇒ 처방: 깊이를 n 단계로 **양자화해 층으로 쪼개고**, 층별 상수 z 로 이동시켜 **뒤→앞 알파 합성**.
경계에서 이동이 끊기고, 가려짐이 벗겨지고, 뒤 층이 드러납니다.
비용: 프레임당 0.71~0.94초 (평면 워프 0.08초의 9~12배).

### 교훈 160 — 색상 코딩 3D 프리비즈 → Video-to-Video 가 정답이다

§11 ⑭ 전문. 요약하면 **"카메라 무빙을 말로 전달하지 말고 3D 로 계산해서 영상으로 전달한다."**

### 교훈 161 — 프리비즈는 Cycles 1샘플이면 프레임당 0.2초다

발광 원색은 광선 추적이 필요 없습니다. `samples=1 / max_bounces=0 / denoise OFF / 832x468`
⇒ **0.196 s/frame**. 같은 엔진의 기본 설정 대비 **52배** 빠릅니다.
GPU 없는 2코어·3GB 환경에서도 500초 전량 프리비즈가 **약 40분**에 끝납니다.

### 교훈 162 — 프리비즈는 "무엇이 될지"는 통제하지만 "무슨 글자냐"는 통제하지 못한다

색→역할 매핑은 완벽히 작동했습니다(마젠타→문서, 레드→강조어). 그러나 강조어의 **글자 내용**은
AI 가 임의로 채워 오철자 영문이 나왔습니다.

> **구조로 전달되지 않은 것은 통제되지 않습니다.**
> 글자를 통제하려면 글자를 **프리비즈 안에 텍스처로 넣어** 구조로 만들어야 합니다.

### 교훈 163 — 프리비즈 세트는 카메라 궤적보다 넉넉해야 한다

**증상.** 1세대 프리비즈(pv1)의 프레임 1·48 상단에 **검은 구멍**이 생겼습니다.
카메라가 배경 지오메트리 밖을 봤기 때문입니다.
그리고 **AI 는 그 검은 영역을 "검은 것"으로 충실히 재현합니다** — 구조 레퍼런스이므로 당연합니다.

**처방.**

| 항목 | 값 | 근거 |
|---|---|---|
| 바닥 | 16 x 12 m | 카메라 최대 반경(8.4 m)의 **2배 이상** |
| 벽 | 후면·좌·우 3면 · 높이 4.4 m | 카메라 최대 높이(4.2 m)보다 높게 |

**검증.** 2·3세대(pv2/pv3)에서 검은 구멍 완전 소멸.

> 프리비즈에서 "안 보일 것 같은 곳"은 반드시 보입니다. 세트는 항상 과하게 짓습니다.

### 교훈 164 — ffmpeg drawtext 의 `text=` 는 콜론을 못 받는다

**증상.** 3안 A/B 조립기(ab3.py) 1차 실행이 **exit status 234** 로 죽었습니다.

```
drawtext=...:text='판정: 컷 안 모션 없음':...   <- 이 한 글자가 원인
```

**원인.** drawtext 필터 파서는 `:` 를 **옵션 구분자**로 읽습니다. 따옴표 안이어도 마찬가지입니다.
`#RRGGBB` 의 샵도 셸·파서를 흔듭니다.

**처방 (3중).**

1. `text=` 대신 **`textfile=<경로>`** — 파일에서 읽으면 콜론이 안전합니다.
2. 본문에서 `판정:` → `판정 -` 로 교체 (이중 안전장치)
3. `fontcolor` 는 `#FFD54A` 가 아니라 **`0xFFD54A`**

**검증.** `AB3_A6.mp4` 10.000초 · 한글 3층 캡션 정상 렌더.

> 한글 자막을 ffmpeg 로 구울 때는 **항상 textfile 방식**을 씁니다. 이것을 기본값으로 규정합니다.

### 교훈 165 — 구조로 전달하되, 그것이 "교체 대상"인지 "최종물"인지도 전달해야 한다

**교훈 162 의 완결편입니다.**

교훈 162 에서 우리는 *"구조로 전달되지 않은 것은 통제되지 않는다"* 를 배웠습니다.
그래서 한글 글자를 UV 텍스처로 프리비즈에 심었습니다 — **구조로 전달했습니다.**
그런데도 위험이 남았습니다. 왜냐하면:

```
프리비즈에 들어간 것은 AI 에게 기본적으로 전부 "프록시(교체 대상)" 로 해석된다.
그것이 색깔 평면이든, 원기둥이든, 한글 글자든 마찬가지다.
```

색상 코딩 프리비즈의 **전제 자체가 "이 도형들은 진짜가 아니다, 바꿔라"** 이기 때문입니다.
따라서 글자를 아무리 정확히 그려 넣어도, AI 는 그것마저 "무언가로 바꿔야 할 표식"으로 읽습니다.

**처방 — 프롬프트에서 예외를 명시적으로 선언합니다.**

```
The RED KOREAN TEXT visible in @Video1 is NOT a marker — it is the actual final text
and must be reproduced EXACTLY as the identical Korean word, same glyphs, same position,
same rotation, same scale, frame for frame.
Preserve the Korean characters exactly as shown in @Video1 — do not translate,
do not substitute Latin letters, do not invent new words.
```

**검증.** BEFORE `Report` / `Renes` (오철자 영문) → AFTER **「조율자」** (정확한 한글).
동일 모델·동일 설정·동일 plate. 바뀐 것은 **프리비즈의 글자 텍스처 + 이 두 문장**뿐입니다.

**일반화.**

| 프리비즈 안의 요소 | AI 에게 무엇인가 | 프롬프트 처리 |
|---|---|---|
| 색깔 평면·원기둥·큐브 | **프록시** — 실물로 교체 | `the magenta plane becomes …` |
| 한글 글자·로고·수치 | **최종물** — 그대로 보존 | `… is NOT a marker — it is the actual final …` |

> 구조로 전달하는 것만으로는 부족합니다.
> **"이것은 바꿔라" 와 "이것은 그대로 둬라" 를 갈라서 말해야** 통제가 완성됩니다.

### 교훈 166 — GenTeam 에이전트는 본문의 `@이름` 텍스트로는 깨어나지 않는다

**증상.** 전담 채널 `previz-3d-pipeline` 에 교본과 과업을 2회 발송했고 둘 다 `status: ok` 였는데,
**에이전트 회신이 하나도 오지 않았습니다.** 발송 실패가 아니었습니다.

**원인.** 채널을 폴링해 보니 제 메시지 바로 뒤에 시스템 메시지가 붙어 있었습니다.

```json
{"kind": "system_message",
 "content": "The agents here reply only when @mentioned. @ a teammate to get an answer.",
 "metadata": {"digital_employee": {"event": "agents_mentions_only", "importance": "high"}}}
```

본문에 `@3D Motion Director …` 라고 **텍스트로** 썼지만, 그것은 멘션으로 파싱되지 않습니다.
메시지는 채널에 남았지만 **에이전트를 깨우는 이벤트가 발생하지 않았습니다.**

**처방.** `gsk genteam send` 의 **`--mentions` 플래그**로 agent_id 를 넘깁니다.

```bash
gsk genteam send --args-file order.json \
  --mentions agent_pt2dn9rvtxrc agent_fmzqbh39p4k3 agent_4gettwpxdxp6
```

- 여러 명을 **공백으로 나열**할 수 있습니다(멤버 추가와 달리 1명씩 나눌 필요 없음).
- 응답의 `data.mentions` 배열에 3명이 들어갔는지로 확인합니다.
- 캐노니컬 토큰 `@[name](id)` 은 **자동으로 본문 앞에 붙습니다.** 직접 쓰지 마십시오.
- 대상은 **이미 채널 멤버**여야 합니다.

**검증.** 재발주 mid `3044026` · `mentions` 3명 인식 · `status ok`.

> 발송 성공(`status: ok`)은 **도달**의 증거일 뿐 **발동**의 증거가 아닙니다.
> 팀에 일을 시킨 뒤에는 반드시 **채널을 폴링해 시스템 메시지를 확인**합니다.
> 경영자가 "지시했다"고 믿는 것과 "지시가 전달됐다"는 것은 다릅니다.

---

## 교훈 167 — 한 컷을 위해 쓴 제스처를 전량에 무조건 적용하면 안 된다

### 증상

승인받은 프리비즈 `pv5` 를 배치 렌더러로 일반화해 60잡 전량에 적용하고,
그중 1컷(`J_A3-02`)만 시범으로 v2v 를 돌려 컨택트시트로 직독했습니다.

| 항목 | 판정 |
|---|---|
| 질감·조명 (실사 사무실) | ✅ 성공 |
| 원근·시차 (3D 가 계산한 경로) | ✅ 성공 |
| 정지 구간 (0~60프레임 완전 동일) | ✅ 성공 |
| **글자** | ❌ **붉은 낙서로 뭉개짐** |

### 원인 — 서사가 다른 컷에 같은 제스처를 썼다

`pv5` 의 제스처는 **「세 글자가 한 점으로 수렴」** 이었습니다.
그것이 옳았던 이유는 그 컷의 대사가 이것이었기 때문입니다.

> "나는 어떤 역할을 반복해 왔는가 — **세 프로젝트에서 같은 역할이 반복된다**"

세 개의 문서에 **같은 단어**가 있고, 그것이 **한 곳으로 모이는 것**이 곧 그 문장의 뜻입니다.
[CEO-51] 이 지목한 바로 그 장면입니다.

그런데 이 제스처를 60잡 전체에 무조건 적용하니, 서사가 전혀 다른 컷에서도
글자 평면 3장이 같은 좌표로 이동했습니다. 세 장이 한 점에 겹쳐 쌓이면
생성기 입장에서는 그것이 **글자가 아니라 붉은 덩어리**입니다. 그래서 낙서로 렌더했습니다.

### 처방 — 제스처를 컷의 대사에서 유도한다

제스처는 연출자가 고르는 것이 아니라 **그 컷이 무슨 말을 하는지에서 나옵니다.**

```python
CONVERGE_KEYS = ("반복", "세 프로젝트", "3개 프로젝트", "같은 역할", "공통")
NONE_KEYS     = ("새 정보 금지", "읽히는 글자 금지")

def gesture_of(row_src, cam_row):
    if cam_row["seam_id"].startswith("A8"):          # 리포트 지면 구간
        return "none"
    if any(k in (row_src.get("protect") or "") for k in NONE_KEYS):
        return "none"
    blob = " ".join([cam_row["narr_line"], cam_row["objects"], cam_row["motion_reason"]])
    if any(k in blob for k in CONVERGE_KEYS):
        return "converge"
    return "lift"
```

| 제스처 | 조건 | 화면에서 일어나는 일 | 실측 분포 |
|---|---|---|---|
| `converge` | 대사에 "반복 / 세 프로젝트 / 같은 역할 / 공통" | 글자 **3개**가 한 점으로 수렴 | **5잡** |
| `none` | `protect` 에 "새 정보 금지 / 읽히는 글자 금지", 또는 ACT8 | 글자 **없음** | **9잡** |
| `lift` | 기본값 | 글자 **1개**가 문서 한 장 위에서 제자리 부상 | **46잡** |

**검증**: CEO 가 승인한 A6-02 컷이 이 규칙에서 정확히 `converge` 로 분류되었습니다.
즉 규칙은 승인본을 반려하지 않습니다(교훈 131 준수).

### 부수 교정 — 스케일과 배율

세 장이 겹칠 때 덩어리가 되는 것을 막기 위해 글자 평면을 줄였습니다.

| 항목 | 이전 | 이후 |
|---|---|---|
| 글자 평면 스케일 | 0.30 / 0.15 | **0.26 / 0.13** |
| 부상 배율 (converge) | 0.9 | 0.9 (유지) |
| 부상 배율 (lift) | — | **0.35** (제자리 부상이므로 작게) |

### 프롬프트도 함께 갈라야 한다

프리비즈에 글자가 1개인데 프롬프트가 "three panels ... the RED KOREAN TEXT" 라고
말하면, 생성기는 **없는 두 개를 만들어 냅니다.** 그래서 프롬프트도 3분기했습니다.

```
converge → "There are THREE separate copies of 「조율자」 ... they must never
            merge, overlap or smear into a blob."
lift     → "There is exactly ONE copy of 「방식」 ... do not duplicate it onto
            the other documents, do not add any second word."
none     → "There is NO text of any kind in this shot."
```

게이트로 강제합니다 — `converge` 프롬프트에 one-copy 절이 섞이거나
`lift` 프롬프트에 three-copy 절이 섞이면 `PROMPT GATE FAILED` 로 정지합니다.

> **구조(프리비즈)와 언어(프롬프트)가 같은 것을 말해야 합니다.**
> 하나는 글자 1개를 보여주고 다른 하나는 3개를 요구하면, 생성기는 그 모순을
> 자기 방식으로 메꿉니다. 그 결과가 낙서였습니다.

---

## 교훈 168 — 시선 목표는 제스처마다 달라야 한다

### 증상

같은 시범 컷의 후반에서 **문서가 화면 아래로 흘러넘쳤습니다.**
카메라가 책상이 아니라 그 위의 빈 공간을 올려다보고 있었습니다.

### 원인 — 전 컷에 같은 시선 종점을 줬다

`camtab.py` 가 80컷 전부에 동일한 값을 넣고 있었습니다.

```python
tgt_start_xyz = [0.0, -0.55, 0.15]
tgt_end_xyz   = [0.0, -0.35, 0.77]     # ← 전 컷 동일
```

`z = 0.77` 이 옳았던 이유는 `pv5` 의 글자가 `z = 0.92` 까지 올라가기 때문입니다.
**글자를 따라 올려다보는 것**이 그 컷의 연출이었습니다.

그러나 `lift` 컷의 글자는 `+0.34` 만 뜨고, `none` 컷에는 글자가 아예 없습니다.
그런 컷에서 `0.77` 을 올려다보면 카메라는 **아무것도 없는 허공**을 향하고,
정작 보여줘야 할 문서는 프레임 밖으로 밀려납니다.

### 처방 — 시선 종점을 제스처에서 유도한다

```python
TGT_END = {
    "converge": [0.0, -0.35, 0.77],   # 글자가 z=0.92 로 모인다 → 함께 올려다본다
    "lift":     [0.0, -0.45, 0.30],   # 글자가 0.34 만 뜬다 → 지면에 머문다
    "none":     [0.0, -0.50, 0.20],   # 글자가 없다 → 책상면을 지킨다
}
```

게이트로 강제합니다 — `tgt_end_xyz != TGT_END[gesture]` 이면 `PLAN GATE FAILED`.

프롬프트에도 프레임 종착지를 명시합니다.

```
lift/none → "The shot ends still looking down at the desk surface; the documents
             remain fully inside the frame at all times and never slide out of
             the bottom."
converge  → "The shot ends looking slightly up, following the words above the
             desk; the documents stay inside the frame."
```

> **카메라가 무엇을 향하는지는 그 컷에 무엇이 있는지에 종속됩니다.**
> 대상이 올라가지 않는데 시선만 올리면, 화면에는 대상이 사라진 빈 하늘이 남습니다.

---

## 교훈 169 — 유료 대량 착수 전 "1컷 시범 + 컨택트시트 직독" 은 규칙이다

이번에 실제로 아낀 금액을 기록합니다.

| 만약 시범 없이 진행했다면 | 실제 |
|---|---|
| 프리비즈 60잡 전량 렌더 (29분) → **전량 폐기** | 33잡에서 중단 |
| v2v **61회 호출 · 약 383 billed seconds** → **전량 폐기** | **1회 호출 · 9초** |
| 재작업까지 포함해 사실상 2배 소모 | 결함 2건을 9초로 적발 |

**규칙으로 승격합니다.**

1. 파이프라인을 수정한 뒤에는 **반드시 1컷만** 유료 호출한다.
2. 결과물을 **컨택트시트로 직독**한다(무료·최상위 결함 발견 수단).
3. 합격하기 전에는 **두 번째 유료 호출을 하지 않는다.**
4. 시범 컷은 **가장 앞 컷**을 쓴다 — 결함이 있으면 관객이 가장 먼저 보는 곳이다.

> 시범은 비용이 아니라 **보험**입니다.
> 9초로 383초를 지켰습니다.

---

## 교훈 170 — 글자 저품질의 원인은 텍스처 해상도가 아니라 「화면 점유율」이다

대표님이 세 번 지적하신 "글자만 저품질" 의 원인을 처음에는 텍스처 해상도로
추정했습니다. 틀렸습니다. **실측이 원인을 뒤집었습니다.**

```
J_A4-03 첫 프레임 글자 bbox = 66 x 30 px   (화면 폭의 8%)
J_A4-03 마지막 프레임 붉은 픽셀 = 0
```

512px 텍스처든 4096px 텍스처든, 화면에서 66px 로 표시되면 66px 짜리 글자입니다.
생성 AI 는 **획을 추적(trace)** 할 수 없어 **글자를 발명(invent)** 하고, 그 결과가
낙서입니다.

**픽셀 예산이라는 개념으로 재설계했습니다.**

| 항목 | 값 | 근거 |
|---|---|---|
| 텍스처 폭 | 2048 px | 2600pt 슈퍼샘플 → 잉크 bbox 크롭 → LANCZOS |
| 렌더 해상도 | 1280x720 | v2v 출력과 **동일** (업스케일 금지) |
| 샘플 | 4 | 1.373 s/f (s8 = 2.613 s/f 는 6.1시간 → 기각) |
| 잉크 하한 | 210 px | 기각본 66px 의 3.2배 · 1280px 의 16% |
| 실측 결과 | **237~435 px** | 하한을 전 잡에서 상회 |

> **글자 품질은 텍스처가 아니라 「화면에서 몇 픽셀인가」로 결정됩니다.**
> 이 값을 게이트로 만들지 않으면 매번 같은 실패를 반복합니다.

---

## 교훈 171 — `arc_deg` 는 크기(magnitude)다. 부호는 다른 곳에 있다

카메라 궤적표(`camtab.py`)는 회전 방향을 이렇게 다뤘습니다.

```python
sign = 1.0 if idx % 2 == 0 else -1.0   # 부호는 cam_end_xyz 에만 반영
arc_deg = abs(...)                      # arc_deg 에는 절대값만 저장
```

렌더러가 `arc_deg` 를 그대로 회전량으로 쓰자 **절반이 반대로 돌았습니다.**

```
60잡 중 27잡(45%) 이 반대 방향으로 회전 → 문서를 지나쳐 맨 벽만 촬영
```

**처방 — 종점에서 부호를 역산하고, 게이트로 대조한다.**

```python
a1 = math.atan2(b[0], -b[1])
arc = a1 - a0
while arc > math.pi:  arc -= 2*math.pi     # 최단 부호 경로
while arc < -math.pi: arc += 2*math.pi
if abs(math.degrees(arc)) - abs(job["arc_deg"]) > 1.0:
    raise SystemExit("ARC GATE FAILED ...")
```

**렌더로 실증했습니다 (J_A4-03).**

| 프레임 | 붉은 픽셀 | 글자 bbox |
|---|---|---|
| 첫 | 17,852 | 418 x 200 |
| 중간 | 19,724 | 418 x 158 |
| **마지막** | **35,444** (이전 **0**) | **582 x 282** (이전 66x30, **8.8배**) |

> **크기와 방향을 같은 필드에 담지 마십시오.**
> 담았다면, 두 곳이 일치하는지 검사하는 게이트를 반드시 두십시오.

---

## 교훈 172 — 「정확한 글자」와 「이미지 수준의 글자」는 다른 문제다

잉크를 435px 까지 키워 **정확성(축 A)** 을 확보한 뒤에도 대표님은 다시
지적하셨습니다.

> "글자가 정확한 것 뿐만 아니라 **시각적으로도 이미지 수준**이 되어야 한다"

**품질에는 두 축이 있습니다.**

| 축 | 정의 | 측정 수단 |
|---|---|---|
| **A 정확성** | 올바른 한글로, 충분한 픽셀로 읽힌다 | 잉크 px 게이트 (산술·무료) |
| **B 시각 등급** | **이미지 본체와 같은 사진 품질** | **확대 직독 (육안·무료)** |

축 B 는 산술로 잡히지 않습니다. **확대해서 눈으로 보는 것만이 정본입니다.**
그 직독으로 세 결함을 적발하고 처방했습니다.

| 결함 | 증상 | 처방 |
|---|---|---|
| ① 종이 넘침 | 「변화」가 **「벼하」** 로 읽힘 | `PAPER_FIT` 클램프 |
| ② 에지 지글거림 | 획 경계에 빨강/청록 잡픽셀 | 알파 `GREATER_THAN` 이진화 |
| ③ 스티커 부유 | 종이 위에 떠 보임 | 정지 구간 pitch/yaw = 0 |

> **잉크를 키우는 것으로는 축 B 가 해결되지 않습니다.**
> 각 결함마다 별도의 제어 변수를 두어야 합니다.

---

## 교훈 173 — 슬롯 오프셋은 월드축이 아니라 「카메라 화면축」으로 주어야 한다

단어 여러 개를 나란히 배치할 때 월드 X축으로 밀었습니다.

```python
CLUS = [(-1.0,-0.05,-0.16), (0.0,0.0,0.14), (1.0,0.05,-0.16)]   # 월드 X
```

**카메라에 가까운 쪽 단어의 화면 각도가 커져 프레임을 넘습니다.**

```
J_A3-07 frame 96 slot +1 : ndcX +0.832 + 반폭 0.20 = edge 1.05  *** 잘림 ***
```

여기서 중요한 오독이 있었습니다. 컨택트시트의 `clusters~5` 를 저는 "잔여 겹침"
으로 읽었으나, 실제로는 **잘린 조각이 별개 덩어리로 세어진 것**이었습니다.

**처방 — 스칼라 슬롯 + 카메라 up 축 배치.**

```python
CLUS = [1.0, 0.0, -1.0]                       # 스칼라
fwd = normalize(tgt - cam)
rgt = normalize((fwd.y, -fwd.x, 0))
upv = rgt × fwd
off = upv * slot * gap_m
gap_m = d_ref * SENSOR/34 * (RES[1]/RES[0]) * CLUS_VFRAC   # 프레임 「높이」 비율
```

**부수 효과가 본질이었습니다.** 세로 스택이므로 대표님이 요구하신
"같은 역할 단어만 붉게 떠올라 **한 곳으로 모이는**" 서사가 화면에서
실제로 성립합니다.

> **레이아웃을 월드 좌표로 표현하면 카메라가 움직일 때 반드시 깨집니다.**
> 화면에서 지켜야 할 것은 화면 좌표로 표현하십시오.

---

## 교훈 174 — 데이터의 라운드로빈 배정이 결함의 근원일 수 있다

글자를 실을 문서를 이렇게 배정했습니다.

```python
doc = "ABC"[order.index(a) % 3] if g == "lift" else ""
```

공평해 보이지만, **카메라 스윕이 외곽 문서를 화면 밖으로 밀어냅니다.**

```
51 glyph 잡 중 30잡(59%) edge > 1.0   최악 4.104
```

**처방 — 중앙 문서 고정.**

```python
doc = "B" if g == "lift" else ""
```

| 항목 | 라운드로빈 | 중앙 고정 |
|---|---|---|
| 프레임 밖 잘림 | **30 / 51** | **0 / 46** |
| 최악 edge | 4.104 | **0.520** (여유 48%) |
| 최악 잉크 | — | **237 px** |

`(slot, ink_frac)` 격자 탐색으로 **잉크를 깎지 않고** 전부 해결됨을 확인한 뒤,
렌더러가 아니라 **데이터 생성기(`plan.py`)** 를 고쳤습니다.

> **시범 1컷에서 본 결함은 대개 전량의 절반에 퍼져 있습니다.**
> 증상을 렌더러에서 가리지 말고, 원인을 데이터에서 고치십시오.

---

## 교훈 175 — 프레임 밖 문서 위의 글자는 어떤 크기 조절로도 살릴 수 없다

수렴(converge) 컷 2건이 계속 실패했습니다. 시도한 처방을 **전부 무료로 기각**했습니다.

| 시도 | 결과 | 판정 |
|---|---|---|
| 파라미터 격자 16조합 | edge 1.078 → 1.031 | **전부 실패** — 파라미터가 원인이 아니다 |
| 정지/수렴 잉크 예산 분리 5조합 | off-frame 51~64건 잔존 | 실패 |
| 프레임 안으로 클램프 | off-frame 0 · 겹침 0 · **잉크 137px** | **기각 — 축 A 희생** |

세 번째가 특히 중요합니다. **잘림은 사라지지만 글자가 137px 로 줄어듭니다.**
축 B 를 위해 축 A 를 희생하는 것은 처방이 아닙니다.

**정답 — 화면 안에 있는 문서만 수렴에 참여시킨다.**

```python
def visible_docs(job, wm, limit=0.94):
    """정지(hold) 프레임 기준으로 화면 안에 온전히 들어오는 문서만 반환.
    첫 프레임이 이미 자르는 시트는 어떻게 해도 온전한 단어를 못 보여준다."""
    ...
    if len(keep) < 2:
        raise SystemExit("CONVERGE GATE FAILED ...")
    return keep

slot = (n_vis - 1) / 2.0 - k     # 남은 문서를 중심 기준 균등 배치
```

```
J_A3-07[0,1,2]  J_A4-08[0,1,2]  J_A5-03[1,2]  J_A6-02[0,1,2]  J_A7-03[1,2]
off-frame 30건 → 0건 (edge 0.996)   전 컷 2~3장 유지
```

> **화면 밖에 있는 것은 크기를 조절할 대상이 아니라, 제외할 대상입니다.**

---

## 교훈 176 — 게이트를 별도 파일로 복제하면 반드시 코드와 어긋난다

검증을 빠르게 하려고 `/tmp/preflight.py` 에 기하 계산을 **손으로 복제**했습니다.
상수 하나를 바꿀 때마다 두 곳을 고쳐야 했고, **실제로 어긋났습니다.**

**처방 — 출하 코드 자체를 렌더 없이 실행한다.**

```python
if os.environ.get("PREVIZ_DRY"):
    for j in jobs:
        build(j)          # 모든 게이트가 build() 안에 있다
    print("DRY OK  jobs %d  every gate passed" % len(jobs))
    return
```

```
$ PREVIZ_DRY=1 python3 -u previz_batch.py
DRY OK  jobs 60  every gate passed        # 약 100초
```

**100초로 3.2시간 렌더 낭비를 막습니다.** 그리고 복제본이 없으므로
코드와 게이트가 어긋날 수 없습니다.

> **검증 로직은 출하 코드 안에 두고, 렌더만 끄십시오.**
> 게이트의 복제본은 언젠가 반드시 거짓말을 합니다.

---

## 교훈 177 — 두 개의 램프를 독립적으로 두면 기하가 깨진다

시범 e 에서 원경 문서의 「방식」이 **「바시」** 로 읽혔습니다. 받침 ㅇ·ㄱ 이
사라진 것입니다. 처음에는 알파 이진화가 얇은 획을 잘랐다고 의심했습니다.
**두 가지 실측이 그 가설을 기각했습니다.**

```
텍스처의 획 두께 = 35~67 px (2048px 중 2~3%)    → 얇지 않다
같은 프레임의 근경 단어 = 「방식」 정확          → 알파 문제라면 전부 깨져야 한다
```

**결정적 실측은 이것이었습니다.**

```
노란 시트 bbox   y[268, 459]
그 위 붉은 글자   y[268, 433]
   글자 상단 - 시트 상단 = gap 0 px        ← 정확히 맞물렸다
```

글자가 **종이에 가려진 것**입니다. 원인은 서로 독립적인 두 램프였습니다.

```python
t_off = min(1.0, e / LIFTOFF)   # 회전 램프  (LIFTOFF = 0.35)
z_lift = z_total * e            # 상승 램프
```

`LIFTOFF=0.35` 는 **회전을 상승보다 약 3배 빠르게** 만듭니다.

```
J_A3-07 frame 70 :  e = 0.285 (28% 상승)  vs  t_off = 0.814 (81% 회전)
  → 높이 0.71m 평면의 아래쪽이 종이 아래로 0.36m 파고듦 → 시트가 받침을 가림
```

`LIFTOFF` 를 1.00 으로 올려 두 램프를 묶었지만 **부족했습니다.** 전 잡을
훑어보니 lift 잡은 상승이 0.30m 뿐인데 글자 높이가 0.71m 여서, 회전만
늦춰도 **46/51 잡이 여전히 가려졌습니다.**

**정답 — 한계가 기하학적이므로 제어도 기하학적으로 한다.**

평면을 `p` 만큼 세우면 아래 끝이 중심보다 `hh·sin(p)` 내려갑니다. 종이를
넘지 않을 조건은 `sin(p) ≤ 상승량 / hh` 이므로:

```python
risen = max(0.0, wz - loc[2])
pitch = min(pitch, math.asin(min(1.0, risen / max(1e-6, hw / aspect))))
```

| LIFTOFF 만 조정 | 최악 dip | 판정 |
|---|---|---|
| 0.35 | +0.356 m | 가려짐 |
| 0.60 | +0.164 m | 가려짐 |
| 0.80 | +0.052 m | 가려짐 |
| 1.00 | +0.350 m (lift 잡) | **여전히 가려짐** |
| **1.00 + 기하 캡** | **0.00000 m** | **원리적으로 불가능** |

최종 각도는 25~66° 로 남아 글자가 여전히 렌즈를 향합니다. 그리고 게이트가
매 실행마다 이를 증명합니다.

```python
dip = (hw / aspect) * math.sin(abs(pitch)) - max(0.0, wz - loc[2])
if dip > MAX_DIP_M:
    raise SystemExit("GLYPH GATE FAILED %s: sheet occludes glyph by %.3f m ...")
```

> **애니메이션 램프가 두 개 이상이면, 그 비율이 곧 기하학적 제약입니다.**
> 각각 "자연스러운" 값을 주면 그 사이에서 물체가 서로를 관통합니다.
> 제약이 기하학이면, 파라미터를 스윕하지 말고 **각도를 직접 계산**하십시오.

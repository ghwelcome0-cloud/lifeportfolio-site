#!/usr/bin/env node
import assert from "node:assert/strict";import fs from "node:fs";import {execFileSync} from "node:child_process";
const c=JSON.parse(fs.readFileSync("contracts/age-gate-capability.v1.json"));assert.deepEqual(Object.keys(c).sort(),["schema","contract_status","runtime_status","capability_status","project_id","required","evidence","decision","runtime_activation_allowed"].sort());assert.equal(c.contract_status,"target");assert.equal(c.runtime_status,"absent");assert.equal(c.capability_status,"unverified");assert.equal(c.runtime_activation_allowed,false);assert.equal(c.decision,"release_blocked_until_verified");for(const v of Object.values(c.required))assert.ok(v===false||v==="unverified");assert.equal(c.evidence.authenticated_project_query,false);assert.equal(c.evidence.checked_at,null);assert.deepEqual(c.evidence.blocking_function_exports,[]);const out=JSON.parse(execFileSync(process.execPath,["scripts/check-age-blocking-capability.mjs"],{encoding:"utf8"}));assert.equal(out.read_only,true);assert.equal(out.authenticated_project_query,false);assert.equal(out.runtime_status,"absent");assert.equal(out.release_decision,"blocked");assert.deepEqual(out.repo_blocking_exports,[]);const src=fs.readFileSync("functions/index.js","utf8");assert.doesNotMatch(src,/beforeUserCreated|beforeUserSignedIn|identity\.before/i);console.log("Age blocking capability target/unverified/absent read-only contract passed");

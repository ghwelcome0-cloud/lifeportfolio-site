#!/usr/bin/env node
import assert from "node:assert/strict";import crypto from "node:crypto";import fs from "node:fs";import {execFileSync} from "node:child_process";
const a=JSON.parse(fs.readFileSync("contracts/activation.json")),e=a.contracts.legal_l0,files=["contracts/legal-manifest.b2c.json","contracts/legal-manifest.b2b.json","contracts/legal-event-schemas.json","contracts/legal-manifests.sha256.json","contracts/legal-migrations.json","contracts/legal-consumers.json","contracts/legal-authority-reference.json","scripts/legal-contract-lib.mjs","scripts/test-legal-contracts.mjs","scripts/test-legal-consumer-antibypass.mjs","scripts/test-legal-governance.mjs","scripts/test-all.mjs",".github/workflows/required-checks.yml"];
assert.equal(e.active,true);assert.equal(e.activation_pr,252);assert.deepEqual(e.files,files);assert.deepEqual(e.command,{command:"node",args:["scripts/test-legal-governance.mjs"]});for(const f of files)assert.ok(fs.existsSync(f));const lib=fs.readFileSync("scripts/legal-contract-lib.mjs","utf8");assert.doesNotMatch(lib,/^\s*(?:await|fetch\(|process\.|console\.)/m,"import side effect");
const ref=JSON.parse(fs.readFileSync("contracts/legal-authority-reference.json")),rb=fs.readFileSync(ref.registry_path),db=fs.readFileSync(ref.evidence_digest_registry_path),sha=b=>crypto.createHash("sha256").update(b).digest("hex"),registry=JSON.parse(rb),digests=JSON.parse(db);assert.deepEqual(Object.keys(ref).sort(),["schema","source","registry_path","registry_schema_version","registry_sha256","evidence_digest_registry_path","evidence_digest_registry_sha256","issue_ids","required_registry_status","runtime_enforced","legal_approval_inferred"].sort());assert.equal(ref.source,"internal_evidence_ssot");assert.equal(ref.registry_schema_version,registry.schema_version);assert.equal(sha(rb),ref.registry_sha256);assert.equal(sha(db),ref.evidence_digest_registry_sha256);assert.equal(digests.files[ref.registry_path],ref.registry_sha256);assert.deepEqual(registry.issues.map(x=>x.issue_id),ref.issue_ids);assert.equal(registry.status,ref.required_registry_status);assert.equal(ref.runtime_enforced,false);assert.equal(ref.legal_approval_inferred,false);
const migration=JSON.parse(fs.readFileSync("contracts/legal-migrations.json")),mr=migration.records[0];assert.equal(e.activation_scope,"ci_contract_only");assert.equal(e.future_change_status,"unsupported_until_external_verifier");assert.equal(mr.pr,252);assert.equal(mr.pr_base_main_sha,process.env.CONTRACT_BASE_SHA||"1fca4237bf438a355879e8b5911227b09e841c73");assert.equal(mr.historical_corpus_base_main_sha,"66ba64fb46586a1ef4eed18e5b31a0da33390fc1");assert.equal(mr.relationship,"pr_base_descends_from_historical_corpus_base");assert.equal(mr.to_registry_sha256,sha(fs.readFileSync("contracts/legal-manifests.sha256.json")));assert.equal(mr.authority_registry_sha256,ref.registry_sha256);assert.equal(mr.approval_evidence,"pending_legal_review");assert.equal(mr.migration_state,"initial_pending");assert.equal(mr.future_change_status,"unsupported_until_external_verifier");
for(const link of registry.issue_authority_links){const source=registry.sources.find(x=>x.source_id===link.source_id);assert.ok(source);if(source.verification_status!=="verified")for(const m of [JSON.parse(fs.readFileSync("contracts/legal-manifest.b2c.json")),JSON.parse(fs.readFileSync("contracts/legal-manifest.b2b.json"))]){assert.equal(m.runtime_enforced,false);assert.equal(m.payment_ready,false);assert.notEqual(m.legal_review_status,"approved");}}
for(const test of ["scripts/test-legal-contracts.mjs","scripts/test-legal-consumer-antibypass.mjs"])execFileSync(process.execPath,[test],{stdio:"inherit"});console.log("Legal L0 immutable activation, exhaustive contracts, consumer anti-bypass, import side-effect-zero, and #260 SSOT reference passed");

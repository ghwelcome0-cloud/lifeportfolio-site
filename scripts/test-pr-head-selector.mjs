#!/usr/bin/env node
import assert from "node:assert/strict";import {checkoutExpectedHead,requirePrHead,selectCurrentHead} from "./pr-head-selector.mjs";const h="1".repeat(40),m="2".repeat(40);assert.equal(requirePrHead(h),h);for(const x of["",undefined,"g".repeat(40),h.slice(1)])assert.throws(()=>requirePrHead(x));assert.equal(selectCurrentHead({event:"pull_request",prHeadSha:h,githubSha:m}),h);assert.equal(selectCurrentHead({event:"push",prHeadSha:"",githubSha:h}),h);assert.equal(selectCurrentHead({event:"workflow_dispatch",prHeadSha:"",githubSha:h}),h);assert.throws(()=>selectCurrentHead({event:"pull_request",prHeadSha:"",githubSha:h}));assert.throws(()=>selectCurrentHead({event:"push",prHeadSha:h,githubSha:""}));assert.throws(()=>selectCurrentHead({event:"schedule",prHeadSha:h,githubSha:h}));const ok=(c,a)=>a[0]==="rev-parse"?h+"\n":"";assert.equal(checkoutExpectedHead({repo:"r",dir:"d",expectedHead:h,mergeSha:m,event:"pull_request",exec:ok}),h);assert.equal(checkoutExpectedHead({repo:"r",dir:"d",expectedHead:h,event:"push",exec:ok}),h);assert.throws(()=>checkoutExpectedHead({repo:"r",dir:"d",expectedHead:h,event:"pull_request",exec:ok}));assert.throws(()=>checkoutExpectedHead({repo:"r",dir:"d",expectedHead:h,mergeSha:h,event:"pull_request",exec:ok}));assert.throws(()=>checkoutExpectedHead({repo:"r",dir:"d",expectedHead:h,mergeSha:m,event:"push",exec:ok}));assert.throws(()=>checkoutExpectedHead({repo:"r",dir:"d",expectedHead:h,mergeSha:m,event:"pull_request",exec:(c,a)=>a[0]==="rev-parse"?m+"\n":""}));console.log("PR/push event matrix and head/source-swap negatives passed");
